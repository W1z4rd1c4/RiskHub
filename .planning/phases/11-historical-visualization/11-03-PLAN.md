---
phase: 11-historical-visualization
plan: 03
type: execute
---

<objective>
Build a historical comparison view for KRI values.

Purpose: Allow side-by-side diff between two reporting periods with clear deltas and breach shifts.
Output: Comparison panel integrated into the KRI history tab.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-plan.md
@.agent/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@frontend/src/pages/KRIDetailPage.tsx
@frontend/src/types/kri.ts
@frontend/src/components/history/HistoryChangeCard.tsx
@frontend/src/components/history/HistoryTimeline.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comparison panel for two history entries</name>
  <files>frontend/src/components/history/HistoryComparisonPanel.tsx, frontend/src/components/history/index.ts</files>
  <action>
    Build HistoryComparisonPanel:
    - Props: entries: KRIHistoryEntry[], formatValue?: (n) => string, className?
    - Sort a copy by period_end desc.
    - Initialize leftId/rightId to the two most recent entries; update via useEffect when entries change.
    - Provide two select dropdowns (native select or shadcn Select) with options labeled "YYYY-MM-DD (value unit)".
    - Disable selecting the same entry in both selectors; if it happens, show a small warning and skip delta calc.
    
    Comparison output:
    - For each field (value, lower_limit, upper_limit, breach_status, recorded_at, recorded_by), build HistoryComparisonField:
      - before/after strings formatted with formatValue and unit
      - delta for numeric fields with +/- and direction
      - tone based on breach_status change (danger if moving to above/below, success if moving to within).
    - Render fields via HistoryChangeCard in a 2-column grid.
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <done>Comparison panel renders with two selectable entries and delta indicators</done>
</task>

<task type="auto">
  <name>Task 2: Integrate comparison panel into KRI history tab</name>
  <files>frontend/src/pages/KRIDetailPage.tsx</files>
  <action>
    Integrate into KRIDetailPage History tab:
    - Add a "Compare Periods" section below the trend chart/timeline stack.
    - If history.length < 2, render a muted empty message and skip the panel.
    - Pass the existing history state directly; avoid extra fetch.
    - Use useMemo to pass a sorted copy if the panel expects sorted entries.
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <done>KRI History tab exposes a working comparison view with valid selections</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>KRI history comparison panel</what-built>
  <how-to-verify>
    1. Run: `cd frontend && npm run dev`
    2. Visit: http://localhost:5173/kris/{id} and open History
    3. Use the Compare Periods selectors to choose two entries
    4. Confirm: delta values update, breach status changes are obvious, and labels match selected periods
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or list issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npx tsc --noEmit` passes
- [ ] Manual comparison flow verified
</verification>

<success_criteria>
- Comparison view shows two history entries side-by-side with deltas
- Selection defaults to latest vs previous when possible
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/11-historical-visualization/11-03-SUMMARY.md`
</output>
