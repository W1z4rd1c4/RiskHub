---
phase: 11-historical-visualization
plan: 01
type: execute
---

<objective>
Create reusable frontend history components (timeline, change cards, trend chart).

Purpose: Standardize historical data visualization so detail pages and the dashboard can reuse consistent UI.
Output: Shared history components and types ready to integrate into Phase 11 pages.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@frontend/src/index.css
@frontend/src/components/dashboard/ControlTrendChart.tsx
@frontend/src/components/dashboard/CategoryBreakdownCharts.tsx
@frontend/src/components/executions/ExecutionHistory.tsx
@frontend/src/pages/KRIDetailPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared history timeline + change card components</name>
  <files>frontend/src/types/history.ts, frontend/src/components/history/HistoryTimeline.tsx, frontend/src/components/history/HistoryChangeCard.tsx, frontend/src/components/history/index.ts</files>
  <action>
    Create frontend/src/types/history.ts with:
    - HistoryStatus union: 'success' | 'warning' | 'danger' | 'neutral'
    - HistoryMetaItem { label: string; value: string; tone?: HistoryStatus }
    - HistoryTimelineItem { id: string | number; title: string; subtitle?: string; timestamp: string; status?: HistoryStatus; meta?: HistoryMetaItem[]; icon?: LucideIcon | ReactNode; badge?: string }
    - HistoryComparisonField { label: string; before: string; after: string; delta?: string; direction?: 'up' | 'down' | 'flat'; tone?: HistoryStatus }
    
    Implement HistoryTimeline:
    - Props: items: HistoryTimelineItem[], loading?: boolean, emptyMessage?: string, className?: string
    - Render a vertical rail (absolute left) with status dots aligned to each item.
    - Map status to tailwind classes (emerald for success, amber for warning, rose for danger, slate for neutral).
    - Show title + optional subtitle, a time element for timestamp, and meta pills for key/value pairs.
    - Support loading and empty states without throwing when items is empty.
    
    Implement HistoryChangeCard:
    - Props: title: string, fields: HistoryComparisonField[], className?: string
    - Render before/after columns and a delta badge with +/- and arrow based on direction.
    - Use monospace for numeric values and neutral styling when before == after.
    - Use existing glass-card/typography utilities and cn() to avoid new CSS tokens.
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <done>History timeline + change card components compile and render with sample props</done>
</task>

<task type="auto">
  <name>Task 2: Add reusable trend chart component for historical series</name>
  <files>frontend/src/components/history/HistoryTrendChart.tsx, frontend/src/components/history/index.ts</files>
  <action>
    Build HistoryTrendChart with reusable props:
    - Data point type: { label: string; value: number; status?: 'within' | 'above' | 'below' | 'neutral' }
    - Props: data, lowerLimit?, upperLimit?, valueLabel?, formatValue?, emptyMessage?, className?
    
    Implementation:
    - Use Recharts AreaChart (or LineChart) inside ResponsiveContainer with height 260-300px.
    - XAxis dataKey="label" and YAxis with tick styles cloned from ControlTrendChart.
    - Tooltip styling should match ControlTrendChart; use formatValue for tooltip values.
    - Add ReferenceLine for lowerLimit and upperLimit when provided (amber/rose).
    - Use a gradient fill tied to the accent color; avoid new color tokens.
    - Render a centered empty state when data.length === 0.
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <done>Trend chart renders with reference lines and styled tooltip without TypeScript errors</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npx tsc --noEmit` passes
- [ ] HistoryTimeline, HistoryChangeCard, and HistoryTrendChart render with sample props (local harness ok)
</verification>

<success_criteria>
- Shared history components exist under `frontend/src/components/history`
- Components match existing glass-card styling and chart tooltips
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/11-historical-visualization/11-01-SUMMARY.md`
</output>
