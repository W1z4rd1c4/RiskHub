---
phase: 11-historical-visualization
plan: 02
type: execute
---

<objective>
Add history tabs to KRI, Control, and Risk detail pages.

Purpose: Separate overview content from historical data and surface trend/timeline visualizations.
Output: Tabbed detail pages with history views wired to existing KRI and control execution data.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-plan.md
@.agent/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@frontend/src/pages/KRIDetailPage.tsx
@frontend/src/pages/ControlDetailPage.tsx
@frontend/src/pages/RiskDetailPage.tsx
@frontend/src/components/executions/ExecutionHistory.tsx
@frontend/src/components/history/HistoryTimeline.tsx
@frontend/src/components/history/HistoryTrendChart.tsx
@frontend/src/services/kriApi.ts
@frontend/src/types/kri.ts
@frontend/src/pages/DepartmentDetailPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tabbed layout for KRI and Control detail pages</name>
  <files>frontend/src/pages/KRIDetailPage.tsx, frontend/src/pages/ControlDetailPage.tsx</files>
  <action>
    Add tab state:
    - const [activeTab, setActiveTab] = useState<'overview' | 'history'>('overview')
    - Use the same tab header markup/classes as DepartmentDetailPage (border-b border-white/10, active underline).
    
    KRIDetailPage:
    - Wrap current layout blocks (current value, reporting info, linked risk, metadata) in activeTab === 'overview'.
    - Build historyChartData with useMemo from history:
      - sort copy by period_end asc for chart x-axis
      - map to { label: formatDate(period_end), value: entry.value, status: entry.breach_status }
    - Build timelineItems with useMemo:
      - id: entry.id, title: `${formatNumber(entry.value)} ${entry.unit}`
      - subtitle: `Period end ${formatDate(entry.period_end)}`
      - timestamp: entry.recorded_at
      - status: entry.breach_status === 'within' ? 'success' : 'danger'
      - meta: [{ label: 'Recorded by', value: entry.recorded_by_name ?? 'System' }, { label: 'Period', value: `${formatDate(entry.period_start)} - ${formatDate(entry.period_end)}` }]
    - Replace the table with a HistoryTrendChart + HistoryTimeline stack.
    - Keep Record Value/Edit/Delete buttons in the header so they are visible from both tabs.
    
    ControlDetailPage:
    - Add the same overview/history tabs.
    - Move the ExecutionHistory card and "Log Execution" button into the History tab only.
    - Keep the overview grid and risk linkage layout unchanged to avoid reflow.
    - Ensure ExecutionLogModal and historyKey refresh stay wired from the page root.
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <done>KRI and Control detail pages have working Overview/History tabs with no TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: Add risk history tab with aggregated KRI timelines</name>
  <files>frontend/src/pages/RiskDetailPage.tsx</files>
  <action>
    Add tab state at top of RiskDetailPage:
    - const [activeTab, setActiveTab] = useState<'overview' | 'history'>('overview')
    - Add history state: kriHistoryItems, isHistoryLoading, historyError? (optional).
    
    Fetch history only when activeTab === 'history':
    - useEffect with deps [activeTab, risk?.kris]
    - if no KRIs, set empty state and return.
    - Promise.all(risk.kris.map(kri => kriApi.getHistory(kri.id, { size: 50 }))) to avoid serial calls.
    - Use a cancel flag to avoid setState after unmount.
    
    Flatten into timeline items:
    - For each entry, include KRI name in title, value/unit, period_end, recorded_by.
    - id: `${kri.id}-${entry.id}`; timestamp: entry.recorded_at; status: 'success' if within else 'danger'.
    - Sort by recorded_at desc before rendering.
    
    Render:
    - History tab shows HistoryTimeline with loading/empty states.
    - Overview tab stays unchanged to avoid impacting risk/control/KRI actions.
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <done>Risk detail page shows a history tab with combined KRI timelines and stable layout</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Tabbed history views for KRI, Control, and Risk detail pages</what-built>
  <how-to-verify>
    1. Run: `cd frontend && npm run dev`
    2. Visit: http://localhost:5173/kris/{id} and switch to History
       - Trend chart renders, timeline entries show period/value/breach status
    3. Visit: http://localhost:5173/controls/{id} and switch to History
       - Execution history renders and Log Execution button is visible
    4. Visit: http://localhost:5173/risks/{id} and switch to History
       - Aggregated KRI timeline renders or shows empty state
    5. Confirm: No layout overlap or horizontal scroll in any tab
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or list issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npx tsc --noEmit` passes
- [ ] Manual tab verification completed
</verification>

<success_criteria>
- KRI, Control, and Risk detail pages have tabbed layouts
- History tabs render trend/timeline views using existing history data
- No regression to existing edit/log actions
</success_criteria>

<output>
After completion, create `.planning/phases/11-historical-visualization/11-02-SUMMARY.md`
</output>
