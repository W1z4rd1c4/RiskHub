---
phase: 11-historical-visualization
plan: 05
type: execute
---

<objective>
Enable audit trail exports (PDF/Excel) for control executions.

Purpose: Allow auditors to download execution history for reviews and compliance reporting.
Output: New report endpoints and Audit Trail page export buttons.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-plan.md
@.agent/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/app/api/v1/endpoints/reports.py
@backend/app/services/report_service.py
@backend/app/api/v1/endpoints/executions.py
@backend/app/models/control_execution.py
@frontend/src/pages/AuditTrailPage.tsx
@frontend/src/services/reportApi.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add audit trail PDF/Excel generation and report endpoints</name>
  <files>backend/app/services/report_service.py, backend/app/api/v1/endpoints/reports.py, backend/tests/api/v1/test_reports_audit.py</files>
  <action>
    Report generation:
    - Add generate_audit_trail_pdf and generate_audit_trail_excel in report_service.py.
    - Input should be a list of ControlExecution models with preloaded relations:
      executed_by, control (department, control_owner), control.risk_links -> risk.
    - Build row data: executed_at, control name, department, executor, result, findings, evidence_reference, next_scheduled, linked_risks (joined string).
    - PDF: use reportlab Table with column widths similar to other reports, truncate findings/evidence to ~80 chars, and include a summary footer.
    - Excel: include full findings/evidence, enable wrap_text on those columns, set column widths.
    
    Endpoints:
    - Add /reports/audit-trail/pdf and /reports/audit-trail/excel.
    - Query params: department_id, result, control_id, from_date, to_date.
    - Enforce reports:read permission and department scoping (join Control to filter by department_id/dept_ids).
    - Order by executed_at desc.
    
    Tests:
    - Create department, control, user, control_risk_link, and a ControlExecution.
    - Call both endpoints, assert status 200 and correct Content-Type.
    - Verify result filter and department filter reduce rows (optional: check response size or that empty report still returns file).
  </action>
  <verify>cd backend && pytest tests/api/v1/test_reports_audit.py -v</verify>
  <done>Audit trail report endpoints stream PDF/Excel with correct headers</done>
</task>

<task type="auto">
  <name>Task 2: Wire Audit Trail page export buttons to new report endpoints</name>
  <files>frontend/src/services/reportApi.ts, frontend/src/pages/AuditTrailPage.tsx</files>
  <action>
    reportApi:
    - Extend ReportFilters to include result, controlId, fromDate, toDate.
    - Update buildQueryString to append these params.
    - Add downloadAuditTrailPdf/downloadAuditTrailExcel.
    
    AuditTrailPage:
    - Replace disabled buttons with onClick handlers.
    - Pass resultFilter into reportApi; if backend expects 'passed/failed/warning', map UI values ('pass','fail','issues_found') to backend before calling.
    - Keep button styling unchanged; log errors on failure.
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <done>Audit trail page triggers PDF/Excel downloads without TypeScript errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Audit trail PDF/Excel export</what-built>
  <how-to-verify>
    1. Start backend: `cd backend && uvicorn app.main:app --reload`
    2. Start frontend: `cd frontend && npm run dev`
    3. Visit: http://localhost:5173/audit-trail
    4. Apply a result filter and click PDF and Excel buttons
    5. Confirm: files download and include recent execution rows with correct columns
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or list issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Backend report generation works for audit trail exports
- [ ] Frontend TypeScript check passes
- [ ] Manual download verification completed
</verification>

<success_criteria>
- Audit trail PDF/Excel endpoints respect access controls and filters
- Audit Trail page exports match the selected result filter
- No regression to existing report exports
</success_criteria>

<output>
After completion, create `.planning/phases/11-historical-visualization/11-05-SUMMARY.md`
</output>
