# Plan 252-09: Full-Green Closeout

## Objective

Run the full repo verification gate once, record the final closure summary, and mark Phase 252 complete only if every gate is green.

## Assumptions

- Full Playwright is only required here.
- Local runtime may need `./scripts/install.sh dev` before Playwright.

## Steps

1. Run the full repo gate:
   - `python3 scripts/check_docs_contract.py`
   - `make -f scripts/Makefile docs-topology-consistency`
   - `make -f scripts/Makefile test`
   - `TEST_DATABASE_URL=postgresql+asyncpg://riskhub:riskhub_test@127.0.0.1:55432/riskhub_test make -f scripts/Makefile test-postgres-ci`
   - `cd frontend && npm run test:run`
   - `cd frontend && npm run lint && npx tsc --noEmit && npm run build && npm run quality:debt -- --report-json && node scripts/quality/validate-debt-budget-report.mjs && npm run cleanup:deadcode && node scripts/cleanup/validate-unreachable-report.mjs && node scripts/quality/validate-no-inline-styles.mjs`
   - `cd frontend && BACKEND_URL=http://localhost:8000 npx playwright test -c playwright.config.ts --project=chromium --workers=1`
2. Write `252-09-SUMMARY.md` with final verification output, closed findings, and any explicit deferrals.
3. Mark Phase 252 complete in `.planning/STATE.md` and `.planning/ROADMAP.md` only when every command above is green.
