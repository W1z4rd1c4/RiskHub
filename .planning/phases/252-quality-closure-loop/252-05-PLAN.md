# Plan 252-05: Issue Detail Page Split

## Objective

Convert `frontend/src/pages/IssueDetailPage.tsx` into a stable route facade over typed hooks, tabs, and formatter helpers.

## Assumptions

- The route entrypoint and route contract remain unchanged.
- The current “Unknown risk” fallback remains and numeric IDs stay hidden from the UI.

## Steps

1. Create `frontend/src/pages/issues/issue-detail/`.
2. Move container behavior to `IssueDetailPageContainer.tsx`.
3. Extract fetch logic to `useIssueDetail.ts`.
4. Extract history loading to `useIssueHistory.ts`.
5. Split overview/workflow/history tabs.
6. Extract formatting helpers to `issueDetail.formatters.ts`.
7. Extend existing tabs tests and add formatter tests if needed.

## Wave Gate

- `cd frontend && npm run test:run -- src/pages/__tests__/IssueDetailPage.tabs.test.tsx`
- `cd frontend && npm run lint && npx tsc --noEmit`
