# Plan 252-00: Phase Scaffolding and Fast Sanity Lock

## Objective

Establish Phase 252 in the planning system, lock the current relevant findings, and verify that the repo is still green on the reduced fast-sanity gate before code changes continue.

## Assumptions

- The full-green verification captured in `.planning/STATE.md` on `2026-04-06` is accepted as the starting baseline.
- Full Playwright is deferred to the final closeout wave.
- Unrelated dirty worktree changes are not part of this phase.

## Steps

1. Add Phase 252 to `.planning/ROADMAP.md`.
2. Mark Phase 252 active in `.planning/STATE.md` while keeping Phase 90 active.
3. Create the Phase 252 folder and per-wave plan files.
4. Copy the surviving relevant items into `252-CONTEXT.md`.
5. Run the fast sanity gate:
   - `python3 scripts/check_docs_contract.py`
   - `make -f scripts/Makefile docs-topology-consistency`
   - `make -f scripts/Makefile test`
   - `cd frontend && npm run test:run`
   - `cd frontend && npm run lint && npx tsc --noEmit && npm run build && npm run quality:debt -- --report-json && node scripts/quality/validate-debt-budget-report.mjs && npm run cleanup:deadcode && node scripts/cleanup/validate-unreachable-report.mjs && node scripts/quality/validate-no-inline-styles.mjs`

## Success Criteria

- Phase 252 exists in planning docs.
- Fast sanity gate is green.
- If fast sanity is not green, stop before Wave 252-01.
