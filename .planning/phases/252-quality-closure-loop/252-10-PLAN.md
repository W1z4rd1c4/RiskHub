# Plan 252-10: Repo Professionalism and Artifact Hygiene

## Objective

Remove broken tracked utilities and placeholder product-surface artifacts, repair README coverage, and add regression contracts so the retired surfaces do not silently return.

## Assumptions

- Canonical documentation flows already come from the backend/docs-reader pipeline, not `frontend/public/docs/*.md`.
- Removing broken unused helper scripts is lower risk than repairing and carrying them.

## Steps

1. Expand Phase 252 context/state to the broader five-area closure scope.
2. Remove broken tracked PDF helper utilities and their stale references.
3. Retire placeholder static docs under `frontend/public/docs/` and keep only a README pointing to canonical docs.
4. Remove the tracked `docs/reference/file_list.txt` archive inventory and update references.
5. Add missing README coverage for newly added component/test directories.
6. Fix the stricter query-param typing regressions in frontend service clients.
7. Add repo hygiene contract tests for the retired artifact surfaces.

## Wave Gate

- `make -f scripts/Makefile docs-topology-consistency`
- `bash -n scripts/install.sh scripts/compose.sh scripts/dev.sh scripts/deploy.sh`
- `cd frontend && npm run test:run`
- `cd frontend && npm run lint && npx tsc --noEmit && npm run build && npm run quality:debt -- --report-json && node scripts/quality/validate-debt-budget-report.mjs && npm run cleanup:deadcode && node scripts/cleanup/validate-unreachable-report.mjs && node scripts/quality/validate-no-inline-styles.mjs`
