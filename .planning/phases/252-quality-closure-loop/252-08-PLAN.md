# Plan 252-08: Narrow Backend Cleanup and Polish

## Objective

Close the remaining small backend and polish issues after the main quality work is complete.

## Assumptions

- No additional backend architecture wave is needed.
- Low-priority metadata/doc polish should stay tightly scoped.

## Steps

1. Deduplicate the default DB URL between `bootstrap_validation.py` and `core/settings/database.py`.
2. Replace `except BaseException:` with `except Exception:` around Redis ping while preserving close-and-reraise behavior.
3. Replace or remove `backend/app/core/README.md` if it cannot be made specific.
4. Give `frontend/package.json` a real package name/version.
5. Clarify that `docker-compose.yml` is dev-biased and not a production-default source.

## Wave Gate

- `cd backend && ./venv/bin/python -m pytest -q ../tests/backend/pytest/test_log_rotation_config.py ../tests/backend/pytest/test_production_hardening.py ../tests/backend/pytest/test_bootstrap_split_contracts.py`
- `cd frontend && npm run lint && npx tsc --noEmit`
- `python3 scripts/check_docs_contract.py && make -f scripts/Makefile docs-topology-consistency`
