# Plan 252-11: Quality Gate Hardening for Artifact Hygiene

## Objective

Turn the artifact-hygiene cleanup from Wave `252-10` into a default local and CI contract so broken utilities, tracked ignored paths, and placeholder product-surface artifacts cannot silently drift back in.

## Steps

1. Add a fast local Make target that validates:
   - startup/deploy shell syntax
   - migration/seed script Python syntax
   - repo hygiene contracts for tracked ignored paths and retired artifact surfaces
2. Wire that target into the blocking lint workflow and upload its evidence alongside the other quality logs.
3. Update the testing and scripts docs so the new verification path is documented as a normal repo gate.
4. Record the wave completion in Phase 252 state/summary docs after verification is green.

## Verification

- `make -f scripts/Makefile quality-repo-contracts`
- `make -f scripts/Makefile verify`
- `make -f scripts/Makefile docs-topology-consistency`
