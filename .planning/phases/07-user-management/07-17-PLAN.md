# Phase 7.17: Verify Export Department Filtering (MEDIUM)

> **Source**: [PERMISSIONAUDIT.MD](PERMISSIONAUDIT.MD) - Revised

---
phase: 07-user-management
type: execute
---

<objective>
Verify that report exports correctly filter data to user's department scope.

Purpose: Users should only export data they can view - exports must respect department filtering.
Output: Tests confirming export endpoints return only department-scoped data.
</objective>

<context>
@backend/app/api/v1/endpoints/reports.py
@backend/app/core/permissions.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify export endpoints use department filtering</name>
  <files>backend/app/api/v1/endpoints/reports.py</files>
  <action>
Review export endpoints to confirm they:
1. Call `get_user_department_ids(current_user)` 
2. Filter data to those department IDs
3. Handle empty dept_ids (users with no access) correctly

If any gaps exist, add the filtering. Document findings.
  </action>
  <verify>Code inspection confirms department filtering in all export endpoints</verify>
  <done>All export endpoints filter by user's department scope</done>
</task>

<task type="auto">
  <name>Task 2: Add export department filtering test</name>
  <files>backend/tests/test_reports_rbac.py</files>
  <action>
Add test case `test_export_filters_to_user_department`:
1. Create user in Department A with some controls
2. Create controls in Department B (user should NOT see)
3. Call export endpoint (e.g., /reports/controls/pdf)
4. Verify exported data only includes Department A controls
  </action>
  <verify>pytest tests/test_reports_rbac.py -v -k "filters_to_user"</verify>
  <done>Test verifies exports respect department scope</done>
</task>

<task type="auto">
  <name>Task 3: Remove unused reports:export permission (cleanup)</name>
  <files>backend/app/db/seed.py</files>
  <action>
If `reports:export` permission exists in seed.py but is unused, remove it to avoid confusion.
Also remove from PERMISSIONS list if present.
  </action>
  <verify>grep -rn "reports:export" backend/</verify>
  <done>No orphaned reports:export permission in codebase</done>
</task>

</tasks>

<verification>
Before declaring complete:
- [ ] Export endpoints filter data by user's department scope
- [ ] Users cannot export data from other departments
- [ ] Test documents the expected behavior
</verification>

<success_criteria>
- Exports return same data scope as viewing
- Department filtering works correctly in exports
- No unused permissions left in codebase
</success_criteria>

<output>
After completion, create `.planning/phases/07-user-management/07-17-SUMMARY.md`
</output>
