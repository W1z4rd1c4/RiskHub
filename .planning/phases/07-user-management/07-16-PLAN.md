# Phase 7.16: Approval Request Resource Access Check (MEDIUM)

> **Source**: [PERMISSIONAUDIT.MD](PERMISSIONAUDIT.MD) - MEDIUM Issue

---
phase: 07-user-management
type: execute
---

<objective>
Add department access verification when creating approval requests.

Purpose: Prevent users from creating approval requests for resources outside their department scope.
Output: Cross-department approval requests return 403 Forbidden.
</objective>

<context>
@.planning/phases/07-user-management/PERMISSIONAUDIT.MD
@backend/app/api/v1/endpoints/approvals.py
@backend/app/core/permissions.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add department access check to create_approval_request</name>
  <files>backend/app/api/v1/endpoints/approvals.py</files>
  <action>
1. Import `check_department_access` from app.core.permissions
2. After loading each resource (Risk, Control, KRI), call:
   ```python
   check_department_access(resource.department_id, current_user)
   ```
3. This will raise HTTPException 403 if user cannot access the resource's department
  </action>
  <verify>Code inspection shows check_department_access called for each resource type</verify>
  <done>Department access is verified before creating approval request</done>
</task>

<task type="auto">
  <name>Task 2: Add cross-department approval test</name>
  <files>backend/tests/test_approvals.py</files>
  <action>
Add test case `test_create_approval_cross_department_forbidden`:
1. Create user in Department A
2. Create risk in Department B
3. Attempt to create approval request for that risk
4. Assert 403 response
  </action>
  <verify>pytest tests/test_approvals.py -v -k "cross_department"</verify>
  <done>Test verifies cross-department approval requests are rejected</done>
</task>

</tasks>

<verification>
Before declaring complete:
- [ ] `pytest tests/test_approvals.py -v` passes all tests
- [ ] User cannot create approval for resource in another department
- [ ] Privileged users (admin/cro) can still create approvals for any resource
</verification>

<success_criteria>
- Cross-department approval requests return 403
- Same-department approval requests still work
- Privileged users bypass department check
- Test documents the expected behavior
</success_criteria>

<output>
After completion, create `.planning/phases/07-user-management/07-16-SUMMARY.md`
</output>
