# Phase 7.15: Control-Trends Department Filter Fix (MEDIUM)

> **Source**: [PERMISSIONAUDIT.MD](file:///Users/stefanlesnak/Antigravity/Risk%20App/.planning/phases/07-user-management/PERMISSIONAUDIT.MD) - MEDIUM Issue

---
phase: 07-user-management
type: execute
---

<objective>
Fix empty department list bypass in control-trends endpoint.

Purpose: Users without department access should see empty results, not all departments.
Output: `dept_ids = []` returns empty control-trends instead of bypassing filter.
</objective>

<context>
@.planning/phases/07-user-management/PERMISSIONAUDIT.MD
@backend/app/api/v1/endpoints/dashboard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix dept_ids condition logic</name>
  <files>backend/app/api/v1/endpoints/dashboard.py</files>
  <action>
At line 299, change:
```python
if dept_ids or department_id or control_status:
```
To:
```python
if dept_ids is not None or department_id or control_status:
```

Also add early return for empty dept_ids before the subquery:
```python
if dept_ids is not None and len(dept_ids) == 0:
    return []  # User has no department access
```
  </action>
  <verify>Code inspection shows explicit None check</verify>
  <done>Empty list dept_ids returns empty results instead of all data</done>
</task>

<task type="auto">
  <name>Task 2: Add test for empty department scope</name>
  <files>backend/tests/test_dashboard.py</files>
  <action>
Add test case `test_control_trends_empty_dept_ids_returns_empty`:
1. Create user with no department (dept_ids = [])
2. Call GET /dashboard/control-trends
3. Assert response is empty list []
  </action>
  <verify>pytest tests/test_dashboard.py -v -k "empty_dept"</verify>
  <done>Test verifies empty dept_ids returns empty results</done>
</task>

</tasks>

<verification>
Before declaring complete:
- [ ] `pytest tests/test_dashboard.py -v` passes all tests
- [ ] User with no department gets empty control-trends
- [ ] User with departments still sees filtered data
</verification>

<success_criteria>
- Empty department list returns empty results (not all data)
- Existing department filtering still works correctly
- Test documents the expected behavior
</success_criteria>

<output>
After completion, create `.planning/phases/07-user-management/07-15-SUMMARY.md`
</output>
