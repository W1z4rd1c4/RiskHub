# Phase 7.14: Production Security Defaults (HIGH)

> **Source**: [PERMISSIONAUDIT.MD](file:///Users/stefanlesnak/Antigravity/Risk%20App/.planning/phases/07-user-management/PERMISSIONAUDIT.MD) - HIGH Issue

---
phase: 07-user-management
type: execute
---

<objective>
Harden default configuration to prevent insecure settings in production.

Purpose: Prevent RBAC bypass via mock auth or forged JWTs in production deployments.
Output: App fails to start in production mode with insecure defaults; dev mode logs warnings.
</objective>

<context>
@.planning/phases/07-user-management/PERMISSIONAUDIT.MD
@backend/app/core/config.py
@backend/app/api/deps.py
@backend/app/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change security defaults to False</name>
  <files>backend/app/core/config.py</files>
  <action>
1. Change `debug: bool = False` (was True)
2. Change `mock_auth_enabled: bool = False` (was True)
3. Keep secret_key placeholder but add comment warning
  </action>
  <verify>python -c "from app.core.config import get_settings; s = get_settings(); print(f'debug={s.debug}, mock={s.mock_auth_enabled}')" → "debug=False, mock=False"</verify>
  <done>Default values are False for both debug and mock_auth_enabled</done>
</task>

<task type="auto">
  <name>Task 2: Add startup validation for secret_key</name>
  <files>backend/app/main.py</files>
  <action>
Add startup check in `lifespan` or module level:
```python
settings = get_settings()
if not settings.debug and settings.secret_key == "your-secret-key-change-in-production-use-env-var":
    raise RuntimeError("FATAL: secret_key must be set via environment variable in production mode")
```
Log warning if debug=True with placeholder secret.
  </action>
  <verify>Start app without .env in non-debug mode → should fail with RuntimeError</verify>
  <done>App refuses to start with insecure secret in production mode</done>
</task>

<task type="auto">
  <name>Task 3: Update .env.example with required settings</name>
  <files>backend/.env.example</files>
  <action>
Create or update .env.example with:
```
# Required for production
SECRET_KEY=your-secret-key-here-min-32-chars
DEBUG=false
MOCK_AUTH_ENABLED=false

# Development only (optional)
# DEBUG=true
# MOCK_AUTH_ENABLED=true
```
  </action>
  <verify>File exists with documented settings</verify>
  <done>.env.example documents required production settings</done>
</task>

</tasks>

<verification>
Before declaring complete:
- [ ] Default config has debug=False, mock_auth_enabled=False
- [ ] App fails to start with placeholder secret when debug=False
- [ ] .env.example documents production requirements
</verification>

<success_criteria>
- Production deployments cannot accidentally use insecure defaults
- Developers must explicitly enable debug mode
- Clear documentation of required environment variables
</success_criteria>

<output>
After completion, create `.planning/phases/07-user-management/07-14-SUMMARY.md`
</output>
