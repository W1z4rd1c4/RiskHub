---
phase: 500-production-installation-scripts
plan: 500-03
type: execute
wave: 3
depends_on: [500-01, 500-04]
files_modified:
  - scripts/prod/install_frontend.sh
autonomous: true
---

<objective>
Implement frontend installation path so the frontend container can be installed independently while routing API traffic to the backend container endpoint defined by deployment config.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Implement frontend install/replace script</name>
  <files>scripts/prod/install_frontend.sh</files>
  <action>
Create script that:
- loads frontend env file,
- runs preflight checks,
- builds or pulls frontend image,
- creates/replaces frontend container idempotently,
- binds only intended public ports,
- joins configured Docker network to reach backend upstream.
  </action>
  <verify>Frontend container can be installed/replaced independently of backend install script.</verify>
  <done>Frontend lifecycle is decoupled and script-managed.</done>
</task>

<task type="auto">
  <name>Task 2: Make API upstream routing configurable for split deployment</name>
  <files>scripts/prod/install_frontend.sh, scripts/prod/install_backend.sh</files>
  <action>
Prefer no nginx templating for the same-host deployment topology.
Enforce a stable upstream by Docker network alias:
- backend API container must have network alias `backend` on `riskhub-network`
- frontend container must join `riskhub-network`
- nginx continues to proxy `/api/` to `http://backend:8000` (existing config)

Ensure `/api/` proxy behavior remains unchanged for application requests and preserves auth headers.
  </action>
  <verify>Frontend `/api/*` requests reach backend in the split-container installation model.</verify>
  <done>Frontend-backend routing no longer relies on implicit compose-only assumptions.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve SPA behavior and health checks in production image</name>
  <files>scripts/prod/install_frontend.sh</files>
  <action>
Validate that route fallback (`/index.html`), cache headers, and `/nginx-health` endpoint continue to work after upstream changes.
No nginx templating is expected for the Phase 500 same-host path.
  </action>
  <verify>Static assets, SPA routing, and health endpoint behavior remain stable.</verify>
  <done>Frontend install remains production-ready after upstream decoupling.</done>
</task>

</tasks>

<must_haves>
- Frontend container installation is independent from backend install script execution.
- API proxy routing is explicit and configurable.
- Existing SPA and security-header behavior is preserved.
</must_haves>

<verification>
- [ ] `bash -n scripts/prod/install_frontend.sh`
- [ ] `docker run --rm -v "$PWD":/work koalaman/shellcheck:stable scripts/prod/install_frontend.sh`
- [ ] `docker build -t riskhub-frontend:test ./frontend`
- [ ] `scripts/prod/install_frontend.sh --frontend-env scripts/prod/config/frontend.env.example --frontend-image riskhub-frontend:test --dry-run`
</verification>
