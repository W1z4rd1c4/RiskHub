---
phase: 500-production-installation-scripts
plan: 500-07
type: execute
wave: 5
depends_on: [500-05, 500-06]
files_modified:
  - scripts/prod/smoke_test.sh
  - scripts/prod/verify_runtime.sh
  - backend/tests/test_production_hardening.py
  - Makefile
autonomous: true
---

<objective>
Add deployment verification automation so split-container installs can be validated quickly and repeatedly.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Implement deployment smoke-test script</name>
  <files>scripts/prod/smoke_test.sh</files>
  <action>
Create smoke test script that validates:
- frontend responds successfully,
- backend health endpoint responds,
- backend environment guard-sensitive behavior is observable (for example docs disabled in production),
- container status is healthy after deployment.

Include configurable timeouts and retries for cold starts.

Concrete assertions for the Phase 500 topology (backend not published):
- `curl -f http://localhost:$FRONTEND_HOST_PORT/` returns 200
- `curl -f http://localhost:$FRONTEND_HOST_PORT/api/v1/health` returns JSON where:
  - `status` is `healthy`
  - `database` is `connected`
- `docker exec riskhub-backend curl -sS -o /dev/null -w "%{http_code}" http://localhost:8000/docs` returns 404
- optionally also assert `/openapi.json` is 404 in production mode
  </action>
  <verify>Smoke script provides clear pass/fail output and non-zero exit on failure.</verify>
  <done>Post-deploy health checks are automated and repeatable.</done>
</task>

<task type="auto">
  <name>Task 2: Add runtime verification helper for operator diagnostics</name>
  <files>scripts/prod/verify_runtime.sh</files>
  <action>
Implement a diagnostic script to print:
- effective container image tags,
- backend/frontend network attachment,
- key endpoint reachability summary,
- migration revision status (optional read-only check).

Keep it read-only and safe for incident response.
  </action>
  <verify>Operators can run diagnostics without mutating deployment state.</verify>
  <done>Runtime troubleshooting path is codified.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression checks to repo test/command surface</name>
  <files>backend/tests/test_production_hardening.py, Makefile</files>
  <action>
Extend tests/commands so script-path production assumptions are guarded:
- preserve production hardening invariants in backend tests,
- add a Make target `verify-prod-install-scripts` that runs:
  - `bash -n` over scripts
  - dockerized shellcheck (koalaman/shellcheck image)
  - `cd backend && venv/bin/pytest tests/test_production_hardening.py -q`
  </action>
  <verify>Targeted hardening tests and script checks run from a single documented command.</verify>
  <done>Install automation has ongoing regression protection.</done>
</task>

</tasks>

<must_haves>
- Smoke validation covers both backend and frontend containers.
- Verification scripts are safe and read-only by default.
- Regression checks are easy to run in CI and locally.
</must_haves>

<verification>
- [ ] `bash -n scripts/prod/smoke_test.sh scripts/prod/verify_runtime.sh`
- [ ] `docker run --rm -v "$PWD":/work -w /work koalaman/shellcheck:stable -x scripts/prod/*.sh`
- [ ] `cd backend && venv/bin/pytest tests/test_production_hardening.py -q`
- [ ] `make verify-prod-install-scripts`
</verification>
