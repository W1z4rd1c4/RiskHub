---
phase: 500-production-installation-scripts
plan: 500-08
type: execute
wave: 6
depends_on: [500-07]
files_modified:
  - /Users/stefanlesnak/Antigravity/Risk App 2/docs/deployment/external-postgres-install-scripts.md
  - /Users/stefanlesnak/Antigravity/Risk App 2/docs/deployment/README.md
  - /Users/stefanlesnak/Antigravity/Risk App 2/docs/deployment/docker-compose-prod.md
  - /Users/stefanlesnak/Antigravity/Risk App 2/docs/deployment/security-checklist.md
  - /Users/stefanlesnak/Antigravity/Risk App 2/.planning/ROADMAP.md
  - /Users/stefanlesnak/Antigravity/Risk App 2/.planning/STATE.md
autonomous: true
---

<objective>
Finalize operator-facing documentation and planning-state reconciliation for the split-container external-PostgreSQL production install path.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Publish external PostgreSQL install runbook</name>
  <files>docs/deployment/external-postgres-install-scripts.md</files>
  <action>
Create a full runbook covering:
- prerequisites and host preparation,
- external PostgreSQL prerequisites (DB/user created; network reachability),
- Redis container provisioning and password requirements,
- env file setup for backend/frontend,
- script execution order (`preflight`, backend install, migrations, frontend install, smoke checks),
- RBAC + departments seeding step,
- bootstrap initial admin by email and why this is required for SSO,
- scheduler container (single worker) and why this prevents duplicate jobs,
- upgrade/rollback operations,
- rollback posture: forward-fix + backups/PITR (no automatic DB downgrade),
- troubleshooting and incident-response commands.
  </action>
  <verify>A first-time operator can deploy using only the runbook and scripts.</verify>
  <done>External PostgreSQL split-container deployment is fully documented.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile deployment/security docs with new installation path</name>
  <files>docs/deployment/README.md, docs/deployment/docker-compose-prod.md, docs/deployment/security-checklist.md</files>
  <action>
Update existing docs to:
- link to the new script-based runbook,
- clearly differentiate compose deployment from split-container script deployment,
- document that PostgreSQL is external for the new path,
- keep production guardrails and Redis requirements explicit.
  </action>
  <verify>Deployment docs no longer imply that db container startup is required for script-based production installs.</verify>
  <done>Documentation set is coherent across all production deployment entry points.</done>
</task>

<task type="auto">
  <name>Task 3: Closeout planning metadata after execution evidence is collected</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
After implementation and verification pass:
- mark completed 500 plans in roadmap,
- update STATE progress row and session context with executed commands/results,
- add `500-XX-SUMMARY.md` artifacts for each executed plan.

Do not mark complete without verification evidence.
  </action>
  <verify>Roadmap/state accurately reflect what was implemented and tested.</verify>
  <done>Phase closeout is audit-ready and resumable.</done>
</task>

</tasks>

<must_haves>
- Deployment docs are actionable and aligned with implemented scripts.
- External PostgreSQL requirement is explicit in both runbook and security checklist.
- Planning metadata is reconciled only after evidence-backed execution.
</must_haves>

<verification>
- [ ] `rg -n "external PostgreSQL|split-container|install scripts" docs/deployment/*.md`
- [ ] `rg -n "500-0[1-8]" .planning/ROADMAP.md`
- [ ] `rg -n "Phase 500" .planning/STATE.md`
</verification>
