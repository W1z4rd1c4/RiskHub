---
phase: 500-production-installation-scripts
plan: 500-06
type: execute
wave: 4
depends_on: [500-02, 500-03, 500-05]
files_modified:
  - /Users/stefanlesnak/Antigravity/Risk App 2/scripts/prod/preflight.sh
  - /Users/stefanlesnak/Antigravity/Risk App 2/scripts/prod/install_backend.sh
  - /Users/stefanlesnak/Antigravity/Risk App 2/scripts/prod/install_frontend.sh
  - /Users/stefanlesnak/Antigravity/Risk App 2/scripts/prod/lib/preflight.sh
  - /Users/stefanlesnak/Antigravity/Risk App 2/docs/deployment/security-checklist.md
autonomous: true
---

<objective>
Harden installation scripts so production-safe behavior is enforced by default and unsafe configurations fail closed.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Enforce production guardrails in preflight and install scripts</name>
  <files>scripts/prod/preflight.sh, scripts/prod/lib/preflight.sh, scripts/prod/install_backend.sh</files>
  <action>
Add explicit checks aligned with backend production invariants:
- `DEBUG=false`, `MOCK_AUTH_ENABLED=false`, `AUTH_MODE=microsoft_sso`,
- non-placeholder `DATABASE_URL`,
- `SECRET_KEY` minimum length,
- explicit `CORS_ORIGINS`,
- Redis and Entra variables required when production mode is enabled:
  - `REDIS_PASSWORD` (for the redis container) and `REDIS_URL`
  - `ENTRA_TENANT_ID`, `ENTRA_CLIENT_ID`

Fail before container changes if any check fails.
  </action>
  <verify>Unsafe config exits non-zero with actionable error messages before deployment starts.</verify>
  <done>Script path enforces the same policy boundaries as app startup.</done>
</task>

<task type="auto">
  <name>Task 2: Apply secure container runtime defaults</name>
  <files>scripts/prod/install_backend.sh, scripts/prod/install_frontend.sh</files>
  <action>
Harden `docker run` defaults:
- run non-privileged/non-root where image supports it,
- avoid host network mode,
- expose only required ports,
- apply `--restart unless-stopped`,
- apply `--security-opt no-new-privileges`,
- redact secrets from logs and command output.

Keep required writable paths functional (for example log volume mounts).
  </action>
  <verify>Containers run with expected least-privilege defaults and remain functional.</verify>
  <done>Install scripts reduce accidental insecure runtime configurations.</done>
</task>

<task type="auto">
  <name>Task 3: Explicitly disallow PostgreSQL container orchestration in this install path</name>
  <files>scripts/prod/preflight.sh, scripts/prod/install_backend.sh, docs/deployment/security-checklist.md</files>
  <action>
Add explicit checks/messages that this production install mode expects externally managed PostgreSQL and will not:
- start `db` via compose,
- run `postgres` container provisioning commands,
- silently fall back to localhost defaults.

Document the control in deployment security checklist.
  </action>
  <verify>Attempting to run scripts with compose-db assumptions produces clear validation errors.</verify>
  <done>External-PostgreSQL requirement is technically enforced and documented.</done>
</task>

</tasks>

<must_haves>
- Fail-closed behavior for unsafe production configuration.
- No secret leakage in script logs/stdout.
- External PostgreSQL requirement is enforced, not advisory.
</must_haves>

<verification>
- [ ] `scripts/prod/preflight.sh --env-file scripts/prod/config/backend.env.example --check-only`
- [ ] `scripts/prod/install_backend.sh --dry-run --verbose`
- [ ] `scripts/prod/install_frontend.sh --dry-run --verbose`
- [ ] `rg -n "external PostgreSQL|not containerized|no db service" docs/deployment/security-checklist.md`
</verification>
