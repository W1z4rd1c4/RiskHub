# Plan 157-06: E2E Business Logic Test Coverage

---
wave: 3
depends_on: [157-01, 157-02, 157-04]
files_modified:

- frontend/e2e/approval-cancellation.spec.ts
- frontend/e2e/kri-correction.spec.ts
- frontend/e2e/cross-department-access.spec.ts
autonomous: true

---

## Objective

Accelerate Phase 180 E2E Business Logic Testing by creating targeted E2E tests for the fixes implemented in Phase 157.

## Context

Phase 180 is 1/9 complete. This plan creates E2E tests specifically for:

- 157-01: Approval cancellation by privileged users
- 157-02: KRI correction CRO approval
- 157-04: Approval edge cases

This provides regression protection for the Phase 157 fixes while contributing to Phase 180 goals.

## Tasks

<task id="1" title="Create approval-cancellation.spec.ts">
Create `frontend/e2e/approval-cancellation.spec.ts`:

```typescript
import { test, expect } from '@playwright/test';
import { loginAs, createApprovalRequest } from './helpers';

test.describe('Approval Cancellation - §5.5 Compliance', () => {
  test('requester can cancel their own pending request', async ({ page }) => {
    await loginAs(page, 'ops.head@riskhub.local');
    // Create and cancel own request
  });

  test('privileged user (CRO) can cancel any pending request', async ({ page }) => {
    // Setup: dept head creates approval
    // CRO logs in and cancels it
  });

  test('privileged user (Risk Manager) can cancel any pending request', async ({ page }) => {
    // Risk Manager cancels dept head's request
  });

  test('non-privileged user cannot cancel other users requests', async ({ page }) => {
    // Dept head A cannot cancel Dept head B's request
  });

  test('cannot cancel already resolved requests', async ({ page }) => {
    // APPROVED/REJECTED/CANCELLED requests cannot be cancelled
  });
});
```

</task>

<task id="2" title="Create kri-correction.spec.ts">
Create `frontend/e2e/kri-correction.spec.ts`:

```typescript
import { test, expect } from '@playwright/test';
import { loginAs, navigateToKRI } from './helpers';

test.describe('KRI Value Correction - §5.3 Compliance', () => {
  test('correction creates approval with CRO escalation', async ({ page }) => {
    await loginAs(page, 'ops.analyst@riskhub.local');
    // Navigate to KRI, correct value
    // Verify approval created with requires_privileged_approval=true
  });

  test('Risk Owner approval escalates to PENDING_PRIVILEGED', async ({ page }) => {
    // Risk Owner approves correction
    // Status becomes PENDING_PRIVILEGED
  });

  test('CRO can directly correct KRI without approval', async ({ page }) => {
    await loginAs(page, 'cro@riskhub.local');
    // Correct value directly, no approval needed
  });

  test('initial submission does not require privileged for non-priority risk', async ({ page }) => {
    // First submission to non-priority risk
    // Only needs Risk Owner approval, not CRO
  });
});
```

</task>

<task id="3" title="Create cross-department-access.spec.ts">
Create `frontend/e2e/cross-department-access.spec.ts`:

```typescript
import { test, expect } from '@playwright/test';
import { loginAs } from './helpers';

test.describe('Cross-Department Access - §7 Compliance', () => {
  test('KRI reporting owner can view cross-department KRI', async ({ page }) => {
    // Employee from Dept A is reporting owner of KRI in Dept B
    // Should be able to view that KRI
  });

  test('Control owner can view cross-department control', async ({ page }) => {
    // Control owner sees control outside their department
  });

  test('Control owner can view linked risks from other departments', async ({ page }) => {
    // §7.2: Control Owner can view Risk's linked Controls
  });

  test('KRI reporting owner can view linked risk and its controls', async ({ page }) => {
    // §7.2: Access inheritance
  });

  test('privileged users see all departments', async ({ page }) => {
    await loginAs(page, 'cro@riskhub.local');
    // All departments visible
  });
});
```

</task>

<task id="4" title="Add E2E test helpers">
Create or extend `frontend/e2e/helpers.ts` with:

```typescript
export async function createApprovalRequest(
  page: Page,
  resourceType: 'risk' | 'control' | 'kri',
  resourceId: number,
  reason: string
): Promise<number>;

export async function navigateToKRI(page: Page, kriId: number): Promise<void>;

export async function submitKRIValue(
  page: Page,
  kriId: number,
  value: number
): Promise<void>;

export async function correctKRIValue(
  page: Page,
  kriId: number,
  newValue: number,
  reason: string
): Promise<void>;
```

</task>

<task id="5" title="Run E2E tests">
Execute the new tests:

```bash
cd frontend && npx playwright test e2e/approval-cancellation.spec.ts e2e/kri-correction.spec.ts e2e/cross-department-access.spec.ts --reporter=html
```

Fix any failures.
</task>

<task id="6" title="Update Phase 180 progress">
After tests pass, update ROADMAP.md Phase 180 progress:
- Mark partial completion of 180-04 (if approval tests added)
- Mark partial completion of 180-06 (if cross-dept tests added)
</task>

## Verification Criteria

- [ ] All E2E tests pass
- [ ] Tests cover §5.5 (cancellation), §5.3 (KRI correction), §7 (cross-dept)
- [ ] Helper functions reusable for future Phase 180 work
- [ ] Test coverage for both happy path and error cases

## must_haves

- Regression protection for Phase 157 fixes
- Contribution to Phase 180 E2E coverage
- Reusable test infrastructure
- CI-compatible test suite
