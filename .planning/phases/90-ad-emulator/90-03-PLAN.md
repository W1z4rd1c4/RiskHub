---
phase: 90-ad-emulator
plan: 03
type: execute
---

<objective>
Validate the AD Emulator end-to-end with automated tests and a manual verification checklist.

Purpose: Ensure the sync rules are correct and UI behavior matches expected outcomes before real Entra integration.
Output: Backend tests, frontend service/UI tests, and a verified manual checklist.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-ad-emulator/90-DISCOVERY.md
@backend/tests/conftest.py
@backend/tests/test_users.py
@frontend/src/services/__tests__/api.test.ts
@frontend/src/test/mocks/handlers.ts
@frontend/src/test/utils.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backend tests for directory sync and mapping</name>
  <files>backend/tests/test_directory_sync.py</files>
  <action>
    Create pytest tests that cover:
    - Creating DirectoryUser and running preview returns correct counts and diffs.
    - Apply sync creates users with RoleType.EMPLOYEE, department created if missing.
    - Manager mapping works when manager_external_id references another DirectoryUser.
    - Deactivation flow: account_enabled=false updates user.is_active to False.
    - Conflict rule: role_id is not overwritten by sync.

    Use existing async test patterns from test_users.py and conftest fixtures.
  </action>
  <verify>`cd backend && pytest tests/test_directory_sync.py` passes</verify>
  <done>Sync behavior is covered by automated tests</done>
</task>

<task type="auto">
  <name>Task 2: Add frontend API/UI smoke tests</name>
  <files>frontend/src/services/__tests__/directoryApi.test.ts, frontend/src/test/mocks/handlers.ts, frontend/src/pages/__tests__/DirectoryEmulatorPage.test.tsx</files>
  <action>
    Add MSW handlers for /api/v1/directory endpoints (list, preview, apply) in handlers.ts.
    Create a directoryApi test to verify list/preview/apply return expected data.
    Add a lightweight UI test for DirectoryEmulatorPage that renders the page and shows preview counts when the button is clicked.

    Use existing test utilities and patterns from api.test.ts and component tests.
  </action>
  <verify>`cd frontend && npm run test` passes</verify>
  <done>Directory API and UI have baseline test coverage</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>AD Emulator backend + frontend + tests</what-built>
  <how-to-verify>
    1. Start backend: `cd backend && uvicorn app.main:app --reload`
    2. Start frontend: `cd frontend && npm run dev`
    3. Visit: http://localhost:5173/directory-emulator
    4. Create a directory user with a new email + department
    5. Click "Preview Sync" and confirm counts are correct
    6. Click "Apply Sync" and confirm user appears on /users
    7. Disable the user and resync; confirm user becomes inactive in /users
  </how-to-verify>
  <resume-signal>Type "approved" if the workflow passes, or list issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Backend tests pass for directory sync
- [ ] Frontend tests pass for directory API/UI
- [ ] Manual verification checklist completed
</verification>

<success_criteria>

- Automated tests validate sync mapping rules
- UI and API behave correctly under test and manual checks
- No regressions in existing test suites
  </success_criteria>

<output>
After completion, create `.planning/phases/90-ad-emulator/90-03-SUMMARY.md`
</output>
