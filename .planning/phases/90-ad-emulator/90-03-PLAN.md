---
phase: 90-ad-emulator
plan: 03
type: execute
---

<objective>
Validate the current provider-backed directory identity lifecycle.

Purpose: Ensure Graph and AD-emulator provider flows use the same RiskHub identity rules: provider search/import, local access-field ownership, admin directory checks, auto-deprovision, break-glass re-enable, and frontend Add from AD / Check AD behavior.
Output: Backend regressions, frontend service/UI regressions, and a current manual verification checklist.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/BUSINESS_LOGIC.md
@backend/app/api/v1/endpoints/directory.py
@backend/app/api/v1/endpoints/admin/directory_sync.py
@backend/app/services/directory_provider_service.py
@backend/app/services/directory_identity_service.py
@backend/app/services/ad_deprovision_service.py
@frontend/src/pages/UsersPage.tsx
@frontend/src/components/users/DirectoryUserImportPanel.tsx
@frontend/src/components/access/UsersTable.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend directory identity lifecycle regression coverage</name>
  <files>tests/backend/pytest/test_directory_import.py, tests/backend/pytest/test_ad_deprovision_service.py, tests/backend/pytest/test_admin_directory_sync.py, tests/backend/pytest/test_access_management.py</files>
  <action>
    Add or update tests for:
    - provider-backed directory import creates a new user with safe default role and initial department seeding
    - reimport of an existing linked or email-matched user preserves RiskHub-local `department_id`, `role_id`, `access_scope`, and `manager_id`
    - admin `check-user` / `check-all` preserve local access fields while updating directory-owned profile metadata
    - disabled or missing provider users auto-deprovision, revoke sessions, and flag orphan follow-up
    - break-glass re-enable is platform-admin-only, audited, expires within 1-24 hours, and returns the current response contract
    - access-user responses include backend capability metadata for active-status changes and break-glass eligibility
  </action>
  <verify>`cd backend && ./venv/bin/pytest -q ../tests/backend/pytest/test_directory_import.py ../tests/backend/pytest/test_ad_deprovision_service.py ../tests/backend/pytest/test_admin_directory_sync.py ../tests/backend/pytest/test_access_management.py ../tests/backend/pytest/test_users.py` passes</verify>
  <done>Backend directory lifecycle invariants are covered by current automated tests.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend capability-first directory lifecycle UX coverage</name>
  <files>tests/frontend/unit/src/pages/__tests__/UsersPage.modes.test.tsx, tests/frontend/unit/src/components/users/__tests__/DirectoryUserImportPanel.test.tsx, tests/frontend/unit/src/pages/__tests__/UsersPage.sso-cta.test.tsx</files>
  <action>
    Add or update tests for:
    - Add from AD remains available only for allowed auth/admin modes
    - Check AD actions handle success and provider errors without leaking raw IDs
    - activate/deactivate row actions prefer backend capability metadata with local fallback only when metadata is absent
    - backend active-status rejection messages render to the user
    - break-glass eligible users expose a reason/expiry modal and submit to `/api/v1/admin/directory/break-glass-enable/{user_id}`
  </action>
  <verify>`cd frontend && npm run test:run -- ../tests/frontend/unit/src/pages/__tests__/UsersPage.modes.test.tsx ../tests/frontend/unit/src/pages/__tests__/UsersPage.sso-cta.test.tsx ../tests/frontend/unit/src/components/users/__tests__/DirectoryUserImportPanel.test.tsx` passes</verify>
  <done>Frontend directory identity actions mirror backend capabilities and expose the break-glass path.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Provider-backed directory identity lifecycle hardening</what-built>
  <how-to-verify>
    1. Start local dev using `./scripts/dev.sh --daemon`.
    2. Sign in as platform Admin and open `/users`.
    3. Use Add from AD to import a directory user; confirm the user appears on `/users`.
    4. As CRO, change that user's department; as Admin, run Check AD; confirm the CRO-selected department remains unchanged.
    5. Configure or simulate a disabled directory account; run Check AD; confirm the user deactivates and session rows are no longer active.
    6. Use Break-glass on the deprovisioned user with a reason and expiry; confirm the user is active and the directory status remains visible.
  </how-to-verify>
  <resume-signal>Type "approved" if the lifecycle passes, or list issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [x] Backend directory lifecycle tests pass
- [x] Frontend directory lifecycle tests pass
- [x] `cd frontend && npx tsc --noEmit` passes
- [x] `git diff --check` passes
- [x] Manual verification checklist completed or explicitly deferred
</verification>

<success_criteria>

- Current provider-backed directory workflow is the documented Phase 90 validation target.
- Deleted emulator sync preview/apply tables and routes are not restored.
- RiskHub-local access fields remain authoritative after import and directory checks.
- Frontend directory lifecycle actions are backend-capability-first and include break-glass UX.
  </success_criteria>

<output>
After completion, create `.planning/phases/90-ad-emulator/90-03-SUMMARY.md`.
</output>
