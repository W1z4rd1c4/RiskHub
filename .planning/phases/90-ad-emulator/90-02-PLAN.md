---
phase: 90-ad-emulator
plan: 02
type: execute
---

<objective>
Build the frontend management UI for the AD Emulator to manage directory users and run sync preview/apply.

Purpose: Let admins create, edit, and validate directory data, then verify how it will map into RiskHub users.
Output: Directory emulator page, API client, and navigation entry.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-ad-emulator/90-DISCOVERY.md
@frontend/src/pages/UsersPage.tsx
@frontend/src/components/layout/MainLayout.tsx
@frontend/src/services/userApi.ts
@frontend/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add directory types and API client</name>
  <files>frontend/src/types/directory.ts, frontend/src/services/directoryApi.ts</files>
  <action>
    Create TypeScript types aligned with backend schemas:
    - DirectoryUserRead (id, external_id, user_principal_name, email, display_name, given_name, surname, department, job_title, manager_external_id, account_enabled, user_id, created_at, updated_at)
    - DirectoryUserCreate/Update (allow partial updates)
    - DirectorySyncPreview (created_count, updated_count, deactivated_count, error_count, diffs[])
    - DirectorySyncLogRead (started_at, finished_at, status, counts, errors)

    Implement directoryApi with functions:
    - listDirectoryUsers(filters)
    - createDirectoryUser(payload)
    - updateDirectoryUser(id, payload)
    - deactivateDirectoryUser(id)
    - previewDirectorySync()
    - applyDirectorySync()
    - listDirectorySyncHistory()

    Use apiClient like other services, pass query params for filters.
  </action>
  <verify>TypeScript builds and directoryApi functions compile</verify>
  <done>Types and API client exist with all endpoints covered</done>
</task>

<task type="auto">
  <name>Task 2: Create Directory Emulator page UI</name>
  <files>frontend/src/pages/DirectoryEmulatorPage.tsx</files>
  <action>
    Build a page with the same design language as UsersPage:
    - Header: "Directory Emulator" with a short subtitle
    - Stats cards: total directory users, active, disabled
    - Table listing directory users with columns: display name, email/UPN, department, manager_external_id, status, last updated
    - Search + filters (department, status)
    - "Add Directory User" action that opens a modal or side panel form
    - Inline edit action per row (edit fields + status toggle)

    Add a Sync panel:
    - "Preview Sync" button → fetch preview and display counts + a diff list (created/updated/deactivated)
    - "Apply Sync" button → runs sync and shows results + toast/alert
    - Sync history list showing last 5 runs with status + counts

    Handle loading and error states (simple alert/banner).
  </action>
  <verify>Page renders without errors and loads directory users list</verify>
  <done>Admins can manage directory users and see preview/apply sync results</done>
</task>

<task type="auto">
  <name>Task 3: Add routing + navigation entry</name>
  <files>frontend/src/pages/index.ts, frontend/src/App.tsx, frontend/src/components/layout/MainLayout.tsx</files>
  <action>
    - Export DirectoryEmulatorPage from pages/index.ts
    - Add route `/directory-emulator` in App.tsx (inside protected layout)
    - Add sidebar/nav item in MainLayout with an icon (e.g., Users/Server)
    - Place it near Users section for admin-only tools (respect canManageUsers if available)
  </action>
  <verify>Route is reachable at /directory-emulator and nav item appears for admins</verify>
  <done>Directory emulator UI is accessible from the app layout</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run dev` renders /directory-emulator without console errors
- [ ] Create/edit directory user succeeds and table refreshes
- [ ] Preview + Apply sync buttons show results
</verification>

<success_criteria>

- Directory emulator UI exists and is wired to backend API
- Admins can manage directory users and run sync operations
- Navigation entry added and route works
  </success_criteria>

<output>
After completion, create `.planning/phases/90-ad-emulator/90-02-SUMMARY.md`
</output>
