---
phase: 90-ad-integration
type: execute
---

<objective>
Create the Governance page in RiskHub frontend with navigation, layout, and orphan overview.

Purpose: Add a new "Governance" tab to the main navigation that displays orphaned items requiring administrative attention. This provides visibility into risks and controls that lost their owners.

Output: New Governance page with tab navigation, orphan statistics dashboard, and list view.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-phase.md
@.agent/references/plan-format.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@frontend/src/App.tsx
@frontend/src/components/Navigation.tsx
@frontend/src/pages/DashboardPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Governance route and navigation</name>
  <files>frontend/src/App.tsx, frontend/src/components/Navigation.tsx (or Sidebar.tsx)</files>
  <action>
    1. Add new route in App.tsx:
       ```tsx
       <Route path="/governance" element={<GovernancePage />} />
       ```
    
    2. Add navigation item in sidebar/navigation:
       - Label: "Governance"
       - Icon: Shield or Scales icon (use existing icon library)
       - Position: After "Workflow" in the menu
       - Show badge with pending orphan count (similar to notifications)
    
    3. Create placeholder GovernancePage.tsx in pages/ directory
    
    4. Import and lazy-load if following existing patterns
  </action>
  <verify>
    1. Run frontend: npm run dev
    2. Navigate to http://localhost:5173
    3. Verify "Governance" appears in sidebar
    4. Click it - should navigate to /governance
  </verify>
  <done>Governance route exists and is accessible from navigation.</done>
</task>

<task type="auto">
  <name>Task 2: Create orphaned items API service</name>
  <files>frontend/src/services/orphanedItemsApi.ts, frontend/src/types/orphanedItem.ts</files>
  <action>
    Create TypeScript types:
    ```typescript
    interface OrphanedItem {
      id: number;
      item_type: "risk" | "control" | "kri";
      item_id: number;
      item_name: string;
      item_identifier: string;
      department_name: string | null;
      previous_owner_name: string;
      previous_owner_email: string;
      orphaned_at: string;
      status: "pending" | "resolved";
    }
    
    interface OrphanStats {
      total_pending: number;
      by_type: {
        risks: number;
        controls: number;
        kris: number;
      };
    }
    ```
    
    Create API service:
    - `getOrphanedItems(params?: { item_type?: string, status?: string })` 
    - `getOrphanStats()`
    - `resolveOrphan(id: number, new_owner_id: number)`
    - `getOrphanDetail(id: number)`
  </action>
  <verify>Import service in browser console and verify no TypeScript errors.</verify>
  <done>TypeScript types and API service for orphaned items created.</done>
</task>

<task type="auto">
  <name>Task 3: Build Governance page layout with stats cards</name>
  <files>frontend/src/pages/GovernancePage.tsx</files>
  <action>
    Create GovernancePage with:
    
    1. Page header with title "Governance" and description
    
    2. Stats cards row (similar to Dashboard pattern):
       - Total Pending Orphans (with icon)
       - Orphaned Risks count
       - Orphaned Controls count
       - Orphaned KRIs count (if applicable)
    
    3. Use existing card styling (glassmorphism from other pages)
    
    4. Fetch stats on mount using useEffect and orphanedItemsApi
    
    5. Loading states and error handling
    
    Follow existing page patterns (DashboardPage, WorkflowPage).
  </action>
  <verify>
    1. Navigate to /governance
    2. Verify stats cards render (may be 0 if no orphans)
    3. Verify styling matches other pages
    4. Check browser console for errors
  </verify>
  <done>Governance page renders with stats cards showing orphan counts.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Governance tab appears in navigation
- [ ] Route /governance works
- [ ] Stats cards display orphan counts
- [ ] No console errors
- [ ] Styling consistent with rest of app
</verification>

<success_criteria>
- Governance page accessible from main navigation
- Orphan statistics displayed prominently
- Page follows existing design patterns
- API integration working
</success_criteria>

<output>
After completion, create `.planning/phases/90-ad-integration/90-08-SUMMARY.md`

## Next Step
Ready for 90-09-PLAN.md (Governance Orphan List & Resolution UI)
</output>
