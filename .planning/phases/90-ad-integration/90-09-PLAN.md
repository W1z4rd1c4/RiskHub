---
phase: 90-ad-integration
type: execute
---

<objective>
Build the orphan list view and resolution UI for the Governance page.

Purpose: Allow administrators to see all orphaned items in a filterable table, view details, and assign new owners to resolve the orphan status.

Output: Orphan list table with filters, detail modal, and owner assignment flow.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-phase.md
@.agent/references/plan-format.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@frontend/src/pages/GovernancePage.tsx
@frontend/src/services/orphanedItemsApi.ts
@frontend/src/components/tables/ (existing table patterns)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OrphanedItemsTable component</name>
  <files>frontend/src/components/governance/OrphanedItemsTable.tsx</files>
  <action>
    Create a table component displaying orphaned items:
    
    1. Columns:
       - Type (icon for risk/control/kri + label)
       - Item Name / Identifier
       - Department
       - Previous Owner
       - Orphaned Date (relative time, e.g., "2 days ago")
       - Actions (View, Resolve buttons)
    
    2. Features:
       - Filter bar: Type dropdown (All, Risks, Controls, KRIs)
       - Sorting by orphaned date (newest first default)
       - Pagination if many items
       - Empty state: "No orphaned items - all risks and controls have owners"
    
    3. Row styling:
       - Use subtle warning color for older orphans (>7 days)
       - Hover effects consistent with other tables
    
    4. Follow existing table patterns from RisksPage, ControlsPage
  </action>
  <verify>
    1. Add component to GovernancePage
    2. Verify table renders with mock or real data
    3. Test filter functionality
    4. Check responsive behavior
  </verify>
  <done>Orphan table component with filters, sorting, pagination.</done>
</task>

<task type="auto">
  <name>Task 2: Create ResolveOrphanModal component</name>
  <files>frontend/src/components/governance/ResolveOrphanModal.tsx</files>
  <action>
    Create modal for resolving an orphaned item:
    
    1. Modal shows:
       - Item details (type, name, identifier)
       - Previous owner info
       - Department (read-only)
       - "Orphaned since" date
    
    2. Owner selection:
       - User dropdown/searchable select
       - Filter users by department (suggest same department first)
       - Show user's role and current owned item count
    
    3. Actions:
       - "Assign Owner" button (calls resolveOrphan API)
       - "Cancel" button
    
    4. Success handling:
       - Show toast notification
       - Close modal
       - Refresh orphan list
    
    Use existing modal patterns from the codebase.
  </action>
  <verify>
    1. Click "Resolve" on an orphan row
    2. Modal opens with item details
    3. Select a new owner
    4. Submit and verify item disappears from list
  </verify>
  <done>Resolve modal with user selection and API integration.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate table and modal into GovernancePage</name>
  <files>frontend/src/pages/GovernancePage.tsx</files>
  <action>
    Complete the Governance page:
    
    1. Add OrphanedItemsTable below stats cards
    
    2. Manage state:
       - Selected orphan for modal
       - Modal open/close state
       - Refresh trigger after resolution
    
    3. Wire up:
       - Table "Resolve" button opens modal
       - Modal submission triggers API call
       - Success refreshes stats and table
    
    4. Add page-level loading state
    
    5. Add refresh button to manually reload data
  </action>
  <verify>
    1. Full end-to-end test:
       - Create orphan (deactivate user who owns risk)
       - Navigate to /governance
       - See orphan in table
       - Click Resolve
       - Assign new owner
       - Verify orphan removed from list
       - Verify stats updated
  </verify>
  <done>Complete Governance page with working orphan resolution flow.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Orphan table displays items with all columns
- [ ] Filters work correctly
- [ ] Resolve modal opens and submits
- [ ] Orphan disappears after resolution
- [ ] Stats update after resolution
- [ ] Consistent styling
</verification>

<success_criteria>
- Admins can view all orphaned items in one place
- Easy-to-use resolution workflow
- Real-time updates after actions
- Complete governance oversight capability
</success_criteria>

<output>
After completion, create `.planning/phases/90-ad-integration/90-09-SUMMARY.md`

## Next Step
Ready for 90-10-PLAN.md (End-to-End Testing & Polish)
</output>
