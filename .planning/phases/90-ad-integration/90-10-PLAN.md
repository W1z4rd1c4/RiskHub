---
phase: 90-ad-integration
type: execute
---

<objective>
Complete end-to-end testing, polish, and documentation for the AD integration feature.

Purpose: Ensure the entire AD→RiskHub sync flow works reliably, with proper error handling, user-friendly UI, and documentation for future maintenance.

Output: Tested integration, polished UI, updated documentation.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-phase.md
@.agent/references/plan-format.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/app/services/directory_sync_service.py
@frontend/src/pages/GovernancePage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration test script</name>
  <files>scripts/test_ad_integration.py</files>
  <action>
    Create a comprehensive test script that validates the full flow:
    
    1. Setup:
       - Start with clean state (clear orphaned_items)
       - Create test user in AD Emulator
    
    2. Test Cases:
       a) Webhook create flow:
          - Create user in AD Emulator
          - Verify webhook received by RiskHub
          - Verify user created in RiskHub
       
       b) Webhook update flow:
          - Update user in AD Emulator
          - Verify changes reflected in RiskHub
       
       c) Orphan creation flow:
          - Assign risk to user
          - Deactivate user in AD Emulator
          - Verify orphaned_item created
          - Verify notification created for admins
       
       d) Orphan resolution flow:
          - Call resolve API
          - Verify risk has new owner
          - Verify orphan marked as resolved
    
    3. Cleanup:
       - Remove test data
    
    Use httpx for HTTP calls, clear output for pass/fail.
  </action>
  <verify>Run: python scripts/test_ad_integration.py - all tests pass</verify>
  <done>Integration test script validates full AD sync flow.</done>
</task>

<task type="auto">
  <name>Task 2: Error handling and edge cases</name>
  <files>backend/app/api/v1/endpoints/directory.py, backend/app/services/orphaned_item_service.py</files>
  <action>
    Harden error handling:
    
    1. Webhook endpoint:
       - Handle malformed payloads gracefully
       - Return 400 for invalid event types
       - Log all errors with context
    
    2. Sync service:
       - Handle missing/invalid external_id
       - Handle non-existent departments gracefully
       - Rollback on partial failures
    
    3. Orphan service:
       - Handle already-resolved orphans
       - Validate new_owner_id exists and is active
       - Prevent assigning to deactivated users
    
    4. Add proper error messages in API responses
  </action>
  <verify>
    1. Send malformed webhook - verify 400 response
    2. Try to resolve non-existent orphan - verify 404
    3. Try to assign to inactive user - verify validation error
  </verify>
  <done>Robust error handling for all edge cases.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete AD Integration with Governance UI</what-built>
  <how-to-verify>
    1. Start all services:
       - docker-compose up -d (database)
       - Backend: cd backend && source venv/bin/activate && uvicorn app.main:app --reload --port 8000
       - Frontend: cd frontend && npm run dev
       - AD Emulator Backend: cd "AD Emulator/backend" && source venv/bin/activate && uvicorn app.main:app --port 8001
       - AD Emulator Frontend: cd "AD Emulator/frontend" && npm run dev
    
    2. In AD Emulator (http://localhost:5174):
       - Create a new user with department "Operations"
       - Note the user's email
    
    3. In RiskHub (http://localhost:5173):
       - Go to Directory Integration, verify user appears (may need sync for first time)
       - Assign the new user as owner of any risk
       
    4. In AD Emulator:
       - Deactivate the user you created
       
    5. In RiskHub:
       - Check notification bell for orphan notification
       - Go to Governance tab
       - Verify the risk appears in orphaned items
       - Click "Resolve" and assign a new owner
       - Verify it disappears from the list
    
    6. Confirm: Governance stats update, no console errors
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Integration test script passes
- [ ] Error cases handled gracefully
- [ ] Human verification of full flow approved
</verification>

<success_criteria>
- Complete AD integration working end-to-end
- Governance page functional for admin oversight
- Robust error handling
- User approved the implementation
</success_criteria>

<output>
After completion, create `.planning/phases/90-ad-integration/90-10-SUMMARY.md`

## Next Step
Phase 90 (AD Integration) complete. Update ROADMAP.md and STATE.md.
</output>
