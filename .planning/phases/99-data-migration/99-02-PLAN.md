# Phase 99-02: Controls Migration

## Objective
Migrate controls from `Katalog kontrol Provoz_06 2025.xlsx` to the database using the 13-point control structure.

## Execution Context
- Source: `Katalog kontrol Provoz_06 2025.xlsx` (21 rows, 15 columns)
- Models: `Control`, `Department`, `User`

## Column Mapping (Excel → Database)

| Excel Column (CZ) | DB Field | Notes |
|---|---|---|
| Pořadové číslo | (row ID) | Not stored |
| Název kontroly | name | |
| Popis kontroly | description | |
| Zdroj dat | data_source | |
| Směrnice | methodology_reference | |
| Forma | control_form | 'Manuální' → 'manual', 'Automatická' → 'automatic' |
| Vlastník procesu | process_owner_position | |
| Vlastník kontroly | (need to match to user) | control_owner_id FK |
| Pozice vykonávající kontrolu | executor_position | |
| Frekvence | frequency | 'měsíčně' → 'monthly' etc. |
| Výstup kontroly | output_description | |
| Reportovací povinnost | report_recipient | |
| Dokumentace | documentation_location | |
| Která rizika snižuje | (Control-Risk links) | Foreign keys to risks |
| Nápravné opatření | (notes) | Could add to description |

## Frequency Mapping
```python
FREQ_MAP = {
    'denně': 'daily', 'týdně': 'weekly', 'měsíčně': 'monthly',
    'čtvrtletně': 'quarterly', 'ročně': 'annually', 'ad hoc': 'ad_hoc'
}
```

## Tasks

- [ ] 1. Create `migrate_controls.py` script with proper column mapping
- [ ] 2. Map 'Vlastník kontroly' to existing users or default to dept head
- [ ] 3. Parse 'Která rizika snižuje' to create ControlRiskLink entries
- [ ] 4. Run migration and verify control count
- [ ] 5. Verify department associations (all are Provoz/OPS)

## Verification
```bash
docker exec riskhub-db psql -U riskhub -c "SELECT COUNT(*) FROM controls;"
docker exec riskhub-db psql -U riskhub -c "SELECT COUNT(*) FROM control_risk_links;"
```

## Success Criteria
- All 21 controls imported from Excel
- Controls linked to OPS department
- Control-Risk links created where specified
