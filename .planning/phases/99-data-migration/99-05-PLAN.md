---
phase: 99-data-migration
plan: 05
type: execute
---

<objective>
Create standalone AD Emulator frontend with premium glassmorphism design.

Purpose: Build a separate React/Vite frontend for the AD Emulator that mirrors RiskHub's premium design but operates independently, allowing directory user management without RiskHub.
Output: Standalone React app in `/AD Emulator/frontend` with directory user management UI.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@frontend/src/pages/DirectoryEmulatorPage.tsx
@frontend/src/services/directoryApi.ts
@frontend/src/types/directory.ts
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize AD Emulator React/Vite frontend</name>
  <files>AD Emulator/frontend/package.json, AD Emulator/frontend/vite.config.ts, AD Emulator/frontend/tsconfig.json, AD Emulator/frontend/index.html, AD Emulator/frontend/src/main.tsx, AD Emulator/frontend/src/App.tsx</files>
  <action>
    Create a new React+Vite+TypeScript project in `AD Emulator/frontend/`:
    
    1. Initialize with: `npm create vite@latest . -- --template react-ts`
    
    2. Install dependencies:
       - tailwindcss, postcss, autoprefixer (styling)
       - lucide-react (icons)
       - @tanstack/react-query (data fetching, optional)
       - clsx or tailwind-merge for class merging
       
    3. Configure Vite to run on port 5174 (different from RiskHub's 5173)
    
    4. Set up basic App.tsx with routing placeholder
    
    5. Configure tailwind.config.js with similar color scheme to RiskHub:
       - Dark theme by default
       - Accent color (can be different from RiskHub, e.g. purple/violet for AD branding)
       - Glass morphism utilities
    
    The frontend will connect to AD Emulator backend at localhost:8001.
  </action>
  <verify>
    - Run: `cd "AD Emulator/frontend" && npm run dev -- --port 5174`
    - Access: http://localhost:5174 shows the app
  </verify>
  <done>AD Emulator frontend runs on port 5174 with Tailwind configured</done>
</task>

<task type="auto">
  <name>Task 2: Create premium design system and base styles</name>
  <files>AD Emulator/frontend/src/index.css, AD Emulator/frontend/src/lib/utils.ts, AD Emulator/frontend/tailwind.config.js</files>
  <action>
    Port the premium design system from RiskHub with AD-specific branding:
    
    1. `src/index.css` - Copy and adapt RiskHub's premium styles:
       - Mesh gradient background (use purple/violet tones for AD branding)
       - Glassmorphism utilities (.glass-card, .glass-panel)
       - Custom animations (fade-in, slide-in)
       - Form input styles
       - Button styles (primary, secondary, danger)
       - Table styles
       
    2. `tailwind.config.js` - Configure:
       - Colors: purple-based accent instead of RiskHub's teal
       - Dark mode as default
       - Container and typography settings
    
    3. `src/lib/utils.ts` - Copy `cn()` utility from RiskHub for class merging
    
    The design should feel like RiskHub but with distinct AD Emulator branding
    (purple tones suggest enterprise/directory theme).
  </action>
  <verify>
    - Gradient background visible
    - Glass card styling works
    - Purple accent color applied
  </verify>
  <done>AD Emulator has premium glassmorphism design with purple AD branding</done>
</task>

<task type="auto">
  <name>Task 3: Build Directory User Management UI</name>
  <files>AD Emulator/frontend/src/services/api.ts, AD Emulator/frontend/src/types/directory.ts, AD Emulator/frontend/src/pages/DirectoryUsersPage.tsx, AD Emulator/frontend/src/components/UserForm.tsx, AD Emulator/frontend/src/components/Layout.tsx</files>
  <action>
    Create the directory user management interface:
    
    1. `src/services/api.ts` - API client:
       - Configure base URL: http://localhost:8001/api/v1
       - Methods: listUsers, getUser, createUser, updateUser, deleteUser
       
    2. `src/types/directory.ts` - TypeScript types:
       - DirectoryUser, DirectoryUserCreate, DirectoryUserUpdate
       
    3. `src/components/Layout.tsx` - App shell:
       - Header with AD Emulator branding
       - No sidebar (simpler than RiskHub, single-purpose app)
       - Main content area
       - Footer with connection status to AD Emulator backend
    
    4. `src/pages/DirectoryUsersPage.tsx` - Main management UI:
       - Stats cards: Total users, Active, Disabled
       - Search and filter bar
       - User table with columns: Name, Email, Department, Status, Actions
       - Create/Edit modal dialog
       - Deactivate/Activate toggle
       - All styled with glassmorphism cards
       
    5. `src/components/UserForm.tsx` - Create/Edit form:
       - external_id (required, disabled on edit)
       - display_name (required)
       - email / user_principal_name (at least one required)
       - given_name, surname
       - department
       - job_title
       - manager_external_id (dropdown of other users)
       - account_enabled toggle
    
    6. Update `src/App.tsx` to render DirectoryUsersPage as main content
    
    Port the UI patterns from RiskHub's DirectoryEmulatorPage.tsx but adapt for standalone use.
    Remove any RiskHub-specific sync UI (that logic moves to RiskHub side in phase 99-06).
  </action>
  <verify>
    - User list loads and displays
    - Create user form works
    - Edit user form works
    - Disable/enable user works
    - UI has premium glassmorphism styling
  </verify>
  <done>AD Emulator has complete directory user management UI</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Frontend runs on port 5174
- [ ] Premium glassmorphism design with purple AD branding
- [ ] Can list, create, edit, and deactivate directory users
- [ ] Connects to AD Emulator backend on port 8001
</verification>

<success_criteria>

- AD Emulator frontend is a separate, runnable React application
- Uses purple/violet accent colors for AD branding (distinct from RiskHub teal)
- Runs on different port (5174) from RiskHub (5173)
- Full CRUD operations for directory users
- Premium design matching RiskHub quality
</success_criteria>

<output>
After completion, create `.planning/phases/99-data-migration/99-05-SUMMARY.md`
</output>
