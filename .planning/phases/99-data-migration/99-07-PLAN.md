---
phase: 99-data-migration
plan: 07
type: execute
---

<objective>
Harden and refine the AD Emulator integration with RiskHub to improve reliability, matching robustness, and frontend quality.

Purpose: Address critical bugs (deactivation sync), implement robust user matching via `external_id`, improve error handling/logging, and polish the AD Emulator standalone application.
Output: Hardened sync engine, consolidated user matching, externalized configuration, and polished frontend.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/app/services/directory_sync_service.py
@backend/app/integrations/ad_emulator_client.py
@backend/app/models/user.py
@AD Emulator/backend/app/config.py
@AD Emulator/frontend/src/pages/DirectoryUsersPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve Critical Sync Bugs</name>
  <files>backend/app/integrations/ad_emulator_client.py</files>
  <action>
    Fix the bug preventing deactivation sync:
    1. Remove `active=True` filter from `get_users` request.
    2. Add reasonable timeouts (10s) to `httpx.AsyncClient` to prevent hanging.
  </action>
  <verify>
    - `get_users` returns all users (active and disabled) from AD Emulator.
    - RiskHub sync recognizes "deactivate" action for disabled directory users.
  </verify>
  <done>Deactivation sync bug fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Robust User Matching (`external_id`)</name>
  <files>backend/app/models/user.py, backend/app/services/directory_sync_service.py, backend/alembic/versions/</files>
  <action>
    Hardened user matching logic to avoid duplicates and incorrect mappings:
    1. Add `external_id` (Indexed, Unique, String) to RiskHub `User` model.
    2. Create and apply Alembic migration.
    3. Update `DirectorySyncService` to:
       - Prioritize matching by `external_id`.
       - Fallback to email/UPN matching and "auto-link" by updating the `external_id` of the local user.
       - Prevent conflicts (e.g., email already linked to another `external_id`).
  </action>
  <verify>
    - Migration applied successfully.
    - Sync matches users correctly even if email changes (as long as `external_id` is stable).
    - Existing users without `external_id` are linked on first sync.
  </verify>
  <done>Robust matching via `external_id` implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Harden Error Handling & Logging</name>
  <files>backend/app/services/directory_sync_service.py, backend/app/models/directory_sync_log.py</files>
  <action>
    Improve reliability of the sync operation:
    1. Log fetch failures directly to `DirectorySyncLog`.
    2. Ensure DB transactions are committed/rolled back correctly.
    3. Fix case sensitivity in `DirectorySyncStatus` enum (use lowercase to match DB constraint).
    4. Guard against data inconsistencies (missing `external_id` in directory payload).
  </action>
  <verify>
    - Sync log shows "failed" status and error details if AD Emulator is down.
    - Partial successes (some users synced, some failed) are correctly logged.
  </verify>
  <done>Hardened sync engine with better reporting.</done>
</task>

<task type="auto">
  <name>Task 4: Externalize AD Emulator Configuration</name>
  <files>AD Emulator/backend/app/config.py, AD Emulator/frontend/src/services/api.ts</files>
  <action>
    Make the app production-ready:
    1. Update backend `Settings` to allow overrides for `DATABASE_URL` and `SECRET_KEY` via env vars.
    2. Use `import.meta.env.VITE_API_BASE_URL` in frontend with fallback.
  </action>
  <verify>
    - Backend picks up env vars correctly.
    - Frontend base URL can be controlled via `.env`.
  </verify>
  <done>Configuration externalized.</done>
</task>

<task type="auto">
  <name>Task 5: Frontend Interface Polish</name>
  <files>AD Emulator/frontend/src/pages/DirectoryUsersPage.tsx, AD Emulator/frontend/src/components/UserForm.tsx</files>
  <action>
    Improve code quality and accessibility:
    1. Fix `useEffect` dependencies in `DirectoryUsersPage` (wrap `fetchUsers` in `useCallback`).
    2. Add keyboard accessibility (Escape key) and focus management to `UserForm` modal.
    3. Ensure consistent layout styles (import `App.css`).
  </action>
  <verify>
    - No console warnings about missing dependencies.
    - Escape key closes modal.
  </verify>
  <done>Frontend interface polished.</done>
</task>

</tasks>

<verification>
- [ ] User deactivation in AD Emulator reflects as "DEACTIVATE" action in RiskHub sync.
- [ ] `external_id` is populated in RiskHub `users` table after sync.
- [ ] Sync history correctly logs successes and failures.
- [ ] All AD Emulator endpoints and forms work as expected.
</verification>

<success_criteria>
- Integration is robust enough for varied data states.
- System is easily configurable via environment variables.
- User experience in AD Emulator feels premium and accessible.
</success_criteria>
