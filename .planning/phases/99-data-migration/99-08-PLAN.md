# Phase 99-08: Risk Naming Improvement

## Goal
Backfill proper, descriptive names for all 83 risks in the database to replace generic process-based names.

## Proposed Changes

### Data Migration
- Create `backend/scripts/migrate_risk_names.py` to extract names from descriptions.
- Update `risks` table set `name` field.

## Verification
- Run script and check console output.
- Database query: `SELECT risk_id_code, name FROM risks LIMIT 20;`
