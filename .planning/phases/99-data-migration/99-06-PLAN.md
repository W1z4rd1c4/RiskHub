---
phase: 99-data-migration
plan: 06
type: execute
---

<objective>
Integrate RiskHub with standalone AD Emulator via HTTP API and clean up embedded code.

Purpose: Make RiskHub fetch directory users from the external AD Emulator API and sync them into its local users table. Remove the embedded DirectoryUser model and related code from RiskHub since that now lives in the AD Emulator.
Output: RiskHub syncs users from external AD Emulator, embedded directory code removed.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/app/services/directory_sync_service.py
@backend/app/api/v1/endpoints/directory.py
@backend/app/api/v1/router.py
@frontend/src/pages/DirectoryEmulatorPage.tsx
@frontend/src/components/layout/Sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AD Emulator client service in RiskHub</name>
  <files>backend/app/services/ad_emulator_client.py, backend/app/config.py</files>
  <action>
    Create an HTTP client service in RiskHub to fetch directory users from the standalone AD Emulator:
    
    1. Update `backend/app/config.py`:
       - Add AD_EMULATOR_URL setting (default: http://localhost:8001/api/v1)
       - Add AD_EMULATOR_ENABLED setting (default: True for dev)
    
    2. Create `backend/app/services/ad_emulator_client.py`:
       - ADEmulatorClient class using httpx.AsyncClient
       - Methods:
         - `list_users()` -> list[dict]: Fetch all active directory users from AD Emulator
         - `get_user(external_id: str)` -> dict | None: Fetch single user
       - Handle connection errors gracefully (AD Emulator might not be running)
       - Add timeout configuration (5 seconds default)
       - Log fetch results for debugging
    
    This client replaces direct database access to DirectoryUser table.
    RiskHub now treats directory data as EXTERNAL, fetched via HTTP.
  </action>
  <verify>
    - Client can be imported: `python -c "from app.services.ad_emulator_client import ADEmulatorClient; print('OK')"`
    - With AD Emulator running, client fetches users successfully
  </verify>
  <done>RiskHub has HTTP client for fetching from AD Emulator</done>
</task>

<task type="auto">
  <name>Task 2: Refactor DirectorySyncService to use AD Emulator client</name>
  <files>backend/app/services/directory_sync_service.py</files>
  <action>
    Modify the existing DirectorySyncService to fetch from AD Emulator API instead of local DirectoryUser table:
    
    1. Remove all direct queries to DirectoryUser model
    
    2. Replace with ADEmulatorClient.list_users() call
    
    3. The sync logic remains the same:
       - Compare fetched directory users with existing RiskHub User records
       - Match by email (primary) or user_principal_name (fallback)
       - Create new User records for unmatched directory users
       - Update existing User fields: display_name, department, is_active
       - Preserve role_id (never overwrite existing role assignments)
       - Handle manager mapping
    
    4. Update preview_sync() to show what WOULD change
    
    5. Update apply_sync() to actually make changes
    
    6. Keep DirectorySyncLog model in RiskHub - it tracks sync history for RiskHub admin
    
    Key changes:
    - Data source: AD Emulator API (external) instead of local DirectoryUser table
    - DirectoryUser model in RiskHub will be deprecated/removed
    - The "directory" pages in RiskHub now only show "Sync" controls, not user management
  </action>
  <verify>
    - With AD Emulator running, preview_sync() returns expected diff
    - apply_sync() creates/updates RiskHub users from directory data
  </verify>
  <done>DirectorySyncService fetches from external AD Emulator</done>
</task>

<task type="auto">
  <name>Task 3: Update RiskHub Directory endpoints and frontend</name>
  <files>backend/app/api/v1/endpoints/directory.py, backend/app/api/v1/router.py, frontend/src/pages/DirectoryEmulatorPage.tsx, frontend/src/components/layout/Sidebar.tsx</files>
  <action>
    Simplify RiskHub's directory-related code since user management now happens in AD Emulator:
    
    1. `backend/app/api/v1/endpoints/directory.py`:
       - REMOVE endpoints: POST/PATCH/DELETE for directory users (now in AD Emulator)
       - KEEP endpoints:
         - GET /directory/status - Check AD Emulator connection health
         - POST /directory/sync/preview - Preview what sync would do
         - POST /directory/sync/apply - Apply sync from AD Emulator
         - GET /directory/sync/history - View past sync runs
       - ADD endpoint:
         - GET /directory/users - Proxy to fetch users from AD Emulator (read-only view)
    
    2. `frontend/src/pages/DirectoryEmulatorPage.tsx`:
       - Rename to `DirectorySyncPage.tsx` (more accurate name)
       - REMOVE: Create/Edit directory user form
       - REMOVE: User table with edit/delete buttons
       - KEEP: Connection status indicator (AD Emulator reachable?)
       - KEEP: Sync preview panel
       - KEEP: Apply sync button
       - KEEP: Sync history
       - ADD: Link/button to open AD Emulator UI (http://localhost:5174)
       - UPDATE: Read-only view of directory users (fetched from AD Emulator)
    
    3. `frontend/src/components/layout/Sidebar.tsx`:
       - Rename nav item from "Directory Emulator" to "AD Sync"
       - Keep in admin section
    
    4. Update route in App.tsx if page was renamed
    
    The RiskHub UI now focuses on SYNC operations, not directory management.
    Users go to AD Emulator app (port 5174) to manage directory entries.
  </action>
  <verify>
    - RiskHub /directory/sync/preview works when AD Emulator is running
    - RiskHub /directory/sync/apply syncs users correctly
    - UI shows connection status and sync controls
    - No directory user CRUD in RiskHub
  </verify>
  <done>RiskHub directory features simplified to sync-only</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] RiskHub can connect to AD Emulator API
- [ ] Preview sync shows differences between AD Emulator and RiskHub users
- [ ] Apply sync creates/updates RiskHub users from AD Emulator
- [ ] RiskHub no longer has directory user management (moved to AD Emulator)
- [ ] Sidebar shows "AD Sync" instead of "Directory Emulator"
</verification>

<success_criteria>

- RiskHub treats AD Emulator as external service (HTTP API)
- Directory user management is done in AD Emulator app (port 5174)
- RiskHub only handles sync operations from AD Emulator
- Sync logic works correctly: preview, apply, history
- Clean separation of concerns between the two applications
</success_criteria>

<output>
After completion, create `.planning/phases/99-data-migration/99-06-SUMMARY.md`
</output>
