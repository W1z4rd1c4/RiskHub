---
phase: 18-vendor-risk
plan: 18-09
title: Reporting (annual management report) + exports (incl. DORA register)
type: execute
wave: 3
depends_on:
  - 18-01
  - 18-04
  - 18-08
autonomous: true
files_modified:
  - backend/app/api/v1/endpoints/vendor_reports.py
  - backend/app/services/vendor_reporting_service.py
  - backend/app/api/v1/endpoints/reports.py
  - backend/app/schemas/vendor_reports.py
  - frontend/src/pages/VendorReportsPage.tsx
  - frontend/src/services/vendorReportApi.ts
  - frontend/src/types/vendorReport.ts
---

<objective>
Deliver PI‑18‑03 reporting outcomes:
- Generate the annual “vendor risk management report” that includes the required fields (vendor list, outsourcing owners, last review, major breaches, major incidents, process evaluation).
- Provide exports (PDF/Excel) and a DORA “Register of Information” export dataset (at least MVP columns).
</objective>

<context>
@.planning/phases/18-vendor-risk/18-RESEARCH.md
@backend/app/api/v1/endpoints/reports.py
@backend/app/services/report_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define report schema and minimum content (PI-aligned)</name>
  <action>
Implement a single “Annual Vendor Management Report” definition that includes:
- Vendor list with flags:
  - supports critical/important functions
  - DORA relevant
  - significant vendor
- Outsourcing owner per vendor
- Date of last documentation review / last assessment decision
- Major breaches (contract/regulatory) count + details (from incidents)
- Major security/operational incidents caused by ICT vendor (from incidents)
- “Process evaluation” section: list counts + gaps (e.g., missing exit plans, overdue reassessments)

Keep the report deterministic and filterable by year.
  </action>
  <done>Report schema is defined and backed by a single aggregation service.</done>
</task>

<task type="auto">
  <name>Task 2: Export endpoints (PDF/Excel) using existing report patterns</name>
  <action>
Add endpoints under a VRM report namespace:
- `GET /vendor-reports/annual?year=YYYY&format=pdf|xlsx`
- `GET /vendor-reports/dora-register?format=xlsx`

Reuse the streaming patterns and permission checks from existing `reports.py`.
  </action>
  <done>Users can export annual VRM report and DORA register to PDF/Excel.</done>
</task>

<task type="auto">
  <name>Task 3: Frontend reports page</name>
  <action>
Add a “Vendor Reports” page:
- Select year
- Download annual report (PDF/Excel)
- Download DORA register export

RBAC:
- Restrict exports to privileged roles (Risk Manager, CRO, Compliance, Internal Audit).
  </action>
  <done>Reporting is accessible in the UI and gated by RBAC.</done>
</task>

</tasks>

<verification>
- [ ] `cd backend && pytest -q` (report endpoints + permission tests)
- [ ] Manual: generate report for current year; verify it lists vendors and includes major incidents and overdue counts.
</verification>

<must_haves>
- Annual report content matches PI‑18‑03 minimum bullet list.
- Exports work (PDF/Excel) without manual data gathering.
- DORA register export exists as a structured dataset (MVP columns are acceptable).
</must_haves>
