---
phase: 18-vendor-risk
plan: 18-07
title: Exit strategy + contingency plan / BCP artifacts and statuses
type: execute
wave: 2
depends_on:
  - 18-01
autonomous: true
files_modified:
  - backend/app/models/vendor_exit_plan.py
  - backend/app/models/vendor_contingency_plan.py
  - backend/app/models/__init__.py
  - backend/app/schemas/vendor_resilience.py
  - backend/app/api/v1/endpoints/vendor_resilience.py
  - backend/alembic/versions/*_add_vendor_exit_bcp.py
  - frontend/src/components/vendors/VendorResilienceTab.tsx
  - frontend/src/services/vendorResilienceApi.ts
  - frontend/src/types/vendorResilience.ts
---

<objective>
Implement PI‑18‑03 exit and continuity requirements for vendors supporting critical/important functions:
- Record an **exit strategy** artifact (status, owners, last review/test date).
- Record a **contingency plan** for vendor outage risk, especially when outage tolerance exceeds 24h or data CIA is impacted.
- Ensure artifacts are reviewable, reportable, and tied to vendor classification.
</objective>

<context>
@.planning/phases/18-vendor-risk/18-RESEARCH.md
@backend/app/models/vendor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resilience data model (exit + contingency)</name>
  <action>
Create models:
- `VendorExitPlan`:
  - `vendor_id`
  - `status` (not_started / in_progress / complete)
  - `plan_reference` (link/path) + `notes`
  - `last_reviewed_at`, `last_tested_at`
- `VendorContingencyPlan`:
  - `vendor_id`
  - `max_tolerable_outage_hours` (number)
  - `data_cia_impact` flags (confidentiality/integrity/authenticity/availability)
  - `status` + `plan_reference`
  - `last_reviewed_at`, `last_tested_at`

Rules:
- Required only when `supports_important_core_insurance_function=true` (enforce in UI and/or backend validation).
  </action>
  <done>Exit and contingency artifacts are first-class, reviewable records tied to critical vendor classification.</done>
</task>

<task type="auto">
  <name>Task 2: API + UI — resilience tab with validations</name>
  <action>
Endpoints:
- `GET /vendors/{id}/resilience`
- `PUT/PATCH /vendors/{id}/resilience`

UI:
- Add “Resilience (Exit/BCP)” tab:
  - Shows required status when vendor is critical/important
  - Captures outage tolerance hours and CIA impact checkboxes
  - Stores references to exit/BCP docs (URLs/paths)

Validation:
- If outage tolerance >24h OR any CIA impact is flagged, contingency plan must be present (PI intent).
  </action>
  <verify>Critical vendor cannot be marked “compliant” without an exit strategy; contingency required when thresholds are met.</verify>
  <done>Users can maintain exit/BCP readiness data with PI-aligned validations.</done>
</task>

</tasks>

<verification>
- [ ] `cd backend && alembic upgrade head`
- [ ] `cd backend && pytest -q`
- [ ] Manual: mark vendor as critical/important and confirm the UI prompts for exit plan + contingency plan when criteria match.
</verification>

<must_haves>
- Exit strategy tracked for critical/important vendors.
- 24h/outage + CIA criteria drive contingency plan requirement.
- Artifacts are reportable (fields exist for last review/test).
</must_haves>
