---
phase: 18-vendor-risk
plan: 18-11
title: Dashboard + Risk Committee integration (vendors, reassessments, SLA breaches)
type: execute
wave: 3
depends_on:
  - 18-01
  - 18-03
  - 18-04
  - 18-08
autonomous: true
files_modified:
  - backend/app/api/v1/endpoints/dashboard.py
  - backend/app/schemas/dashboard.py
  - backend/app/core/snapshot_service.py
  - backend/app/services/quarterly_comparison_service.py
  - backend/app/api/v1/router.py
  - backend/tests/test_dashboard_committee_vendor_metrics.py
  - frontend/src/types/dashboard.ts
  - frontend/src/pages/DashboardPage.tsx
  - frontend/src/components/dashboard/RiskCommitteeSection.tsx
  - frontend/src/components/dashboard/QuarterlyComparisonWidget.tsx
  - frontend/src/services/dashboardApi.ts
  - frontend/src/components/layout/Sidebar.tsx
  - frontend/src/components/notifications/NotificationBell.tsx
  - frontend/src/i18n/locales/en/dashboard.json
  - frontend/src/i18n/locales/cs/dashboard.json
---

<objective>
Make Vendor Risk Management visible where RiskHub leadership operates:
- Surface Vendor counts and risk posture on the main Dashboard (overview).
- Extend Risk Committee view to include Vendor-specific decision signals:
  - overdue vendor reassessments
  - breached vendor SLAs
  - major vendor incidents (recent window)
  - “top vendors by risk score” list (1–5 scale)
- Ensure department scoping and committee access rules remain consistent with existing Dashboard and Risk Committee patterns.
</objective>

<context>
@backend/app/api/v1/endpoints/dashboard.py
@backend/app/core/snapshot_service.py
@backend/app/services/quarterly_comparison_service.py
@frontend/src/pages/DashboardPage.tsx
@frontend/src/components/dashboard/RiskCommitteeSection.tsx
@frontend/src/components/dashboard/QuarterlyComparisonWidget.tsx
@backend/app/api/deps.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Dashboard summary response with vendor metrics (optional fields)</name>
  <action>
Update `DashboardSummaryResponse` and `/dashboard/summary` to include Vendor metrics (as additive fields so existing UI doesn’t break):
- `total_vendors`
- `high_risk_vendors_count` (define simple rule: `vendor.risk_score_1_5 >= 4`)
- `overdue_vendor_reassessments_count` (based on `vendor.next_reassessment_due_at < now`)
- `breached_vendor_slas_count` (count vendor SLAs where current_value outside limits)

Scope:
- Respect existing dashboard filters:
  - department filter (dept scoping + optional explicit department_id) should apply to vendor metrics using vendor.department_id.
- Keep computations efficient (single aggregate queries, no N+1 loops).
  </action>
  <done>Dashboard summary includes VRM key metrics without breaking existing consumers.</done>
</task>

<task type="auto">
  <name>Task 2: Extend Risk Committee summary to include vendors (new cards)</name>
  <action>
Extend `/dashboard/committee-summary` to return vendor-focused sections in addition to existing critical risks, department exposure, and activity:
- `critical_vendors` (top 5 by `risk_score_1_5 desc`, include: name, process, department, outsourcing_owner, flags, next_reassessment_due_at)
- `vendor_alerts` (aggregated counts + short lists):
  - overdue reassessments (top N)
  - SLA breaches (top N)
  - major vendor incidents in last 30 days (top N)

Scoping:
- Use `get_user_department_ids(current_user)` the same way as risks/controls.
- Do not leak vendor existence across departments.
  </action>
  <verify>Department heads see only their department vendors; privileged users see global; admins still blocked by committee rules.</verify>
  <done>Risk Committee summary exposes VRM decision points alongside risk posture.</done>
</task>

<task type="auto">
  <name>Task 3: Quarterly comparison (committee) — include vendor snapshot metrics</name>
  <action>
Extend `capture_snapshot_metrics` and `QuarterlyComparisonWidget` to add VRM snapshot metrics so quarter-over-quarter comparisons remain truthful:
- `active_vendors` (current active vendors in scope)
- `overdue_vendor_reassessments`
- `vendor_sla_breaches`

Implementation notes:
- Snapshot metrics must be point-in-time current state (like existing KRI breach counts).
- Update `QuarterlyComparisonWidget` metric labels + color mapping (vendor metrics should behave like “bad when up” for breaches/overdue).
  </action>
  <done>Quarterly Comparison reflects VRM governance health without misleading “changes”.</done>
</task>

<task type="auto">
  <name>Task 4: Frontend — render vendor metrics on Dashboard and Risk Committee</name>
  <action>
Frontend updates:
- Add a Vendor stat card in `DashboardPage` (navigate to `/vendors`), using the new summary fields.
- Update `RiskCommitteeSection` to render Vendor sections:
  - “Critical Vendors” list (like Critical Risks card)
  - “Vendor Alerts” list/summary (overdue reassessments, SLA breaches, major incidents)
- Keep styling consistent with existing dashboard cards.
  </action>
  <done>Leadership can see vendor posture in the same UI surfaces as risks and KRIs.</done>
</task>

<task type="auto">
  <name>Task 5: Navigation + notifications — route vendor-related items</name>
  <action>
Ensure navigation parity:
- Add Vendors to sidebar (gated by `vendors:read`).
- Update `NotificationBell` resource routing to support vendor-related notifications introduced in Phase 18 (vendor reassessment + vendor SLA notifications), navigating to:
  - `/vendors/{id}` for vendor resources
  - `/vendors/{vendor_id}?tab=sla` or similar for SLA-related notifications (choose a stable UI contract).
  </action>
  <done>Vendor alerts in notifications take users directly to the correct Vendor context.</done>
</task>

</tasks>

<verification>
Before declaring complete:
- [ ] `cd backend && pytest -q` (include tests for committee summary scoping + vendor metric counts)
- [ ] `cd frontend && npx tsc --noEmit`
- [ ] Manual: as department head, open Dashboard → Committee view; confirm only department-scoped vendors and vendor alerts show.
- [ ] Manual: as privileged user, confirm global vendor metrics appear and links navigate to Vendors pages.
</verification>

<must_haves>
- Vendor metrics appear on Dashboard (overview).
- Vendor decision signals appear in Risk Committee view.
- Quarter-over-quarter comparison includes VRM snapshot metrics (no misleading “changes”).
- All outputs respect existing committee access rules and department scoping.
</must_haves>

