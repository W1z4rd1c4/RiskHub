---
phase: 18-vendor-risk
plan: 18-03
title: Due diligence workflow (questionnaires, evidence, committee recommendation)
type: execute
wave: 2
depends_on:
  - 18-01
  - 18-02
autonomous: true
files_modified:
  - backend/app/models/vendor_assessment.py
  - backend/app/models/__init__.py
  - backend/app/schemas/vendor_assessment.py
  - backend/app/api/v1/endpoints/vendor_assessments.py
  - backend/app/services/vendor_assessment_service.py
  - backend/app/i18n/en.py
  - backend/app/i18n/cs.py
  - backend/alembic/versions/*_add_vendor_assessments.py
  - frontend/src/components/vendors/VendorAssessmentsTab.tsx
  - frontend/src/services/vendorAssessmentApi.ts
  - frontend/src/types/vendorAssessment.ts
  - frontend/src/i18n/locales/en/vendors.json
  - frontend/src/i18n/locales/cs/vendors.json
---

<objective>
Implement PI‑18‑03 due diligence as a workflow:
- Outsourcing owner (PI “sponsor”) performs assessment using a structured questionnaire + evidence references.
- Outputs are reviewable by 2nd line (Risk Manager/Compliance) and can be escalated to a “risk committee recommendation” decision.
- Support DORA‑relevant vendors with a longer/more exhaustive questionnaire scope.
</objective>

<context>
@.planning/phases/18-vendor-risk/18-RESEARCH.md
@backend/app/services/notification_service.py
@backend/app/models/notification.py
@frontend/src/components/PermissionGate.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assessment model + lifecycle</name>
  <action>
Add `VendorAssessment` with:
- `vendor_id`
- `status` (draft → submitted → in_review → committee_recommended → approved/rejected)
- `template_key` + `template_version`
- `scope` (standard vs dora) derived from `vendor.dora_relevant`
- `answers_json` (stable keys)
- `submitted_at`, `reviewed_at`, `decision_at`
- `submitted_by_user_id`, `reviewed_by_user_id`, `decided_by_user_id`
- `committee_recommendation` (approve/approve_with_conditions/reject) + optional `conditions_text`

Evidence:
- Start with “evidence_reference” strings (URLs/paths) similar to control execution evidence patterns.
- Keep it simple: a single `evidence_reference` text field per assessment in MVP (no evidence taxonomy, no uploads).
- Immutability (match Phase 18 context): once submitted, `answers_json` + `evidence_reference` are frozen; review/recommend/decision metadata remains writable for workflow completion.
  </action>
  <done>Assessment instances are immutable audit records with clear workflow states and evidence references.</done>
</task>

<task type="auto">
  <name>Task 2: Questionnaire templates (standard vs DORA)</name>
  <action>
Define stable question keys grouped by PI themes:
- Financial stability
- Regulatory/legal (GDPR, DORA, Solvency II)
- Cyber/security + supply chain
- BCP/DR + outage tolerance
- Governance/controls + auditability
- Concentration/replaceability + subcontractors (4th parties)

Template rules:
- `standard` template is required for all vendors.
- `dora` template extends standard with additional DORA-focused sections and explicit subcontractor disclosure.

Implementation approach:
- Reuse the same “template_key + template_version + answers JSON” strategy used for `RiskQuestionnaire` (consistent field naming and versioning semantics), but do not require a vendor login portal in this phase.
  </action>
  <done>Standard and DORA assessment templates are defined as stable key sets, ready for scoring and reporting.</done>
</task>

<task type="auto">
  <name>Task 3: API endpoints + permissions</name>
  <action>
Endpoints:
- `GET /vendors/{vendor_id}/assessments`
- `POST /vendors/{vendor_id}/assessments` (create draft)
- `POST /vendor-assessments/{id}/submit`
- `POST /vendor-assessments/{id}/review` (2nd line)
- `POST /vendor-assessments/{id}/committee-recommend` (risk committee recommendation)
- `POST /vendor-assessments/{id}/decide` (CRO/board-like decision; align with RBAC roles available)

RBAC:
- Outsourcing owner can create/edit drafts and submit.
- Risk Manager/Compliance can review and recommend.
- CRO can decide (or map to a configured committee role).

Auditability:
- Log major workflow transitions (submit/review/recommend/decide) to Activity Log under `ActivityEntityType.VENDOR_ASSESSMENT` with `entity_id=<assessment_id>` (use `ActivityAction.STATUS_CHANGE` and include a small change set, e.g., old/new `status`). Scope `department_id` via the linked vendor.
  </action>
  <verify>Illegal transitions (e.g., edit after submit, decide without review) are blocked with clear 400/403 errors.</verify>
  <done>Due diligence workflow works end-to-end with correct role gating and state transitions.</done>
</task>

<task type="auto">
  <name>Task 4: Notifications + UI tab</name>
  <action>
Notifications:
- On submit → notify Risk Manager + Compliance (and optional CRO) to review.
- On committee recommendation → notify CRO to decide.
- On decision → notify outsourcing owner.

Notification routing contract (Phase 18 locked decision):
- Store `resource_type="vendor"` and `resource_id=<vendor_id>` for all vendor assessment notifications (frontend deep-linking derives tab from `notification.type`).

Notification type keys (single source of truth):
- Use the stable Phase 18 notification types introduced in Plan 18-00:
  - `vendor_assessment_submitted`
  - `vendor_assessment_committee_recommended`
  - `vendor_assessment_decided`

Frontend:
- Add “Assessments” tab on vendor detail:
  - list assessment history
  - “Start assessment” (draft)
  - assessment form with sections, evidence fields, submit action
  - review/decision panels visible only to authorized roles
  </action>
  <verify>`cd backend && pytest -q` covers state transitions + notification triggers.</verify>
  <done>Sponsor can complete due diligence; 2nd line can review; decision is recorded and communicated.</done>
</task>

</tasks>

<verification>
- [ ] `cd backend && alembic upgrade head`
- [ ] `cd backend && pytest -q`
- [ ] `cd frontend && npx tsc --noEmit`
- [ ] Manual: run through draft → submit → review → recommend → decide and confirm UI gates per role.
</verification>

<must_haves>
- Workflow states reflect PI “assessment evidence → committee input → decision”.
- DORA vendors get an expanded assessment scope (template difference is visible).
- Evidence references can be captured and reviewed.
</must_haves>

<output>
After completion, create `.planning/phases/18-vendor-risk/18-03-SUMMARY.md` documenting:
- Assessment statuses + allowed transitions
- Template keys/versions and how DORA scope is chosen
- Notification types added
</output>
