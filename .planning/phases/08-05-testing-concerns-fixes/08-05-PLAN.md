---
phase: 08-05-testing-concerns-fixes
type: execute
---

<objective>
Fix remaining testing gaps and major concerns identified in TESTING.md and CONCERNS.md.

Purpose: Address technical debt, security risks, and testing infrastructure gaps while in DEV phase to improve code quality and prepare for production hardening.
Output: Fixed backend/frontend code with improved test infrastructure, secure endpoints, and optimized database queries.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
@backend/requirements.txt
@backend/app/core/config.py
@backend/app/api/v1/endpoints/reports.py
@backend/app/api/v1/endpoints/departments.py
@backend/app/api/v1/endpoints/risks.py
@backend/app/api/deps.py
@backend/app/db/session.py
@frontend/src/services/apiClient.ts
@frontend/playwright.config.ts
</context>

<tasks>

<!-- ============================================= -->
<!-- TASK 1: Testing Infrastructure Fixes -->
<!-- ============================================= -->

<task type="auto">
  <name>Task 1: Add pytest-cov and run coverage report</name>
  <files>backend/requirements.txt, backend/pytest.ini</files>
  <action>
    1. Add `pytest-cov` to `backend/requirements.txt`
    2. Update `backend/pytest.ini` to include coverage configuration:
       - Add `addopts = --cov=app --cov-report=term-missing --cov-report=html:coverage_html`
    3. Create `.gitignore` entry for `coverage_html/` if not present
    4. Run `pip install pytest-cov` to install
  </action>
  <verify>Run `cd backend && pytest --cov=app` and confirm coverage report is generated</verify>
  <done>pytest-cov installed, coverage report shows percentage for all app modules</done>
</task>

<task type="auto">
  <name>Task 2: Create explicit Playwright config file</name>
  <files>frontend/playwright.config.ts</files>
  <action>
    1. Check if `frontend/playwright.config.ts` exists - if not, create it
    2. Configure with:
       - testDir: './tests'
       - timeout: 30000
       - webServer pointing to `npm run dev` on port 5173
       - projects for chromium (primary)
       - baseURL: 'http://localhost:5173'
    3. Ensure `@playwright/test` is in devDependencies
    4. Add playwright test script to package.json if missing: `"test:e2e": "playwright test"`
  </action>
  <verify>Run `cd frontend && npx playwright test --list` to confirm config is valid and tests are discovered</verify>
  <done>Playwright config exists, lists available tests without errors</done>
</task>

<!-- ============================================= -->
<!-- TASK 3: Major Security Fixes -->
<!-- ============================================= -->

<task type="auto">
  <name>Task 3: Secure report endpoints with authentication</name>
  <files>backend/app/api/v1/endpoints/reports.py</files>
  <action>
    1. Import `get_current_user` from `app.api.deps`
    2. Add `Depends(get_current_user)` to ALL report download endpoints:
       - `/controls/pdf` (line ~34)
       - `/controls/excel` (line ~154)
       - `/risks/pdf`, `/risks/excel`, `/summary/pdf` etc.
    3. The user object can be used for audit logging but is not strictly required for filtering
    4. Keep existing functionality, just add auth requirement
  </action>
  <verify>Call a report endpoint without auth token: `curl http://localhost:8000/api/v1/reports/controls/pdf` should return 401</verify>
  <done>All report endpoints require valid JWT, return 401 without token</done>
</task>

<task type="auto">
  <name>Task 4: Disable mock auth by default, require explicit dev flag</name>
  <files>backend/app/api/deps.py, backend/app/core/config.py</files>
  <action>
    1. In `config.py`, change `mock_auth_enabled` default from True to False
    2. In `deps.py`, add additional check: only allow mock auth if BOTH:
       - `settings.mock_auth_enabled` is True
       - `settings.debug` is True (extra safety)
    3. Add warning log when mock auth is used: `logger.warning("MOCK AUTH USED - DO NOT USE IN PRODUCTION")`
  </action>
  <verify>Start server without `MOCK_AUTH_ENABLED=true` env var, confirm `X-Mock-User-Id` header is ignored</verify>
  <done>Mock auth disabled by default, requires explicit env flags, logs warning when used</done>
</task>

<!-- ============================================= -->
<!-- TASK 5: Performance Fix -->
<!-- ============================================= -->

<task type="auto">
  <name>Task 5: Fix N+1 queries in department summary endpoint</name>
  <files>backend/app/api/v1/endpoints/departments.py</files>
  <action>
    1. Locate the department summary/aggregation endpoint (likely ~line 38, 173)
    2. Replace multiple per-department count queries with a single aggregated query using:
       - Subqueries for risk_count, control_count, kri_count
       - Or use SQLAlchemy `func.count` with `group_by`
    3. Example pattern:
       ```python
       subq = (
           select(Risk.department_id, func.count(Risk.id).label('risk_count'))
           .group_by(Risk.department_id)
           .subquery()
       )
       result = await db.execute(
           select(Department, subq.c.risk_count)
           .outerjoin(subq, Department.id == subq.c.department_id)
       )
       ```
    4. Ensure existing response schema is maintained
  </action>
  <verify>Enable SQL echo, call `/api/v1/departments/summary`, confirm single aggregated query instead of N+1</verify>
  <done>Department summary uses 1-3 optimized queries instead of O(N) queries</done>
</task>

<task type="auto">
  <name>Task 6: Fix race condition in risk_id_code generation</name>
  <files>backend/app/api/v1/endpoints/risks.py, backend/app/models/risk.py</files>
  <action>
    1. In `risks.py` (line ~161), wrap the ID code generation in a transaction with `FOR UPDATE`:
       ```python
       # Get latest count with row lock
       result = await db.execute(
           select(func.count(Risk.id))
           .with_for_update()
       )
       count = result.scalar_one()
       ```
    2. Alternative approach: Use PostgreSQL SEQUENCE or generate UUID-based codes
    3. If using SQLite for dev, implement retry logic on unique constraint violation:
       ```python
       for attempt in range(3):
           try:
               # generate code and commit
               break
           except IntegrityError:
               await db.rollback()
               continue
       ```
    4. Document the chosen approach in code comments
  </action>
  <verify>Create multiple risks concurrently (e.g., 10 parallel requests), confirm no duplicate IDs or constraint violations</verify>
  <done>Risk ID generation handles concurrent writes without duplicates</done>
</task>

<!-- ============================================= -->
<!-- TASK 7-9: Minor Fixes -->
<!-- ============================================= -->

<task type="auto">
  <name>Task 7: Fix double-commit pattern in database session</name>
  <files>backend/app/db/session.py</files>
  <action>
    1. Review `session.py` (line ~19) - remove automatic commit from the dependency
    2. Change pattern to:
       - Yield session without auto-commit
       - Let endpoints manage their own commits via `await db.commit()`
       - Only use `finally` for rollback on exception
    3. Ensure all endpoints that were relying on auto-commit still work by auditing a few key endpoints
  </action>
  <verify>Run existing backend tests to confirm no regression from commit pattern change</verify>
  <done>Database commits are explicit in endpoints, no double-commit</done>
</task>

<task type="auto">
  <name>Task 8: Log silent auth errors instead of swallowing</name>
  <files>backend/app/api/deps.py</files>
  <action>
    1. In `get_current_user_optional` (line ~59), change the exception handling:
       - Instead of silently returning None, log the exception
       - Use `logger.debug(f"Optional auth failed: {e}")` so it's visible in debug mode
       - Still return None (optional auth behavior unchanged)
    2. Add import for logger if not present: `import logging; logger = logging.getLogger(__name__)`
  </action>
  <verify>Trigger an auth parsing error (malformed token), confirm it's logged at DEBUG level</verify>
  <done>Auth parsing errors are logged, debugging is easier</done>
</task>

<task type="auto">
  <name>Task 9: Add .gitignore entry for database file</name>
  <files>.gitignore</files>
  <action>
    1. Add the following entries to `.gitignore`:
       - `backend/risk_management.db`
       - `backend/*.db`
       - `*.db-journal`
    2. If the file is already tracked, stage removal: `git rm --cached backend/risk_management.db`
  </action>
  <verify>Run `git status` to confirm db file is ignored</verify>
  <done>Database files are gitignored, not tracked in repository</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && pytest` passes all tests
- [ ] `cd backend && pytest --cov=app` shows coverage report
- [ ] All report endpoints return 401 without auth token
- [ ] Mock auth is disabled by default
- [ ] Department summary endpoint uses optimized queries
- [ ] Database files are in .gitignore
</verification>

<success_criteria>
- All 9 tasks completed
- No regression in existing tests
- Security gaps addressed (report auth, mock auth)
- Performance optimized (N+1 queries fixed)
- Testing infrastructure improved (pytest-cov, Playwright config)
- Technical debt reduced (double-commit, silent errors, gitignore)
</success_criteria>

<output>
After completion, create `.planning/phases/08-05-testing-concerns-fixes/08-05-SUMMARY.md`:

# Plan 08-05 Summary: Testing and Concerns Fixes

**[Substantive one-liner describing what shipped]**

## Accomplishments
- [Key outcomes]

## Files Created/Modified
- [List of files and what changed]

## Decisions Made
- [Any technical decisions]

## Issues Encountered
- [Problems and resolutions, or "None"]

## Next Step
- Phase complete, ready for next phase or production hardening
</output>
