---
phase: 20-czech-localization
type: execute
---

<objective>
Wire remaining hardcoded English strings to existing i18n translations.

Purpose: Complete Phase 20 Czech Localization by connecting ~60 hardcoded strings to the existing common.json translations.
Output: All runtime UI messages (loading, empty states, errors, confirmations) properly localized.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-plan.md
@.planning/phases/20-czech-localization/20-15-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@frontend/src/i18n/locales/cs/common.json
@frontend/src/i18n/locales/en/common.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Loading States</name>
  <files>
    frontend/src/pages/NotificationsPage.tsx,
    frontend/src/App.tsx,
    frontend/src/components/notifications/NotificationBell.tsx,
    frontend/src/components/access/AccessEditModal.tsx,
    frontend/src/components/ControlForm.tsx
  </files>
  <action>
Replace hardcoded "Loading..." strings with `t('loading.generic')` from common namespace.

Pattern to find: `>Loading...</` or `Loading...`
Replace with: `{t('loading.generic')}` (ensure useTranslation('common') is imported)

Files:
1. NotificationsPage.tsx:185 - `Loading...` → `{t('loading.generic')}`
2. App.tsx:51 - `Loading...` → `{t('loading.generic')}`  
3. NotificationBell.tsx:193 - `Loading...` → `{t('loading.generic')}`
4. AccessEditModal.tsx:161 - `Loading...` → `{t('loading.generic')}`
5. ControlForm.tsx:372 - `Loading...` → `{t('loading.generic')}`

Add `import { useTranslation } from 'react-i18next';` and `const { t } = useTranslation('common');` where missing.
  </action>
  <verify>npm run build passes, grep -r "Loading\.\.\." frontend/src returns 0 results in TSX files</verify>
  <done>All 5 "Loading..." strings replaced with t('loading.generic')</done>
</task>

<task type="auto">
  <name>Task 2: Wire Empty State Messages</name>
  <files>
    frontend/src/components/notifications/NotificationBell.tsx,
    frontend/src/components/dashboard/CategoryBreakdownCharts.tsx,
    frontend/src/pages/DepartmentDetailPage.tsx,
    frontend/src/components/kris/KRIDetailHistoryTab.tsx,
    frontend/src/components/ControlForm.tsx,
    frontend/src/components/RiskForm.tsx,
    frontend/src/components/KRIForm.tsx,
    frontend/src/components/tables/CategoryDrillDown.tsx,
    frontend/src/components/tables/GroupedView.tsx,
    frontend/src/components/risks/RiskDetailOverviewTab.tsx
  </files>
  <action>
Replace hardcoded empty-state strings with existing common.json translations.

Mappings:
- "No notifications" → `t('empty.no_notifications', { ns: 'common' })`
- "No data" → `t('empty.no_data', { ns: 'common' })`
- "No data available" → `t('empty.no_data_available', { ns: 'common' })`
- "No controls found for this department." → Add to common.json as `empty.no_controls_department`
- "No KRIs found for this department." → Add to common.json as `empty.no_kris_department`
- "No users found for this department." → Add to common.json as `empty.no_users_department`
- "No history recorded yet..." → Add to kris.json as `history.no_records`
- "No description available" → `t('empty.no_description', { ns: 'common' })`
- "No users found" → `t('empty.no_users_found', { ns: 'common' })`
- "Add control" (RiskDetailOverviewTab:287) → `t('actions.add_control', { ns: 'common' })` (add key if missing)

Add missing keys to cs/common.json:
```json
"empty": {
  "no_controls_department": "Pro toto oddělení nebyly nalezeny žádné kontroly.",
  "no_kris_department": "Pro toto oddělení nebyly nalezeny žádné KRI.",
  "no_users_department": "Pro toto oddělení nebyli nalezeni žádní uživatelé."
}
"actions": {
  "add_control": "Přidat kontrolu"
}
```

Add to cs/kris.json:
```json
"history": {
  "no_records": "Zatím nebyla zaznamenána žádná historie. Klikněte na 'Zaznamenat hodnotu' pro zahájení sledování."
}
```
  </action>
  <verify>npm run build passes, grep for hardcoded empty messages returns 0 results</verify>
  <done>All 10+ empty state strings wired to i18n translations</done>
</task>

<task type="auto">
  <name>Task 3: Wire Confirm Dialogs and Alerts</name>
  <files>
    frontend/src/pages/ApprovalsPage.tsx,
    frontend/src/components/LinkManagementDialog.tsx,
    frontend/src/pages/ControlDetailPage.tsx,
    frontend/src/pages/RiskDetailPage.tsx,
    frontend/src/pages/AdminConsolePage.tsx
  </files>
  <action>
Replace hardcoded confirm() and alert() strings with i18n translations.

Confirm dialogs:
1. ApprovalsPage.tsx:99 - `confirm('Are you sure you want to cancel this request?')` 
   → Add to cs/approvals.json: `confirmations.cancel_request`
   → `confirm(t('confirmations.cancel_request', { ns: 'approvals' }))`

2. LinkManagementDialog.tsx:178 - `confirm('Are you sure you want to remove this link?')`
   → Add to cs/common.json: `confirmation.remove_link`
   → `confirm(t('confirmation.remove_link', { ns: 'common' }))`

Alert dialogs:
1. ApprovalsPage.tsx:75 - `alert('Resolution notes are mandatory.')`
   → Add to cs/approvals.json: `errors.notes_required`
   
2. AdminConsolePage.tsx:36 - `alert('Log settings updated...')`
   → Add to cs/admin.json: `messages.log_settings_updated`

3. RiskDetailPage.tsx:145,160,172,187,199 - Various "Failed to..." alerts
   → Use cs/common.json errors section, add missing keys
   
4. ControlDetailPage.tsx:105,117 - "Failed to link/unlink risk."
   → Use cs/common.json errors section

Add to cs/common.json:
```json
"confirmation": {
  "remove_link": "Opravdu chcete odebrat toto propojení?"
},
"errors": {
  "failed_archive_risk": "Nepodařilo se archivovat riziko.",
  "failed_link_control": "Nepodařilo se propojit kontrolu.",
  "failed_unlink_control": "Nepodařilo se odpojit kontrolu.",
  "failed_link_risk": "Nepodařilo se propojit riziko.",
  "failed_unlink_risk": "Nepodařilo se odpojit riziko.",
  "failed_save_kri": "Nepodařilo se uložit KRI.",
  "failed_delete_kri": "Nepodařilo se smazat KRI."
}
```

Add to cs/approvals.json:
```json
"confirmations": {
  "cancel_request": "Opravdu chcete zrušit tento požadavek?"
},
"errors": {
  "notes_required": "Poznámky k řešení jsou povinné."
}
```

Add to cs/admin.json:
```json
"messages": {
  "log_settings_updated": "Nastavení logování bylo aktualizováno. Změny vyžadují restart backendu."
}
```
  </action>
  <verify>npm run build passes, grep -E "alert\\\(['\"]|confirm\\\(['\"]" finds no hardcoded English strings</verify>
  <done>All confirm() and alert() calls use translated strings</done>
</task>

<task type="auto">
  <name>Task 4: Wire Success Messages and Misc UI</name>
  <files>
    frontend/src/pages/UserDetailPage.tsx,
    frontend/src/components/kri/KRIHistoryEditModal.tsx,
    frontend/src/components/kri/KRIValueModal.tsx,
    frontend/src/components/notifications/NotificationBell.tsx,
    frontend/src/pages/UserNewPage.tsx
  </files>
  <action>
Replace remaining hardcoded success messages and misc UI strings.

Success messages:
1. UserDetailPage.tsx:178 - "Profile updated successfully!"
   → `t('success.profile_updated', { ns: 'admin' })`
   → Add to cs/admin.json: `success.profile_updated: "Profil byl úspěšně aktualizován!"`

2. KRIHistoryEditModal.tsx:44 - "History entry corrected successfully."
   → `t('success.history_corrected', { ns: 'kris' })`
   → Add to cs/kris.json: `success.history_corrected: "Záznam historie byl úspěšně opraven."`

3. KRIValueModal.tsx:115 - "Value recorded successfully!"
   → `t('success.value_recorded', { ns: 'kris' })`
   → Add to cs/kris.json: `success.value_recorded: "Hodnota byla úspěšně zaznamenána!"`

Misc UI:
4. NotificationBell.tsx:250 - "View all →"
   → Add to cs/common.json: `actions.view_all: "Zobrazit vše →"`
   → `{t('actions.view_all', { ns: 'common' })}`

5. NotificationBell.tsx:166 - `aria-label="Notifications"`
   → Leave as-is (accessibility labels can remain in English or use t() for ARIA)

6. UserNewPage.tsx:128 - "Add New User" 
   → `t('titles.add_user', { ns: 'admin' })`
   → Add to cs/admin.json: `titles.add_user: "Přidat nového uživatele"`
  </action>
  <verify>npm run build passes</verify>
  <done>All success messages and misc UI strings wired to i18n</done>
</task>

<task type="auto">
  <name>Task 5: Wire Error Messages in setError Calls</name>
  <files>
    frontend/src/pages/ApprovalsPage.tsx,
    frontend/src/pages/RiskDetailPage.tsx,
    frontend/src/pages/DashboardPage.tsx,
    frontend/src/pages/UserNewPage.tsx,
    frontend/src/pages/DepartmentsPage.tsx,
    frontend/src/pages/ControlDetailPage.tsx,
    frontend/src/pages/UserDetailPage.tsx,
    frontend/src/components/riskhub/SystemSettingsPanel.tsx,
    frontend/src/components/riskhub/RolesPanel.tsx,
    frontend/src/components/riskhub/DepartmentsPanel.tsx,
    frontend/src/components/riskhub/ApprovalScenariosPanel.tsx,
    frontend/src/components/kri/KRIHistoryEditModal.tsx
  </files>
  <action>
Replace hardcoded error strings in setError() calls with i18n translations.

Pattern: `setError('Failed to...')` or inline error text
Replace with: `setError(t('errors.specific_key', { ns: 'common' }))`

Key mappings to add to cs/common.json errors section:
```json
"errors": {
  "failed_load_approvals": "Nepodařilo se načíst žádosti o schválení. Zkuste to prosím znovu.",
  "failed_process_request": "Nepodařilo se zpracovat požadavek.",
  "failed_load_risk": "Nepodařilo se načíst podrobnosti rizika.",
  "failed_load_dashboard": "Nepodařilo se načíst data dashboardu. Zkontrolujte prosím připojení.",
  "failed_create_user": "Nepodařilo se vytvořit uživatele. Zkontrolujte prosím zadané údaje.",
  "failed_load_departments": "Nepodařilo se načíst oddělení.",
  "failed_load_control": "Nepodařilo se načíst podrobnosti kontroly.",
  "failed_update_profile": "Nepodařilo se aktualizovat profil uživatele.",
  "failed_save": "Nepodařilo se uložit.",
  "failed_delete_role": "Nepodařilo se smazat roli.",
  "failed_delete_department": "Nepodařilo se smazat oddělení.",
  "failed_load_settings": "Nepodařilo se načíst nastavení.",
  "failed_load_scenarios": "Nepodařilo se načíst scénáře schvalování.",
  "failed_submit_correction": "Nepodařilo se odeslat žádost o opravu."
}
```

Also wire inline error text like:
- SystemSettingsPanel.tsx:181 - "Failed to load settings"
- ApprovalScenariosPanel.tsx:230 - "Failed to load approval scenarios"
- RolesPanel.tsx:426 - "Cannot delete: X users are assigned"
- DepartmentsPanel.tsx:351 - "Cannot delete because of linked items"

Add to cs/admin.json:
```json
"errors": {
  "cannot_delete_role_users": "Nelze smazat: {{count}} uživatelů je přiřazeno k této roli.",
  "cannot_delete_department_linked": "Nelze smazat kvůli propojeným položkám:"
}
```
  </action>
  <verify>npm run build passes, grep for "Failed to" in TSX returns only console.error calls (not user-facing)</verify>
  <done>All setError() and inline error text wired to i18n translations</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds without errors
- [ ] grep -r "Loading\.\.\." --include="*.tsx" frontend/src returns no results
- [ ] grep -rE "alert\(['\"](Failed|Are you)" --include="*.tsx" frontend/src returns no results
- [ ] grep -rE "confirm\(['\"](Are you)" --include="*.tsx" frontend/src returns no results
- [ ] Manual verification: Switch language to Czech and verify translated messages appear
</verification>

<success_criteria>

- All 5 tasks completed
- All hardcoded Loading/Empty/Error/Confirm strings replaced with t() calls
- Build passes with no TypeScript errors
- Czech translations display correctly when language is set to Czech
</success_criteria>

<output>
After completion, create `.planning/phases/20-czech-localization/20-16-SUMMARY.md`:

# Phase 20-16 Summary: Runtime Message Localization

**Wired ~60 hardcoded runtime strings to existing i18n translations.**

## Accomplishments

- Replaced Loading states (5 files)
- Replaced Empty state messages (10+ occurrences)
- Replaced Confirm dialogs (2 occurrences)
- Replaced Alert messages (9 occurrences)
- Replaced Success messages (3 occurrences)
- Replaced Error messages (15+ occurrences)

## Files Modified

- Various pages and components
- cs/common.json - Added new error/empty/confirmation keys
- cs/kris.json - Added success/history keys
- cs/admin.json - Added success/error/title keys
- cs/approvals.json - Added confirmation/error keys

## Next Step

Phase 20 truly complete - all localization gaps addressed.
</output>
