---
phase: 501-production-readiness-hardening
plan: 501-07
type: execute
wave: 7
depends_on: [501-06]
files_modified:
  - .github/workflows/lint.yml
  - .github/workflows/security.yml
  - Makefile
  - backend/.bandit
  - backend/security/pip-audit-allowlist.txt
autonomous: true
---

<objective>
Harden CI quality/security gates so previously slipping issue classes block builds.
</objective>

<tasks>
<task type="auto">
  <name>Harden lint workflow gates</name>
  <action>
Add frontend typecheck/build gates and extend backend Ruff hard gate to include `tests` and `scripts`.
  </action>
</task>

<task type="auto">
  <name>Harden security workflow gates</name>
  <action>
Remove advisory `continue-on-error` behavior for actionable scans; enforce high-severity blocking and explicit allowlist mechanics.
  </action>
</task>

<task type="auto">
  <name>Add explicit pip-audit allowlist file</name>
  <action>
Add versioned allowlist file (`backend/security/pip-audit-allowlist.txt`) consumed by CI.
  </action>
</task>
</tasks>

<verification>
- [ ] local-equivalent commands for workflow jobs pass
- [ ] workflow YAML parses cleanly
</verification>
