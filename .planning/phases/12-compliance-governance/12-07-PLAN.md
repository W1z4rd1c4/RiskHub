---
phase: 12-07
type: execute
---

<objective>
Verification, Documentation, and Admin Console Audit Logs Tab + Log Settings for "Option A" Log Forwarding.

Purpose: Provide the "Instruction Manual" for the security team to ingest these files, verify output meets schema requirements (no secrets leaking), and give admins a UI to view audit logs in real-time and configure log rotation settings.
Output: `SIEM_INTEGRATION.md`, Verification Script, Admin Console "Audit Logs" Tab with log settings panel.
</objective>

<context>
@backend/logs/audit.json.log
@docs/
@frontend/src/pages/AdminConsolePage.tsx
@backend/app/api/v1/admin.py
</context>

## Technical Details
We need to prove that "Option A" works. We will assume the consumer is **Elastic Filebeat** or **Splunk Universal Forwarder**. Additionally, System Admins need a UI to view and search audit logs without requiring direct file access.

<tasks>

<task type="auto">
  <name>Task 1: Log Verification Script</name>
  <files>scripts/verify_audit_logs.py</files>
  <action>
    Write a script that:
    1. Reads `backend/logs/audit.json.log`.
    2. Parses each line as JSON.
    3. Asserts required fields: `timestamp`, `event`, `user_id`, `request_id`, `remote_ip`.
    4. Scans for "forbidden" keys/values (e.g., checks if any value looks like a password or token).
  </action>
  <verify>Run `python scripts/verify_audit_logs.py` against sample logs.</verify>
  <done>Script confirms log quality and hygiene.</done>
</task>

<task type="manual">
  <name>Task 2: SIEM Integration Guide</name>
  <files>docs/SIEM_INTEGRATION.md</files>
  <action>
    Write comprehensive documentation.
    - **Architecture**: Diagram showing RiskHub -> JSON Files -> Filebeat -> SIEM.
    - **Filebeat Config**: Provide a copy-paste `filebeat.yml` snippet to ingest these logs.
    - **Splunk Config**: Provide `inputs.conf` snippet.
    - **Schema Reference**: Table defining all fields in the JSON.
  </action>
  <verify>Review MD file.</verify>
  <done>Docs provided.</done>
</task>

<task type="auto">
  <name>Task 3: Admin Console Audit Logs Tab + Log Settings</name>
  <files>frontend/src/pages/AdminConsolePage.tsx</files>
  <action>
    Add a new tab "Audit Logs" (icon: Shield or FileText) to the Admin Console:
    
    1. **Tab Definition**: Add to the `tabs` array with id `'audit'`, label `'Audit Logs'`, icon `Shield`.
    
    2. **Log Settings Panel** (top of tab):
       - Display current log rotation settings from Risk Hub config
       - Editable fields:
         - `Log Rotation Size (MB)`: Number input, current value: 10
         - `Log Retention Count`: Number input, current value: 10
       - "Save Settings" button to update via API
       - Show "Requires backend restart" note
    
    3. **AuditLogsPanel Component**: Create new panel that:
       - Fetches from `GET /admin/logs/audit` endpoint
       - Displays a table with columns: Timestamp, Event Type, User, Entity, Entity ID, Details
       - Color-code event types (create=green, update=amber, delete=red)
       - Include "Refresh" button and auto-refresh toggle
       - Limit selector (50, 100, 200, 500 entries)
       - Event type filter dropdown
       - Search filter for entity/details
    
    4. **Export Functionality**: Add "Export CSV" and "Export JSON" buttons for downloading logs.
    
    5. **Panel Conditional Render**: Add the new panel to the tab switch in `AdminConsolePage`.
  </action>
  <verify>
    As admin, open Admin Console. See "Audit Logs" tab. 
    Verify log settings panel shows current values (10MB, 10 backups).
    Click tab, see audit entries in table format.
    Filter by event type. Export works.
  </verify>
  <done>Admin can configure log settings and view/export audit logs in UI.</done>
</task>

</tasks>

<verification>
- [ ] `verify_audit_logs.py` passes on generated logs
- [ ] `docs/SIEM_INTEGRATION.md` contains executable config examples
- [ ] No secrets found in log samples
- [ ] Admin Console shows "Audit Logs" tab (admin-only)
- [ ] Log settings panel displays current values (10MB size, 10 retention count)
- [ ] Log settings can be updated via UI and saved to Risk Hub config
- [ ] Audit logs table displays with filters and export functionality
</verification>

<output>
After completion, create .planning/phases/12-compliance-governance/12-07-SUMMARY.md
</output>
