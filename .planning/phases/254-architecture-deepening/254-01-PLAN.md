# Phase 254.01: Architecture Deepening

## Goal

Deepen the Modules identified in the architecture review while preserving public behavior:
shared collection workflow, issue register, vendor links, orphan resolution, KRI history intake,
risk questionnaire interaction, unified exports, admin telemetry, and codebase-map documentation.

## Acceptance Criteria

- Public HTTP routes and response shapes remain backward-compatible.
- Frontend user-visible behavior remains unchanged.
- No database migration is introduced unless a RED test proves it is required.
- Each behavior-changing refactor follows RED -> GREEN -> REFACTOR.
- `.planning/codebase/` is refreshed after implementation and verification.

## Execution Notes

- Work remains on `main` unless the user explicitly requests branch work.
- Baseline targeted suites passed before edits:
  - Backend issue/vendor/orphan group: `47 passed`
  - Backend KRI/questionnaire group: `45 passed, 1 skipped`
  - Backend report/admin group: `48 passed`
  - Frontend collection/linking group: `25 passed`
  - Frontend orphan/questionnaire/admin group: `15 passed`
  - Frontend `tsc --noEmit`: passed
