---
phase: 72-risk-hub-resolution
type: execute
domain: thresholds
---

<objective>
Remove remaining hardcoded risk-threshold logic in the backend so CRO-configured values affect reporting and “critical/high risk” approval gating consistently.

Purpose: Even after Phase 72-02, some code paths still hardcode `>= 16` or use sync default thresholds in async flows (e.g., report PDF summary and control-critical checks). This causes mismatches between Risk Hub settings and backend behavior.
Output: Backend uses `global_config` keys (`medium_risk_min_net_score`, `high_risk_min_net_score`, `critical_risk_min_net_score`) consistently, sync defaults match seeds, and regression tests prove thresholds change behavior.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@backend/alembic/versions/74f4ad1b68cb_add_risk_hub_tables.py
@backend/app/models/global_config.py
@backend/app/core/permissions.py
@backend/app/services/report_service.py
@backend/app/api/v1/endpoints/reports.py
@backend/app/core/approval_helpers.py
@backend/app/api/v1/endpoints/controls.py
@backend/tests/test_global_config_usage.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align ConfigDefaults with seeded threshold defaults</name>
  <files>backend/app/models/global_config.py</files>
  <action>
    Update `ConfigDefaults` so fallback values match the seed migration (`74f4ad1b68cb_add_risk_hub_tables.py`):
    - `MEDIUM_RISK_MIN_NET_SCORE = 5`
    - `HIGH_RISK_MIN_NET_SCORE = 10`
    - `CRITICAL_RISK_MIN_NET_SCORE = 16`

    Keep backwards compatibility:
    - If existing code references `ConfigDefaults.HIGH_RISK_MIN_NET_SCORE`, ensure it now reflects “high risk” (10), not the previous 15.
    - Add new constants instead of renaming existing ones if you want to avoid wide refactors.
  </action>
  <verify>`pytest backend/tests/test_global_config_usage.py -k ConfigDefaults`</verify>
  <done>Default threshold fallbacks align with what the DB seed inserts.</done>
</task>

<task type="auto">
  <name>Task 2: Remove hardcoded threshold logic from ReportService risk PDF summary</name>
  <files>backend/app/services/report_service.py, backend/app/api/v1/endpoints/reports.py</files>
  <action>
    Current bug:
    - `generate_risks_pdf()` hardcodes `>= 16` for “High/Critical” count.

    Fix approach (avoid async DB access inside ReportService):
    1) Change signature of `generate_risks_pdf` to accept thresholds from caller:
       - `def generate_risks_pdf(risks: list[Risk], *, high_threshold: int) -> bytes`
       - Use `high_threshold` for “High/Critical” count (because that label means “>= high threshold”).
       - Optional: also accept `critical_threshold` if you want to display both counts.

    2) In `download_risks_pdf` endpoint:
       - Load `high_threshold = await get_config_int(db, "high_risk_min_net_score", ConfigDefaults.HIGH_RISK_MIN_NET_SCORE)`
       - Pass it to `generate_risks_pdf(risks, high_threshold=high_threshold)`

    Testing tip:
    - Add a small pure helper in `report_service.py` like `count_high_risks(risks, high_threshold)` so you can unit test without parsing PDF bytes.
  </action>
  <verify>`rg -n \"\\b16\\b\" backend/app/services/report_service.py` no longer shows threshold hardcode</verify>
  <done>Risk PDF summary count is driven by config-loaded thresholds (passed from the async endpoint).</done>
</task>

<task type="auto">
  <name>Task 3: Make control “critical-linked” checks config-driven in async flows</name>
  <files>backend/app/core/approval_helpers.py, backend/app/api/v1/endpoints/controls.py, backend/app/core/permissions.py</files>
  <action>
    Fix config propagation gaps:

    1) `backend/app/core/approval_helpers.py`:
       - `check_control_requires_privileged_approval` is async but currently uses sync `is_critical_risk(risk)` (defaults-only).
       - Replace with `await is_critical_risk_async(risk, db)` so CRO threshold config applies.

    2) `backend/app/api/v1/endpoints/controls.py`:
       - In the loop that checks linked risks for critical/high status, replace `is_critical_risk(risk)` with `await is_critical_risk_async(risk, db)`.
       - Because `get_config_int` is cached (TTL), this is still efficient in practice.

    3) Optional cleanup:
       - If naming is confusing (`is_critical_risk_*` uses the “high” threshold), document it clearly in the docstring and consider a future rename; do not rename in this plan unless you update all call sites + tests.
  </action>
  <verify>`rg -n \"is_critical_risk\\(\" backend/app/core/approval_helpers.py backend/app/api/v1/endpoints/controls.py` shows no sync usage in async flows</verify>
  <done>All async approval gating logic uses config-driven thresholds (not stale defaults).</done>
</task>

<task type="auto">
  <name>Task 4: Add regression tests proving thresholds change behavior</name>
  <files>backend/tests/test_threshold_propagation.py</files>
  <action>
    Add tests that override `global_config` and assert behavior changes:

    1) `test_is_critical_risk_async_respects_high_risk_min_net_score`:
       - Insert/override GlobalConfig row:
         - key: `high_risk_min_net_score`
         - value_type: `int`
         - value: `"3"`
       - Create a Risk with `net_probability=2`, `net_impact=2` (net_score=4)
       - Assert `await is_critical_risk_async(risk, db_session)` is True
       - Update config value to `"10"` and assert it becomes False

    2) `test_check_control_requires_privileged_approval_respects_threshold`:
       - Create Control + Risk + ControlRiskLink.
       - Set the linked risk net_score just above/below the configured high threshold.
       - Assert `await check_control_requires_privileged_approval(db_session, control.id)` flips as config changes.

    3) `test_count_high_risks_helper_uses_threshold` (if you add the helper in Task 2):
       - Build 3 risks with net_scores [4, 10, 16], pass thresholds 10 and 16, assert counts match expected.

    Cache note:
    - `get_config_int` uses TTL caching. In tests, call `clear_config_cache()` (from `global_config.py`) after updating config values to avoid stale reads.
  </action>
  <verify>`pytest backend/tests/test_threshold_propagation.py`</verify>
  <done>Tests prove that changing global_config thresholds changes approval gating and report summary calculations.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `pytest backend/tests/test_threshold_propagation.py`
- [ ] `rg -n \"\\b16\\b\" backend/app/services/report_service.py` shows no threshold hardcode
- [ ] Manual: change Risk Hub thresholds and confirm control-edit “critical-linked” behavior changes
</verification>

<success_criteria>
- Default thresholds in code match seed defaults (5/10/16)
- Report PDF “High/Critical” count uses config-driven threshold passed from endpoint
- Async approval gating for controls uses config-driven helper (no sync-default leak)
- Regression tests protect threshold propagation (including cache clear)
</success_criteria>

<output>
After completion, create `.planning/phases/72-risk-hub-resolution/72-09-SUMMARY.md`:

# Phase 72 Plan 09: Risk Hub Resolution Summary

**[One-liner about removing backend hardcoded thresholds]**

## Accomplishments
- Aligned threshold defaults with Risk Hub seeds
- Removed hardcoded `>= 16` from risk PDF summary and passed config thresholds from reports endpoint
- Ensured control critical-linked checks use async config-driven threshold logic
- Added regression tests proving threshold changes affect behavior

## Files Created/Modified
- `backend/app/models/global_config.py`
- `backend/app/services/report_service.py`
- `backend/app/api/v1/endpoints/reports.py`
- `backend/app/core/approval_helpers.py`
- `backend/app/api/v1/endpoints/controls.py`
- `backend/tests/test_threshold_propagation.py`

## Next Step
Execute `72-10-PLAN.md`.
</output>
