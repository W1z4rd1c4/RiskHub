---
phase: 72-risk-hub-resolution
type: execute
---

<objective>
Integrate dynamic risk types into backend risk workflows and ensure risk type counts are accurate.

Purpose: Make Risk Hub risk type configuration enforceable in risk create/update/list APIs and visible in config views.
Output: Updated risk schemas/endpoints with dynamic risk type validation, accurate risk type counts, and tests.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/71-risk-hub/71-01-FINDINGS.md
@backend/app/models/risk.py
@backend/app/models/risk_type.py
@backend/app/schemas/risk.py
@backend/app/api/v1/endpoints/risks.py
@backend/app/api/v1/endpoints/riskhub.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded risk type enum with dynamic validation</name>
  <files>backend/app/schemas/risk.py, backend/app/api/v1/endpoints/risks.py, backend/app/models/risk.py</files>
  <action>Remove RiskTypeEnum from schemas and accept risk_type as a string. In risk create/update and list filters, validate the provided risk_type against active records in risk_types (code list) and return a 400 with a clear message if the code is unknown. Keep the default to "operational" when missing. Avoid hardcoding enum values because Risk Hub config must drive allowed types.</action>
  <verify>Creating or updating a risk with an unknown risk_type returns 400, while a configured code succeeds.</verify>
  <done>Risk endpoints accept dynamic codes and reject unknown values without relying on the hardcoded enum.</done>
</task>

<task type="auto">
  <name>Task 2: Compute risk type counts dynamically in Risk Hub endpoints</name>
  <files>backend/app/api/v1/endpoints/riskhub.py, backend/app/models/risk_type.py</files>
  <action>Update risk type list/detail responses to compute risk_count from the risks table (grouped by risk_type code) instead of the stale denormalized column. Keep the response field name for UI compatibility. Avoid mutating risk_count on writes since it drifts without full coverage of all risk changes.</action>
  <verify>Risk Hub risk type list shows correct counts after creating risks with different types.</verify>
  <done>risk_count values in API responses match actual risk records.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for dynamic risk types and counts</name>
  <files>backend/tests/test_riskhub_risk_types.py</files>
  <action>Create tests that add a new risk type config, verify risk creation with that code succeeds, verify unknown codes return 400, and confirm risk type list counts match created risks. Avoid relying on seed data so tests are deterministic.</action>
  <verify>`pytest backend/tests/test_riskhub_risk_types.py`</verify>
  <done>Tests cover dynamic risk type validation and risk_count computation.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `pytest backend/tests/test_riskhub_risk_types.py`
- [ ] Risk create/update rejects unknown risk_type and accepts configured codes
- [ ] Risk type list shows accurate risk_count values
</verification>

<success_criteria>

- Dynamic risk types are accepted end-to-end in the risk API
- Unknown risk types return a clear 400 error
- Risk Hub risk type list shows accurate counts
- Tests cover validation and counts
  </success_criteria>

<output>
After completion, create `.planning/phases/72-risk-hub-resolution/72-01-SUMMARY.md`:

# Phase 72 Plan 01: Risk Hub Resolution Summary

**[Substantive one-liner describing the backend risk type integration]**

## Accomplishments

- [Risk type validation now uses dynamic config]
- [Risk type counts are accurate in Risk Hub]

## Files Created/Modified

- `backend/app/schemas/risk.py` - [describe]
- `backend/app/api/v1/endpoints/risks.py` - [describe]
- `backend/app/api/v1/endpoints/riskhub.py` - [describe]
- `backend/tests/test_riskhub_risk_types.py` - [describe]

## Decisions Made

None

## Issues Encountered

None

## Next Step

Ready for `72-02-PLAN.md`.
</output>
