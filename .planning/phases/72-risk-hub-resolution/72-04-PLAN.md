---
phase: 72-risk-hub-resolution
type: execute
---

<objective>
Harden Risk Hub CRUD endpoints and public-config access based on Phase 71 findings.

Purpose: Prevent runtime errors, unsafe deletions, silent permission issues, and information leakage in Risk Hub admin APIs.
Output: Fixed riskhub endpoints with validation and guards plus regression tests.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/71-risk-hub/71-02-FINDINGS.md
@backend/app/api/v1/endpoints/riskhub.py
@backend/app/models/department.py
@backend/app/models/role.py
@backend/tests/test_riskhub_roles.py
@backend/tests/test_riskhub_departments.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Risk Hub CRUD edge cases and public-config exposure</name>
  <files>backend/app/api/v1/endpoints/riskhub.py, backend/app/models/department.py</files>
  <action>Fix department update response to return is_active (remove is_hidden). Add guard to prevent deletion of system departments. Validate permission_ids on role create/update and return 400 if any are unknown. Enforce department code uniqueness on create/update with clear 400 errors. Restrict public-config endpoint to a safe allowlist or CRO-only categories to prevent exposing internal settings. Avoid breaking existing response shapes so the frontend continues to work.</action>
  <verify>API returns 400 on invalid permission IDs and duplicate department codes; system departments cannot be deleted; public-config does not expose all keys.</verify>
  <done>Risk Hub CRUD operations are guarded and public-config exposure is limited.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression tests for Risk Hub CRUD guards</name>
  <files>backend/tests/test_riskhub_roles.py, backend/tests/test_riskhub_departments.py, backend/tests/test_riskhub_public_config.py</files>
  <action>Extend existing role/department tests to cover update response fields, system department deletion guard, permission_ids validation, and department code uniqueness. Add a focused test for public-config to verify allowlisted keys only. Avoid relying on pre-seeded data by creating fixtures in the test setup.</action>
  <verify>`pytest backend/tests/test_riskhub_roles.py backend/tests/test_riskhub_departments.py backend/tests/test_riskhub_public_config.py`</verify>
  <done>Tests cover CRUD guardrails and public-config restrictions.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `pytest backend/tests/test_riskhub_roles.py backend/tests/test_riskhub_departments.py backend/tests/test_riskhub_public_config.py`
- [ ] System departments cannot be deleted
- [ ] Duplicate department codes return 400
- [ ] public-config does not leak non-allowlisted keys
</verification>

<success_criteria>

- All Risk Hub CRUD fixes are in place
- public-config is safely restricted
- Regression tests validate the guardrails
  </success_criteria>

<output>
After completion, create `.planning/phases/72-risk-hub-resolution/72-04-SUMMARY.md`:

# Phase 72 Plan 04: Risk Hub Resolution Summary

**[Substantive one-liner describing CRUD hardening and public-config gating]**

## Accomplishments

- [Risk Hub CRUD endpoints are hardened]
- [Public-config exposure is constrained]

## Files Created/Modified

- `backend/app/api/v1/endpoints/riskhub.py` - [describe]
- `backend/tests/test_riskhub_roles.py` - [describe]
- `backend/tests/test_riskhub_departments.py` - [describe]
- `backend/tests/test_riskhub_public_config.py` - [describe]

## Decisions Made

None

## Issues Encountered

None

## Next Step

Ready for `72-05-PLAN.md`.
</output>
