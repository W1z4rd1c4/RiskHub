---
phase: 72-risk-hub-resolution
type: execute
domain: frontend
---

<objective>
Update the frontend to consume Risk Hub config via non-CRO “public” endpoints and remove remaining hardcoded assumptions (threshold keys + S/O-only risk type UI).

Purpose: The current frontend hook (`useRiskHubConfig.ts`) incorrectly looks up `risk_threshold_*` keys and uses CRO-only endpoints for config/types, causing silent fallbacks and mislabeling for non-CRO users.
Output: Frontend uses `/riskhub/public-config/{key}` for thresholds and `/riskhub/public-risk-types` for risk types; risk type badges use config display_name/color; risk typing supports custom types; tests verify the frontend is calling the right endpoints.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/72-risk-hub-resolution/72-05-SUMMARY.md
@.planning/phases/72-risk-hub-resolution/72-10-PLAN.md
@frontend/src/services/riskHubApi.ts
@frontend/src/hooks/useRiskHubConfig.ts
@frontend/src/types/risk.ts
@frontend/src/pages/RisksPage.tsx
@frontend/src/pages/RiskDetailPage.tsx
@frontend/tests/approval_workflow_ui.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Risk Hub API client wrappers for public endpoints</name>
  <files>frontend/src/services/riskHubApi.ts</files>
  <action>
    Extend `riskHubApi` with wrappers that match the new backend endpoints from 72-10:
    - `getPublicRiskTypes(): Promise<PublicRiskType[]>` → `GET /riskhub/public-risk-types`
      - Define `PublicRiskType` interface with: `code`, `display_name`, `color`, `icon`, `sort_order`
    - Reuse existing `getConfigValue(key)` for `GET /riskhub/public-config/{key}`

    Keep existing CRO-only methods (getRiskTypes/getAllConfig) unchanged for Risk Hub admin screens.
  </action>
  <verify>`rg -n \"getPublicRiskTypes\" frontend/src/services/riskHubApi.ts`</verify>
  <done>Frontend has explicit API methods for the safe public endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Fix `useRiskThresholds()` to use correct keys via public-config</name>
  <files>frontend/src/hooks/useRiskHubConfig.ts</files>
  <action>
    Replace the current CRO-only `/riskhub/config` category lookup and wrong keys:
    - Remove `risk_threshold_critical|high|medium`
    - Use the real seeded keys via `riskHubApi.getConfigValue(...)`:
      - `critical_risk_min_net_score` (default 16)
      - `high_risk_min_net_score` (default 10)
      - `medium_risk_min_net_score` (default 5)

    Implementation sketch (inside react-query queryFn):
    - `const [critical, high, medium] = await Promise.all([...])`
    - Parse values robustly:
      - If API returns a number → use it
      - If API returns string → `parseInt`
      - On error/missing → fallback defaults

    Keep the helper methods `getScoreColor/getMatrixCellColor/getScoreBadgeColor` unchanged, but ensure they use the fetched thresholds (not silent defaults due to key mismatch).
  </action>
  <verify>Manual: open `/risks` as non-CRO and confirm network calls hit `/riskhub/public-config/*_risk_min_net_score`</verify>
  <done>Thresholds are read from the correct keys for all authenticated users.</done>
</task>

<task type="auto">
  <name>Task 3: Fix `useRiskTypes()` to use public risk types endpoint</name>
  <files>frontend/src/hooks/useRiskHubConfig.ts</files>
  <action>
    Update `useRiskTypes`:
    - Query `riskHubApi.getPublicRiskTypes()` instead of CRO-only `getRiskTypes()`.
    - Maintain fallback `FALLBACK_RISK_TYPES` when the public endpoint fails.
    - Ensure `getDisplayName(code)` and `getColor(code)` work with the public-type shape.

    Tip: You can keep the internal type as the existing `RiskTypeConfig` shape by mapping:
    - `PublicRiskType` → the internal `RiskTypeConfig` (fill unused fields with safe defaults),
    OR simplify the hook to work directly with the smaller public type.
  </action>
  <verify>Manual: open `/risks` as non-CRO and confirm network calls hit `/riskhub/public-risk-types` (not `/riskhub/risk-types`).</verify>
  <done>Risk types used on non-CRO pages come from public endpoint and not CRO-only Risk Hub routes.</done>
</task>

<task type="auto">
  <name>Task 4: Remove remaining S/O-only risk type UI and relax typing</name>
  <files>frontend/src/types/risk.ts, frontend/src/pages/RisksPage.tsx, frontend/src/pages/RiskDetailPage.tsx</files>
  <action>
    1) Update typing:
    - Replace `export type RiskType = 'strategic' | 'operational'` with `export type RiskType = string` (or remove entirely and use `string` for `risk_type`).
    - Update `Risk`, `RiskSummary`, `RiskCreate` interfaces so `risk_type: string`.
    - Keep constants for system codes if useful (`STRATEGIC`, `OPERATIONAL`), but do not constrain the type.

    2) RisksPage type badge:
    - Replace `{risk.risk_type === 'strategic' ? 'S' : 'O'}` with config-driven label:
      - Use `useRiskTypes().getDisplayName(risk.risk_type)` for tooltip/full label.
      - For compact badge text, use initials from display_name (e.g., “IT Security” → “IS”) or the first 2 chars of the code.
    - Use config color:
      - Use `useRiskTypes().getColor(code)` to get a hex color.
      - Implement a tiny helper `hexToRgba(hex, alpha)` for background/border and set inline `style`:
        - `style={{ color: hex, backgroundColor: hexToRgba(hex, 0.12), borderColor: hexToRgba(hex, 0.24) }}`
      - Keep a neutral fallback when hex is missing/invalid.

    3) RiskDetailPage classification type chip:
    - Replace the strategic/operational conditional styling with the same config-driven approach (prefer showing full display_name here).
  </action>
  <verify>`rg -n \"\\? 'S' : 'O'|risk_type === 'strategic'\" frontend/src/pages/RisksPage.tsx frontend/src/pages/RiskDetailPage.tsx` returns no matches</verify>
  <done>Risk type display is fully dynamic and supports custom types, with consistent styling driven by config.</done>
</task>

<task type="auto">
  <name>Task 5: Add automated tests that prove the frontend uses public endpoints + correct keys</name>
  <files>frontend/tests/riskhub_public_config_consumption.spec.ts</files>
  <action>
    Add a Playwright spec focused on the “no silent fallback” guarantee by asserting network calls.

    Suggested test:
    - Login as a non-CRO user (reuse the existing test users used in `approval_workflow_ui.spec.ts`).
    - Navigate to `/risks`.
    - Capture requested URLs:
      - Listen on `page.on('request', ...)` and record requests containing:
        - `/api/v1/riskhub/public-risk-types`
        - `/api/v1/riskhub/public-config/high_risk_min_net_score`
        - `/api/v1/riskhub/public-config/critical_risk_min_net_score`
        - `/api/v1/riskhub/public-config/medium_risk_min_net_score`
    - Assert each expected URL was requested at least once.

    Optional UI assertions (keep non-brittle):
    - Ensure the type badge text is NOT strictly limited to single-letter “S” or “O” by checking it has length >= 2 or by asserting the tooltip contains the display name.
  </action>
  <verify>`npm --prefix frontend test` (or `npx --prefix frontend playwright test`) in your environment</verify>
  <done>Automated test confirms the frontend calls the correct public endpoints/keys (prevents future regressions to CRO-only endpoints or wrong key names).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Frontend risk pages consume public Risk Hub config and display risk types/thresholds dynamically for non-CRO users</what-built>
  <how-to-verify>
    1. Run backend + frontend locally.
    2. Login as a non-CRO user.
    3. Open `/risks`:
       - Confirm network calls hit `/riskhub/public-risk-types` and `/riskhub/public-config/*_risk_min_net_score`.
       - Confirm type badges show meaningful labels for strategic/operational AND don’t break for custom types returned by backend.
       - Confirm score colors change if thresholds are edited in Risk Hub by CRO.
    4. Open a risk detail page and confirm the “Type” chip uses config display_name/color (no strategic-only special casing).
  </how-to-verify>
  <resume-signal>Type “approved” to continue, or describe what’s wrong</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Playwright spec passes: `frontend/tests/riskhub_public_config_consumption.spec.ts`
- [ ] No remaining `risk_threshold_*` key usage in frontend
- [ ] Manual verification approved
</verification>

<success_criteria>
- Non-CRO frontend pages fetch thresholds via `*_risk_min_net_score` keys (public-config)
- Non-CRO frontend pages fetch risk types via `public-risk-types`
- Risk type display/typing supports arbitrary configured types (no S/O-only assumptions)
- Automated test prevents regression to CRO-only endpoints or wrong keys
</success_criteria>

<output>
After completion, create `.planning/phases/72-risk-hub-resolution/72-11-SUMMARY.md`:

# Phase 72 Plan 11: Risk Hub Resolution Summary

**[One-liner about frontend using public config and removing S/O hardcoding]**

## Accomplishments
- Updated Risk Hub config hooks to use public endpoints and correct threshold keys
- Removed remaining hardcoded strategic/operational-only risk type display and relaxed typing
- Added Playwright test asserting correct public endpoint usage

## Files Created/Modified
- `frontend/src/services/riskHubApi.ts`
- `frontend/src/hooks/useRiskHubConfig.ts`
- `frontend/src/types/risk.ts`
- `frontend/src/pages/RisksPage.tsx`
- `frontend/src/pages/RiskDetailPage.tsx`
- `frontend/tests/riskhub_public_config_consumption.spec.ts`

## Next Step
Phase 72 resolution is fully planned; execute the remaining plans in order.
</output>
