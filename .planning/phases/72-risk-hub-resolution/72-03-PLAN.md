---
phase: 72-risk-hub-resolution
type: execute
---

<objective>
Apply Risk Hub approval scenario configuration to approval creation and notification routing.

Purpose: Ensure CRO-configured approval rules and approver roles are enforced throughout risk, control, and KRI workflows.
Output: Approval scenario-driven enforcement, dynamic approver routing, and tests.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/71-risk-hub/71-01-FINDINGS.md
@backend/app/models/approval_scenario.py
@backend/app/models/approval_request.py
@backend/app/api/v1/endpoints/riskhub.py
@backend/app/api/v1/endpoints/approvals.py
@backend/app/api/v1/endpoints/risks.py
@backend/app/api/v1/endpoints/controls.py
@backend/app/api/v1/endpoints/kris.py
@backend/app/services/notification_service.py
@backend/app/core/approval_helpers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce approval scenarios during approval creation</name>
  <files>backend/app/api/v1/endpoints/risks.py, backend/app/api/v1/endpoints/controls.py, backend/app/api/v1/endpoints/kris.py, backend/app/core/approval_helpers.py</files>
  <action>Introduce a helper that loads approval scenario config by key and returns requires_approval and approver_roles. Apply it where approvals are created (risk edit/delete, control edit/delete, KRI edit/delete/value submission) to decide whether to create an approval request or apply changes immediately. Avoid bypassing existing privileged logic; instead combine scenario rules with current "privileged" and "risk owner" flows so configured scenarios can disable or require approvals without breaking tiered approvals.</action>
  <verify>Scenario set to requires_approval=false allows direct edit/delete without creating approval requests.</verify>
  <done>Approval creation flows respect scenario requires_approval and approver roles.</done>
</task>

<task type="auto">
  <name>Task 2: Route approval notifications using scenario approver roles</name>
  <files>backend/app/services/notification_service.py, backend/app/core/approval_helpers.py</files>
  <action>Update NotificationService to resolve approver recipients from scenario approver_roles, including support for dynamic roles like risk_owner (map to the relevant owner on the resource). Preserve existing fallbacks so notifications still work if a scenario is missing. Avoid hardcoded role lists to prevent divergence from Risk Hub configuration.</action>
  <verify>Approval notifications target users determined by scenario approver_roles, including risk owner mapping.</verify>
  <done>Notification routing aligns with configured approval scenarios.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for approval scenario enforcement</name>
  <files>backend/tests/test_approval_scenarios.py</files>
  <action>Add tests that toggle scenario requires_approval and approver_roles, then assert approval creation behavior and approver assignment. Cover at least one risk edit and one control edit scenario. Avoid brittle test setup by creating required users, roles, and scenario records in the test itself.</action>
  <verify>`pytest backend/tests/test_approval_scenarios.py`</verify>
  <done>Tests confirm scenario config affects approvals and notifications.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `pytest backend/tests/test_approval_scenarios.py`
- [ ] Scenario requires_approval toggles approval creation
- [ ] Notification recipients align with scenario approver roles
</verification>

<success_criteria>

- Approval requests are created or bypassed based on scenario config
- Approver selection uses configured roles, including risk_owner mapping
- Tests validate scenario enforcement
  </success_criteria>

<output>
After completion, create `.planning/phases/72-risk-hub-resolution/72-03-SUMMARY.md`:

# Phase 72 Plan 03: Risk Hub Resolution Summary

**[Substantive one-liner describing approval scenario enforcement]**

## Accomplishments

- [Scenario config now drives approval creation]
- [Notifications route to configured approvers]

## Files Created/Modified

- `backend/app/core/approval_helpers.py` - [describe]
- `backend/app/api/v1/endpoints/risks.py` - [describe]
- `backend/app/api/v1/endpoints/controls.py` - [describe]
- `backend/app/api/v1/endpoints/kris.py` - [describe]
- `backend/app/services/notification_service.py` - [describe]
- `backend/tests/test_approval_scenarios.py` - [describe]

## Decisions Made

None

## Issues Encountered

None

## Next Step

Ready for `72-04-PLAN.md`.
</output>
