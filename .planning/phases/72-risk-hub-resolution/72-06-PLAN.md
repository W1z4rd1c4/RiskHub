---
phase: 72-risk-hub-resolution
type: execute
domain: approvals
---

<objective>
Split out the “Approval Scenarios” foundation work: persist scenario identity on approvals, fix API schema gaps, and ensure the database has scenario rows for every approval-producing flow we already have.

Purpose: Without a persisted `scenario_key` + complete scenario seed coverage, later changes cannot reliably enforce `requires_approval` or `approver_roles` across different approval flows (delete vs edit vs KRI value submissions).
Output: `approval_requests.scenario_key` exists (nullable + indexed), approval API can represent tiered `pending_privileged`, and required scenario keys are present in DB (seed migration) and in tests (fixture).
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/71-risk-hub/71-01-FINDINGS.md
@backend/alembic/versions/74f4ad1b68cb_add_risk_hub_tables.py
@backend/app/models/approval_request.py
@backend/app/models/approval_scenario.py
@backend/app/schemas/approval_request.py
@backend/app/api/v1/endpoints/approvals.py
@backend/tests/conftest.py
@backend/tests/test_approvals.py
@backend/tests/test_approval_workflow.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `scenario_key` to ApprovalRequest (ORM + migration)</name>
  <files>backend/app/models/approval_request.py, backend/alembic/versions/*_add_approval_request_scenario_key.py</files>
  <action>
    1) Update SQLAlchemy model:
       - Add `scenario_key: Mapped[str | None] = mapped_column(String(50), nullable=True)`
       - Add an index for it (either `Index("ix_approval_scenario_key", "scenario_key")` in `__table_args__` or `index=True` on the column; prefer explicit Index to match Alembic naming).
       - Keep it nullable to avoid breaking existing rows.

    2) Create Alembic migration:
       - `op.add_column("approval_requests", sa.Column("scenario_key", sa.String(length=50), nullable=True))`
       - `op.create_index("ix_approval_requests_scenario_key", "approval_requests", ["scenario_key"], unique=False)`

    Notes:
    - Tests use `Base.metadata.create_all` (SQLite) so migrations are not run in CI. You must still add the column to the ORM model for tests to pass.
  </action>
  <verify>
    - `rg -n "scenario_key" backend/app/models/approval_request.py` shows the field exists
    - Alembic revision file exists under `backend/alembic/versions/`
  </verify>
  <done>ApprovalRequest can persist scenario identity in both ORM and prod DB via migration.</done>
</task>

<task type="auto">
  <name>Task 2: Fix approval API schema gaps (`pending_privileged` + `scenario_key`)</name>
  <files>backend/app/schemas/approval_request.py, backend/app/api/v1/endpoints/approvals.py</files>
  <action>
    1) Update `backend/app/schemas/approval_request.py`:
       - Add `pending_privileged = "pending_privileged"` to `ApprovalStatusEnum`.
       - Add `scenario_key: str | None = None` to `ApprovalRequestRead`.

    2) Update approval response builder in `backend/app/api/v1/endpoints/approvals.py`:
       - In `_build_approval_read(...)`, include `"scenario_key": approval.scenario_key`.

    Why this matters:
    - The backend already uses tiered approvals (`ApprovalStatus.PENDING_PRIVILEGED`). If the API schema cannot represent this status, FastAPI response-model validation can raise 500s on `GET /approvals/{id}` and on approve transitions.
  </action>
  <verify>`pytest backend/tests -k \"pending_privileged|approval.*schema\"`</verify>
  <done>API can safely return `pending_privileged` approvals and includes `scenario_key` field (nullable for now).</done>
</task>

<task type="auto">
  <name>Task 3: Seed missing ApprovalScenario rows (migration, idempotent)</name>
  <files>backend/alembic/versions/*_seed_missing_approval_scenarios.py</files>
  <action>
    Create a new Alembic migration that adds the missing scenarios needed for current code paths.

    Required new scenario keys:
    - `risk_edit_sensitive`: approvals created by `PATCH /risks/{id}` for sensitive field changes
    - `kri_edit`: approvals created by `PUT /kris/{id}` for non-privileged edits

    Recommended defaults (only applied on insert):
    - `risk_edit_sensitive` approver_roles: `["risk_manager","cro"]`
    - `kri_edit` approver_roles: `["risk_manager","cro"]`

    Also patch tiered scenario defaults ONLY when unchanged from their original seed (to avoid overwriting CRO edits):
    - If `kri_value_submit.approver_roles` is exactly `["risk_owner"]`, update to `["risk_owner","risk_manager","cro"]`
    - If `control_edit.approver_roles` is exactly `["risk_owner"]`, update to `["risk_owner","risk_manager","cro"]`

    Implementation detail:
    - Use idempotent “insert if missing” SQL:
      - Postgres: `INSERT ... ON CONFLICT (key) DO NOTHING`
      - Or portable SQL: `INSERT ... SELECT ... WHERE NOT EXISTS (SELECT 1 FROM approval_scenarios WHERE key = ...)`
    - For the default-role patch, use `UPDATE ... WHERE key = '...' AND approver_roles = '[...]'`.
  </action>
  <verify>Migration upgrades cleanly on a real DB (dev environment): `alembic upgrade head`</verify>
  <done>Prod DB will contain all scenario rows required by existing approval-producing flows.</done>
</task>

<task type="auto">
  <name>Task 4: Add a reusable test fixture that seeds approval scenarios</name>
  <files>backend/tests/conftest.py</files>
  <action>
    Add a fixture (function-scoped) like `seed_approval_scenarios(db_session)` that inserts ApprovalScenario rows for all scenario keys used by the app:
    - `risk_delete`, `control_delete`, `kri_delete`
    - `risk_edit_priority`, `risk_edit_sensitive`
    - `control_edit`
    - `kri_edit`, `kri_value_submit`

    Keep the roles aligned with the intended behavior:
    - delete/edit scenarios: `["risk_manager","cro"]`
    - tiered scenarios: `["risk_owner","risk_manager","cro"]`

    Make it explicit in the fixture docstring: “Tests do not run Alembic seeds; this fixture emulates the seed state.”
  </action>
  <verify>`pytest backend/tests -k seed_approval_scenarios` (after adding tests in Task 5)</verify>
  <done>All later approval tests can opt-in to a consistent scenario baseline via one fixture.</done>
</task>

<task type="auto">
  <name>Task 5: Add regression tests for `pending_privileged` and `scenario_key` exposure</name>
  <files>backend/tests/test_approval_scenario_schema.py</files>
  <action>
    Add focused tests that fail on the current bug and protect the schema moving forward:

    1) `test_approval_read_allows_pending_privileged_status`:
       - Create an ApprovalRequest row directly with:
         - `status=ApprovalStatus.PENDING_PRIVILEGED`
         - `scenario_key="kri_value_submit"`
       - Call `GET /api/v1/approvals/{id}` as `client_risk_manager`.
       - Assert:
         - `status_code == 200`
         - `json["status"] == "pending_privileged"`
         - `json["scenario_key"] == "kri_value_submit"`

    2) `test_approval_read_includes_scenario_key_field_even_when_null`:
       - Create ApprovalRequest with `scenario_key=None`
       - Call `GET /api/v1/approvals/{id}`
       - Assert key exists (value is `null`)

    3) Optional unit-level schema test (very fast):
       - Call `ApprovalRequestRead.model_validate({...})` with `status="pending_privileged"` and ensure it validates.
  </action>
  <verify>`pytest backend/tests/test_approval_scenario_schema.py`</verify>
  <done>Tiered approval status + scenario_key are safely representable in API responses (no response-model 500s).</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `pytest backend/tests/test_approval_scenario_schema.py`
- [ ] `pytest backend/tests/test_approvals.py -k \"get_approval|list_approvals\"` (smoke)
- [ ] Alembic migration files exist for scenario_key + scenario seed coverage
</verification>

<success_criteria>
- `ApprovalRequest` has nullable indexed `scenario_key`
- Approval API schemas support `pending_privileged` and include `scenario_key`
- DB seed migration adds missing scenario keys without overwriting CRO-edited config
- Tests cover `pending_privileged` response safety and `scenario_key` exposure
</success_criteria>

<output>
After completion, create `.planning/phases/72-risk-hub-resolution/72-06-SUMMARY.md`:

# Phase 72 Plan 06: Risk Hub Resolution Summary

**[One-liner about scenario_key persistence + API schema hardening]**

## Accomplishments
- Added `approval_requests.scenario_key` (nullable + indexed) and surfaced it in approval API responses
- Fixed approval API schema to support tiered `pending_privileged` status
- Added seed migration for missing approval scenarios and a test fixture to emulate seeds in SQLite tests

## Files Created/Modified
- `backend/app/models/approval_request.py`
- `backend/app/schemas/approval_request.py`
- `backend/app/api/v1/endpoints/approvals.py`
- `backend/alembic/versions/*_add_approval_request_scenario_key.py`
- `backend/alembic/versions/*_seed_missing_approval_scenarios.py`
- `backend/tests/conftest.py`
- `backend/tests/test_approval_scenario_schema.py`

## Next Step
Execute `72-07-PLAN.md`.
</output>
