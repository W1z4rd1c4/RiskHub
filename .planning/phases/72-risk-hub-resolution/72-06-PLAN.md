---
phase: 72-risk-hub-resolution
type: execute
---

<objective>
Make Risk Hub “Approval Scenarios” actually drive approval behavior end-to-end (creation, enforcement, and notifications) across every approval-producing Risk Hub flow.

Purpose: CRO-edited `approval_scenarios` must control (1) whether approvals are required, (2) who is allowed to approve/reject, and (3) who gets notified — without scattered hardcoded logic in endpoints.
Output: Every ApprovalRequest persists `scenario_key`, all approval creation flows consult `ApprovalScenario.requires_approval`, approval resolution enforces `ApprovalScenario.approver_roles` (incl. `risk_owner`), and notifications are consistently sent from a single place with regression tests.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/71-risk-hub/71-01-FINDINGS.md
@.planning/phases/71-risk-hub/71-02-FINDINGS.md
@.planning/phases/71-risk-hub/71-03-FINDINGS.md
@backend/app/models/approval_scenario.py
@backend/app/models/approval_request.py
@backend/app/schemas/approval_request.py
@backend/app/core/permissions.py
@backend/app/core/approval_helpers.py
@backend/app/api/v1/endpoints/approvals.py
@backend/app/api/v1/endpoints/risks.py
@backend/app/api/v1/endpoints/controls.py
@backend/app/api/v1/endpoints/kris.py
@backend/app/services/notification_service.py
@backend/alembic/versions/74f4ad1b68cb_add_risk_hub_tables.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory approval-producing flows and finalize scenario coverage</name>
  <files>backend/app/api/v1/endpoints/risks.py, backend/app/api/v1/endpoints/controls.py, backend/app/api/v1/endpoints/kris.py, backend/app/api/v1/endpoints/approvals.py, backend/alembic/versions/*_seed_missing_approval_scenarios.py</files>
  <action>
    Use `rg "ApprovalRequest\\(" backend/app/api/v1/endpoints` to enumerate every code path that creates ApprovalRequest and confirm each maps to a scenario_key.
    
    Ensure the scenario table covers all *existing* approval-producing flows (not just the originally seeded keys). Minimum required scenario keys to make coverage complete:
    - Existing: `risk_delete`, `control_delete`, `kri_delete`, `risk_edit_priority`, `kri_value_submit`, `control_edit`
    - Add (new, seeded): `risk_edit_sensitive` (risk sensitive-field edits), `kri_edit` (KRI edits that currently create approvals)
    
    Create an Alembic migration that inserts any missing scenarios (idempotent insert by key) with sensible defaults:
    - `risk_edit_sensitive`: default approver_roles `["risk_manager","cro"]`
    - `kri_edit`: default approver_roles `["risk_manager","cro"]`
    
    Also update seeded defaults for tiered scenarios so privileged approvers exist by default (without overriding CRO-changed configs):
    - If `control_edit.approver_roles` is exactly `["risk_owner"]`, update to `["risk_owner","risk_manager","cro"]`
    - If `kri_value_submit.approver_roles` is exactly `["risk_owner"]`, update to `["risk_owner","risk_manager","cro"]`
  </action>
  <verify>`pytest backend/tests -k riskhub_approval_scenarios` (new tests from Task 5)</verify>
  <done>Every approval-producing flow has a scenario_key mapping, and DB has scenario rows for all required keys.</done>
</task>

<task type="auto">
  <name>Task 2: Persist scenario identity on ApprovalRequest (schema + migration)</name>
  <files>backend/app/models/approval_request.py, backend/app/schemas/approval_request.py, backend/app/api/v1/endpoints/approvals.py, backend/alembic/versions/*_add_approval_request_scenario_key.py</files>
  <action>
    Add nullable `scenario_key` (String) to `approval_requests` with an index; create an Alembic migration. Keep it nullable to avoid breaking existing rows.
    
    Update API schemas and response builders to surface `scenario_key` (for debugging/UI) and fix the status enum mismatch by including `pending_privileged` in `ApprovalStatusEnum` so tiered approvals don’t 500 on response validation.
  </action>
  <verify>`pytest backend/tests -k approval_request_schema` (new tests from Task 5)</verify>
  <done>ApprovalRequest persists scenario_key and API responses can safely return `pending_privileged` without validation errors.</done>
</task>

<task type="auto">
  <name>Task 3: Centralize scenario lookup + approval creation + notifications</name>
  <files>backend/app/services/approval_workflow_service.py, backend/app/services/notification_service.py</files>
  <action>
    Create a small service (e.g., `ApprovalWorkflowService`) that becomes the only supported way to create an ApprovalRequest from business endpoints.
    
    Responsibilities:
    - Load ApprovalScenario by key and decide `requires_approval` (fallback-safe for legacy/missing keys).
    - Create ApprovalRequest with `scenario_key` set and the correct primary/tiered fields (`primary_approver_id`, `requires_privileged_approval`) when applicable.
    - Trigger notifications for all relevant recipients based on scenario approver_roles:
      - Special role `risk_owner` resolves to `approval.primary_approver_id` (and errors loudly in logs if missing).
      - Other roles resolve to active users with `users.role.name` in the scenario role list.
      - Dedupe recipients and skip the requester.
    
    Keep `NotificationService.create_notification` as the low-level primitive, but route *all* approval notifications through a single high-level method so risks/kris/controls don’t diverge.
  </action>
  <verify>`pytest backend/tests -k approval_notification` (new tests from Task 5)</verify>
  <done>There is exactly one codepath that creates approvals + sends notifications, and it is scenario-driven.</done>
</task>

<task type="auto">
  <name>Task 4: Wire scenario enforcement into all approval-producing endpoints</name>
  <files>backend/app/api/v1/endpoints/risks.py, backend/app/api/v1/endpoints/controls.py, backend/app/api/v1/endpoints/kris.py, backend/app/api/v1/endpoints/approvals.py</files>
  <action>
    Replace direct ApprovalRequest construction in endpoints with `ApprovalWorkflowService` so every flow:
    - sets scenario_key
    - consults `ApprovalScenario.requires_approval` (if false → apply change immediately, no ApprovalRequest)
    - sends notifications consistently
    
    Required mapping rules (must match actual current flows):
    - Risk delete (`DELETE /risks/{id}` non-privileged): `risk_delete`
    - Control delete (`DELETE /controls/{id}` non-privileged): `control_delete`
    - KRI delete (`DELETE /kris/{id}` non-privileged): `kri_delete`
    - Risk update approvals:
      - If non-privileged edit hits sensitive-field approval path → `risk_edit_sensitive`
      - If Risk Hub policy intends approvals for “priority/high risk edits”, ensure the `risk_edit_priority` scenario is actually used (e.g., when editing a priority/high-risk risk) and produces an edit ApprovalRequest instead of applying.
    - KRI update approvals (`PUT /kris/{id}` non-privileged): `kri_edit`
    - KRI value submission approvals (`POST /kris/{id}/values` non-privileged): `kri_value_submit`
    - Control edit approvals (`PATCH /controls/{id}` non-privileged approval path): `control_edit`
    
    Ensure risk and KRI endpoints that currently return 202 on approval creation also trigger notifications (they currently don’t).
  </action>
  <verify>Manual smoke: create approvals via risk delete + risk edit + KRI edit and confirm notifications exist for expected approvers.</verify>
  <done>All ApprovalRequest rows created by business endpoints have scenario_key set and are created only when scenario requires approval.</done>
</task>

<task type="auto">
  <name>Task 5: Enforce approver_roles on approve/reject and add regression tests</name>
  <files>backend/app/api/v1/endpoints/approvals.py, backend/tests/test_approval_scenarios.py</files>
  <action>
    Update approve/reject logic to enforce scenario roles:
    - Load scenario using `approval.scenario_key` (fallback to legacy behavior if null/missing).
    - Allow Risk Owner approvals only when `risk_owner` is in approver_roles AND `approval.primary_approver_id == current_user.id`.
    - Allow privileged approvals/rejections only when `current_user.role.name` is in approver_roles AND `can_resolve_approvals(current_user)` is true.
    - Keep the existing tiered state machine, but ensure `pending_privileged` remains representable in API output.
    
    Add regression tests covering:
    - `pending_privileged` response schema does not fail
    - `requires_approval=false` bypasses ApprovalRequest creation for at least one delete scenario and one edit scenario
    - scenario_key is set for each mapped flow
    - notification recipients are derived from scenario roles (incl. `risk_owner`)
    - non-listed roles cannot approve/reject scenario-backed approvals
  </action>
  <verify>`pytest backend/tests/test_approval_scenarios.py`</verify>
  <done>Scenario roles affect who can approve/reject and who is notified, with tests preventing regressions.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `pytest backend/tests/test_approval_scenarios.py`
- [ ] Creating approvals via risks/kris endpoints sends notifications (not just controls/approvals endpoints)
- [ ] `ApprovalStatusEnum` supports `pending_privileged` (no response validation 500)
- [ ] `requires_approval=false` bypasses ApprovalRequest creation for at least one delete scenario and one edit scenario
- [ ] Approve/reject endpoints enforce scenario approver_roles (incl. `risk_owner`)
</verification>

<success_criteria>

- Every ApprovalRequest created by Risk Hub flows has `scenario_key`
- `ApprovalScenario.requires_approval` governs whether approvals are created across all mapped flows
- `ApprovalScenario.approver_roles` governs both notifications and who can approve/reject (with `risk_owner` mapping)
- No hardcoded approver role lists remain in approval notification routing
- Regression tests cover scenario enforcement + pending_privileged schema
  </success_criteria>

<output>
After completion, create `.planning/phases/72-risk-hub-resolution/72-06-SUMMARY.md`:

# Phase 72 Plan 06: Risk Hub Resolution Summary

**[Substantive one-liner describing scenario-driven approval creation + enforcement + notifications]**

## Accomplishments

- [All approval-producing flows map to scenario_key]
- [Scenario requires_approval governs approval creation]
- [Scenario approver_roles govern notifications + approve/reject enforcement]

## Files Created/Modified

- `backend/app/models/approval_request.py` - [describe]
- `backend/app/schemas/approval_request.py` - [describe]
- `backend/app/services/approval_workflow_service.py` - [describe]
- `backend/app/services/notification_service.py` - [describe]
- `backend/app/api/v1/endpoints/approvals.py` - [describe]
- `backend/app/api/v1/endpoints/risks.py` - [describe]
- `backend/app/api/v1/endpoints/kris.py` - [describe]
- `backend/alembic/versions/*_add_approval_request_scenario_key.py` - [describe]
- `backend/alembic/versions/*_seed_missing_approval_scenarios.py` - [describe]
- `backend/tests/test_approval_scenarios.py` - [describe]

## Decisions Made

None

## Issues Encountered

None

## Next Step

Ready for `72-07-PLAN.md`.
</output>
