---
phase: 72-risk-hub-resolution
type: execute
---

<objective>
Wire Risk Hub global configuration into risk thresholds and notification timing in the backend.

Purpose: Ensure CRO-configured settings affect risk severity and KRI reminders instead of hardcoded constants.
Output: Config-driven threshold and notification logic with regression tests.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/71-risk-hub/71-01-FINDINGS.md
@backend/app/models/global_config.py
@backend/app/api/v1/endpoints/riskhub.py
@backend/app/core/permissions.py
@backend/app/services/report_service.py
@backend/app/services/kri_deadline_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed config lookup and apply it to risk thresholds</name>
  <files>backend/app/models/global_config.py, backend/app/core/permissions.py, backend/app/services/report_service.py</files>
  <action>Create a helper to fetch typed global_config values with defaults (and lightweight caching) to avoid repeated DB hits. Replace hardcoded threshold values in risk severity logic and reporting with config-driven values. Avoid per-row queries inside loops; load config once per operation.</action>
  <verify>Changing risk threshold config affects is_critical_risk and report outputs.</verify>
  <done>Risk severity and reporting thresholds are sourced from global_config with safe defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Apply notification timing config to KRI deadline processing</name>
  <files>backend/app/services/kri_deadline_service.py</files>
  <action>Replace hardcoded reminder and escalation windows with values from global_config (notifications category). Ensure values are validated as integers and fall back to defaults if missing. Avoid breaking existing schedule semantics by preserving the same units (days) and relative offsets.</action>
  <verify>KRI deadline service uses configured reminder and escalation values in its calculations.</verify>
  <done>Reminder/escalation timing is driven by global_config values.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for config-driven thresholds</name>
  <files>backend/tests/test_global_config_usage.py</files>
  <action>Add tests that override relevant global_config keys and assert threshold-dependent logic changes (e.g., is_critical_risk, report severity classification, or notification timing calculations). Avoid relying on seed state by inserting config values within the test.</action>
  <verify>`pytest backend/tests/test_global_config_usage.py`</verify>
  <done>Tests prove config values are read and applied in critical paths.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `pytest backend/tests/test_global_config_usage.py`
- [ ] Risk severity thresholds use global_config values
- [ ] KRI reminder/escalation timing uses global_config values
</verification>

<success_criteria>

- All threshold and notification timing constants are config-driven
- Defaults are used safely when config is missing
- Tests validate the new behavior
  </success_criteria>

<output>
After completion, create `.planning/phases/72-risk-hub-resolution/72-02-SUMMARY.md`:

# Phase 72 Plan 02: Risk Hub Resolution Summary

**[Substantive one-liner describing config-driven thresholds and notifications]**

## Accomplishments

- [Risk severity thresholds now use global_config]
- [KRI reminders now respect notification settings]

## Files Created/Modified

- `backend/app/models/global_config.py` - [describe]
- `backend/app/core/permissions.py` - [describe]
- `backend/app/services/report_service.py` - [describe]
- `backend/app/services/kri_deadline_service.py` - [describe]
- `backend/tests/test_global_config_usage.py` - [describe]

## Decisions Made

None

## Issues Encountered

None

## Next Step

Ready for `72-03-PLAN.md`.
</output>
