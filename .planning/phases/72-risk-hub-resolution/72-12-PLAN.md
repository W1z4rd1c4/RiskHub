---
phase: 72-risk-hub-resolution
type: execute
domain: naming
---

<objective>
Clean up confusing “critical risk” helper naming so code reflects actual semantics: approval gating uses the configured *high-risk* threshold (plus `is_priority`), while “critical risk” uses `critical_risk_min_net_score` for reporting/metrics.

Purpose: `is_critical_risk*` currently checks `high_risk_min_net_score`, which is semantically misleading and makes future changes risky.
Output: New helpers with correct names exist, call sites use them, and tests/rg checks prevent regressions.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@backend/app/core/permissions.py
@backend/app/core/approval_helpers.py
@backend/app/api/v1/endpoints/controls.py
@backend/tests/test_global_config_usage.py
@backend/tests/test_threshold_propagation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce correctly named helpers and keep backwards-compatible aliases</name>
  <files>backend/app/core/permissions.py</files>
  <action>
    Add new helpers with explicit semantics:
    - `is_high_risk_for_approval(risk) -> bool` (sync; uses `ConfigDefaults.HIGH_RISK_MIN_NET_SCORE`)
    - `is_high_risk_for_approval_async(risk, db) -> bool` (async; uses `high_risk_min_net_score`)

    Keep `is_critical_risk` / `is_critical_risk_async` as thin aliases (calling the new helpers) for backward compatibility,
    but update their docstrings to clearly state they are deprecated/misnamed and refer to the new names.

    Do NOT change behavior in this task—only naming, structure, and docs.
  </action>
  <verify>`rg -n \"def is_high_risk_for_approval\" backend/app/core/permissions.py`</verify>
  <done>New helper names exist; old names remain as compatibility aliases.</done>
</task>

<task type="auto">
  <name>Task 2: Update approval gating call sites to use new helper names</name>
  <files>backend/app/core/approval_helpers.py, backend/app/api/v1/endpoints/controls.py</files>
  <action>
    Replace direct calls to `is_critical_risk(_async)` with the new `is_high_risk_for_approval(_async)` equivalents.
    Keep behavior identical.
  </action>
  <verify>`rg -n \"is_critical_risk(_async)?\\(\" backend/app/core/approval_helpers.py backend/app/api/v1/endpoints/controls.py` returns no matches</verify>
  <done>Approval gating code no longer uses the misleading names.</done>
</task>

<task type="auto">
  <name>Task 3: Update tests to use new helper names and add a regression guard</name>
  <files>backend/tests/test_global_config_usage.py, backend/tests/test_threshold_propagation.py</files>
  <action>
    Update tests that reference `is_critical_risk(_async)` to call `is_high_risk_for_approval(_async)` instead.

    Add a small regression guard test that asserts `is_critical_risk(_async)` still behaves identically to the new helper (alias contract),
    so downstream imports won’t break unexpectedly.
  </action>
  <verify>`pytest backend/tests/test_global_config_usage.py backend/tests/test_threshold_propagation.py`</verify>
  <done>Tests reflect new naming and lock in alias behavior.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `rg -n \"is_critical_risk(_async)?\\(\" backend/app/core/approval_helpers.py backend/app/api/v1/endpoints/controls.py` shows no usage
- [ ] `pytest backend/tests/test_global_config_usage.py backend/tests/test_threshold_propagation.py backend/tests/test_executions.py -q`
</verification>

<success_criteria>
- Approval gating uses helpers whose names match the high-risk threshold semantics
- Old `is_critical_risk*` names remain as aliases (non-breaking) but are clearly documented as misnamed/deprecated
- Tests prevent regressions in naming/usage and preserve backward compatibility
</success_criteria>

<output>
After completion, create `.planning/phases/72-risk-hub-resolution/72-12-SUMMARY.md`:

# Phase 72 Plan 12: Risk Hub Resolution Summary

**[One-liner about clarifying threshold semantics and deconfusing helper naming]**

## Accomplishments
- Added `is_high_risk_for_approval(_async)` helpers and deprecated/mapped `is_critical_risk(_async)`
- Updated approval gating call sites to use the correct helper names
- Updated tests to reflect the new naming and preserve alias behavior

## Files Created/Modified
- `backend/app/core/permissions.py`
- `backend/app/core/approval_helpers.py`
- `backend/app/api/v1/endpoints/controls.py`
- `backend/tests/test_global_config_usage.py`
- `backend/tests/test_threshold_propagation.py`

## Next Step
Phase 72 resolution work is complete (or continue with any remaining unexecuted Phase 72 plans).
</output>

