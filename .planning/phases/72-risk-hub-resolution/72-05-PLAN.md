---
phase: 72-risk-hub-resolution
type: execute
---

<objective>
Align the Risk Hub frontend with backend configuration for risk types, thresholds, approvals, and admin workflows.

Purpose: Ensure CRO-configured settings are reflected in the UI and prevent destructive edits caused by missing data.
Output: Updated Risk Hub UI and risk pages driven by dynamic config, with a manual verification checkpoint.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/71-risk-hub/71-03-FINDINGS.md
@frontend/src/services/riskHubApi.ts
@frontend/src/types/risk.ts
@frontend/src/components/RiskForm.tsx
@frontend/src/pages/RisksPage.tsx
@frontend/src/pages/RiskDetailPage.tsx
@frontend/src/components/RiskScoreMatrix.tsx
@frontend/src/components/riskhub/ApprovalScenariosPanel.tsx
@frontend/src/components/riskhub/RolesPanel.tsx
@frontend/src/components/riskhub/DepartmentsPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drive risk types UI from Risk Hub configuration</name>
  <files>frontend/src/services/riskHubApi.ts, frontend/src/types/risk.ts, frontend/src/components/RiskForm.tsx, frontend/src/pages/RisksPage.tsx, frontend/src/pages/RiskDetailPage.tsx</files>
  <action>Fetch risk types from Risk Hub (react-query) and use them to populate the risk type select, list filters, and display labels/colors. Replace the hardcoded RiskType union with a string type or a config-backed map. Handle missing/failed config by falling back to existing system types so the UI still works. Avoid per-render fetches by using a shared query or hook.</action>
  <verify>Risk create/edit forms and list filters show configured risk types from Risk Hub.</verify>
  <done>Risk type selection and display are driven by Risk Hub config with safe fallbacks.</done>
</task>

<task type="auto">
  <name>Task 2: Apply configurable risk thresholds to UI scoring</name>
  <files>frontend/src/services/riskHubApi.ts, frontend/src/pages/RisksPage.tsx, frontend/src/components/RiskScoreMatrix.tsx</files>
  <action>Load risk_thresholds config values and pass them into list score coloring and RiskScoreMatrix. Update RiskScoreMatrix to accept threshold props (critical/high/medium) with defaults. Avoid hardcoding 16/10/5 so CRO settings are reflected in UI severity.</action>
  <verify>Changing risk threshold config updates score color bands and matrix labels.</verify>
  <done>Risk severity colors use configurable thresholds in both list and detail views.</done>
</task>

<task type="auto">
  <name>Task 3: Fix approval role selection and admin modal edge cases</name>
  <files>frontend/src/components/riskhub/ApprovalScenariosPanel.tsx, frontend/src/components/riskhub/RolesPanel.tsx, frontend/src/components/riskhub/DepartmentsPanel.tsx</files>
  <action>Populate approval role options from Risk Hub roles (merge with a special-case risk_owner entry) and preserve unknown roles in the UI. Gate Role modal actions until permissions are loaded to prevent submitting empty permission_ids. Send manager_id: null when clearing department managers so the backend can remove the assignment. Avoid removing existing selected roles when the roles list loads late.</action>
  <verify>Approval role picker lists backend roles, role edit modal no longer wipes permissions, and manager can be cleared.</verify>
  <done>Approval and admin modals handle dynamic roles and null manager updates safely.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Risk Hub frontend now uses dynamic risk types and thresholds, with updated approval role selection and admin safeguards</what-built>
  <how-to-verify>
    1. Run: `npm --prefix frontend run dev`
    2. Visit: http://localhost:5173/risk-hub
    3. Confirm Risk Types list reflects configured entries and is used in Risk create/edit forms
    4. Update risk thresholds in Risk Hub System Settings and confirm list/matrix colors adjust
    5. Open Approval Scenarios and confirm roles list comes from backend (including risk_owner)
    6. Edit a role before permissions load and confirm Save is disabled or guarded
    7. Clear a department manager and confirm it persists after refresh
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Risk types are config-driven in forms, lists, and detail views
- [ ] Threshold colors reflect Risk Hub settings
- [ ] Approval role picker and admin modals handle dynamic data safely
</verification>

<success_criteria>

- Frontend risk workflows reflect Risk Hub configuration
- No destructive edits occur due to missing permissions data
- Manual verification confirms UI behavior
  </success_criteria>

<output>
After completion, create `.planning/phases/72-risk-hub-resolution/72-05-SUMMARY.md`:

# Phase 72 Plan 05: Risk Hub Resolution Summary

**[Substantive one-liner describing frontend alignment with Risk Hub config]**

## Accomplishments

- [Risk type and threshold UI now use config]
- [Approval roles and admin modals handle dynamic data safely]

## Files Created/Modified

- `frontend/src/types/risk.ts` - [describe]
- `frontend/src/components/RiskForm.tsx` - [describe]
- `frontend/src/pages/RisksPage.tsx` - [describe]
- `frontend/src/components/RiskScoreMatrix.tsx` - [describe]
- `frontend/src/components/riskhub/ApprovalScenariosPanel.tsx` - [describe]
- `frontend/src/components/riskhub/RolesPanel.tsx` - [describe]
- `frontend/src/components/riskhub/DepartmentsPanel.tsx` - [describe]

## Decisions Made

None

## Issues Encountered

None

## Next Step

Phase 72 complete; ready for execution or remediation follow-up.
</output>
