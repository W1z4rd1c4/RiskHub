---
phase: 72-risk-hub-resolution
type: execute
---

<objective>
Finish Risk Hub configuration propagation so risk types + thresholds are (1) used in backend logic, and (2) readable + displayed correctly for *all* authenticated users (not just CRO), with no remaining frontend hardcoding.

Purpose: CRO-configured risk types and thresholds must affect backend reporting/approval gating and frontend severity/type display consistently. Today, several paths still hardcode values or rely on CRO-only endpoints, causing silent fallback behavior and mislabeling (e.g., “S/O” only).
Output: Backend removes remaining hardcoded threshold usage, public endpoints expose safe config for non-CRO users, frontend hooks use correct config keys/endpoints, and risk type badges/labels are fully config-driven with regression tests + a manual verification checkpoint.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/71-risk-hub/71-01-FINDINGS.md
@.planning/phases/71-risk-hub/71-03-FINDINGS.md
@.planning/phases/72-risk-hub-resolution/72-05-SUMMARY.md
@backend/app/models/global_config.py
@backend/app/core/permissions.py
@backend/app/services/report_service.py
@backend/app/core/approval_helpers.py
@backend/app/api/v1/endpoints/controls.py
@backend/app/api/v1/endpoints/reports.py
@backend/app/api/v1/endpoints/riskhub.py
@frontend/src/hooks/useRiskHubConfig.ts
@frontend/src/services/riskHubApi.ts
@frontend/src/types/risk.ts
@frontend/src/pages/RisksPage.tsx
@frontend/src/pages/RiskDetailPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove remaining hardcoded thresholds in backend reporting + approval gating</name>
  <files>backend/app/models/global_config.py, backend/app/core/permissions.py, backend/app/services/report_service.py, backend/app/api/v1/endpoints/reports.py, backend/app/core/approval_helpers.py, backend/app/api/v1/endpoints/controls.py, backend/tests/test_threshold_propagation.py</files>
  <action>
    Align defaults and key usage with seeded global_config keys:
    - `medium_risk_min_net_score` (default 5)
    - `high_risk_min_net_score` (default 10)
    - `critical_risk_min_net_score` (default 16)
    
    Backend requirements for this plan:
    1) Eliminate hardcoded `>= 16` in `ReportService` and compute “High/Critical” counts using config (typically `>= high_risk_min_net_score`, optionally include a separate `>= critical_risk_min_net_score` count if the report wants to show both).
    2) Ensure approval gating checks that depend on “linked critical/high risk” do not use sync defaults in async flows:
       - Replace `is_critical_risk()` calls in async endpoints/helpers with `await is_critical_risk_async(...)` (or an equivalent config-driven helper) so CRO changes take effect.
       - Update `ConfigDefaults` to match seed defaults (10 for high threshold at minimum) so fallback behavior is sane if config is missing.
  </action>
  <verify>`pytest backend/tests/test_threshold_propagation.py`</verify>
  <done>Backend reporting + approval gating no longer hardcode thresholds and respond to global_config changes with regression tests.</done>
</task>

<task type="auto">
  <name>Task 2: Expose safe “public” Risk Hub reads for all authenticated users</name>
  <files>backend/app/api/v1/endpoints/riskhub.py, backend/tests/test_riskhub_public_config.py, backend/tests/test_riskhub_public_risk_types.py, frontend/src/services/riskHubApi.ts</files>
  <action>
    Make “read-only business config needed by the whole app” available without CRO-only gating:
    - Expand `PUBLIC_CONFIG_ALLOWLIST` to include: `medium_risk_min_net_score`, `high_risk_min_net_score`, `critical_risk_min_net_score`.
    - Add an authenticated endpoint `GET /riskhub/public-risk-types` returning only active risk types with the fields needed for display (code, display_name, color, icon, sort_order).
      - Do NOT expose risk hub admin-only metadata beyond what the app needs.
      - Keep CRO-only endpoints for create/update/delete and include_inactive.
    
    Add tests proving:
    - Non-CRO can read allowlisted threshold keys, but cannot read non-allowlisted keys.
    - Non-CRO can list public risk types; inactive types are excluded.
  </action>
  <verify>`pytest backend/tests/test_riskhub_public_config.py backend/tests/test_riskhub_public_risk_types.py`</verify>
  <done>Non-CRO users can fetch thresholds + risk types via safe public endpoints with tests enforcing the restrictions.</done>
</task>

<task type="auto">
  <name>Task 3: Fix frontend key mismatch + remove remaining risk type hardcoding</name>
  <files>frontend/src/hooks/useRiskHubConfig.ts, frontend/src/services/riskHubApi.ts, frontend/src/types/risk.ts, frontend/src/pages/RisksPage.tsx, frontend/src/pages/RiskDetailPage.tsx</files>
  <action>
    Update `useRiskHubConfig` so it works for non-CRO users without silent fallback:
    - `useRiskThresholds()` should fetch thresholds via `/riskhub/public-config/{key}` using the real keys:
      - `medium_risk_min_net_score`, `high_risk_min_net_score`, `critical_risk_min_net_score`
      - Remove the incorrect `risk_threshold_*` key lookups.
    - `useRiskTypes()` should use the new `GET /riskhub/public-risk-types` endpoint (add a `riskHubApi.getPublicRiskTypes()` wrapper).
    
    Remove remaining “Strategic/Operational only” UI assumptions:
    - `RisksPage`: replace the `S/O` type badge with a config-driven badge using risk type `display_name` and `color` (fallback to code if unknown).
    - `RiskDetailPage`: replace `risk.risk_type === 'strategic' ? ... : ...` styling with config-driven display name/color.
    
    Fix typing to support dynamic risk types:
    - Change `frontend/src/types/risk.ts` `risk_type` fields from `RiskType` union to `string` (keep constants/helpers for legacy system codes if desired).
  </action>
  <verify>`npm --prefix frontend test` (or existing frontend unit tests), plus the manual checkpoint below</verify>
  <done>Frontend reads the correct threshold keys, risk type display is config-driven (no S/O-only logic), and typing no longer blocks custom risk types.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Config-driven risk thresholds and risk type display for all users (backend + frontend), including safe public endpoints</what-built>
  <how-to-verify>
    1. Run backend + frontend locally (your usual `docker-compose up` or separate dev commands).
    2. Login as a non-CRO business user.
    3. Visit `/risks` and confirm:
       - Risk type filter options reflect Risk Hub types (not just Strategic/Operational).
       - Risk type badges display configured labels/colors (no S/O-only badge).
       - Score coloring uses CRO-configured thresholds (not silent defaults).
    4. Login as CRO and change Risk Hub thresholds and add/edit a risk type.
    5. Re-check `/risks` and a risk detail page to confirm updates are reflected.
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `pytest backend/tests/test_threshold_propagation.py`
- [ ] `pytest backend/tests/test_riskhub_public_config.py backend/tests/test_riskhub_public_risk_types.py`
- [ ] Frontend thresholds use `*_risk_min_net_score` keys via public-config
- [ ] Frontend risk pages do not depend on CRO-only Risk Hub endpoints for read-only config
- [ ] Manual verification approved
</verification>

<success_criteria>

- Backend reporting/critical logic respects global_config thresholds
- Public endpoints provide safe risk type/threshold reads for non-CRO users
- Frontend risk type display and typing are fully dynamic/config-driven (no S/O-only assumptions)
  </success_criteria>

<output>
After completion, create `.planning/phases/72-risk-hub-resolution/72-07-SUMMARY.md`:

# Phase 72 Plan 07: Risk Hub Resolution Summary

**[Substantive one-liner describing thresholds propagation + public endpoints + frontend cleanup]**

## Accomplishments

- [Threshold logic is config-driven in backend reports/critical checks]
- [Non-CRO users can read risk types and thresholds safely]
- [Frontend risk type UI is fully config-driven]

## Files Created/Modified

- `backend/app/services/report_service.py` - [describe]
- `backend/app/core/permissions.py` - [describe]
- `backend/app/core/approval_helpers.py` - [describe]
- `backend/app/api/v1/endpoints/riskhub.py` - [describe]
- `backend/tests/test_threshold_propagation.py` - [describe]
- `backend/tests/test_riskhub_public_risk_types.py` - [describe]
- `frontend/src/hooks/useRiskHubConfig.ts` - [describe]
- `frontend/src/services/riskHubApi.ts` - [describe]
- `frontend/src/types/risk.ts` - [describe]
- `frontend/src/pages/RisksPage.tsx` - [describe]
- `frontend/src/pages/RiskDetailPage.tsx` - [describe]

## Decisions Made

None

## Issues Encountered

None

## Next Step

Phase 72 resolution complete; proceed to remediation verification or next roadmap phase.
</output>
