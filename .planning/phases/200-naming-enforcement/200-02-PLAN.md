# Plan: Backend API & Logic Updates (Risk Name)

## Objective
Update the Backend API to support and enforce the new `name` field for Risks.

## Execution Context
- **Phase**: 200 Entity Naming Enforcement
- **Plan**: 200-02

## Tasks

### 1. Update Pydantic Schemas
- [ ] `backend/app/schemas/risk.py`:
    - `RiskBase`: Add `name: str = Field(..., max_length=255, description="Risk Name")`.
    - `RiskSummary`: Add `name`.
- [ ] `backend/app/schemas/kri.py`:
    - `KRIBase`: Add `description: str = Field(..., description="KRI Description")`.
    - `KRIUpdate`: Add `description: Optional[str] = None`.

### 2. Update Service Layer (if needed)
- [ ] Check `backend/app/crud/crud_risk.py` and `backend/app/crud/crud_kri.py`.
- [ ] Ensure create/update methods propagate the new fields.

### 3. Update Tests
- [ ] `backend/tests/api/api_v1/test_risks.py`:
    - Update payloads with "name".
- [ ] `backend/tests/api/api_v1/test_kris.py` (if exists):
    - Update payloads with "description".

### 4. Verification
- [ ] Run backend tests `pytest backend/tests/api/api_v1/test_risks.py`.

## Output
- Updated API with Name support.
- All tests passing.
