# Plan: Export & Reporting Updates

## Objective
Reconcile all export/reporting surfaces with Phase 200 naming enforcement so entity names are present and consistent, while keeping the current supported export contract:
- Unified list exports: `xlsx` and `csv`
- Summary and audit trail: Excel-only
- Vendor annual/DORA exports: Excel-only
- No PDF export contract for these endpoints

## Execution Context
- **Phase**: 200 Entity Naming Enforcement
- **Plan**: 200-08

## Scope
- In scope:
  - Backend export payload/header alignment for `name`-based columns.
  - API export format contract enforcement (`xlsx|csv` or Excel-only, depending on endpoint).
  - Frontend export flow alignment (dialog options + endpoint usage).
  - Reporting docs consistency with implemented contract.
- Out of scope:
  - New report families/endpoints.
  - Full-suite reruns.

## Primary Files
- `backend/app/api/v1/endpoints/reports.py`
- `backend/app/api/v1/endpoints/vendor_reports.py`
- `backend/app/services/report_service.py`
- `backend/tests/test_reports_rbac.py`
- `backend/tests/test_vendor_reports.py`
- `frontend/src/services/reportApi.ts`
- `frontend/src/components/reports/ExportDialog.tsx`
- `frontend/src/services/vendorReportApi.ts`
- `docs/admin/reports.md`
- `docs/admin-cs/reports.md`

## Tasks

### 1. Contract Reconciliation
- [ ] Remove stale PDF assumptions from plan scope and verify runtime contract:
  - Unified exports accept only `format=xlsx|csv`.
  - Vendor report endpoints remain `xlsx` only.
  - Summary export remains `/api/v1/reports/summary/excel`.
- [ ] Ensure tests explicitly guard unsupported `pdf` format where applicable.

### 2. Name-Field Export Coverage
- [ ] Verify risk export headers/rows include `Name` and use `risk.name` values.
- [ ] Verify control/KRI/vendor export mappings preserve relevant name fields (entity + linked names where expected).
- [ ] Verify summary/audit outputs remain coherent with naming updates (no legacy label drift).

### 3. Frontend Export Flow Consistency
- [ ] Confirm export dialog offers only supported formats (`xlsx`, `csv`) for unified exports.
- [ ] Confirm report API services call the correct endpoints and filenames/extensions.
- [ ] Confirm vendor report UI uses `xlsx` flow only.

### 4. Documentation Reconciliation
- [ ] Ensure `docs/admin/reports.md` and `docs/admin-cs/reports.md` match the implemented export contract and naming behavior.
- [ ] Remove stale PDF wording in Phase 200 references tied to this plan.

### 5. Targeted Verification (No Full Suite)
- [ ] Backend targeted tests:
  - `cd backend && pytest tests/test_reports_rbac.py -k "export or summary or pdf"`
  - `cd backend && pytest tests/test_vendor_reports.py`
- [ ] Frontend targeted export smoke:
  - `cd frontend && CI=1 npx playwright test --project=chromium --workers=1 --retries=0 e2e/risks.spec.ts e2e/controls.spec.ts e2e/kris.spec.ts e2e/vendors.spec.ts`
- [ ] If a targeted run fails, apply minimal fix and rerun only the failed targeted set.

## Definition of Done
- Export/reporting behavior matches naming enforcement and current endpoint format contract.
- Targeted backend and frontend verification commands pass.
- Docs and phase references no longer describe this work as PDF-focused.
- `200-08-SUMMARY.md` records exact changes, commands, and results.

## Output
- Export/reporting surfaces are naming-consistent, contract-consistent, and verified with targeted tests.
