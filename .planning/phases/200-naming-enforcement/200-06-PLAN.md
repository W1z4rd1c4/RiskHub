# Plan: KRI Naming & Description Consistency (UI/UX)

## Objective
Ensure KRI "Metric Name" is labeled consistently and add the "Description" field to forms and details.

## Execution Context
- **Phase**: 200 Entity Naming Enforcement
- **Plan**: 200-06

## Tasks

### 1. Update Frontend Types
- [ ] `frontend/src/types/kri.ts` (or similar):
    - Add `description: string` to `KRI` and creation types.

### 2. Update KRI Form
- [ ] `frontend/src/components/kri/KRIForm.tsx`:
    - Label `metric_name` as "KRI Name" / "Metric Name".
    - Add "Description" TextArea below Name.
    - Set Description as required.

### 3. Review KRI List
- [ ] `frontend/src/pages/KRIPage.tsx`:
    - Ensure column header is "KRI Name".

### 4. Review KRI Detail
- [ ] `frontend/src/pages/KRIDetailPage.tsx`:
    - Show Description prominent in the header or logic section.

## Output
- KRI UI consistently uses "Name" paradigm.
