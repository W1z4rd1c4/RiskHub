---
phase: 153-audit-resolution-3
type: execute
---

<objective>
Fix datetime inconsistency: Replace all `datetime.utcnow()` with `datetime.now(UTC)` in approval-related files.

Purpose: Ensure timezone-aware datetimes are used consistently throughout the codebase, preventing potential timezone comparison bugs and PostgreSQL warnings when comparing aware vs naive datetimes.
Output: All datetime fields in approval files use `datetime.now(UTC)` for consistency with the ApprovalRequest model definition.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/app/api/v1/endpoints/approvals.py
@backend/app/services/approval_execution_service.py
@backend/app/models/approval_request.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix datetime.utcnow() in approvals.py</name>
  <files>backend/app/api/v1/endpoints/approvals.py</files>
  <action>
  Replace all occurrences of `datetime.utcnow()` with `datetime.now(UTC)` in the approvals.py file.
  
  Specific locations to fix:
  - Line 422: `approval.resolved_at = datetime.utcnow()` → `datetime.now(UTC)`
  - Line 486: `approval.resolved_at = datetime.utcnow()` → `datetime.now(UTC)`
  
  The file already imports `UTC` from datetime on line 4 (`from datetime import datetime, UTC`), so no additional imports are needed.
  
  **Why this matters:** The ApprovalRequest model defines `created_at` with `DateTime(timezone=True)` and uses `datetime.now(UTC)`. Mixing timezone-naive (`utcnow()`) and timezone-aware values in the same table can cause comparison issues and database warnings.
  </action>
  <verify>grep -n "datetime.utcnow" backend/app/api/v1/endpoints/approvals.py should return no results</verify>
  <done>No occurrences of datetime.utcnow() remain in approvals.py</done>
</task>

<task type="auto">
  <name>Task 2: Fix datetime.utcnow() in approval_execution_service.py</name>
  <files>backend/app/services/approval_execution_service.py</files>
  <action>
  Replace all occurrences of `datetime.utcnow()` with `datetime.now(UTC)` in approval_execution_service.py.
  
  First, ensure UTC is imported at the top of the file:
  ```python
  from datetime import datetime, UTC
  ```
  
  Then fix the following lines:
  - Line 145: `approval.resolved_at = datetime.utcnow()` → `datetime.now(UTC)`
  - Line 150: `approval.primary_approved_at = datetime.utcnow()` → `datetime.now(UTC)`
  - Line 160: `approval.resolved_at = datetime.utcnow()` → `datetime.now(UTC)`
  - Line 168: `approval.privileged_approved_at = datetime.utcnow()` → `datetime.now(UTC)`
  - Line 170: `approval.resolved_at = datetime.utcnow()` → `datetime.now(UTC)`
  
  **Why this matters:** All datetime assignments in this file must be timezone-aware to match the model definition and prevent subtle bugs during datetime comparisons.
  </action>
  <verify>grep -n "datetime.utcnow" backend/app/services/approval_execution_service.py should return no results</verify>
  <done>No occurrences of datetime.utcnow() remain in approval_execution_service.py</done>
</task>

<task type="auto">
  <name>Task 3: Verify backend builds and tests pass</name>
  <files>backend/</files>
  <action>
  Run the backend tests to ensure the datetime changes don't break any existing functionality.
  
  Commands to run:
  1. `cd backend && python -c "from app.api.v1.endpoints import approvals; print('approvals OK')"` - verify import works
  2. `cd backend && python -c "from app.services.approval_execution_service import load_approval; print('service OK')"` - verify import works
  3. Optional: Run approval-related tests if available
  
  **No behavior should change** - this is a purely syntactic fix that produces the same result (UTC datetime) using the modern, timezone-aware approach.
  </action>
  <verify>Both import checks pass without errors</verify>
  <done>Backend imports work correctly, no syntax or import errors</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -rn "datetime.utcnow" backend/app/api/v1/endpoints/approvals.py` returns no matches
- [ ] `grep -rn "datetime.utcnow" backend/app/services/approval_execution_service.py` returns no matches
- [ ] Python imports for both files succeed without errors
</verification>

<success_criteria>
- All `datetime.utcnow()` calls replaced with `datetime.now(UTC)`
- No import errors or syntax errors introduced
- Datetime consistency achieved across approval-related files
</success_criteria>

<output>
After completion, create `.planning/phases/153-audit-resolution-3/153-01-SUMMARY.md`
</output>
