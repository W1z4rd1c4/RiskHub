---
phase: 153-audit-resolution-3
type: execute
---

<objective>
Fix KRI permission mismatch (kri:record vs kri:submit) and Activity Log picker truncation.

Purpose: Align frontend permission checks with backend and ensure complete risk picker options.
Output: Consistent permission enforcement, full risk list in Activity Log picker.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@frontend/src/hooks/usePermissions.ts
@frontend/src/hooks/useActivityLogPageState.ts
@backend/app/api/v1/endpoints/kris.py
@backend/app/api/v1/endpoints/risks.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix KRI permission check to use kri:submit</name>
  <files>frontend/src/hooks/usePermissions.ts</files>
  <action>
  Update the `canRecordKRI` check (line 35-36) to use `kri:submit` instead of `kri:record`:
  
  **Current code:**
  ```typescript
  canRecordKRI: hasPermission('kri', 'record') || hasPermission('risks', 'write') || hasPermission('approvals', 'write'),
  ```
  
  **Change to:**
  ```typescript
  canRecordKRI: hasPermission('kri', 'submit') || hasPermission('risks', 'write') || hasPermission('approvals', 'write'),
  ```
  
  The backend endpoint `record_kri_value()` checks `kri:submit` permission.
  </action>
  <verify>grep -n "kri:submit" frontend/src/hooks/usePermissions.ts</verify>
  <done>Frontend uses kri:submit to match backend</done>
</task>

<task type="auto">
  <name>Task 2: Increase Activity Log risk picker limit</name>
  <files>frontend/src/hooks/useActivityLogPageState.ts</files>
  <action>
  Update the risk fetching call (around line 141) to match backend limit or accept truncation gracefully.
  
  **Option A:** Reduce frontend request to 100 (match backend cap):
  ```typescript
  const risksData = await riskApi.getRisks({ limit: 100 });
  ```
  
  **Option B:** Increase backend cap (less preferred as may impact performance).
  
  Choose Option A for simplicity. The backend caps at 100 (line 87 in risks.py).
  </action>
  <verify>grep -n "limit: 200" frontend/src/hooks/useActivityLogPageState.ts should return no matches</verify>
  <done>Risk picker limit matches backend cap</done>
</task>

<task type="auto">
  <name>Task 3: Verify permissions work correctly</name>
  <files>frontend/src/hooks/usePermissions.ts</files>
  <action>
  Review the full permission mapping and verify alignment with backend:
  
  - `canRecordKRI` → `kri:submit` ✓
  - `canCreateKRIs` → `risks:write` (KRIs inherit from risks) ✓
  - `canResolveApprovals` → `approvals:write` ✓
  
  Ensure no other mismatches exist.
  </action>
  <verify>cd frontend && npm run build</verify>
  <done>All permission checks align with backend</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `kri:submit` used instead of `kri:record`
- [ ] Risk picker limit is 100 or less
- [ ] Frontend build passes
</verification>

<success_criteria>
- KRI value recording permission check matches backend
- No truncated picker options
- Consistent permission enforcement
</success_criteria>

<output>
After completion, create `.planning/phases/153-audit-resolution-3/153-08-SUMMARY.md`
</output>
