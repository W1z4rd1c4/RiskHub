---
phase: 153-audit-resolution-3
type: execute
---

<objective>
Fix Sidebar navigation inconsistencies: Remove duplicate Activity Log routes and fix Approvals cancel button.

Purpose: Eliminate duplicate navigation entries and enable cancellation of pending_privileged approvals.
Output: Clean navigation, correct approval cancellation UX.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@frontend/src/components/layout/Sidebar.tsx
@frontend/src/pages/ApprovalsPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate and fix Activity Log navigation duplicate</name>
  <files>frontend/src/components/layout/Sidebar.tsx</files>
  <action>
  **BEFORE MAKING CHANGES - VERIFY:**
  The Sidebar has two Activity Log-related entries:
  - Line 47: `{ name: t('sidebar.activity_log'), href: '/audit-trail', icon: History }` in base navigation (under Governance section)
  - Line 101-105: `activityLogItem` with `href: '/activity-log'` (permission-gated)
  
  **These may serve DIFFERENT purposes:**
  - `/audit-trail` - May be part of Governance tab for all users
  - `/activity-log` - Dedicated Activity Log page with permission gating
  
  **Verification steps:**
  1. Check app router - do both routes exist and render different content?
  2. Check if `/audit-trail` is in the Governance page or a separate route
  3. If both are legitimate routes, keep them but ensure correct translation keys
  
  **If confirmed duplicate:**
  Remove line 47 from base navigation since `activityLogItem` handles it with proper permission gating.
  
  **If NOT duplicate (different purposes):**
  Keep both but ensure:
  - Different translation keys (e.g., `sidebar.audit_trail` vs `sidebar.activity_log`)
  - Different icons if appropriate
  - Both routes work correctly
  </action>
  <verify>Check router and confirm whether one or both routes are needed</verify>
  <done>Activity Log navigation is correct (either single entry or intentional dual entries)</done>
</task>

<task type="auto">
  <name>Task 2: Fix Approvals cancel button to include pending_privileged</name>
  <files>frontend/src/pages/ApprovalsPage.tsx</files>
  <action>
  Update the cancel button visibility check (line 307) to include `pending_privileged` status.
  
  **Current code:**
  ```typescript
  {user?.id === approval.requested_by_id && approval.status === 'pending' && (
  ```
  
  **Change to:**
  ```typescript
  {user?.id === approval.requested_by_id && (approval.status === 'pending' || approval.status === 'pending_privileged') && (
  ```
  
  The backend already allows cancellation for both statuses (line 481).
  </action>
  <verify>grep -n "pending_privileged" frontend/src/pages/ApprovalsPage.tsx | wc -l should be >= 3</verify>
  <done>Cancel button shows for both pending and pending_privileged</done>
</task>

<task type="auto">
  <name>Task 3: Verify frontend build passes</name>
  <files>frontend/</files>
  <action>
  Run the frontend build to verify no import or syntax errors:
  
  ```bash
  cd frontend && npm run build
  ```
  
  Check for TypeScript errors related to navigation or approvals.
  </action>
  <verify>cd frontend && npm run build exits with 0</verify>
  <done>Frontend builds successfully</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Only one Activity Log entry in sidebar
- [ ] Cancel button shows for pending_privileged approvals
- [ ] Frontend build passes
</verification>

<success_criteria>
- No duplicate navigation entries
- Users can cancel their own pending_privileged approval requests
- Clean navigation experience
</success_criteria>

<output>
After completion, create `.planning/phases/153-audit-resolution-3/153-07-SUMMARY.md`
</output>
