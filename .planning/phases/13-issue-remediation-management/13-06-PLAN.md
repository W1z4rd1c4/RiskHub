---
phase: 13-issue-remediation-management
plan: 13-06
title: Contextual issue entry points on Risk/Control/KRI/Vendor detail pages
type: execute
wave: 3
depends_on:
  - 13-05
autonomous: true
files_modified:
  - frontend/src/pages/RiskDetailPage.tsx
  - frontend/src/pages/ControlDetailPage.tsx
  - frontend/src/pages/KRIDetailPage.tsx
  - frontend/src/pages/VendorDetailPage.tsx
  - frontend/src/components/issues/IssueQuickCreateModal.tsx
  - frontend/src/pages/__tests__/RiskDetailPage.issue-entry.test.tsx
  - frontend/src/pages/__tests__/ControlDetailPage.issue-entry.test.tsx
  - frontend/src/pages/__tests__/KRIDetailPage.issue-entry.test.tsx
  - frontend/src/pages/__tests__/VendorDetailPage.issue-entry.test.tsx
---

<objective>
Add contextual “Create Issue” actions directly on source entity detail pages.

Purpose: Let users create linked issues at the point of discovery without switching context.
Output: Permission-gated issue-entry actions on Risk/Control/KRI/Vendor detail pages with prefilled contextual modal.
</objective>

<context>
@.planning/phases/13-issue-remediation-management/13-05-PLAN.md
@frontend/src/pages/RiskDetailPage.tsx
@frontend/src/pages/ControlDetailPage.tsx
@frontend/src/pages/KRIDetailPage.tsx
@frontend/src/pages/VendorDetailPage.tsx
@frontend/src/hooks/usePermissions.ts
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add permission-gated issue actions to detail page headers</name>
  <files>frontend/src/pages/RiskDetailPage.tsx, frontend/src/pages/ControlDetailPage.tsx, frontend/src/pages/KRIDetailPage.tsx, frontend/src/pages/VendorDetailPage.tsx</files>
  <action>
For each detail page, add a header action button opening `IssueQuickCreateModal`.

Requirements:
1. Show action only when user has `issues:write`.
2. Use existing page action style hierarchy (match neighboring edit/export actions).
3. Button label localized via `issues` namespace.
4. Keep page-specific existing actions unchanged.
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <done>Users with issue-write permission can launch contextual issue creation from each source detail page.</done>
</task>

<task type="auto">
  <name>Task 2: Wire contextual metadata and post-create navigation</name>
  <files>frontend/src/pages/RiskDetailPage.tsx, frontend/src/pages/ControlDetailPage.tsx, frontend/src/pages/KRIDetailPage.tsx, frontend/src/pages/VendorDetailPage.tsx</files>
  <action>
Pass entity context into modal:
- Risk detail: `entity_type='risk'`, `entity_id=risk.id`, label uses risk name.
- Control detail: `entity_type='control'`, `entity_id=control.id`, label uses control name.
- KRI detail: `entity_type='kri'`, `entity_id=kri.id`, label uses KRI metric name.
- Vendor detail: `entity_type='vendor'`, `entity_id=vendor.id`, label uses vendor name.

On success:
- navigate to `/issues/:id` for created issue.

No-ID rule:
- labels and hints must use business names; no raw IDs shown.
  </action>
  <verify>manual smoke on each detail page creates and opens linked issue</verify>
  <done>Contextual issue creation is one click from each supported source page.</done>
</task>

<task type="auto">
  <name>Task 3: Add page-level tests for action visibility and modal invocation</name>
  <files>frontend/src/pages/__tests__/RiskDetailPage.issue-entry.test.tsx, frontend/src/pages/__tests__/ControlDetailPage.issue-entry.test.tsx, frontend/src/pages/__tests__/KRIDetailPage.issue-entry.test.tsx, frontend/src/pages/__tests__/VendorDetailPage.issue-entry.test.tsx</files>
  <action>
Add tests that assert:
1. `issues:write` users see create-issue action on each detail page.
2. non-writable users do not see the action.
3. clicking action opens modal with correct business label context.
4. no raw ID strings are rendered in action text or modal context text.
  </action>
  <verify>cd frontend && npm run test:run -- issue-entry</verify>
  <done>Entry-point behavior and permission gating are regression-protected.</done>
</task>

</tasks>

<must_haves>
- All four detail pages expose contextual issue creation for `issues:write` users.
- Context is correctly bound by entity type/id and business label.
- No raw numeric IDs are displayed in new UI copy.
</must_haves>

<verification>
- [ ] `cd frontend && npx tsc --noEmit`
- [ ] `cd frontend && npm run test:run -- issue-entry`
- [ ] manual create-from-detail smoke for risk/control/kri/vendor
</verification>

<output>
After completion, create `.planning/phases/13-issue-remediation-management/13-06-SUMMARY.md` with:
- pages updated and action placement notes
- permission gating behavior
- navigation/result behavior
- verification evidence
</output>
