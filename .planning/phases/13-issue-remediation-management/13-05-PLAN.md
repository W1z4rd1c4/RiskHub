---
phase: 13-issue-remediation-management
plan: 13-05
title: Shared frontend contextual issue quick-create surface
type: execute
wave: 2
depends_on:
  - 13-04
autonomous: true
files_modified:
  - frontend/src/components/issues/IssueQuickCreateModal.tsx
  - frontend/src/components/issues/issueUi.ts
  - frontend/src/services/issuesApi.ts
  - frontend/src/types/issue.ts
  - frontend/src/i18n/locales/en/issues.json
  - frontend/src/i18n/locales/cs/issues.json
  - frontend/src/components/issues/__tests__/IssueQuickCreateModal.test.tsx
---

<objective>
Build a reusable contextual issue creation modal that minimizes required input and delegates contextual linking to backend.

Purpose: Provide one consistent low-friction issue-intake UX across source detail pages.
Output: `IssueQuickCreateModal`, API/type support for contextual endpoint, localized strings, and unit tests.
</objective>

<context>
@.planning/phases/13-issue-remediation-management/13-04-PLAN.md
@frontend/src/components/issues/IssueCreateForm.tsx
@frontend/src/services/issuesApi.ts
@frontend/src/types/issue.ts
@frontend/src/i18n/locales/en/issues.json
@frontend/src/i18n/locales/cs/issues.json
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend issue API client/types for contextual create</name>
  <files>frontend/src/services/issuesApi.ts, frontend/src/types/issue.ts</files>
  <action>
Add typed contextual request/response contracts:
1. `IssueContextEntityType = 'risk' | 'control' | 'execution' | 'kri' | 'vendor'`
2. `IssueContextCreatePayload` with fields:
   - `entity_type`, `entity_id`, `title`, `description?`, `severity?`, `due_at?`, `owner_user_id?`
3. API client method:
   - `issuesApi.createContextual(payload)` => `POST /issues/contextual`
4. Extend `IssueListFilters` with optional `linked_vendor_id`.
5. Keep existing `create(...)` manual path unchanged.
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <done>Frontend can call contextual backend contract with strong typing.</done>
</task>

<task type="auto">
  <name>Task 2: Build reusable IssueQuickCreateModal component</name>
  <files>frontend/src/components/issues/IssueQuickCreateModal.tsx, frontend/src/components/issues/issueUi.ts</files>
  <action>
Create modal component with minimal fields only:
- title (required)
- severity (default medium)
- due date (optional)
- description (optional)

Component contract:
- Inputs:
  - `isOpen`, `onClose`, `contextEntityType`, `contextEntityId`, `contextEntityLabel`
  - `defaultTitlePrefix?`
  - `onCreated(issue)` callback
- Behavior:
  - invokes `issuesApi.createContextual(...)`
  - closes on success and returns created issue
  - shows backend error messages in inline alert region

UI requirements:
- Uses app-standard modal/card patterns and issue UI tokens.
- No raw IDs shown in labels or helper text.
- Context display uses business labels only.
  </action>
  <verify>cd frontend && npm run test:run -- IssueQuickCreateModal</verify>
  <done>A single reusable component can create linked issues for any supported source entity.</done>
</task>

<task type="auto">
  <name>Task 3: Add i18n coverage and regression tests</name>
  <files>frontend/src/i18n/locales/en/issues.json, frontend/src/i18n/locales/cs/issues.json, frontend/src/components/issues/__tests__/IssueQuickCreateModal.test.tsx</files>
  <action>
Add `issues` namespace keys for modal labels, actions, and validation messages.

Test requirements:
1. Renders contextual entity label (business name) and never raw numeric IDs.
2. Submits expected payload shape to `issuesApi.createContextual`.
3. Handles success (`onCreated` called) and backend error path.
4. Keeps existing manual create form behavior untouched.
  </action>
  <verify>cd frontend && npm run test:run -- IssueQuickCreateModal</verify>
  <done>Modal is localized, tested, and compliant with frontend no-ID display guardrail.</done>
</task>

</tasks>

<must_haves>
- Reusable quick-create modal exists and is source-agnostic.
- `issuesApi.createContextual(...)` is typed and integrated.
- Localization is present in EN/CS for all added UI copy.
- No-ID display rule is preserved.
</must_haves>

<verification>
- [ ] `cd frontend && npx tsc --noEmit`
- [ ] `cd frontend && npm run test:run -- IssueQuickCreateModal`
- [ ] existing issues page/new/detail tests remain green
</verification>

<output>
After completion, create `.planning/phases/13-issue-remediation-management/13-05-SUMMARY.md` with:
- new modal component and props contract
- API/type additions
- localization keys added
- verification evidence
</output>
