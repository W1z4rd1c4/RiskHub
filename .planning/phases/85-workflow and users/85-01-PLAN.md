---
phase: "85-workflow and users"
type: execute
domain: riskhub
---

<objective>
Create a complete access map of user/role types and their visibility + capabilities per UI tab (Overview, Risks, Controls, KRIs, and related tabs) to inform the upcoming user-management enhancement.

Purpose: Establish a single source of truth for RBAC + department scoping before building the enhanced user management UI (part 2).
Output: `.planning/phases/85-workflow and users/DISCOVERY.md` with role inventory, per-tab access matrix, and gaps for part 2.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STRUCTURE.md
@backend/app/models/role.py
@backend/app/db/seed.py
@backend/app/core/permissions.py
@backend/app/core/security.py
@backend/app/api/v1/endpoints/dashboard.py
@backend/app/api/v1/endpoints/risks.py
@backend/app/api/v1/endpoints/controls.py
@backend/app/api/v1/endpoints/kris.py
@backend/app/api/v1/endpoints/users.py
@frontend/src/App.tsx
@frontend/src/components/layout/Sidebar.tsx
@frontend/src/contexts/AuthContext.tsx
@frontend/src/hooks/usePermissions.ts
@frontend/src/components/PermissionGate.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory backend roles, permissions, and scope rules</name>
  <files>.planning/phases/85-workflow and users/DISCOVERY.md</files>
  <action>Review role definitions and permissions in `backend/app/models/role.py`, `backend/app/db/seed.py`, `backend/app/core/permissions.py`, and `backend/app/core/security.py`. Enumerate all role types (including seeded roles and RoleType constants), privileged roles, and role-to-permission mappings. Then scan backend endpoints for access enforcement and scoping rules: `backend/app/api/v1/endpoints/risks.py`, `backend/app/api/v1/endpoints/controls.py`, `backend/app/api/v1/endpoints/kris.py`, `backend/app/api/v1/endpoints/dashboard.py`, `backend/app/api/v1/endpoints/approvals.py`, `backend/app/api/v1/endpoints/reports.py`, `backend/app/api/v1/endpoints/departments.py`, and `backend/app/api/v1/endpoints/users.py`. Capture: which endpoints require `require_permission`, which rely only on department filtering, special cases (control owner edits, approvals on critical/sensitive changes, manager department inheritance), and any mismatches between permission model and enforcement. Write findings in a "Backend Access Map" section, with a concise per-resource breakdown and notes for part 2.</action>
  <verify>DISCOVERY.md lists every role found in code + seed data, and each of Risks/Controls/KRIs/Dashboard/Reports/Users/Departments/Approvals has an explicit access + scoping note.</verify>
  <done>Backend Access Map section is complete, including privileged roles, department scope logic, approval triggers, and any missing/weak permission checks.</done>
</task>

<task type="auto">
  <name>Task 2: Map frontend navigation, tab visibility, and action gating</name>
  <files>.planning/phases/85-workflow and users/DISCOVERY.md</files>
  <action>Use `frontend/src/App.tsx` and `frontend/src/components/layout/Sidebar.tsx` to list all tabs/routes and note which items are conditionally shown (e.g., User Management via `canManageUsers`). Then inspect page-level and component-level permission gates: `frontend/src/pages/DashboardPage.tsx`, `ControlsPage.tsx`, `ControlDetailPage.tsx`, `RisksPage.tsx`, `RiskDetailPage.tsx`, `KRIsPage.tsx`, `KRIDetailPage.tsx`, plus workflow/admin tabs (`ApprovalsPage.tsx`, `UsersPage.tsx`, `UserNewPage.tsx`, `UserDetailPage.tsx`, `DepartmentsPage.tsx`, `DepartmentDetailPage.tsx`, `GovernancePage.tsx`, `AuditTrailPage.tsx`, `NotificationsPage.tsx`, `SettingsPage.tsx`). Capture which UI actions are hidden via `PermissionGate` or `usePermissions` (create/edit/delete/export/resolve approvals), and where the UI depends on backend 403s instead of explicit gating. Add a "Frontend Access Map" section with per-tab access notes.</action>
  <verify>DISCOVERY.md includes a per-tab list covering Overview/Dashboard, Risks, Controls, KRIs, plus Workflow/Approvals and User Management, with visibility and action gating called out.</verify>
  <done>Frontend Access Map section captures tab visibility rules, action buttons gated by permission, and any gaps vs backend enforcement.</done>
</task>

<task type="auto">
  <name>Task 3: Synthesize role x tab access matrix and part-2 gaps</name>
  <files>.planning/phases/85-workflow and users/DISCOVERY.md</files>
  <action>Combine backend + frontend findings into a single access matrix: roles down the side, tabs across the top (Overview, Risks, Controls, KRI, Departments, Workflow/Approvals, Governance, Audit Trail, Notifications, Settings, User Management). For each cell, note visibility and actions (read/create/edit/delete/approve/export) and data scope (all departments vs own, manager-inherited). Add a "Gaps & Notes for Part 2" section calling out mismatches with the desired future behavior (e.g., risk_manager not allowed to manage users, no department create/move flow, tabs visible but actions blocked).</action>
  <verify>DISCOVERY.md contains a clear matrix with role/action/scope and a dedicated gaps section referencing part-2 goals.</verify>
  <done>Access matrix is complete and actionable for the upcoming user-management enhancement.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `.planning/phases/85-workflow and users/DISCOVERY.md` exists and is readable
- [ ] All roles defined in code and seed data appear in the matrix
- [ ] Each tab in the UI has an explicit visibility + action rule
- [ ] Department scoping and approval/override rules are documented
</verification>

<success_criteria>

- Access matrix covers roles, tabs, actions, and department scope in one place
- Backend enforcement vs frontend gating differences are explicitly listed
- Gaps for the part-2 user-management enhancement are captured
  </success_criteria>

<output>
After completion, create `.planning/phases/85-workflow and users/85-01-SUMMARY.md`:

# Phase 85 Plan 01: User Access Map Summary

**Access matrix and gaps for part-2 user management compiled.**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `.planning/phases/85-workflow and users/DISCOVERY.md` - Access mapping doc

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for Phase 85 Plan 02 (user management enhancement implementation)
</output>
