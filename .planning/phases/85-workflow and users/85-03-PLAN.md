---
phase: "85-workflow and users"
type: execute
domain: riskhub
---

<objective>
Deliver an enterprise-grade access management UI that shows all users and their effective permissions, with privileged editing controls and admin/CRO-only privilege toggles.

Purpose: Provide a clear, auditable interface for managing access in RiskHub.
Output: Updated user management UI wired to new access APIs.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/85-workflow and users/DISCOVERY.md
@frontend/src/contexts/AuthContext.tsx
@frontend/src/services/authApi.ts
@frontend/src/hooks/usePermissions.ts
@frontend/src/types/user.ts
@frontend/src/pages/UsersPage.tsx
@frontend/src/pages/UserDetailPage.tsx
@frontend/src/components/layout/Sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add access types, API client, and permission helpers</name>
  <files>frontend/src/types/access.ts, frontend/src/types/user.ts, frontend/src/services/accessApi.ts, frontend/src/services/authApi.ts, frontend/src/contexts/AuthContext.tsx, frontend/src/hooks/usePermissions.ts</files>
  <action>Create access management types (AccessUserRead, AccessUserUpdate, RoleWithPermissions, PermissionRead) based on the backend API. Add an accessApi client for GET /access/users and PATCH /access/users/{id}, plus GET /access/roles if exposed. Extend authApi and AuthContext user shape to include new fields (is_privileged/access_scope and effective_permissions if returned), and update usePermissions to include canManageAccess and canManagePrivileged (admin/cro only for privileged toggles). Keep existing hasPermission behavior intact and avoid breaking current routes.</action>
  <verify>cd frontend && npm run build</verify>
  <done>Frontend types compile and permission helpers expose access-management capability flags.</done>
</task>

<task type="auto">
  <name>Task 2: Build access management UI with permissions visibility and guarded edits</name>
  <files>frontend/src/pages/UsersPage.tsx, frontend/src/pages/UserDetailPage.tsx, frontend/src/components/access/PermissionMatrix.tsx, frontend/src/components/access/AccessEditModal.tsx, frontend/src/components/layout/Sidebar.tsx</files>
  <action>Upgrade UsersPage into an access management surface: load AccessUserRead list via accessApi, show role, department, manager, privilege/scope badges, and a permissions summary (chips grouped by resource). Add a PermissionMatrix component for full per-user permissions (expandable row or side drawer). Add an AccessEditModal that lets privileged users update role/department/manager, and allow admin/CRO to toggle privileged status (disable that control for other privileged users). Update UserDetailPage to display effective permissions and scope using the new data. Keep styling aligned with existing glassmorphism components and provide clear read-only states for non-privileged viewers. Optionally rename the nav label to "Access Management" if the UI scope expands beyond basic user CRUD.</action>
  <verify>cd frontend && npm run build</verify>
  <done>Users page shows effective permissions and allows guarded edits with correct role-based access.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Access management UI with permissions visibility and privileged editing controls</what-built>
  <how-to-verify>
    1. Start backend: `cd backend && uvicorn app.main:app --reload`
    2. Start frontend: `cd frontend && npm run dev`
    3. Visit: http://localhost:5173/users
    4. As admin/CRO: confirm all users list with permissions, edit modal works, privileged toggle works
    5. As non-privileged user: confirm view-only state and no edit controls
    6. Confirm permissions summary matches role permissions and scope badges look correct
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Frontend build passes
- [ ] Access UI renders permission matrix and scope badges correctly
- [ ] Privileged toggle only available to admin/CRO
- [ ] Non-privileged users see read-only states
</verification>

<success_criteria>

- Users and effective permissions are visible in the UI
- Privileged accounts can edit access, with admin/CRO-only privilege changes
- UI is consistent with existing RiskHub styling and UX patterns
  </success_criteria>

<output>
After completion, create `.planning/phases/85-workflow and users/85-03-SUMMARY.md`:

# Phase 85 Plan 03: Access Management UI Summary

**Access management UI shipped with permissions visibility and guarded edits.**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `frontend/src/pages/UsersPage.tsx` - Access management UI
- `frontend/src/components/access/PermissionMatrix.tsx` - Permissions display
- `frontend/src/services/accessApi.ts` - Access management API client

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for Phase 85 Plan 04 (optional alignment of permission gating across other tabs)
</output>
