# Plan 253.1-02: Approval and Authorization Integrity

## Objective

Eliminate approval no-op states, align orphaned-item authorization and resolution rules, and restore privileged user lifecycle invariants on direct user mutation endpoints.

## Steps

1. Auto-reject edit approvals when the target resource no longer exists at apply time.
2. Tighten orphaned-items overview authz to governance-operator parity.
3. Replace random orphan fixing with explicit mapping-based dry-run and apply flows.
4. Enforce no-self-demotion and no-last-admin removal invariants in `PATCH /users/{user_id}`.
5. Add focused approval/orphan/access regression coverage.

## Verification

- `cd backend && ./venv/bin/pytest -q ../tests/backend/pytest/test_approval_edit_apply.py ../tests/backend/pytest/test_approval_workflow.py ../tests/backend/pytest/test_access_management.py ../tests/backend/pytest/test_admin_orphans.py`
