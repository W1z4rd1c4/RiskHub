# Plan 253.1-01: Runtime Boundary Integrity

## Objective

Fix middleware ordering, datetime coercion, rate-limit outage posture, and DB liveness so runtime safety and request contracts are correct before authorization and contract cleanup.

## Steps

1. Insert Phase `253.1` into planning state and roadmap, then scaffold the phase directory.
2. Reorder middleware so CORS is outermost and logging/security wrap rate-limit and protocol-guard short circuits.
3. Coerce activity-log and executions filter datetimes to UTC-aware values at the endpoint boundary.
4. Add DB engine liveness settings and production rate-limit fail-closed controls.
5. Extend runtime tests for short-circuit headers, preflight bypass, and Redis outage behavior.

## Verification

- `cd backend && ./venv/bin/pytest -q ../tests/backend/pytest/test_rate_limit_components.py ../tests/backend/pytest/test_rate_limit_redis_resilience.py ../tests/backend/pytest/test_runtime_middleware_contracts.py ../tests/backend/pytest/test_security_headers.py`
- `cd backend && ./venv/bin/pytest -q ../tests/backend/pytest/test_activity_log.py ../tests/backend/pytest/test_executions.py`
