# Plan 253.1-04: KRI Workflow and Defense-in-Depth

## Objective

Validate queued KRI submissions at apply time, harden redirect sanitization and secret validation, and record the remaining operational backlog explicitly at phase closeout.

## Steps

1. Change KRI value approval execution to validate `period_end` against current reporting rules at apply time.
2. Produce a preflight report for pending KRI approvals that would fail after the new validation.
3. Harden backend and frontend `return_to` sanitizers against malformed same-origin bypasses.
4. Block weak default secret values in non-debug mode.
5. Capture deferred follow-ups for rate-limit keying and non-Postgres outbox safety in the phase summary and planning state.

## Verification

- `cd backend && ./venv/bin/pytest -q ../tests/backend/pytest/test_kris_submission_rbac_api.py ../tests/backend/pytest/test_kri_deadline_service.py ../tests/backend/pytest/test_auth_password_hardening.py ../tests/backend/pytest/test_sso_exchange.py`
- `cd frontend && npm run test:run`
- `cd frontend && npx tsc --noEmit`
