# Plan 253.1-01 Summary: Runtime Boundary Integrity

## Completed

- Reordered middleware in `backend/app/main.py` so `CORSMiddleware` is outermost and rate-limit / protocol-guard short-circuit responses now pass through request-id and security-header layers.
- Exempted `OPTIONS` preflight requests from rate limiting.
- Coerced activity-log and executions datetime filters with `coerce_utc()` so naive inputs are treated as UTC.
- Added DB engine liveness defaults in `backend/app/db/session.py` with `pool_pre_ping=True` and `pool_recycle=1800`.
- Switched Redis limiter outage behavior to settings-controlled fail-closed in non-debug mode and retained debug/test fallback.
- Added limiter outage metric wiring and WARN logging without introducing a hard `prometheus_client` dependency.

## Verification

- `cd backend && ./venv/bin/pytest -q ../tests/backend/pytest/test_rate_limit_components.py ../tests/backend/pytest/test_rate_limit_redis_resilience.py ../tests/backend/pytest/test_runtime_middleware_contracts.py ../tests/backend/pytest/test_security_headers.py ../tests/backend/pytest/test_activity_log.py ../tests/backend/pytest/test_executions.py`
- Result: `52 passed`

## Notes

- Production docs were updated later in `253.1-04` to describe the new Redis fail-closed posture and rollback switch.
