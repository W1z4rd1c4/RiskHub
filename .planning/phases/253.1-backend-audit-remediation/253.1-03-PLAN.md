# Plan 253.1-03: Reporting and Approval Contract Normalization

## Objective

Correct reporting aggregations, guarantee committee payload shape, normalize queued-approval `202` responses, and align frontend consumers atomically with the new backend contract.

## Steps

1. Fix dashboard summary form breakdown and quarterly comparison range clamping.
2. Guarantee `critical_vendors` on every committee-summary response.
3. Introduce a shared backend `202` approval response model and migrate all queued mutation routes to it.
4. Bound executions pagination and exclude inactive vendors from DORA export.
5. Update frontend approval response parsing and committee consumers to the normalized contract.

## Verification

- `cd backend && ./venv/bin/pytest -q ../tests/backend/pytest/test_dashboard.py ../tests/backend/pytest/test_dashboard_committee_vendor_metrics.py ../tests/backend/pytest/test_executions.py ../tests/backend/pytest/test_vendor_reports.py ../tests/backend/pytest/test_approval_workflow.py`
- `cd frontend && npm run test:run`
- `cd frontend && npx tsc --noEmit`
