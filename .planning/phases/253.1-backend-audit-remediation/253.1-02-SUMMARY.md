# Plan 253.1-02 Summary: Approval and Authorization Integrity

## Completed

- Edit approvals now auto-reject when the target resource is gone at apply time, covering risk/control edit flows and the KRI edit execution path.
- `/api/v1/orphaned-items/overview` now enforces the same governance-operator guard as the sibling orphan endpoints.
- Replaced `/api/v1/admin/fix-orphans` random mutation behavior with explicit mapping-based remediation and `dry_run`.
- Hardened orphan resolution rules to reject silent owner clearing and cross-department relinking within this phase.
- Restored privileged lifecycle invariants in `PATCH /users/{user_id}`:
  - no self-demotion from privileged access
  - no self-deactivation of privileged access
  - no removal of the last active global privileged admin/CRO
- Added the low-cost lifecycle guard in issue assignment so closed issues cannot be reassigned.

## Verification

- `cd backend && ./venv/bin/pytest -q ../tests/backend/pytest/test_approval_edit_apply.py ../tests/backend/pytest/test_approval_workflow.py ../tests/backend/pytest/test_admin_orphans.py ../tests/backend/pytest/test_orphaned_items_scan_and_stats.py ../tests/backend/pytest/test_users.py`
- Result: `60 passed`

## Notes

- First production deploy after this change will sweep stale pending edit approvals into explicit `REJECTED` outcomes instead of leaving silent no-op approvals behind.
