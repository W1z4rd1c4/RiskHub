---
phase: 156-audit
type: execute
---

<objective>
Ensure RiskHub configuration endpoints persist ActivityLog entries.

Purpose: Fix missing audit trail for CRO configuration changes caused by incorrect commit ordering.
Output: RiskHub endpoints write ActivityLog in the same transaction + tests.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/156-audit/156-CONTEXT.md
@backend/app/api/v1/endpoints/riskhub.py
@backend/app/core/activity_logger.py
@backend/app/models/activity_log.py
@backend/tests/test_riskhub_risk_types.py
@backend/tests/test_activity_log.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor RiskHub endpoints to log before final commit</name>
  <files>backend/app/api/v1/endpoints/riskhub.py</files>
  <action>
Fix RiskHub endpoints where `log_activity()` is called after `await db.commit()` but not persisted:

- For each RiskHub write endpoint (create/update/delete/restore) that calls `log_activity`:
  - Ensure `log_activity(...)` is called before the final `await db.commit()` that persists the business change.
  - Prefer pattern:
    1) mutate DB objects
    2) `await db.flush()` if you need IDs
    3) `await log_activity(...)`
    4) `await db.commit()`
    5) `await db.refresh(...)` if response needs refreshed state

Avoid:
- Doing `commit()` then `log_activity()` without a follow-up commit.
- Changing the semantics of who can access RiskHub (CRO-only must remain).
  </action>
  <verify>cd backend && pytest -q tests/test_riskhub_risk_types.py -q</verify>
  <done>RiskHub write endpoints persist ActivityLog entries reliably.</done>
</task>

<task type="auto">
  <name>Task 2: Add test proving ActivityLog is written for RiskHub changes</name>
  <files>backend/tests/test_riskhub_risk_types.py</files>
  <action>
Add a regression test (or extend an existing one) that:

- Performs a RiskHub write action as CRO (e.g., POST `/api/v1/riskhub/risk-types`).
- Queries `ActivityLog` and asserts a corresponding entry exists:
  - `entity_type == "config"` (matches `ActivityEntityType.CONFIG`)
  - `action` matches create/update/delete
  - `entity_id` matches created config object ID
  - `actor_id` matches CRO user

Implementation notes:
- Use existing CRO test client fixtures from RiskHub test modules.
- Use the test DB session fixture to query the ActivityLog table directly.
  </action>
  <verify>cd backend && pytest -q tests/test_riskhub_risk_types.py -k \"ActivityLog\"</verify>
  <done>Test fails before fix (no log persisted) and passes after refactor.</done>
</task>

</tasks>

<verification>
Before declaring complete:
- [ ] `cd backend && pytest -q tests/test_riskhub_risk_types.py`
- [ ] `cd backend && pytest -q tests/test_activity_log.py` (ensures no regression)
</verification>

<success_criteria>
- Every RiskHub write operation creates a persisted ActivityLog row in the same transaction.
- Test coverage prevents regression.
</success_criteria>

<output>
After completion, create `.planning/phases/156-audit/156-03-SUMMARY.md` with:
- Endpoints updated
- Tests added
- Confirmation of transactional behavior
</output>

