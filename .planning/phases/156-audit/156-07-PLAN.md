---
phase: 156-audit
type: execute
---

<objective>
Add DB-level uniqueness for Control↔Risk links to prevent duplicate relationships under concurrency.

Purpose: Guarantee data integrity for `control_risk_links` even if concurrent requests bypass application-level “already exists” checks, and ensure endpoints don’t 500 when the DB constraint triggers.
Output: Alembic migration to dedupe + unique index, endpoint-level IntegrityError handling, plus regression tests.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
@.planning/phases/156-audit/156-CONTEXT.md
@backend/app/models/risk.py
@backend/app/api/v1/endpoints/controls.py
@backend/app/api/v1/endpoints/risks.py
@backend/alembic/env.py
@backend/alembic/versions/71c378bc4f1c_add_control_risk_and_execution_tables.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration to dedupe and add unique index on (control_id, risk_id)</name>
  <files>backend/alembic/versions/*_unique_control_risk_links.py</files>
  <action>
Create a new Alembic migration that:

0) Generate the migration scaffold:
   - Run: `cd backend && alembic revision -m "unique_control_risk_links"`
   - Edit the newly created file under `backend/alembic/versions/` (use the file name Alembic generates).

1) Deduplicates existing rows (portable SQL):
   - Delete any row whose `id` is not the minimum `id` for its `(control_id, risk_id)` group:
     `DELETE FROM control_risk_links WHERE id NOT IN (SELECT MIN(id) FROM control_risk_links GROUP BY control_id, risk_id);`
   - This must run before adding uniqueness, so the migration does not fail on existing duplicates.

2) Adds a unique index (or unique constraint) on `(control_id, risk_id)`:
   - Prefer a named index like `ux_control_risk_links_control_risk`.
   - Ensure the downgrade removes it.

Avoid:
- Postgres-only syntax unless you also provide SQLite-compatible fallback (tests may use SQLite).
  </action>
  <verify>cd backend && alembic upgrade head</verify>
  <done>DB schema prevents duplicate links; migration succeeds on existing data.</done>
</task>

<task type="auto">
  <name>Task 2: Add endpoint stability + DB-level tests for duplicates</name>
  <files>backend/app/api/v1/endpoints/controls.py, backend/app/api/v1/endpoints/risks.py, backend/tests/test_controls.py</files>
  <action>
Part A (endpoint stability):
- Update link creation endpoints to handle the new DB uniqueness constraint under concurrency:
  - In `backend/app/api/v1/endpoints/controls.py` `link_control_to_risk`:
    - Wrap `db.add(link)` + `await db.commit()` in a try/except for `sqlalchemy.exc.IntegrityError`.
    - On IntegrityError: `await db.rollback()` and return a stable client error:
      - Prefer `HTTP 409` with `detail="Link already exists"`.
  - In `backend/app/api/v1/endpoints/risks.py` (the symmetrical endpoint that links risks to controls):
    - Apply the same pattern so both directions behave identically.

Avoid:
- Returning 500 due to unhandled IntegrityError.
- Returning different status codes/messages across the two endpoints.

Part B (tests):
1) DB-level constraint test:
   - Insert a `Control`, a `Risk`, and one `ControlRiskLink`.
   - Attempt to insert a second `ControlRiskLink` with the same `(control_id, risk_id)` directly via the DB session.
   - Assert an `IntegrityError` (or equivalent) is raised on commit/flush.

2) Endpoint duplicate behavior test (deterministic, not truly concurrent):
   - Call the link endpoint twice with the same `control_id` + `risk_id`.
   - First call returns 201; second call returns 409 with “Link already exists”.

Implementation notes:
- Prefer direct DB insert to ensure you’re testing the constraint, not the endpoint logic.
- Keep test small and deterministic.
  </action>
  <verify>cd backend && pytest -q tests/test_controls.py -k \"control_risk\"</verify>
  <done>DB-level duplicates fail; endpoints return stable 409 (not 500) on duplicates.</done>
</task>

</tasks>

<verification>
Before declaring complete:
- [ ] `cd backend && pytest -q tests/test_controls.py`
- [ ] `cd backend && pytest -q` (full suite if time permits)
</verification>

<success_criteria>
- Duplicate control↔risk links are impossible at the DB level.
- Migration is safe (dedupes before enforcing uniqueness).
- Link endpoints remain stable under duplicates (no 500s).
</success_criteria>

<output>
After completion, create `.planning/phases/156-audit/156-07-SUMMARY.md` including:
- Migration name + approach
- Test added
- Notes on any duplicates found/removed (if observable)
</output>
