---
phase: 156-audit
type: execute
---

<objective>
Fix risk ID code generation for `R100+` and large datasets.

Purpose: Prevent duplicate risk codes and retry loops caused by lexicographic sorting and limited sampling.
Output: Correct generator + regression tests for 3-digit+ risk codes.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/156-audit/156-CONTEXT.md
@backend/app/api/v1/endpoints/risks.py
@backend/tests/test_risks_concurrency.py
@backend/tests/test_risks.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make `generate_risk_id_code()` numeric-correct and unbounded</name>
  <files>backend/app/api/v1/endpoints/risks.py</files>
  <action>
Refactor `generate_risk_id_code()` to remove lexicographic assumptions and arbitrary limits:

- Do NOT rely on `.order_by(Risk.risk_id_code.desc())` because string ordering breaks at `...R100`.
- Do NOT use `limit(100)` (or any small limit) when determining the max suffix.
- Instead:
  1) Select all `Risk.risk_id_code` values matching the prefix pattern.
  2) Parse the suffix after the prefix in Python, accept only fully-digit suffixes.
  3) Compute `max_num` from those suffixes and return `{prefix}{max_num + 1:02d}`.
     - Keep `:02d` formatting; it will naturally produce `R100` for 100+ (no truncation).

Notes:
- Keep the existing atomic retry loop in `create_risk()` unchanged; it remains the concurrency safety net.
- Update docstring and comments to remove “MAX + 1 pattern” wording if it’s misleading.
- Keep behavior stable for existing `R01`…`R99` prefixes.
  </action>
  <verify>cd backend && python -c "from app.api.v1.endpoints.risks import generate_risk_id_code; print('ok')"</verify>
  <done>Generator returns the next numeric suffix even when existing codes include `R100+`.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression test for `R100+` generation</name>
  <files>backend/tests/test_risks.py</files>
  <action>
Add a focused test that fails on the old implementation and passes on the new one:

- Create risks with `risk_id_code` values for one prefix such that the max is >= 100, e.g.:
  - `TEST-R98`, `TEST-R99`, `TEST-R100`, `TEST-R101`
  (Use the real prefix format produced by `process_abbr` + `-R` to match production behavior.)
- Call `generate_risk_id_code(db_session, process="Test")` and assert:
  - It returns `{prefix}{max+1}` (e.g. `TEST-R102`), not a duplicate like `TEST-R100`.

Implementation detail:
- Use an async DB session fixture and insert minimal required fields for `Risk`.
  </action>
  <verify>cd backend && pytest -q tests/test_risks.py -k \"risk_id_code\"</verify>
  <done>Test reproduces the bug on old generator and passes with the fixed one.</done>
</task>

</tasks>

<verification>
Before declaring complete:
- [ ] `cd backend && pytest -q tests/test_risks.py -k risk_id_code`
- [ ] `cd backend && pytest -q` (full suite, if time permits)
</verification>

<success_criteria>
- Risk creation can continue past `R99` without duplicates or infinite retries.
- Regression test locks in correct behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/156-audit/156-02-SUMMARY.md` including:
- Updated generator behavior
- Tests added
- Any performance considerations (selecting codes by prefix)
</output>

