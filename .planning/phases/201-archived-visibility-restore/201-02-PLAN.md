---
phase: 201-archived-visibility-restore
plan: 201-02
type: execute
wave: 1
depends_on: [201-01]
files_modified:
  - backend/app/api/v1/endpoints/risks.py
  - backend/app/api/v1/endpoints/controls.py
  - backend/app/api/v1/endpoints/kris.py
  - backend/app/api/v1/endpoints/vendors.py
  - backend/app/api/v1/endpoints/vendor_slas.py
autonomous: true
---

<objective>
Implement backend unarchive operations and enforce final permission model for archive/unarchive.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add restore endpoints for all covered entities</name>
  <files>risks.py, controls.py, kris.py, vendors.py, vendor_slas.py</files>
  <action>
Add:
- POST /risks/{id}/restore
- POST /controls/{id}/restore
- POST /kris/{id}/restore
- POST /vendors/{id}/restore
- POST /vendor-slas/{id}/restore

State transitions:
- Risk/Control: status -> active
- KRI/VendorSLA: clear archived flags + metadata
- Vendor: status inactive -> active
  </action>
  <verify>Restored entities reappear in default list views and can be retrieved via detail endpoints.</verify>
  <done>All covered entities support unarchive backend operations.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce unarchive permission policy</name>
  <files>risks.py, controls.py, kris.py, vendors.py, vendor_slas.py</files>
  <action>
Permission requirements:
- Risk restore: risks:delete
- Control restore: controls:delete
- KRI restore: risks:delete
- Vendor restore: vendors:delete
- Vendor SLA restore: vendors:delete

Also update Vendor SLA archive endpoint to require vendors:delete for symmetry.
  </action>
  <verify>Users without delete permission receive 403 for restore and vendor SLA archive/restore.</verify>
  <done>Mutation policy matches agreed permission model.</done>
</task>

<task type="auto">
  <name>Task 3: Add activity logging for restore operations</name>
  <files>risks.py, controls.py, kris.py, vendors.py, vendor_slas.py</files>
  <action>
Log restore as ActivityAction.UPDATE with explicit restore descriptions.
Ensure logs are in same transaction pattern used for entity updates.
  </action>
  <verify>Activity log entries created for each restore path with entity type and actor metadata.</verify>
  <done>Auditable restore operations in all covered domains.</done>
</task>

</tasks>

<must_haves>
- Restore endpoints are idempotent-safe in behavior (error or no-op semantics clearly handled).
- No hard-delete paths introduced.
- Existing delete/archive approval flows remain unchanged.
</must_haves>

<verification>
- [ ] Restore endpoints implemented for 5 entity families
- [ ] Permission checks enforced per policy
- [ ] Restore operations are activity-logged
</verification>
