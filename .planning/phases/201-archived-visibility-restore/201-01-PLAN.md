---
phase: 201-archived-visibility-restore
plan: 201-01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - backend/app/api/v1/endpoints/controls.py
  - backend/app/api/v1/endpoints/vendors.py
  - backend/app/api/v1/endpoints/kris.py
  - backend/app/schemas/kri.py
  - backend/app/schemas/risk.py
  - backend/app/schemas/vendor_links.py
  - frontend/src/types/kri.ts
  - frontend/src/types/vendorLink.ts
autonomous: true
---

<objective>
Establish Phase 201 and normalize backend archive-visibility contracts before restore/unarchive UI work.
</objective>

<context>
Current state:
- Risks support include_archived by default-off.
- Controls list excludes archived by default but lacks include_archived parameter.
- KRIs include archived only for privileged users today.
- Vendors use status=inactive as archive semantics and currently expose no include_archived flag.
- Linked risk/control DTOs do not consistently expose status needed for archived styling.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 201 planning anchors</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
Create Phase 201 entry with goal and progress tracking placeholders.
Set status to in-progress with 5 plans.
  </action>
  <verify>Phase 201 appears in roadmap and state with matching counts.</verify>
  <done>Planning metadata ready for execution continuity.</done>
</task>

<task type="auto">
  <name>Task 2: Add include_archived support to controls and vendors list endpoints</name>
  <files>backend/app/api/v1/endpoints/controls.py, backend/app/api/v1/endpoints/vendors.py</files>
  <action>
Controls:
- Add include_archived: bool=false to list endpoint.
- Preserve current status filter behavior; when status is absent and include_archived=false, exclude archived controls.

Vendors:
- Add include_archived: bool=false to list endpoint.
- Treat status=inactive as archived.
- When include_archived=false and explicit status filter is absent, default to status=active.
- Keep department/owner scoping unchanged.
  </action>
  <verify>Endpoint responses match existing behavior with include_archived=false and expose archived rows with include_archived=true.</verify>
  <done>Consistent archive visibility contract across core list endpoints.</done>
</task>

<task type="auto">
  <name>Task 3: Relax KRI archived read gating to read-scope instead of privileged-only</name>
  <files>backend/app/api/v1/endpoints/kris.py</files>
  <action>
Update KRI list/detail/history archived-access checks:
- include_archived should be honored for users with normal risks:read and scope-based access.
- Keep anti-enumeration behavior and department/ownership scoping.
- Do not broaden write/delete permissions.
  </action>
  <verify>Non-privileged read-scoped users can request include_archived and only see allowed archived KRIs.</verify>
  <done>Archived KRI read behavior aligns with feature goal.</done>
</task>

<task type="auto">
  <name>Task 4: Add archive metadata/status fields required by frontend</name>
  <files>backend/app/schemas/kri.py, backend/app/schemas/risk.py, backend/app/schemas/vendor_links.py, frontend/src/types/kri.ts, frontend/src/types/vendorLink.ts</files>
  <action>
- Add is_archived/archived_at/archived_by_id to KRI response schema + TS type.
- Add risk status to risk brief link models used by control/vendor linked views.
- Keep backward-compatible optional typing where feasible.
  </action>
  <verify>Frontend receives fields required for archived section rendering without runtime type errors.</verify>
  <done>Contracts are ready for UI archived grouping and muted rendering.</done>
</task>

</tasks>

<must_haves>
- Existing non-archived behavior unchanged when include_archived is not provided.
- No RBAC widening for mutation operations.
- Vendor inactive semantics preserved.
</must_haves>

<verification>
- [ ] Phase 201 recorded in roadmap/state
- [ ] include_archived added to controls/vendors list endpoints
- [ ] KRI archived read gating aligned to read-scope
- [ ] Schema/type contracts updated for archived rendering
</verification>
