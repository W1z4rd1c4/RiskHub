---
phase: 150-audit
type: execute
---

<objective>
Close remaining backend audit gaps by enforcing department access for KRI approvals and standardizing orphaned timestamps to UTC-aware values.

Purpose: Prevent cross-department approval creation and eliminate mixed naive/UTC timestamps in orphan governance.
Output: Approval checks aligned to department access and consistent UTC timestamps for orphaned items.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/150-audit/150-01-FINDINGS.md
@.planning/phases/150-audit/150-02-FINDINGS.md
@backend/app/api/v1/endpoints/approvals.py
@backend/app/models/key_risk_indicator.py
@backend/app/models/risk.py
@backend/app/core/permissions.py
@backend/app/services/orphaned_item_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce department access on KRI approval requests</name>
  <files>backend/app/api/v1/endpoints/approvals.py</files>
  <action>When `resource_type` is KRI, load the KRI with its linked Risk (join or eager load). If the risk is missing, return 404. Use `check_department_access(risk.department_id, current_user)` before creating the approval request. Keep the resource_name logic intact, but ensure the access check runs for KRIs like it does for risks and controls.</action>
  <verify>`pytest backend/tests/test_approvals.py` passes and a user cannot create KRI approvals for a department they do not belong to.</verify>
  <done>KRI approval creation enforces department access consistently with risks/controls.</done>
</task>

<task type="auto">
  <name>Task 2: Standardize orphaned timestamps to UTC-aware values</name>
  <files>backend/app/services/orphaned_item_service.py</files>
  <action>Replace all `datetime.utcnow()` usage with `datetime.now(UTC)` and ensure UTC is imported from `datetime`. Keep behavior identical otherwise (no schema changes). Avoid mixing naive and aware datetimes in orphaned item creation.</action>
  <verify>`pytest backend/tests/test_access_management.py` passes and orphaned items store UTC-aware timestamps.</verify>
  <done>All orphaned item timestamps are UTC-aware and consistent with the rest of the backend.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `pytest backend/tests/test_approvals.py`
- [ ] `pytest backend/tests/test_access_management.py`
- [ ] Orphaned items store UTC-aware timestamps
</verification>

<success_criteria>

- All tasks completed
- KRI approval requests are department-scoped
- Orphaned item timestamps are UTC-aware
- No regressions in approval workflows
  </success_criteria>

<output>
After completion, create `.planning/phases/150-audit/150-05-SUMMARY.md`:

# Phase 150 Plan 05: KRI Approval + Orphan Timestamp Fixes Summary

**[One-line summary of the backend fixes delivered]**

## Accomplishments

- [KRI approval department access enforced]
- [Orphaned timestamps standardized to UTC]

## Files Created/Modified

- `backend/app/api/v1/endpoints/approvals.py` - KRI department access check
- `backend/app/services/orphaned_item_service.py` - UTC-aware timestamps

## Decisions Made

[None]

## Issues Encountered

[None]

## Next Step

Ready for 150-06-PLAN.md
</output>
