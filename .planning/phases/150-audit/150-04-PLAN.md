---
phase: 150-audit
type: execute
---

<objective>
Harden RiskHub backend auth/webhook security by requiring configured webhook secrets in production and restricting mock-auth code paths to debug-only use.

Purpose: Close remaining security gaps from the Phase 150 audit that allow unsigned webhooks or mock login in production.
Output: Updated backend config + directory endpoint gating with safer mock-auth behavior.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/150-audit/150-01-FINDINGS.md
@backend/app/main.py
@backend/app/core/config.py
@backend/app/api/v1/endpoints/directory.py
@backend/app/core/security.py
@backend/app/api/v1/endpoints/users.py
@backend/app/api/deps.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce WEBHOOK_SECRET in non-debug environments</name>
  <files>backend/app/main.py, backend/app/api/v1/endpoints/directory.py, backend/app/core/config.py</files>
  <action>Add a production startup check that raises if `settings.webhook_secret` is empty when `DEBUG=false`, mirroring the existing SECRET_KEY guard. In `directory.py`, fail closed when the secret is missing (no signature verification bypass) unless `settings.debug` is true. Keep dev warning logs but avoid accepting unsigned payloads outside debug.</action>
  <verify>`pytest backend/tests/test_directory_sync.py` passes and starting the app with `DEBUG=false` + empty `WEBHOOK_SECRET` fails fast.</verify>
  <done>Webhook endpoint rejects unsigned requests unless debug mode explicitly allows it, and startup fails if WEBHOOK_SECRET is missing in production.</done>
</task>

<task type="auto">
  <name>Task 2: Gate mock-auth helpers to debug + mock_auth_enabled</name>
  <files>backend/app/core/security.py, backend/app/api/v1/endpoints/users.py</files>
  <action>Update `core.security.get_current_user` and `/users/mock-login` to require `settings.debug` and `settings.mock_auth_enabled` (not just env var). Prefer `get_settings()` over direct env reads; keep clear error responses when mock auth is disabled. Avoid any behavior change for normal JWT auth paths in `deps.get_current_user`.</action>
  <verify>`pytest backend/tests/test_users.py` passes and mock-login returns 404 unless DEBUG=true + MOCK_AUTH_ENABLED=true.</verify>
  <done>Mock auth endpoints are blocked outside debug, and no JWT-auth flows are impacted.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `pytest backend/tests/test_directory_sync.py`
- [ ] `pytest backend/tests/test_users.py`
- [ ] App startup fails with `DEBUG=false` and missing `WEBHOOK_SECRET`
</verification>

<success_criteria>

- All tasks completed
- Webhook secret required in non-debug environments
- Mock auth helpers only function in debug + mock_auth_enabled
- No regressions in JWT auth paths
  </success_criteria>

<output>
After completion, create `.planning/phases/150-audit/150-04-SUMMARY.md`:

# Phase 150 Plan 04: Webhook + Mock Auth Hardening Summary

**[One-line summary of security hardening delivered]**

## Accomplishments

- [Webhook secret enforcement added]
- [Mock-auth paths gated to debug mode]

## Files Created/Modified

- `backend/app/main.py` - Startup security checks
- `backend/app/api/v1/endpoints/directory.py` - Fail-closed signature enforcement
- `backend/app/core/security.py` - Debug-only mock auth
- `backend/app/api/v1/endpoints/users.py` - Debug-only mock login

## Decisions Made

[None]

## Issues Encountered

[None]

## Next Step

Ready for 150-05-PLAN.md
</output>
