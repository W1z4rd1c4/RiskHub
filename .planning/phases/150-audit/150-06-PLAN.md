---
phase: 150-audit
type: execute
---

<objective>
Remove the 100-item caps on Department Detail lists and align recent execution results with backend enums.

Purpose: Ensure department detail tabs show complete data with consistent execution status rendering.
Output: Department detail page paginates full datasets and uses backend execution result enums.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/150-audit/150-03-FINDINGS.md
@frontend/src/pages/DepartmentDetailPage.tsx
@frontend/src/services/departmentApi.ts
@frontend/src/services/userApi.ts
@frontend/src/services/executionApi.ts
@frontend/src/components/tables/Pagination.tsx
@backend/app/api/v1/endpoints/users.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pagination parameters to scoped user lookup</name>
  <files>backend/app/api/v1/endpoints/users.py, frontend/src/services/userApi.ts</files>
  <action>Add optional `skip` and `limit` query params to `/users/lookup` and apply them with `.offset(skip).limit(limit)` (keep response as a list to avoid breaking pickers). Update `userApi.listVisibleUsers` to accept `skip`/`limit` and forward them as params. Keep defaults (limit=100) intact for existing callers.</action>
  <verify>`pytest backend/tests/test_users.py` passes and `/users/lookup?skip=0&limit=200` returns more than 100 when available.</verify>
  <done>Scoped user lookup supports paging without changing response shape.</done>
</task>

<task type="auto">
  <name>Task 2: Paginate department detail data and align execution result enums</name>
  <files>frontend/src/pages/DepartmentDetailPage.tsx, frontend/src/services/departmentApi.ts, frontend/src/services/userApi.ts</files>
  <action>Replace the fixed `limit: 100` requests with paginated fetch loops for risks/controls/KRIs/users (increment skip by page size until results < page size). Add per-tab client-side pagination using the shared `Pagination` component, so large datasets are navigable. Update recent execution rendering to use backend enums (`passed`, `failed`, `warning`, `not_applicable`) by importing `ExecutionResult` and mapping icons/colors accordingly.</action>
  <verify>`npm run lint` passes and Department Detail tabs show full datasets beyond 100 items with correct execution status badges.</verify>
  <done>Department detail lists are complete and execution statuses match backend enums.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run lint`
- [ ] Department Detail tabs paginate beyond 100 items
- [ ] Recent executions render `passed/failed/warning/not_applicable` correctly
</verification>

<success_criteria>

- All tasks completed
- Department Detail lists no longer capped at 100
- Execution result enums match backend values
- No regressions in department detail navigation
  </success_criteria>

<output>
After completion, create `.planning/phases/150-audit/150-06-SUMMARY.md`:

# Phase 150 Plan 06: Department Detail Pagination + Enum Alignment Summary

**[One-line summary of the department detail fixes delivered]**

## Accomplishments

- [Department detail pagination added for risks/controls/KRIs/users]
- [Execution result enums aligned with backend]

## Files Created/Modified

- `backend/app/api/v1/endpoints/users.py` - Paginated lookup params
- `frontend/src/services/userApi.ts` - Paging parameters
- `frontend/src/pages/DepartmentDetailPage.tsx` - Pagination + enum alignment
- `frontend/src/services/departmentApi.ts` - Paging usage updates

## Decisions Made

[None]

## Issues Encountered

[None]

## Next Step

Phase 150 remediation planning complete; ready to execute 150-04/05/06.
</output>
