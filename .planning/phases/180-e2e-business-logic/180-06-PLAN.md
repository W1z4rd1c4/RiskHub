---
phase: 180-e2e-business-logic
type: execute
---

<objective>
E2E Tests for Cross-Department Access

Purpose: Implement E2E tests covering BUSINESS_LOGIC.md §7 (Cross-Department Access) including ownership-based access and access inheritance.
Output: Test specs verifying cross-department access patterns for owners.
</objective>

<context>
@docs/BUSINESS_LOGIC.md (§7 Cross-Department Access)
@frontend/e2e/fixtures/auth.fixture.ts
@frontend/e2e/pages/RisksPage.ts
@frontend/e2e/pages/ControlsPage.ts
@frontend/e2e/pages/KRIsPage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test Risk Owner Cross-Department Access</name>
  <files>frontend/e2e/cross-department/risk-owner-access.spec.ts</files>
  <action>
    Create tests for BUSINESS_LOGIC.md §7.1 - Risk Owner Access:
    
    1. **Risk Owner Can Access Own Risk in Other Dept**:
       - Create a Risk in Department A
       - Assign owner as user from Department B
       - Login as Dept B user (the owner)
       - Navigate to Risks list, verify the Risk from Dept A is visible
       - Navigate to Risk detail, verify full access
    
    2. **Risk Owner Can Edit Own Risk**:
       - Login as the cross-department Risk owner
       - Edit a non-sensitive field on the Risk
       - Verify edit works (subject to approval rules)
    
    3. **Non-Owner Cannot Access Other Dept Risk**:
       - Login as random Dept B user (not owner)
       - Attempt to access Dept A Risk via direct URL
       - Verify 403 or redirect to dashboard
  </action>
  <verify>Run `cd frontend && npx playwright test risk-owner-access.spec.ts --project=chromium`. All tests pass.</verify>
  <done>risk-owner-access.spec.ts covers risk owner cross-department access patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Test Control Owner Cross-Department Access</name>
  <files>frontend/e2e/cross-department/control-owner-access.spec.ts</files>
  <action>
    Create tests for BUSINESS_LOGIC.md §7.1 - Control Owner Access:
    
    1. **Control Owner Can View Own Control in Other Dept**:
       - Create a Control in Department A
       - Assign control_owner as user from Department B
       - Login as Dept B control owner
       - Verify Control is visible in Controls list
       - Verify Control detail page is accessible
    
    2. **Control Owner Can View Linked Risks**:
       - Link the Control to a Risk in Department A
       - Login as Dept B control owner
       - Navigate to Control detail, verify linked Risk is visible
       - Click linked Risk, verify Risk detail is accessible
    
    3. **Control Owner Can Edit Own Control**:
       - Login as control owner, edit a field
       - Verify edit works (subject to high-risk approval)
  </action>
  <verify>Run `cd frontend && npx playwright test control-owner-access.spec.ts --project=chromium`. All tests pass.</verify>
  <done>control-owner-access.spec.ts covers control owner access and linked risk visibility.</done>
</task>

<task type="auto">
  <name>Task 3: Test KRI Reporting Owner Cross-Department Access</name>
  <files>frontend/e2e/cross-department/kri-owner-access.spec.ts</files>
  <action>
    Create tests for BUSINESS_LOGIC.md §7.1 and §7.2 - KRI Reporting Owner:
    
    1. **Reporting Owner Can View KRI in Other Dept**:
       - Create a KRI linked to a Risk in Department A
       - Assign reporting_owner as user from Department B
       - Login as Dept B reporting owner
       - Verify KRI is visible in KRIs list
       - Verify KRI detail page is accessible
    
    2. **Reporting Owner Can View Linked Risk**:
       - Navigate to KRI detail
       - Verify linked Risk (Dept A) is visible
       - Click linked Risk, verify Risk detail accessible
    
    3. **Reporting Owner Can Submit Values**:
       - Navigate to KRI detail
       - Verify "Record Value" button is visible
       - Submit a value, verify success
    
    4. **Access Inheritance Chain**:
       - From KRI, verify access to linked Risk
       - From linked Risk, verify access to Risk's linked Controls
  </action>
  <verify>Run `cd frontend && npx playwright test kri-owner-access.spec.ts --project=chromium`. All tests pass.</verify>
  <done>kri-owner-access.spec.ts covers reporting owner access and inheritance chain.</done>
</task>

<task type="auto">
  <name>Task 4: Test Risk-Control Linking Access</name>
  <files>frontend/e2e/cross-department/link-access.spec.ts</files>
  <action>
    Create tests for BUSINESS_LOGIC.md §7.3 - Risk-Control Linking:
    
    1. **GET /risks/{id}/controls Access**:
       - Login as Risk owner (cross-department)
       - Navigate to Risk detail, view Controls tab
       - Verify linked controls are displayed
    
    2. **POST /risks/{id}/controls (Link Control)**:
       - Login as user with risks:write permission
       - Navigate to Risk detail, open Link Controls modal
       - Search for a Control, link it
       - Verify link is created (no approval required)
    
    3. **DELETE /risks/{id}/controls/{id} (Unlink Control)**:
       - Login as user with risks:write permission
       - Navigate to Risk detail, Controls tab
       - Unlink a control
       - Verify link is removed (no approval required)
    
    4. **Insufficient Permissions**:
       - Login as Employee without risks:write
       - Navigate to Risk detail
       - Verify Link/Unlink buttons are NOT visible or disabled
  </action>
  <verify>Run `cd frontend && npx playwright test link-access.spec.ts --project=chromium`. All tests pass.</verify>
  <done>link-access.spec.ts covers linking/unlinking access patterns.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run lint` passes
- [ ] `cd frontend && npx playwright test cross-department/ --project=chromium` passes
- [ ] All 3 ownership types (Risk, Control, KRI) tested for cross-department access
- [ ] Access inheritance is verified
- [ ] Linking/unlinking permissions tested
</verification>

<success_criteria>
- Cross-department access via ownership fully tested
- Access inheritance chain verified (KRI → Risk → Controls)
- Linking/unlinking permissions verified as approval-free
</success_criteria>

<output>
After completion, create `.planning/phases/180-e2e-business-logic/180-06-SUMMARY.md`
</output>
