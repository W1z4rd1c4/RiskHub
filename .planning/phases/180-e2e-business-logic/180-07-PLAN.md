---
phase: 180-e2e-business-logic
type: execute
---

<objective>
E2E Tests for Activity Logging & Audit Trail

Purpose: Implement E2E tests covering BUSINESS_LOGIC.md §9 (Activity Logging & Audit Trail) including entity-level logging and change tracking.
Output: Test specs verifying activity log creation for all entity operations.
</objective>

<context>
@docs/BUSINESS_LOGIC.md (§9 Activity Logging & Audit Trail)
@frontend/e2e/fixtures/auth.fixture.ts
@frontend/e2e/pages/ActivityLogPage.ts (to be created)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Activity Log Page Object</name>
  <files>frontend/e2e/pages/ActivityLogPage.ts</files>
  <action>
    Create Page Object Model for Activity Log:
    
    1. Locators:
       - Activity log table/list
       - Filter controls (entity type, action, user, date range)
       - Search input
       - Export buttons (PDF, Excel)
       - Entry detail/diff view
    
    2. Methods:
       - `navigateToActivityLog()` - Go to activity log
       - `filterByEntityType(type)` - Filter by RISK/CONTROL/KRI/APPROVAL
       - `filterByAction(action)` - Filter by CREATE/UPDATE/ARCHIVE
       - `filterByUser(userId)` - Filter by user
       - `searchEntries(query)` - Search logs
       - `getEntryCount()` - Get total entries count
       - `viewEntryDetails(index)` - View entry details including changes
       - `exportToPDF()` - Trigger PDF export
  </action>
  <verify>Import ActivityLogPage in test file without TS errors.</verify>
  <done>ActivityLogPage.ts created with typed locators and methods.</done>
</task>

<task type="auto">
  <name>Task 2: Test Entity-Level Logging</name>
  <files>frontend/e2e/activity-logging/entity-logging.spec.ts</files>
  <action>
    Create tests for BUSINESS_LOGIC.md §9.1 - Entity-Level Logging:
    
    1. **RISK Logging**:
       - Login as Risk Manager, create a new Risk
       - Navigate to Activity Log, filter by RISK
       - Verify CREATE entry appears with correct details
       - Edit the Risk, verify UPDATE entry appears
       - Delete the Risk, verify ARCHIVE entry appears
    
    2. **CONTROL Logging**:
       - Create a Control, verify CREATE logged
       - Edit a Control, verify UPDATE logged
       - Archive a Control, verify ARCHIVE logged
    
    3. **KRI Logging**:
       - Create a KRI, verify CREATE logged
       - Edit a KRI, verify UPDATE logged
       - Archive a KRI, verify ARCHIVE logged
    
    4. **KRI_VALUE Logging**:
       - Submit a KRI value, verify CREATE logged with entity type KRI_VALUE
       - Correct a KRI value, verify UPDATE logged
  </action>
  <verify>Run `cd frontend && npx playwright test entity-logging.spec.ts --project=chromium`. All tests pass.</verify>
  <done>entity-logging.spec.ts covers all entity CRUD logging.</done>
</task>

<task type="auto">
  <name>Task 3: Test Change Tracking</name>
  <files>frontend/e2e/activity-logging/change-tracking.spec.ts</files>
  <action>
    Create tests for BUSINESS_LOGIC.md §9.2 - Change Tracking:
    
    1. **Changes JSON Stored**:
       - Edit a Risk, change multiple fields
       - Navigate to Activity Log, find the UPDATE entry
       - View entry details, verify `changes` object shows:
         - Field name
         - Old value
         - New value
    
    2. **Date/Datetime Serialization**:
       - Edit an entity with a date field
       - Verify the date is stored in ISO format in the changes
    
    3. **Enum Value Serialization**:
       - Edit a Risk status or category (enum field)
       - Verify the enum is stored as its string value (not internal code)
    
    4. **Diff Display**:
       - View an UPDATE entry in the UI
       - Verify old vs new values are displayed clearly
  </action>
  <verify>Run `cd frontend && npx playwright test change-tracking.spec.ts --project=chromium`. All tests pass.</verify>
  <done>change-tracking.spec.ts covers change tracking and serialization.</done>
</task>

<task type="auto">
  <name>Task 4: Test Approval Execution Logging</name>
  <files>frontend/e2e/activity-logging/approval-logging.spec.ts</files>
  <action>
    Create tests for BUSINESS_LOGIC.md §9.3 - Approval Execution Logging:
    
    1. **APPROVAL Entity Logging**:
       - Create an approval request (delete as Employee)
       - Verify APPROVAL CREATE entry in log
       - Approve the request
       - Verify APPROVAL APPROVE entry in log
    
    2. **Underlying Entity Logging**:
       - After approving a delete, verify RISK/CONTROL/KRI ARCHIVE entry
       - Verify description mentions "Archived via approval #123"
    
    3. **Rejection Logging**:
       - Create an approval request, reject it
       - Verify APPROVAL REJECT entry in log
       - Verify underlying entity is NOT archived (no ARCHIVE entry)
    
    4. **Cancellation Logging**:
       - Create an approval request, cancel it
       - Verify APPROVAL CANCEL entry in log
  </action>
  <verify>Run `cd frontend && npx playwright test approval-logging.spec.ts --project=chromium`. All tests pass.</verify>
  <done>approval-logging.spec.ts covers all approval action logging.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run lint` passes
- [ ] `cd frontend && npx playwright test activity-logging/ --project=chromium` passes
- [ ] All entity types (RISK, CONTROL, KRI, KRI_VALUE, APPROVAL) have logging tests
- [ ] Change tracking with old/new values verified
- [ ] Approval execution dual-logging verified
</verification>

<success_criteria>
- All entity-level logging verified
- Change tracking with JSON serialization tested
- Approval execution logging (APPROVE/REJECT/CANCEL) tested
- Underlying entity logging on approval verified
</success_criteria>

<output>
After completion, create `.planning/phases/180-e2e-business-logic/180-07-SUMMARY.md`
</output>
