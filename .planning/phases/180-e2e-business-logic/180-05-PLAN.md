---
phase: 180-e2e-business-logic
type: execute
---

<objective>
E2E Tests for Sensitive Field Rules

Purpose: Implement E2E tests covering BUSINESS_LOGIC.md §6 (Sensitive Field Rules) including approval triggers for sensitive field changes.
Output: Test specs verifying approval triggers for owner_id, department_id, category, is_priority changes.
</objective>

<context>
@docs/BUSINESS_LOGIC.md (§6 Sensitive Field Rules)
@frontend/e2e/fixtures/auth.fixture.ts
@frontend/e2e/pages/RisksPage.ts
@frontend/e2e/pages/ControlsPage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test Risk Sensitive Field Changes</name>
  <files>frontend/e2e/sensitive-fields/risk-sensitive.spec.ts</files>
  <action>
    Create tests for BUSINESS_LOGIC.md §6.1 - Risk Sensitive Fields:
    
    1. **owner_id Change**:
       - Login as non-privileged user, edit Risk to change owner
       - Verify approval request is created
       - Verify changes are NOT applied until approved
    
    2. **department_id Change**:
       - Login as non-privileged user, edit Risk to change department
       - Verify approval request is created
       - Verify the Risk stays in original department until approved
    
    3. **category Change**:
       - Login as non-privileged user, edit Risk to change category
       - Verify approval request is created
    
    4. **is_priority Change (Downgrade)**:
       - Login as non-privileged user with a priority Risk
       - Edit is_priority from true → false
       - Verify approval request is created (§6.3 special case)
    
    5. **is_priority Change (Upgrade)**:
       - Login as non-privileged user with a non-priority Risk
       - Edit is_priority from false → true
       - Verify NO approval required (immediate update)
  </action>
  <verify>Run `cd frontend && npx playwright test risk-sensitive.spec.ts --project=chromium`. All tests pass.</verify>
  <done>risk-sensitive.spec.ts covers all 4 sensitive field changes for Risk.</done>
</task>

<task type="auto">
  <name>Task 2: Test Control Sensitive Field Changes</name>
  <files>frontend/e2e/sensitive-fields/control-sensitive.spec.ts</files>
  <action>
    Create tests for BUSINESS_LOGIC.md §6.1 - Control Sensitive Fields:
    
    1. **control_owner_id Change**:
       - Login as non-privileged user, edit Control to change owner
       - Verify approval request is created
       - Verify owner NOT changed until approved
    
    2. **department_id Change**:
       - Login as non-privileged user, edit Control to change department
       - Verify approval request is created
    
    3. **Privileged User Bypass**:
       - Login as Risk Manager, edit Control to change owner
       - Verify change is IMMEDIATE (no approval request)
  </action>
  <verify>Run `cd frontend && npx playwright test control-sensitive.spec.ts --project=chromium`. All tests pass.</verify>
  <done>control-sensitive.spec.ts covers sensitive field changes and privileged bypass.</done>
</task>

<task type="auto">
  <name>Task 3: Test Priority Risk Edit Rule</name>
  <files>frontend/e2e/sensitive-fields/priority-risk-edit.spec.ts</files>
  <action>
    Create tests for BUSINESS_LOGIC.md §6.2 - Priority Risk Edit Rule:
    
    1. **Any Edit on Priority Risk Requires Approval**:
       - Create a Risk with is_priority=true
       - Login as Department Head, edit a NON-SENSITIVE field (e.g., description)
       - Verify approval request is created (ANY field triggers approval)
    
    2. **CRO/Risk Manager Immediate Update**:
       - Login as CRO, edit priority Risk (any field)
       - Verify change is IMMEDIATE (no approval request)
       - Login as Risk Manager, edit priority Risk
       - Verify change is IMMEDIATE
    
    3. **Employee Requires Approval**:
       - Login as Employee, attempt to edit priority Risk
       - Verify approval request is created
    
    4. **Risk Owner (Non-Privileged) Requires Approval**:
       - Login as Risk Owner who is a Department Head
       - Edit their own priority Risk
       - Verify approval request is created
  </action>
  <verify>Run `cd frontend && npx playwright test priority-risk-edit.spec.ts --project=chromium`. All tests pass.</verify>
  <done>priority-risk-edit.spec.ts covers the any-field-edit rule for priority risks.</done>
</task>

<task type="auto">
  <name>Task 4: Test Clearing to NULL Special Case</name>
  <files>frontend/e2e/sensitive-fields/null-clearing.spec.ts</files>
  <action>
    Create tests for BUSINESS_LOGIC.md §6.3 - Clearing to NULL:
    
    1. **Clear owner_id to NULL**:
       - Login as non-privileged user, edit Risk
       - Clear the owner field (set to empty/null)
       - Verify approval request is created
       - Verify the approval notes mention "removing owner"
    
    2. **Clear department_id to NULL**:
       - Attempt to clear department (if UI allows)
       - Verify appropriate handling (error or approval request)
    
    3. **Approval Execution Applies NULL**:
       - Create approval request for clearing owner
       - Login as approver, approve
       - Verify owner_id is now NULL on the Risk
  </action>
  <verify>Run `cd frontend && npx playwright test null-clearing.spec.ts --project=chromium`. All tests pass.</verify>
  <done>null-clearing.spec.ts covers the NULL clearing special case.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run lint` passes
- [ ] `cd frontend && npx playwright test sensitive-fields/ --project=chromium` passes
- [ ] All sensitive fields from §6.1 are tested
- [ ] Priority risk edit rule (§6.2) is tested
- [ ] is_priority upgrade/downgrade asymmetry is tested
</verification>

<success_criteria>
- All sensitive field changes trigger appropriate approvals
- Priority risk edit rule tested for all user types
- is_priority upgrade vs downgrade asymmetry verified
- NULL clearing protection tested
</success_criteria>

<output>
After completion, create `.planning/phases/180-e2e-business-logic/180-05-SUMMARY.md`
</output>
