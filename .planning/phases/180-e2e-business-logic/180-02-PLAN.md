---
phase: 180-e2e-business-logic
type: execute
---

<objective>
E2E Tests for Entity Ownership & Department Relationships

Purpose: Implement E2E tests covering BUSINESS_LOGIC.md §2 (Entity Ownership Rules) and §3 (Department Relationships).
Output: Test specs verifying ownership assignment, inheritance chains, and department-scoped data access.
</objective>

<context>
@docs/BUSINESS_LOGIC.md (§2 Entity Ownership Rules, §3 Department Relationships)
@frontend/e2e/fixtures/auth.fixture.ts (from 180-01)
@frontend/e2e/pages/RisksPage.ts (from 180-01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test Risk Ownership Assignment</name>
  <files>frontend/e2e/entity-ownership/risk-ownership.spec.ts</files>
  <action>
    Create tests for BUSINESS_LOGIC.md §2.1 Risk Ownership:
    
    1. **Owner Assignment**:
       - Login as Risk Manager, create a new Risk
       - Verify `owner_id` can be set to any active user (cross-department)
       - Verify `department_id` defaults to creator's department
    
    2. **Ownership Hierarchy Display**:
       - Navigate to Risk detail page
       - Verify Risk Owner is displayed
       - Verify Department Head fallback is shown when applicable
    
    3. **Owner-Based Access**:
       - Login as Department Head from Dept A
       - Create a Risk owned by user from Dept B
       - Login as that Dept B owner, verify they CAN access the risk despite different department
    
    Use POMs from 180-01. Capture API responses to verify backend behavior.
  </action>
  <verify>Run `cd frontend && npx playwright test risk-ownership.spec.ts --project=chromium`. All tests pass.</verify>
  <done>risk-ownership.spec.ts covers owner assignment, hierarchy display, and owner-based access.</done>
</task>

<task type="auto">
  <name>Task 2: Test Control Ownership Assignment</name>
  <files>frontend/e2e/entity-ownership/control-ownership.spec.ts</files>
  <action>
    Create tests for BUSINESS_LOGIC.md §2.2 Control Ownership:
    
    1. **Control Owner Assignment**:
       - Login as Risk Manager, create a new Control
       - Verify `control_owner_id` can be set to any active user
       - Verify `department_id` can be set independently from owner's dept
    
    2. **Cross-Department Control Owner**:
       - Create Control in Dept A, assign owner from Dept B
       - Login as Dept B owner, verify they can VIEW the control
       - Verify they can EDIT the control (subject to approval if high-risk linked)
    
    3. **Ownership Display**:
       - Navigate to Control detail page
       - Verify Control Owner, Department, Created By, Updated By are displayed
  </action>
  <verify>Run `cd frontend && npx playwright test control-ownership.spec.ts --project=chromium`. All tests pass.</verify>
  <done>control-ownership.spec.ts covers owner assignment and cross-department access.</done>
</task>

<task type="auto">
  <name>Task 3: Test KRI Ownership & Inheritance</name>
  <files>frontend/e2e/entity-ownership/kri-ownership.spec.ts</files>
  <action>
    Create tests for BUSINESS_LOGIC.md §2.3 KRI Ownership:
    
    1. **Reporting Owner Assignment**:
       - Login as Risk Manager, create a new KRI linked to a Risk
       - Verify `reporting_owner_id` can be set to any user
       - Verify when no reporting owner, Risk Owner is shown as fallback
    
    2. **Department Inheritance**:
       - Create KRI for a Risk in Dept A
       - Verify KRI inherits department from linked Risk (no separate dept field)
       - Login as Dept A employee, verify they can see the KRI
    
    3. **KRI Value Submission Access**:
       - Login as KRI Reporting Owner, navigate to KRI detail
       - Verify "Record Value" button is visible
       - Login as random user (not owner), verify button is NOT visible
  </action>
  <verify>Run `cd frontend && npx playwright test kri-ownership.spec.ts --project=chromium`. All tests pass.</verify>
  <done>kri-ownership.spec.ts covers reporting owner, inheritance, and value submission access.</done>
</task>

<task type="auto">
  <name>Task 4: Test Department Access Rules</name>
  <files>frontend/e2e/department-access.spec.ts</files>
  <action>
    Create tests for BUSINESS_LOGIC.md §3 Department Relationships:
    
    1. **Department List Access**:
       - Login as GLOBAL user, navigate to /departments
       - Verify all departments are listed
       - Login as DEPARTMENT user, navigate to /departments
       - Verify only own department is accessible (or department list is scoped)
    
    2. **Department Detail Page**:
       - Login as Department Head of Dept A
       - Navigate to Dept A detail page, verify Risks/Controls/KRIs tabs populated
       - Navigate to Dept B detail page (via URL), verify 403 or empty/redirect
    
    3. **Department Stats Accuracy**:
       - Navigate to department detail page
       - Verify risk count matches actual risks in that department
       - Verify control count matches actual controls
       - Verify KRI count matches KRIs linked to that department's risks
  </action>
  <verify>Run `cd frontend && npx playwright test department-access.spec.ts --project=chromium`. All tests pass.</verify>
  <done>department-access.spec.ts covers department visibility, detail access, and stats accuracy.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run lint` passes
- [ ] `cd frontend && npx playwright test entity-ownership/ department-access.spec.ts --project=chromium` passes
- [ ] All 4 spec files have 3+ test cases each
</verification>

<success_criteria>
- Entity ownership tests for Risk, Control, KRI complete
- Department access boundary tests complete
- Cross-department ownership scenarios verified
</success_criteria>

<output>
After completion, create `.planning/phases/180-e2e-business-logic/180-02-SUMMARY.md`
</output>
