---
phase: 180-e2e-business-logic
plan: 180-13
type: execute
wave: 2
depends_on: [180-11]
files_modified:
  - frontend/e2e/vendors.spec.ts
  - frontend/e2e/permissions/vendors-crud.spec.ts
  - frontend/e2e/permissions/vendor-slas-crud.spec.ts
  - frontend/e2e/pages/VendorsPage.ts
  - frontend/e2e/pages/VendorDetailPage.ts
autonomous: true
---

<objective>
Introduce deterministic E2E coverage for Vendors and Vendor SLAs, including archive visibility and restore RBAC behavior.
</objective>

<context>
Current E2E suite has no dedicated vendor/vendor-sla coverage despite Phase 201 archive/restore logic changes and new include_archived semantics.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add vendor CRUD/visibility deterministic specs</name>
  <files>frontend/e2e/vendors.spec.ts, frontend/e2e/permissions/vendors-crud.spec.ts</files>
  <action>
Add deterministic tests for:
- Vendor list default behavior (archived/inactive hidden)
- Include archived toggle behavior for inactive vendors
- Vendor detail navigation and restore for users with `vendors:delete`
- Permission denial for restore when delete permission is absent
  </action>
  <verify>Vendor tests pass using deterministic seeded vendor constants.</verify>
  <done>Vendor archive visibility/restore flow is covered by E2E.</done>
</task>

<task type="auto">
  <name>Task 2: Add vendor SLA archive/restore deterministic specs</name>
  <files>frontend/e2e/permissions/vendor-slas-crud.spec.ts</files>
  <action>
Add deterministic tests for:
- SLA default list excluding archived records
- Include archived behavior for archived SLAs
- Archive/restore action gating with `vendors:delete`
- Successful restore transitions for authorized users
  </action>
  <verify>Vendor SLA tests cover allow/deny RBAC and state transitions.</verify>
  <done>Vendor SLA archive semantics are regression-protected.</done>
</task>

<task type="auto">
  <name>Task 3: Extend vendor page objects/helpers</name>
  <files>frontend/e2e/pages/VendorsPage.ts, frontend/e2e/pages/VendorDetailPage.ts</files>
  <action>
Add stable selectors/helpers for include-archived toggles, archived badges, and restore actions.
  </action>
  <verify>Specs use page helpers instead of brittle selector duplication.</verify>
  <done>Vendor E2E coverage remains maintainable.</done>
</task>

</tasks>

<must_haves>
- Vendor inactive semantics treated as archived in tests.
- Vendor SLA archived semantics (`is_archived`) explicitly tested.
- Deterministic fixtures drive all assertions.
</must_haves>

<verification>
- [ ] Vendor deterministic specs added and passing
- [ ] Vendor SLA deterministic specs added and passing
- [ ] POM helpers support archive/restore interactions
</verification>
