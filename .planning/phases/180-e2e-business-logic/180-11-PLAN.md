---
phase: 180-e2e-business-logic
plan: 180-11
type: execute
wave: 1
depends_on: [179-16, 180-10]
files_modified:
  - frontend/e2e/fixtures/e2e-data.ts
  - frontend/e2e/index.ts
autonomous: true
---

<objective>
Create deterministic E2E fixture constants for all seeded entity families, including vendor/SLA and archive-state datasets.
</objective>

<context>
Current E2E tests rely heavily on runtime discovery and `test.skip` due to missing deterministic fixture references. New seed matrix requires shared constants for stable entity targeting.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic fixture constants file</name>
  <files>frontend/e2e/fixtures/e2e-data.ts</files>
  <action>
Define canonical constants for:
- Risks, Controls, KRIs (active + archived variants)
- Vendors (active + inactive)
- Vendor SLAs (active + archived)
- Restore-target entities for unarchive tests
Ensure identifiers map 1:1 to seeded script data.
  </action>
  <verify>Specs can import constants and locate deterministic entities without runtime discovery.</verify>
  <done>Single fixture source exists for all E2E deterministic data.</done>
</task>

<task type="auto">
  <name>Task 2: Export fixtures through E2E barrel</name>
  <files>frontend/e2e/index.ts</files>
  <action>
Expose new constants from fixture exports so specs can use shared data without deep import paths.
  </action>
  <verify>TypeScript imports resolve cleanly in updated spec files.</verify>
  <done>Fixture constants are standardized across suites.</done>
</task>

</tasks>

<must_haves>
- Fixture constants match backend seed identifiers exactly.
- Archive-state constants include expected visibility/restore role use-cases.
- No hard-coded dynamic row assumptions in consuming tests.
</must_haves>

<verification>
- [ ] `e2e-data.ts` includes all entity families and archive variants
- [ ] Fixture barrel exports compile cleanly
- [ ] No identifier drift between seeds and tests
</verification>
