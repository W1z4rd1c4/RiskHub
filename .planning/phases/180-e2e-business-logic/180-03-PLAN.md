---
phase: 180-e2e-business-logic
type: execute
---

<objective>
E2E Tests for Permission Matrix & CRUD Operations

Purpose: Implement E2E tests covering BUSINESS_LOGIC.md §4 (Permission Matrix) and entity CRUD operations.
Output: Test specs verifying read/write/delete permissions for each role and entity type.
</objective>

<context>
@docs/BUSINESS_LOGIC.md (§4 Permission Matrix)
@frontend/e2e/fixtures/auth.fixture.ts
@frontend/e2e/pages/RisksPage.ts
@frontend/e2e/pages/ControlsPage.ts
@frontend/e2e/pages/KRIsPage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test Risk CRUD Permissions</name>
  <files>frontend/e2e/permissions/risks-crud.spec.ts</files>
  <action>
    Create tests for BUSINESS_LOGIC.md §4 - risks permissions:
    
    1. **risks:read** (All roles, scoped by department):
       - Login as each role type, navigate to /risks
       - Verify table loads (GLOBAL sees all, DEPARTMENT sees own)
       - Verify Risk detail page is accessible for in-scope risks
    
    2. **risks:write** (Risk Manager, Compliance, Dept Head):
       - Login as Risk Manager, verify "New Risk" button visible
       - Create a new Risk, verify it appears in the list
       - Login as Employee, verify "New Risk" button NOT visible
    
    3. **risks:delete** (Privileged only via approval):
       - Login as Risk Manager, navigate to Risk detail
       - Verify Delete button triggers immediate archive OR approval request
       - Login as Employee, attempt delete, verify approval request created
  </action>
  <verify>Run `cd frontend && npx playwright test risks-crud.spec.ts --project=chromium`. All tests pass.</verify>
  <done>risks-crud.spec.ts covers read/write/delete permissions for risks.</done>
</task>

<task type="auto">
  <name>Task 2: Test Control CRUD Permissions</name>
  <files>frontend/e2e/permissions/controls-crud.spec.ts</files>
  <action>
    Create tests for BUSINESS_LOGIC.md §4 - controls permissions:
    
    1. **controls:read** (All roles, scoped):
       - Login as each role, verify Controls list loads
       - Verify Control detail is accessible
    
    2. **controls:write** (Risk Manager, Dept Head):
       - Login as Risk Manager, create new Control
       - Login as Dept Head, create new Control in their department
       - Login as Employee, verify Create button NOT visible
    
    3. **controls:delete** (Privileged only via approval):
       - Login as Risk Manager, delete Control, verify immediate archive
       - Login as Dept Head, delete Control linked to high-risk, verify approval request
    
    4. **controls:execute** (Control Owner, Executor):
       - Login as Control Owner, navigate to Control detail
       - Verify "Log Execution" button visible
       - Log an execution, verify it appears in history
       - Login as random user, verify button NOT visible
  </action>
  <verify>Run `cd frontend && npx playwright test controls-crud.spec.ts --project=chromium`. All tests pass.</verify>
  <done>controls-crud.spec.ts covers read/write/delete/execute permissions for controls.</done>
</task>

<task type="auto">
  <name>Task 3: Test KRI CRUD Permissions</name>
  <files>frontend/e2e/permissions/kris-crud.spec.ts</files>
  <action>
    Create tests for BUSINESS_LOGIC.md §4 - KRI permissions:
    
    1. **kri:read** (All roles, scoped):
       - Login as each role, verify KRIs list loads
       - Verify KRI detail is accessible
    
    2. **kri:write** (Risk Manager only):
       - Login as Risk Manager, create new KRI
       - Login as Dept Head, verify Create button NOT visible
    
    3. **kri:submit** (Reporting Owner, Risk Owner):
       - Login as KRI Reporting Owner, navigate to KRI detail
       - Verify "Record Value" button visible
       - Submit a value, verify it appears in history
       - Login as Risk Owner (fallback), verify button also visible
       - Login as random user, verify button NOT visible
    
    4. **KRI Value Correction**:
       - Login as Risk Manager, navigate to KRI history
       - Verify "Correct" button visible on past values
       - Correct a value, verify correction appears with audit trail
  </action>
  <verify>Run `cd frontend && npx playwright test kris-crud.spec.ts --project=chromium`. All tests pass.</verify>
  <done>kris-crud.spec.ts covers read/write/submit/correct permissions for KRIs.</done>
</task>

<task type="auto">
  <name>Task 4: Test Approvals Permission</name>
  <files>frontend/e2e/permissions/approvals-access.spec.ts</files>
  <action>
    Create tests for BUSINESS_LOGIC.md §4 - approvals permissions:
    
    1. **approvals:read** (All roles):
       - Login as any role, navigate to /approvals or approval queue in nav
       - Verify pending approvals list loads (may be empty)
    
    2. **approvals:write** (Privileged users only):
       - Create a pending approval request (delete as Employee)
       - Login as Risk Manager, navigate to Approvals
       - Verify Approve/Reject buttons are visible and functional
       - Login as Employee, navigate to Approvals
       - Verify Approve/Reject buttons are NOT visible (only "Cancel" for own requests)
    
    3. **Self-Approval Prevention**:
       - Login as Risk Manager, create an edit on a Risk they own
       - If approval is required, verify they CANNOT approve their own request
  </action>
  <verify>Run `cd frontend && npx playwright test approvals-access.spec.ts --project=chromium`. All tests pass.</verify>
  <done>approvals-access.spec.ts covers read/write permissions and self-approval prevention.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run lint` passes
- [ ] `cd frontend && npx playwright test permissions/ --project=chromium` passes
- [ ] Each entity type (Risk, Control, KRI) has read/write/delete tests
- [ ] Approvals permission is tested for privileged vs non-privileged
</verification>

<success_criteria>
- Permission matrix from §4 fully covered
- CRUD operations tested for each entity type
- controls:execute and kri:submit special permissions tested
</success_criteria>

<output>
After completion, create `.planning/phases/180-e2e-business-logic/180-03-SUMMARY.md`
</output>
