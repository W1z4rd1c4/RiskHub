---
phase: 180-e2e-business-logic
type: execute
---

<objective>
E2E Test Suite Integration & Full Regression

Purpose: Consolidate all E2E tests into a runnable suite, configure CI-ready execution, and perform full regression verification.
Output: Complete E2E test suite with CI configuration, test reports, and documented run instructions.
</objective>

<context>
@frontend/playwright.config.ts
@frontend/e2e/*.spec.ts (all specs from 180-01 to 180-07)
@docs/BUSINESS_LOGIC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate Test Structure</name>
  <files>frontend/e2e/index.ts, frontend/package.json</files>
  <action>
    Organize and consolidate the E2E test structure:
    
    1. Create barrel exports for shared code:
       - `frontend/e2e/index.ts` - Export all fixtures, helpers, POMs
    
    2. Add npm scripts to package.json:
       ```json
       "e2e": "playwright test",
       "e2e:ui": "playwright test --ui",
       "e2e:headed": "playwright test --headed",
       "e2e:report": "playwright show-report",
       "e2e:business-logic": "playwright test e2e/roles-access.spec.ts e2e/access-scope.spec.ts e2e/entity-ownership e2e/department-access.spec.ts e2e/permissions e2e/approval-workflows e2e/sensitive-fields e2e/cross-department e2e/activity-logging"
       ```
    
    3. Verify all spec files are discoverable by Playwright.
  </action>
  <verify>Run `npm run e2e -- --list` and verify all spec files are listed.</verify>
  <done>Test structure consolidated, npm scripts added, all specs discoverable.</done>
</task>

<task type="auto">
  <name>Task 2: Update Playwright Config for CI</name>
  <files>frontend/playwright.config.ts</files>
  <action>
    Enhance Playwright config for CI/CD:
    
    1. Add project for headless CI:
       ```typescript
       projects: [
         {
           name: 'chromium',
           use: { ...devices['Desktop Chrome'] },
         },
         {
           name: 'ci',
           use: { ...devices['Desktop Chrome'], headless: true },
         },
       ],
       ```
    
    2. Configure retries for CI (2 retries).
    
    3. Configure artifact collection:
       - Screenshots on failure
       - Video on first retry
       - Trace on first retry
    
    4. Set timeout to 60s for complex workflows.
    
    5. Configure reporters:
       - HTML for local
       - JUnit XML for CI (junit.xml output)
  </action>
  <verify>Run `npx playwright test --project=ci --list` - config loads without errors.</verify>
  <done>Playwright config enhanced with CI-ready settings.</done>
</task>

<task type="auto">
  <name>Task 3: Create Test Data Setup Script</name>
  <files>frontend/e2e/setup/global-setup.ts, frontend/e2e/setup/test-data.ts</files>
  <action>
    Create test data setup for consistent E2E runs:
    
    1. `global-setup.ts` - Playwright globalSetup hook:
       - Verify backend is running (health check)
       - Verify frontend is running
       - Optionally seed test data via API
    
    2. `test-data.ts` - Test data helpers:
       - `createTestRisk(data)` - API call to create a risk for testing
       - `createTestControl(data)` - API call to create a control
       - `createTestKRI(data)` - API call to create a KRI
       - `cleanupTestData(ids)` - Clean up created test data
    
    3. Update playwright.config.ts to use globalSetup.
  </action>
  <verify>Run a test that uses test data helpers - data is created and cleaned up.</verify>
  <done>Global setup and test data helpers created and functional.</done>
</task>

<task type="auto">
  <name>Task 4: Full Regression Run & Documentation</name>
  <files>docs/E2E_TESTING.md</files>
  <action>
    Run full regression and document:
    
    1. Run complete E2E suite:
       `cd frontend && npx playwright test --project=chromium`
    
    2. Capture results:
       - Total tests
       - Pass/Fail count
       - Flaky tests
       - Duration
    
    3. Create `docs/E2E_TESTING.md` documenting:
       - How to run E2E tests locally
       - CI configuration
       - Test coverage mapping to BUSINESS_LOGIC.md
       - Troubleshooting common failures
       - Adding new tests guide
    
    4. Update E2E test summary table:
       | Section | Tests | Coverage |
       |---------|-------|----------|
       | §1 Roles & Scopes | X | 100% |
       | §2 Entity Ownership | X | 100% |
       | ...
  </action>
  <verify>Run full suite, generate HTML report, verify >90% pass rate.</verify>
  <done>Full regression passes, docs/E2E_TESTING.md created with coverage mapping.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete E2E test suite covering all BUSINESS_LOGIC.md sections</what-built>
  <how-to-verify>
    1. Run: `cd frontend && npm run e2e:business-logic -- --project=chromium`
    2. Wait for tests to complete (may take 5-10 minutes)
    3. Review HTML report: `npm run e2e:report`
    4. Verify coverage:
       - §1 Roles & Access: roles-access.spec.ts, access-scope.spec.ts
       - §2 Entity Ownership: entity-ownership/*.spec.ts
       - §3 Department Relationships: department-access.spec.ts
       - §4 Permission Matrix: permissions/*.spec.ts
       - §5 Approval Workflows: approval-workflows/*.spec.ts
       - §6 Sensitive Fields: sensitive-fields/*.spec.ts
       - §7 Cross-Department: cross-department/*.spec.ts
       - §9 Activity Logging: activity-logging/*.spec.ts
    5. Confirm all sections have at least 3 passing tests
  </how-to-verify>
  <resume-signal>Type "approved" if coverage is complete, or describe gaps to address</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run e2e -- --project=chromium` passes with >90% success rate
- [ ] HTML report generated and viewable
- [ ] `docs/E2E_TESTING.md` created
- [ ] All BUSINESS_LOGIC.md sections have E2E coverage
</verification>

<success_criteria>
- Complete E2E test suite runnable with single command
- CI configuration ready for GitHub Actions/GitLab CI
- All BUSINESS_LOGIC.md sections covered with E2E tests
- Documentation complete for maintaining and extending tests
</success_criteria>

<output>
After completion, create `.planning/phases/180-e2e-business-logic/180-08-SUMMARY.md`
</output>
