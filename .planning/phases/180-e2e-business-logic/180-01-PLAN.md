---
phase: 180-e2e-business-logic
type: execute
---

<objective>
E2E Test Infrastructure & Role-Based Access Coverage

Purpose: Establish shared E2E test infrastructure (helpers, fixtures, page objects) and implement comprehensive tests for BUSINESS_LOGIC.md §1 (Roles & Access Scopes).
Output: Test fixtures, helper functions, and role-access test specs covering all 12 roles and 3 access scopes.
</objective>

<context>
@docs/BUSINESS_LOGIC.md (§1 Roles & Access Scopes)
@frontend/e2e/auth.spec.ts (existing baseline)
@frontend/playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E Test Infrastructure</name>
  <files>frontend/e2e/fixtures/auth.fixture.ts, frontend/e2e/helpers/login.ts, frontend/e2e/helpers/wait.ts</files>
  <action>
    Create shared test infrastructure:
    1. `auth.fixture.ts` - Playwright test fixtures that extend base test with authenticated contexts for each role type (CRO, Risk Manager, Compliance, Legal, Internal Audit, Actuarial, Department Head, Employee, Admin, Viewer).
    2. `login.ts` - Reusable `loginAsDemoUser(page, accountName, options?)` helper with retry logic and wait-for-nav.
    3. `wait.ts` - Shared waits: `waitForDataLoad()`, `waitForTableRows()`, `waitForToast()`.
    
    Use Playwright's official fixture pattern. Each fixture should pre-authenticate and cache the storageState.
  </action>
  <verify>Import fixtures in a test file without TypeScript errors. `npm run lint` passes in frontend.</verify>
  <done>Three helper files created, fixtures export properly typed. No TS errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create Page Object Models</name>
  <files>frontend/e2e/pages/LoginPage.ts, frontend/e2e/pages/DashboardPage.ts, frontend/e2e/pages/RisksPage.ts, frontend/e2e/pages/ControlsPage.ts, frontend/e2e/pages/KRIsPage.ts</files>
  <action>
    Create Page Object Models for core pages:
    1. `LoginPage` - Demo picker selectors, login methods.
    2. `DashboardPage` - Sidebar nav, metric cards, navigation helpers.
    3. `RisksPage` - Table, search, filter, row click, create button selectors.
    4. `ControlsPage` - Similar structure to RisksPage.
    5. `KRIsPage` - Similar structure to RisksPage.
    
    Each POM should have: locators as getters, action methods (navigate, click, fill), assertion helpers.
  </action>
  <verify>Import all POMs in a test file without TS errors. Each POM has at least 3 action methods.</verify>
  <done>5 POM files created with typed selectors and methods.</done>
</task>

<task type="auto">
  <name>Task 3: Test Privileged vs Non-Privileged Role Visibility</name>
  <files>frontend/e2e/roles-access.spec.ts</files>
  <action>
    Create comprehensive role-access tests covering BUSINESS_LOGIC.md §1.2 and §1.3:
    
    1. **Privileged Users (GLOBAL scope)** - Test each: CEO, CFO, CRO, Risk Manager, Compliance, Legal, Internal Audit, Actuarial
       - Can see all departments in dropdown
       - Can access any department's risks/controls/KRIs
       - See "Approvals" tab in navigation
    
    2. **Non-Privileged Users (DEPARTMENT scope)** - Test: Department Head, Employee
       - Can only see own department's data
       - Cannot access other department's detail pages (403 or empty)
       - Do NOT see Approvals approval capability
    
    3. **Admin (Special)** - Test: System Admin
       - See Admin Console link
       - Do NOT see business data tabs (Risks, Controls, KRIs)
       - Can access /admin routes
    
    4. **CRO Exclusive** - Test: Only CRO sees Risk Hub configuration tab
    
    Use describe.each() for role iteration. Each test should login, navigate, and assert visibility/access.
  </action>
  <verify>Run `cd frontend && npx playwright test roles-access.spec.ts --project=chromium`. All tests pass.</verify>
  <done>roles-access.spec.ts has 20+ test cases covering all 12 roles with PASS status.</done>
</task>

<task type="auto">
  <name>Task 4: Test Access Scope Visibility Boundaries</name>
  <files>frontend/e2e/access-scope.spec.ts</files>
  <action>
    Create tests for BUSINESS_LOGIC.md §1.2 Access Scopes:
    
    1. **GLOBAL Scope Users** (privileged):
       - Navigate to department list, verify all departments visible
       - Access Risk from any department, verify 200 response
       - Verify cross-department navigation works
    
    2. **DEPARTMENT Scope Users** (dept head/employee):
       - Navigate to /risks, verify only own department's risks appear
       - Navigate to /controls, verify only own department's controls appear
       - Attempt to access other department's risk detail via URL, verify 403/redirect
    
    3. **Unassigned Items** (department_id = NULL):
       - Login as GLOBAL user, verify unassigned items visible
       - Login as DEPARTMENT user, verify unassigned items NOT visible
    
    Test should capture network responses to verify API-level access control.
  </action>
  <verify>Run `cd frontend && npx playwright test access-scope.spec.ts --project=chromium`. All tests pass.</verify>
  <done>access-scope.spec.ts has tests for GLOBAL, DEPARTMENT, and unassigned item visibility.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run lint` passes
- [ ] `cd frontend && npx playwright test roles-access.spec.ts access-scope.spec.ts --project=chromium` passes
- [ ] Test report shows 20+ test cases covering role visibility
- [ ] Page Object Models are imported without errors
</verification>

<success_criteria>
- All tasks completed
- E2E infrastructure (fixtures, helpers, POMs) is reusable
- Every role from BUSINESS_LOGIC.md §1 has at least one E2E test
- Access scope boundaries (GLOBAL/DEPARTMENT) are tested
</success_criteria>

<output>
After completion, create `.planning/phases/180-e2e-business-logic/180-01-SUMMARY.md`
</output>
