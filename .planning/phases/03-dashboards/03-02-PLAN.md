---
phase: 03-dashboards
type: execute
---

<objective>
Build executive and department-level dashboard UI with real data and interactive visualizations.

Purpose: Replace hardcoded mock dashboard with live data from backend aggregation endpoints.
Output: Production-ready dashboard with charts, risk matrix, and department metrics.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-plan.md
@.agent/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02.1-risk-register/02.1-01-SUMMARY.md (prerequisite: risk types, API, RiskScoreMatrix)
@frontend/src/pages/DashboardPage.tsx
@frontend/src/services/dashboardApi.ts (created in this phase)
@frontend/src/components/RiskScoreMatrix.tsx (reuse from Phase 2.1)
@backend/app/schemas/dashboard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Recharts and Create Dashboard Service</name>
  <files>frontend/package.json, frontend/src/services/dashboardApi.ts, frontend/src/types/dashboard.ts</files>
  <action>
1. Install Recharts (lightweight, React-native charting):
   `cd frontend && npm install recharts`

2. Create TypeScript types in `dashboard.ts` matching backend schemas:
   - `DashboardSummary` interface
   - `DepartmentMetrics` interface
   - `RiskDistribution` interface
   - `ControlTrend` interface

3. Create `dashboardApi.ts` with API functions:
   - `fetchDashboardSummary(): Promise<DashboardSummary>`
   - `fetchDepartmentMetrics(): Promise<DepartmentMetrics[]>`
   - `fetchRiskDistribution(): Promise<RiskDistribution>`
   - `fetchControlTrends(): Promise<ControlTrend[]>`

Use fetch with base URL from environment (VITE_API_URL), handle errors gracefully.
  </action>
  <verify>npm run build succeeds without type errors</verify>
  <done>Recharts installed, types defined, API service created, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Create Dashboard Chart Components</name>
  <files>frontend/src/components/dashboard/RiskDistributionMatrix.tsx, frontend/src/components/dashboard/ControlTrendChart.tsx, frontend/src/components/dashboard/DepartmentTable.tsx</files>
  <action>
Create dashboard-specific chart components (reuse existing RiskScoreMatrix from Phase 2.1):

1. **RiskDistributionMatrix.tsx** - Aggregated 5x5 risk heatmap for dashboard:
   - REUSE the `RiskScoreMatrix` component from Phase 2.1 as a base pattern
   - Extend for aggregated view: show COUNT of risks in each cell (not a single risk position)
   - Color-coded cells (green → yellow → red based on score)
   - Tooltip showing count in each cell
   - Premium glassmorphism styling consistent with existing UI
   - Props: `distribution: {probability: number, impact: number, count: number}[]`

2. **ControlTrendChart.tsx** - Bar/Line chart for execution trends:
   - Uses Recharts BarChart or AreaChart
   - X-axis: weeks, Y-axis: execution count
   - Accent color gradient matching design system
   - Responsive width

3. **DepartmentTable.tsx** - Department metrics table:
   - Sortable columns (name, controls, risks, compliance)
   - Progress bar for compliance rate
   - Clickable rows navigating to /risks?department={id} or /controls?department={id}
   - Glass card styling

All components accept data via props, handle loading/empty states.
  </action>
  <verify>npm run build succeeds, components export correctly</verify>
  <done>3 chart components created with proper TypeScript types, RiskScoreMatrix pattern reused, build passes</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Dashboard with Live Data</name>
  <files>frontend/src/pages/DashboardPage.tsx</files>
  <action>
Refactor DashboardPage.tsx to use real data:

1. Replace hardcoded `stats` array with fetched DashboardSummary
2. Use React Query or useEffect + useState for data fetching
3. Replace mock bar chart with ControlTrendChart component
4. Replace "Recent Activity" section with RiskMatrix component
5. Add DepartmentTable below main charts
6. Implement loading skeletons during fetch
7. Handle error states gracefully (show message, retry button)

Preserve existing premium styling:
- Glassmorphism cards
- Framer Motion animations
- Accent colors and gradients
- Responsive grid layout

Add auto-refresh every 60 seconds for live dashboard feel.
  </action>
  <verify>npm run dev, visit http://localhost:5173/dashboard, verify data loads from API</verify>
  <done>Dashboard displays real data from API, charts render correctly, loading states work</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete dashboard with live data, risk matrix, control trends, and department metrics</what-built>
  <how-to-verify>
    1. Start backend: `cd backend && source venv/bin/activate && uvicorn app.main:app --reload`
    2. Start frontend: `cd frontend && npm run dev`
    3. Visit: http://localhost:5173 → Login → Dashboard
    4. Verify: Summary stats cards show real counts (may be 0 if empty DB)
    5. Verify: Risk Matrix displays as 5x5 grid with color coding
    6. Verify: Control Trend chart shows bar/area visualization
    7. Verify: Department table lists departments with metrics
    8. Test: Resize browser → verify responsive layout
    9. Test: Network tab → verify API calls to /dashboard/*
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] All dashboard components render without console errors
- [ ] API integration works (data fetched from backend)
- [ ] Charts display correctly with real or mock data
- [ ] Responsive layout on mobile/tablet/desktop
</verification>

<success_criteria>

- Recharts installed and configured
- 3 reusable chart components created
- DashboardPage fetches real data from API
- Premium glassmorphism styling maintained
- Loading and error states implemented
- Human verification passed
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboards/03-02-SUMMARY.md`
</output>
