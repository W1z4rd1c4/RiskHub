---
phase: 02.1-risk-register
type: execute
---

<objective>
Build Control-Risk linking UI for bidirectional navigation between controls and risks.

Purpose: Allow users to manage which controls mitigate which risks, with effectiveness ratings.
Output: Linking modals/dialogs on both ControlDetailPage and RiskDetailPage, plus Risk forms.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-plan.md
@.agent/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@frontend/src/pages/ControlDetailPage.tsx
@frontend/src/pages/RiskDetailPage.tsx
@frontend/src/services/controlApi.ts
@frontend/src/services/riskApi.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Link Management Dialog Component</name>
  <files>frontend/src/components/LinkManagementDialog.tsx</files>
  <action>
Create a reusable modal dialog for managing control-risk links:

**LinkManagementDialog.tsx**:
- Props:
  - `mode: 'control-to-risk' | 'risk-to-control'`
  - `sourceId: number` (control ID or risk ID)
  - `existingLinks: ControlRiskLink[]`
  - `onLink: (targetId: number, effectiveness: string, notes?: string) => Promise<void>`
  - `onUnlink: (targetId: number) => Promise<void>`
  - `isOpen: boolean`
  - `onClose: () => void`

Features:
1. **Search/Select Section**:
   - Search input to find available risks/controls
   - List of available items to link (not already linked)
   - Select effectiveness: High / Medium / Low (dropdown)
   - Optional notes textarea
   - "Link" button to create new link

2. **Existing Links Section**:
   - Table of currently linked items
   - Show effectiveness badge (color-coded)
   - "Unlink" button per row (with confirmation)
   - Show notes if present

3. **Premium Styling**:
   - Glassmorphism modal overlay
   - Smooth open/close animation
   - Responsive layout (mobile-friendly)

Use existing API patterns for fetching available items.
  </action>
  <verify>npm run build succeeds, component exports correctly</verify>
  <done>LinkManagementDialog created with search, link, unlink functionality, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Dialog into Control and Risk Detail Pages</name>
  <files>frontend/src/pages/ControlDetailPage.tsx, frontend/src/pages/RiskDetailPage.tsx</files>
  <action>
Wire up the LinkManagementDialog:

1. **ControlDetailPage.tsx**:
   - Add state: `isLinkDialogOpen`
   - Wire "Manage Risk Linkage" button (line ~286) to open dialog
   - Pass mode='control-to-risk', sourceId=control.id
   - Implement onLink: call controlApi.linkRisk(controlId, riskId, effectiveness, notes)
   - Implement onUnlink: call controlApi.unlinkRisk(controlId, riskId)
   - Refresh linkedRisks after link/unlink operations

2. **RiskDetailPage.tsx**:
   - Add "Manage Control Linkage" button in Linked Controls section
   - Add state: `isLinkDialogOpen`
   - Pass mode='risk-to-control', sourceId=risk.id
   - Implement onLink: call riskApi.linkControl(riskId, controlId, effectiveness, notes)
   - Implement onUnlink: call riskApi.unlinkControl(riskId, controlId)
   - Refresh linkedControls after operations

Both integrations should be permission-gated (require write permission).
  </action>
  <verify>npm run dev, open control detail, click "Manage Risk Linkage", verify dialog opens and link/unlink works</verify>
  <done>Dialog integrated into both pages, linking and unlinking functional, data refreshes</done>
</task>

<task type="auto">
  <name>Task 3: Create Risk Create/Edit Form</name>
  <files>frontend/src/pages/RiskForms.tsx, frontend/src/components/RiskForm.tsx</files>
  <action>
Create Risk CRUD forms matching Control form patterns:

1. **RiskForm.tsx** - Reusable form component:
   - Fields matching RiskCreate/RiskUpdate schemas:
     - risk_id_code (text, required, unique)
     - process (text, required)
     - subprocess (text, optional)
     - risk_type (select: Strategic/Operational)
     - category (text, optional)
     - description (textarea, required)
     - department_id (select from departments)
     - owner_id (select from users)
     - gross_probability (slider/select 1-5)
     - gross_impact (slider/select 1-5)
     - net_probability (slider/select 1-5)
     - net_impact (slider/select 1-5)
     - status (select: Active/Monitoring/Closed/Archived)
     - is_priority (checkbox)
     - KRI fields (collapsible section): indicator, thresholds
   - Live preview of Gross/Net scores as user adjusts sliders
   - Validation with error messages
   - Submit button

2. **RiskForms.tsx** - Page wrappers:
   - RiskCreatePage: uses RiskForm with onSubmit → riskApi.createRisk, redirect to /risks/{id}
   - RiskEditPage: fetches existing risk, uses RiskForm prefilled, onSubmit → riskApi.updateRisk

3. **Update App.tsx**:
   - Wire /risks/new → RiskCreatePage
   - Wire /risks/:id/edit → RiskEditPage
  </action>
  <verify>npm run dev, navigate to /risks/new, fill form, submit, verify risk created and redirected</verify>
  <done>Risk create/edit forms work, validation displays, API integration complete</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Risk Register UI with forms and Control-Risk linking dialogs</what-built>
  <how-to-verify>
    1. Start backend: `cd backend && source venv/bin/activate && uvicorn app.main:app --reload`
    2. Start frontend: `cd frontend && npm run dev`
    3. Visit: http://localhost:5173/risks
    4. Test: Click "Add Risk" → fill form → submit → verify risk appears in list
    5. Test: Click a risk → verify detail page with score matrices
    6. Test: On risk detail, click "Manage Control Linkage" → search controls → link one → verify it appears
    7. Test: Navigate to Controls → open a control → click "Manage Risk Linkage" → link a risk → verify bidirectional
    8. Test: Unlink from either side → verify removed from both views
    9. Verify: Premium styling consistent across all new components
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Risk Create/Edit forms validate and submit correctly
- [ ] LinkManagementDialog opens and closes properly
- [ ] Linking and unlinking works from both directions
- [ ] Data refreshes after link operations
- [ ] Human verification passed
</verification>

<success_criteria>

- LinkManagementDialog reusable component created
- Control-Risk linking works bidirectionally
- Risk Create/Edit forms functional
- All operations permission-gated appropriately
- Premium styling maintained throughout
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-risk-register/02.1-02-SUMMARY.md`
</output>
