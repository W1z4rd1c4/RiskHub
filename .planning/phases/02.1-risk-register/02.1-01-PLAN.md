---
phase: 02.1-risk-register
type: execute
---

<objective>
Build Risk Register UI with list/detail views and gross/net risk matrix visualization.

Purpose: Provide a user interface for managing risks from OS 18 Řízení rizik - complementing the Control Catalog.
Output: RisksPage (list) and RiskDetailPage (detail) with forms, matching Control Catalog UI patterns.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-plan.md
@.agent/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/app/schemas/risk.py
@backend/app/api/v1/endpoints/risks.py
@frontend/src/pages/ControlsPage.tsx
@frontend/src/pages/ControlDetailPage.tsx
@frontend/src/types/control.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Risk Types and API Service</name>
  <files>frontend/src/types/risk.ts, frontend/src/services/riskApi.ts</files>
  <action>
Create TypeScript types matching backend schemas:

1. **risk.ts**:
   - `RiskType` enum (strategic, operational)
   - `RiskStatus` enum (active, monitoring, closed, archived)
   - `ControlEffectiveness` enum (high, medium, low)
   - `Risk` interface matching RiskRead schema (id, risk_id_code, process, subprocess, risk_type, category, description, department, owner, gross_probability/impact/score, net_probability/impact/score, status, is_priority, kri_* fields, timestamps)
   - `RiskSummary` interface for list views
   - `RiskCreate`, `RiskUpdate` interfaces
   - `ControlRiskLink` interface (matching ControlRiskLinkRead)

2. **riskApi.ts**:
   - `listRisks(filters?)` - GET /api/v1/risks with pagination, department_id, status, risk_type, is_priority, search
   - `getRisk(id)` - GET /api/v1/risks/{id}
   - `createRisk(data)` - POST /api/v1/risks
   - `updateRisk(id, data)` - PATCH /api/v1/risks/{id}
   - `deleteRisk(id)` - DELETE /api/v1/risks/{id}
   - `getLinkedControls(riskId)` - GET /api/v1/risks/{id}/controls
   - `linkControl(riskId, data)` - POST /api/v1/risks/{id}/controls
   - `unlinkControl(riskId, controlId)` - DELETE /api/v1/risks/{id}/controls/{controlId}

Use same patterns as controlApi.ts (fetch, base URL, mock user header).
  </action>
  <verify>npm run build succeeds without type errors</verify>
  <done>Types and API service created, build passes, no TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: Create Risk List Page</name>
  <files>frontend/src/pages/RisksPage.tsx, frontend/src/pages/index.ts, frontend/src/App.tsx</files>
  <action>
Create RisksPage following ControlsPage patterns:

1. **RisksPage.tsx** - Risk register list view:
   - Page header: "Risk Register" with "Add Risk" button (permission-gated)
   - Filter bar: Status dropdown, Type dropdown (Strategic/Operational), Priority toggle, Search input
   - Risk table/grid with columns: Risk ID, Process, Type, Category, Gross Score (5×5 matrix badge), Net Score, Status, Priority indicator
   - Pagination controls
   - Click row → navigate to /risks/{id}
   - Score badges color-coded: 1-4 green, 5-9 yellow, 10-15 amber, 16-25 red
   - Premium glassmorphism styling matching Controls page

2. **Update App.tsx routing**:
   - Add `/risks` route → RisksPage
   - Add `/risks/new` route → RiskFormPage (placeholder)
   - Add `/risks/:id` route → RiskDetailPage (placeholder)
   - Add `/risks/:id/edit` route → RiskFormPage (placeholder)

3. **Update index.ts**:
   - Export RisksPage

4. **Update sidebar navigation** in layout to include "Risk Register" link
  </action>
  <verify>npm run dev, visit http://localhost:5173/risks - page loads without errors, shows list (may be empty)</verify>
  <done>RisksPage renders with filters, routing works, sidebar has Risk Register link</done>
</task>

<task type="auto">
  <name>Task 3: Create Risk Detail Page with Risk Matrix</name>
  <files>frontend/src/pages/RiskDetailPage.tsx, frontend/src/components/RiskScoreMatrix.tsx</files>
  <action>
Create RiskDetailPage following ControlDetailPage patterns:

1. **RiskScoreMatrix.tsx** - Visual 5×5 risk matrix component:
   - Props: probability (1-5), impact (1-5), type ('gross' | 'net')
   - 5×5 grid with color gradient (green → yellow → amber → red)
   - Highlight the cell corresponding to probability × impact
   - Show score value prominently
   - Labels: X-axis "Impact", Y-axis "Probability"

2. **RiskDetailPage.tsx**:
   - Back button → /risks
   - Header: Risk ID code, status badge, Edit/Delete buttons (permission-gated)
   - Description section
   - Three-column card layout:
     - **Card 1: Risk Scores** - Side-by-side Gross vs Net RiskScoreMatrix components
     - **Card 2: Classification** - Type, Category, Process, Subprocess, Priority status
     - **Card 3: Ownership** - Department, Risk Owner
   - **KRI Section** (if populated): Indicator, thresholds (green/yellow/red)
   - **Linked Controls** section (similar to ControlDetailPage's Linked Risks):
     - List controls mitigating this risk
     - Show effectiveness badge (high/medium/low)
     - "Manage Control Linkage" button for future enhancement
   - Timestamps: Created, Updated

Fetch data via riskApi.getRisk(id) and riskApi.getLinkedControls(id).
  </action>
  <verify>npm run dev, navigate to a risk detail page (create test risk first), verify matrix renders correctly</verify>
  <done>RiskDetailPage renders with risk matrices, linked controls section, all data displays correctly</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] /risks page loads and shows risk list
- [ ] /risks/:id page shows risk details with score matrices
- [ ] Risk Score Matrix correctly colors cells and highlights current position
- [ ] Linked Controls section shows controls mitigating the risk
- [ ] Navigation between pages works
</verification>

<success_criteria>

- Risk types and API service created
- RisksPage with filtering, pagination, premium styling
- RiskDetailPage with visual 5×5 risk matrix (gross vs net)
- Linked Controls section functional
- Consistent styling with Control Catalog
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-risk-register/02.1-01-SUMMARY.md`
</output>
