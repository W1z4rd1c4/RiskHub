---
phase: 179-e2e-test-data
plan: 179-15
type: execute
wave: 2
depends_on: [179-13, 179-14]
files_modified:
  - backend/scripts/seed_e2e_archives.py
  - backend/scripts/seed_e2e_all.py
  - backend/scripts/seed_e2e_risks.py
  - backend/scripts/seed_e2e_controls.py
  - backend/scripts/seed_e2e_kris.py
autonomous: true
---

<objective>
Seed a deterministic archive matrix across all archive-capable entity families (risk/control/kri/vendor/vendor-sla) for include_archived and unarchive E2E coverage.
</objective>

<context>
Phase 201 introduced archive visibility toggles and restore endpoints. Seed data must include deterministic archived and active entities to avoid runtime `test.skip()` due to missing archive states.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add archive matrix seeding script</name>
  <files>backend/scripts/seed_e2e_archives.py</files>
  <action>
Create script that ensures deterministic archived-state entities exist for each family:
- Risks/Controls: `status='archived'`
- KRIs/Vendor SLAs: `is_archived=true` (+ archive metadata)
- Vendors: `status='inactive'`
Ensure each archived entity has a paired active counterpart for restore assertions.
  </action>
  <verify>Archive matrix script outputs per-entity active/archived counts and is idempotent.</verify>
  <done>Deterministic archive-state matrix is available for E2E tests.</done>
</task>

<task type="auto">
  <name>Task 2: Align existing seed scripts with archive matrix dependencies</name>
  <files>backend/scripts/seed_e2e_risks.py, backend/scripts/seed_e2e_controls.py, backend/scripts/seed_e2e_kris.py</files>
  <action>
- Ensure deterministic entity keys used by archive matrix are stable.
- Avoid random selection from `limit()` queries for archive candidates.
  </action>
  <verify>Archive script can always target deterministic records by known identifiers.</verify>
  <done>Archive matrix is stable across fresh DB reruns.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate archive seeding into orchestrator</name>
  <files>backend/scripts/seed_e2e_all.py</files>
  <action>
- Add `seed_e2e_archives` as an explicit step after base entities and vendor/SLA seeding.
- Include matrix totals in final summary.
  </action>
  <verify>Master seed output includes archive matrix step with deterministic totals.</verify>
  <done>Archive dataset ships with every E2E seed run.</done>
</task>

</tasks>

<must_haves>
- Deterministic active + archived pairs for all archive-capable entities.
- Archive matrix compatible with restore endpoint scenarios.
- No user/department creation.
</must_haves>

<verification>
- [ ] Archive matrix script covers 5 entity families
- [ ] Script is deterministic and idempotent
- [ ] `seed_e2e_all.py` includes archive matrix step
</verification>
