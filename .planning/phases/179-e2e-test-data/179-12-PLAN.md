---
phase: 179-e2e-test-data
plan: 179-12
type: execute
wave: 1
depends_on: [179-11]
files_modified:
  - /Users/stefanlesnak/Antigravity/Risk App 2/backend/scripts/seed_e2e_foundation.py
  - /Users/stefanlesnak/Antigravity/Risk App 2/backend/scripts/e2e_mappings.py
  - /Users/stefanlesnak/Antigravity/Risk App 2/backend/scripts/seed_e2e_all.py
autonomous: true
---

<objective>
Harden E2E seed foundation for fresh database runs while enforcing the constraint that E2E seeding never creates users or departments.
</objective>

<context>
Current foundation scripts verify expected demo users/departments and seed a version marker. Fresh DB workflows need deterministic fail-fast behavior, explicit mapping validation, and compatibility with the expanded vendor/archive seed matrix.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce strict prerequisite contract</name>
  <files>backend/scripts/seed_e2e_foundation.py, backend/scripts/e2e_mappings.py</files>
  <action>
- Validate all required demo users and departments exist.
- Fail with actionable error output when prerequisites are missing.
- Explicitly document and enforce: no user/department creation inside E2E seed scripts.
  </action>
  <verify>Foundation script exits non-zero when any required user/department is absent; exits zero with complete mapping when present.</verify>
  <done>Fresh DB prerequisite checks are deterministic and safe.</done>
</task>

<task type="auto">
  <name>Task 2: Standardize mapping access helpers for downstream scripts</name>
  <files>backend/scripts/e2e_mappings.py</files>
  <action>
- Add helper accessors that resolve required user/department IDs with explicit errors.
- Keep existing mapping keys stable to avoid breaking current 179 scripts.
  </action>
  <verify>Downstream scripts can resolve IDs without ad-hoc mapping logic.</verify>
  <done>Single source of truth for user/department mapping in all E2E seed scripts.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate foundation guardrails into orchestration</name>
  <files>backend/scripts/seed_e2e_all.py</files>
  <action>
- Ensure foundation verification runs first and blocks subsequent seeding on failure.
- Print constraint banner indicating no user/department creation is permitted.
  </action>
  <verify>Master script stops before seeding entities when foundation fails.</verify>
  <done>Orchestration is safe for fresh-DB reruns.</done>
</task>

</tasks>

<must_haves>
- E2E seeding must not create users or departments.
- Foundation validation output must be explicit and actionable.
- Existing 179 scripts remain backward compatible.
</must_haves>

<verification>
- [ ] Foundation verification is strict and deterministic
- [ ] Mapping helpers are reusable across all E2E seed scripts
- [ ] Orchestrator blocks execution on missing prerequisites
</verification>
