---
phase: 179-e2e-test-data
plan: 179-13
type: execute
wave: 1
depends_on: [179-12]
files_modified:
  - /Users/stefanlesnak/Antigravity/Risk App 2/backend/scripts/seed_e2e_vendors.py
  - /Users/stefanlesnak/Antigravity/Risk App 2/backend/scripts/seed_e2e_all.py
autonomous: true
---

<objective>
Add deterministic vendor seed data (active + archived semantics via inactive status) with cross-department ownership and stable naming for E2E test selection.
</objective>

<context>
Phase 179 currently seeds risks/controls/kris/approvals but does not seed vendors. Phase 201 archive behavior introduces vendor list toggles and restore flows that require deterministic vendor datasets.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deterministic vendor seed script</name>
  <files>backend/scripts/seed_e2e_vendors.py</files>
  <action>
Create a new vendor seed script that:
- Seeds deterministic vendor records prefixed with `E2E-`.
- Includes active and inactive vendors (inactive used as archived semantics).
- Covers ownership and department scope scenarios using existing demo users/departments.
- Is idempotent via unique deterministic keys (name/registration_id patterns).
  </action>
  <verify>Second run creates 0 duplicate vendors and returns deterministic counts.</verify>
  <done>Vendor seed dataset supports E2E list/search/archive-visibility scenarios.</done>
</task>

<task type="auto">
  <name>Task 2: Include vendor seeding in master orchestrator</name>
  <files>backend/scripts/seed_e2e_all.py</files>
  <action>
- Import and execute `seed_e2e_vendors` in correct order after foundation.
- Include summary counts for active/inactive vendors.
  </action>
  <verify>Master seed run outputs vendor-seed step and final count summary.</verify>
  <done>Vendor dataset is part of standard E2E seed pipeline.</done>
</task>

</tasks>

<must_haves>
- No user/department creation.
- Deterministic naming suitable for fixture constants.
- Vendor inactive status used to represent archived vendors.
</must_haves>

<verification>
- [ ] `seed_e2e_vendors.py` creates deterministic active/inactive vendors
- [ ] Script is idempotent on rerun
- [ ] `seed_e2e_all.py` integrates vendor step and summary
</verification>
