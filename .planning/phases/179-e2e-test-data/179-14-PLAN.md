---
phase: 179-e2e-test-data
plan: 179-14
type: execute
wave: 2
depends_on: [179-13]
files_modified:
  - /Users/stefanlesnak/Antigravity/Risk App 2/backend/scripts/seed_e2e_vendor_slas.py
  - /Users/stefanlesnak/Antigravity/Risk App 2/backend/scripts/seed_e2e_all.py
autonomous: true
---

<objective>
Add deterministic Vendor SLA seed data (active + archived) linked to seeded vendors for archive/restore and permission test coverage.
</objective>

<context>
Vendor SLA entities use `is_archived` metadata and now have restore flows requiring `vendors:delete`. Deterministic SLA fixtures are needed for E2E tests and RBAC assertions.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create vendor SLA deterministic seed script</name>
  <files>backend/scripts/seed_e2e_vendor_slas.py</files>
  <action>
Create a new SLA seed script that:
- Seeds deterministic SLAs for deterministic `E2E-` vendors.
- Includes active and archived SLAs (`is_archived=true`, `archived_at`, `archived_by_id`).
- Includes reporting owner assignments from existing users.
- Is idempotent by deterministic metric-name/vendor pairing.
  </action>
  <verify>Rerun does not duplicate SLA records and preserves archived metadata.</verify>
  <done>Vendor SLA dataset supports list/detail/archive-visibility/restore scenarios.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate SLA seeding into orchestration</name>
  <files>backend/scripts/seed_e2e_all.py</files>
  <action>
- Add SLA seed step after vendor seeding.
- Include active/archived SLA counts in final summary.
  </action>
  <verify>Master seed run contains SLA step and deterministic counts.</verify>
  <done>Vendor SLA data is part of standard E2E seed execution.</done>
</task>

</tasks>

<must_haves>
- No user/department creation.
- All SLAs link to deterministic seeded vendors.
- Archived SLA records are restore-ready.
</must_haves>

<verification>
- [ ] `seed_e2e_vendor_slas.py` creates deterministic active/archived SLAs
- [ ] SLA seeding is idempotent
- [ ] Master orchestrator includes SLA step and summary
</verification>
