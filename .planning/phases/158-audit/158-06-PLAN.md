---
phase: 158-audit
type: execute
---

<objective>
Align Control frequency enums by adding missing `continuous` to the backend ControlFrequency enum.

Purpose: Prevent silent omissions in dashboards and ensure backend model matches schema/UI.
Output: Backend enum updated + (optional) dashboard regression test.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/158-audit/158-CONTEXT.md
@backend/app/models/control.py
@backend/app/schemas/control.py
@frontend/src/types/control.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preflight - confirm schema/UI allow continuous but model enum does not</name>
  <files>backend/app/models/control.py, backend/app/schemas/control.py</files>
  <action>
Confirm mismatch:
- `backend/app/schemas/control.py` includes `continuous`.
- `backend/app/models/control.py` ControlFrequency enum does not.

Note that dashboards may iterate the model enum, which can silently omit “continuous”.

Wrong code example (current model enum; `backend/app/models/control.py` around `#L14`):
```py
class ControlFrequency(str, PyEnum):
    daily = "daily"
    weekly = "weekly"
    monthly = "monthly"
    quarterly = "quarterly"
    annually = "annually"
    ad_hoc = "ad_hoc"
    # missing: continuous
```

Schema/UI allow it (`backend/app/schemas/control.py` around `#L21`):
```py
class ControlFrequencyEnum(str, Enum):
    ...
    continuous = "continuous"
```
  </action>
  <verify>Open both files and confirm the value exists in schema but not model.</verify>
  <done>Mismatch is confirmed and scoped.</done>
</task>

<task type="auto">
  <name>Task 2: Add continuous to backend ControlFrequency enum</name>
  <files>backend/app/models/control.py</files>
  <action>
Add `continuous = "continuous"` to `ControlFrequency` in `backend/app/models/control.py`.

No DB migration should be required because `controls.frequency` is stored as a string column; this is a model-level enumeration completeness fix.

Fix example:
```py
class ControlFrequency(str, PyEnum):
    ...
    ad_hoc = "ad_hoc"
    continuous = "continuous"
```
  </action>
  <verify>cd backend && python -m compileall -q app</verify>
  <done>Backend enum matches allowed values emitted by API/schema/UI.</done>
</task>

<task type="auto">
  <name>Task 3: Optional regression test for dashboard frequency breakdown</name>
  <files>backend/tests/test_dashboard_frequency_breakdown.py</files>
  <action>
If dashboards expose a frequency breakdown:
- Insert a control with `frequency='continuous'`.
- Call the dashboard summary endpoint and assert the response includes a `continuous` bucket with count >= 1.

If no explicit breakdown endpoint exists, test the underlying function or add a minimal test around the response-building behavior used by dashboard endpoints.
  </action>
  <verify>cd backend && pytest -q tests/test_dashboard_frequency_breakdown.py</verify>
  <done>Regression coverage prevents future enum drift from hiding values.</done>
</task>

</tasks>

<verification>
Before declaring complete:
- [ ] `cd backend && pytest -q`
- [ ] Manual: create a “continuous” control via UI and confirm dashboard frequency breakdown includes it
</verification>

<success_criteria>
- Backend model enum and schema/UI agree on all allowed frequency values.
</success_criteria>

<output>
After completion, create `.planning/phases/158-audit/158-06-SUMMARY.md` with:
- The enum change
- Any tests added
</output>
