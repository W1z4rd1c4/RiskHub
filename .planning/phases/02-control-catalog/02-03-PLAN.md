---
phase: 02-control-catalog
type: execute
domain: frontend-design
---

<objective>
Build the Control Catalog UI with list view, detail view, and forms.

Purpose: Enable users to browse, create, and manage controls through an intuitive interface.
Output: React components for control catalog with premium glassmorphism styling.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-plan.md
./02-03-SUMMARY.md
@.agent/references/checkpoints.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@frontend/src/App.tsx
@frontend/src/pages/Dashboard.tsx
@frontend/src/contexts/AuthContext.tsx
@backend/app/schemas/control.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Control types and API service</name>
  <files>frontend/src/types/control.ts, frontend/src/services/controlApi.ts</files>
  <action>
    Create TypeScript interfaces matching backend schemas:
    
    - Control interface with all 13 fields
    - ControlForm, ControlFrequency, ControlStatus enums
    - ControlExecution interface
    - ControlSummary for list views
    
    Create API service with functions:
    - getControls(params): Paginated list with filters
    - getControl(id): Single control with relationships
    - createControl(data): POST new control
    - updateControl(id, data): PUT update
    - deleteControl(id): DELETE (archive)
    - logExecution(controlId, data): POST execution
    - getExecutions(controlId): GET execution history
    
    Use fetch with proper error handling. Include auth headers from AuthContext.
  </action>
  <verify>TypeScript compiles without errors</verify>
  <done>Types and API service ready for use in components</done>
</task>

<task type="auto">
  <name>Task 2: Create Control List page with table and filters</name>
  <files>frontend/src/pages/ControlList.tsx, frontend/src/App.tsx</files>
  <action>
    Create ControlList page with:
    
    - Premium table with glassmorphism styling (matching Phase 1 design)
    - Columns: Name, Department, Frequency, Risk Level, Status, Last Execution
    - Risk level displayed as colored badge (1-green, 5-red gradient)
    - Frequency displayed with icon (calendar icons)
    - Filter sidebar: department dropdown, status multi-select, risk level range
    - Search bar for name/description full-text search
    - Pagination controls
    - "New Control" button (visible if user has controls:write)
    - Row click navigates to detail view
    
    Apply premium design with:
    - Gradient header
    - Frosted glass effect on filters panel
    - Smooth hover animations on rows
    - Skeleton loading states
    
    Add route "/controls" to App.tsx with navigation link.
  </action>
  <verify>npm run dev shows /controls page with sample data</verify>
  <done>Control list renders with filters, pagination, and navigation works</done>
</task>

<task type="auto">
  <name>Task 3: Create Control Detail page</name>
  <files>frontend/src/pages/ControlDetail.tsx, frontend/src/App.tsx</files>
  <action>
    Create ControlDetail page with:
    
    Header section:
    - Control name as title
    - Status badge
    - Risk level indicator (1-5 with color)
    - Edit/Delete buttons (permission-gated)
    
    Info cards (glassmorphism):
    - Control Information: description, methodology reference
    - Ownership: department, control owner, process owner, executor
    - Scheduling: frequency, next scheduled
    - Documentation: data source, output, report recipient, documentation location
    
    Execution History section:
    - Table of recent executions
    - "Log Execution" button
    - Pagination for history
    
    Add route "/controls/:id" to App.tsx.
  </action>
  <verify>Navigate to /controls/1 shows control details</verify>
  <done>Detail page displays all 13 control fields in organized layout</done>
</task>

<task type="auto">
  <name>Task 4: Create Control Form (Create/Edit)</name>
  <files>frontend/src/components/ControlForm.tsx, frontend/src/pages/ControlNew.tsx, frontend/src/pages/ControlEdit.tsx</files>
  <action>
    Create reusable ControlForm component with:
    
    Form fields for all 13 points:
    - name: text input (required)
    - description: textarea (required)
    - data_source: text input
    - methodology_reference: text input
    - control_form: radio buttons (Manual/Automatic)
    - process_owner_position: text input
    - control_owner_id: user dropdown/search
    - executor_position: text input
    - frequency: select dropdown
    - risk_level: 1-5 slider or radio buttons
    - output_description: textarea
    - report_recipient: text input
    - documentation_location: text input
    
    Additional:
    - department_id: department dropdown
    - status: select (admin only for changing to active)
    
    Validation:
    - Required fields marked with asterisk
    - Risk level 1-5 constraint
    - Show inline validation errors
    
    Create ControlNew page (/controls/new) - uses form in create mode
    Create ControlEdit page (/controls/:id/edit) - uses form in edit mode, pre-fills data
    
    Apply glassmorphism styling matching existing design.
  </action>
  <verify>Create new control through form, verify it appears in list</verify>
  <done>Form validates all fields, creates/updates controls successfully</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Control Catalog UI with list, detail, and forms</what-built>
  <how-to-verify>
    1. Run: `cd frontend && npm run dev`
    2. Ensure backend running: `cd backend && source venv/bin/activate && uvicorn app.main:app --reload`
    3. Visit: http://localhost:5173/controls
    4. Verify: Control list loads with sample data from seed
    5. Click any control row to open detail view
    6. Verify: All 13 fields displayed correctly
    7. Click "New Control" button
    8. Verify: Form has all 13 fields with proper validation
    9. Fill form and submit
    10. Verify: New control appears in list
    11. Edit the control and save
    12. Verify: Changes persisted
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] /controls route shows paginated list with filters
- [ ] /controls/:id shows all control details in organized cards
- [ ] /controls/new form validates and creates controls
- [ ] /controls/:id/edit pre-fills and updates controls
- [ ] Permissions gate edit/delete buttons
- [ ] Styling matches existing glassmorphism theme
- [ ] No TypeScript errors, no console errors
</verification>

<success_criteria>
- All tasks completed
- Human verification checkpoint approved
- Full CRUD flow works end-to-end
- Design matches premium glassmorphism aesthetic
- Performance acceptable (no jank, smooth animations)
</success_criteria>

<output>
After completion, create `.planning/phases/02-control-catalog/02-03-SUMMARY.md`

# Phase 2 Plan 3: Control Catalog UI Summary

**[One-liner describing what was built]**

## Accomplishments
- [Key outcomes]

## Files Created/Modified
- `path/to/file` - Description

## Decisions Made
[Key decisions, or "None"]

## Issues Encountered
[Problems and resolutions, or "None"]

## Next Step
Phase 2 complete. Ready for Phase 3: Dashboards
</output>
