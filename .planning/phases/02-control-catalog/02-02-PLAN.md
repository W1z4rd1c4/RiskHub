---
phase: 02-control-catalog
type: execute
domain: python-development
---

<objective>
Implement CRUD API endpoints for control and risk management with proper RBAC.

Purpose: Enable frontend to manage controls and risks, with bidirectional linking between them.
Output: FastAPI routers for /controls and /risks endpoints, Control-Risk linking, proper permission enforcement.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-plan.md
./02-02-SUMMARY.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@backend/app/models/control.py
@backend/app/schemas/control.py
@backend/app/core/security.py
@backend/app/api/v1/endpoints/users.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create controls API router with CRUD operations</name>
  <files>backend/app/api/v1/endpoints/controls.py, backend/app/api/v1/router.py</files>
  <action>
    Create controls router with endpoints:
    
    GET /api/v1/controls - List controls (paginated, filterable by department_id, status)
    GET /api/v1/controls/{control_id} - Get single control with relationships
    POST /api/v1/controls - Create control (requires controls:write permission)
    PUT /api/v1/controls/{control_id} - Update control (requires controls:write or control owner)
    DELETE /api/v1/controls/{control_id} - Soft delete (set status=archived, requires controls:delete)
    
    Use async SQLAlchemy queries. Include:
    - Pagination with skip/limit parameters
    - Department filtering (department heads see only their controls)
    - Status filtering (active, draft, archived)
    - Full text search on name/description
    - Proper 404 handling for missing controls
    
    Register router in v1/router.py with prefix "/controls".
  </action>
  <verify>curl http://localhost:8000/api/v1/controls returns 200 (may be empty list)</verify>
  <done>All CRUD endpoints return correct status codes, list endpoint paginates</done>
</task>

<task type="auto">
  <name>Task 2: Create control execution logging endpoints</name>
  <files>backend/app/api/v1/endpoints/controls.py</files>
  <action>
    Add execution logging to controls router:
    
    POST /api/v1/controls/{control_id}/executions - Log a control execution
    GET /api/v1/controls/{control_id}/executions - List execution history
    
    Execution creation should:
    - Set executed_by_id from current user
    - Calculate next_scheduled based on control's frequency
    - Validate result enum value
    
    Execution listing should:
    - Return paginated history ordered by executed_at DESC
    - Include executed_by user info in response
  </action>
  <verify>POST to /controls/1/executions creates execution record</verify>
  <done>Execution endpoints work, auto-populate user and next_scheduled</done>
</task>

<task type="auto">
  <name>Task 3: Implement permission checks using security decorators</name>
  <files>backend/app/api/v1/endpoints/controls.py, backend/app/core/security.py</files>
  <action>
    Apply RBAC to control endpoints:
    
    - List/Read: controls:read permission (all authenticated users)
    - Create: controls:write permission (admin, risk_manager, department_head)
    - Update: controls:write OR is control owner
    - Delete: controls:delete permission (admin, risk_manager only)
    - Execute: Any authenticated user can log execution
    
    Department heads see all controls if admin/cro/risk_manager, else only their department's controls.
    Control owners can always update their own controls.
    
    Use the existing require_permission decorator from security.py.
    Add new permission "controls:execute" if needed for granular control.
  </action>
  <verify>Non-admin user cannot DELETE controls (returns 403)</verify>
  <done>Permission checks enforce RBAC, department filtering works</done>
</task>

<task type="auto">
  <name>Task 4: Add seed data for testing</name>
  <files>backend/app/db/seed.py</files>
  <action>
    Create or update seed script to add sample controls:
    
    Add 5-10 sample controls across different departments:
    - 2-3 for each of the seeded departments (from Phase 1)
    - Mix of manual and automatic controls
    - Various frequencies (daily, weekly, monthly)
    - Different risk levels (1-5)
    
    Also add 2-3 execution records for some controls to demonstrate history.
    
    Make seed script idempotent (check if data exists before inserting).
    Include command to run: `python -m app.db.seed`
  </action>
  <verify>After running seed, GET /api/v1/controls returns 5+ controls</verify>
  <done>Seed data creates realistic control samples with executions</done>
</task>

<task type="auto">
  <name>Task 5: Create risks API router with CRUD operations</name>
  <files>backend/app/api/v1/endpoints/risks.py, backend/app/api/v1/router.py</files>
  <action>
    Create risks router with endpoints (foundation for Phase 2.1 Risk Register):
    
    GET /api/v1/risks - List risks (paginated, filterable by department_id, status, risk_type)
    GET /api/v1/risks/{risk_id} - Get single risk with linked controls
    POST /api/v1/risks - Create risk (requires risks:write permission)
    PUT /api/v1/risks/{risk_id} - Update risk (requires risks:write or risk owner)
    DELETE /api/v1/risks/{risk_id} - Soft delete (status=archived)
    
    Include:
    - Gross/Net risk score calculation (probability × impact)
    - Filter by risk_type (strategic/operational)
    - Filter by is_priority (Risk Catalog items)
    - Department filtering for department heads
    
    Register router in v1/router.py with prefix "/risks".
  </action>
  <verify>curl http://localhost:8000/api/v1/risks returns 200</verify>
  <done>Risk CRUD endpoints work, linked controls returned in detail view</done>
</task>

<task type="auto">
  <name>Task 6: Create Control-Risk linking endpoints</name>
  <files>backend/app/api/v1/endpoints/controls.py, backend/app/api/v1/endpoints/risks.py</files>
  <action>
    Add endpoints for linking controls to risks (many-to-many):
    
    POST /api/v1/controls/{control_id}/risks - Link control to a risk
    DELETE /api/v1/controls/{control_id}/risks/{risk_id} - Remove link
    GET /api/v1/controls/{control_id}/risks - List risks mitigated by control
    
    POST /api/v1/risks/{risk_id}/controls - Link risk to a control
    DELETE /api/v1/risks/{risk_id}/controls/{control_id} - Remove link
    GET /api/v1/risks/{risk_id}/controls - List controls that mitigate risk
    
    Each link includes effectiveness rating (high/medium/low) and notes.
    Recalculate net_risk on risk when controls change (optional optimization).
  </action>
  <verify>Link control to risk, verify GET /risks/{id}/controls shows the control</verify>
  <done>Control-Risk linking works bidirectionally, effectiveness tracked</done>
</task>

<task type="auto">
  <name>Task 7: Add seed data for risks and control-risk links</name>
  <files>backend/app/db/seed.py</files>
  <action>
    Extend seed script with sample risks and links:
    
    Add 5-8 sample risks based on OS 18 examples:
    - Marketing risks (Mkt-R01, Mkt-R02)
    - Product development risks (Vnp-R01, Vnp-R02)
    - Operational risks
    - Mix of strategic/operational types
    - Various gross/net risk scores
    
    Link existing controls to risks (2-3 controls per risk on average).
    Set effectiveness ratings.
    
    Make seed script idempotent.
  </action>
  <verify>After running seed, GET /api/v1/risks returns 5+ risks with linked controls</verify>
  <done>Sample risks created with realistic control-risk links</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] GET /api/v1/controls returns list of controls
- [ ] POST /api/v1/controls creates new control
- [ ] GET /api/v1/controls/{id} returns control with relationships
- [ ] PUT /api/v1/controls/{id} updates control
- [ ] DELETE /api/v1/controls/{id} archives control (soft delete)
- [ ] Permission checks work (non-admin cannot delete)
- [ ] Execution logging creates records with proper timestamps
- [ ] GET /api/v1/risks returns list of risks
- [ ] Control-Risk linking endpoints work bidirectionally
- [ ] Seed data populates database with controls, risks, and links
</verification>

<success_criteria>
- All tasks completed
- All CRUD operations work correctly for both controls and risks
- Control-Risk many-to-many relationship works
- Permission checks enforce RBAC
- API responses match Pydantic schemas
- No 500 errors on valid requests
</success_criteria>

<output>
After completion, create `.planning/phases/02-control-catalog/02-02-SUMMARY.md`

# Phase 2 Plan 2: API Endpoints Summary

**[One-liner describing what was built]**

## Accomplishments
- [Key outcomes]

## Files Created/Modified
- `path/to/file` - Description

## Decisions Made
[Key decisions, or "None"]

## Issues Encountered
[Problems and resolutions, or "None"]

## Next Step
Ready for 02-03-PLAN.md (Control catalog UI)
</output>
