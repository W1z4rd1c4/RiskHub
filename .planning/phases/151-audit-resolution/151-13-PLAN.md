---
phase: 151-audit-resolution
type: execute
---

<objective>
Close schema drift with missing migrations, normalize notification types for extensibility, and harden dashboard trend queries.

Purpose: Keep production schemas aligned with models, make notification types evolvable without enum DDL, and ensure trend endpoints run reliably.
Output: Migration(s) for missing columns and notification type normalization plus dashboard query fixes and tests.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/151-audit-resolution/151-CONTEXT.md
@backend/app/models/department.py
@backend/app/models/user.py
@backend/app/models/notification.py
@backend/app/models/__init__.py
@backend/app/schemas/notification.py
@backend/alembic/versions/
@backend/app/api/v1/endpoints/dashboard.py
@backend/tests/test_dashboard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement enterprise notification type normalization + missing columns</name>
  <files>backend/alembic/versions/*.py, backend/app/models/notification.py, backend/app/models/notification_type.py, backend/app/models/__init__.py, backend/app/schemas/notification.py</files>
  <action>
    Create a notification_types table (key PK, label, description, is_active, created_at, updated_at) and seed rows for all known types including kri_breach_detected.
    Add notifications.notification_type_key (string FK to notification_types.key), backfill it from the existing enum column, and update the Notification model to use the new column and relationship. Keep using the NotificationType enum in code, but store its .value string.
    Use an expand/contract approach: migration A adds the table + new column + backfill; after code switches to the new column, migration B drops the old enum column and the enum type. Ensure downgrade restores the previous schema.
    Add missing departments.is_system and users.employee_type columns with defaults and backfill existing rows.
    Update NotificationTypeEnum in schemas to include kri_breach_detected so API responses validate against the new type.
  </action>
  <verify>cd backend && alembic upgrade head</verify>
  <done>Schema changes apply cleanly; notification inserts work without enum errors; missing columns exist with defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Fix dashboard trend queries and add coverage</name>
  <files>backend/app/api/v1/endpoints/dashboard.py, backend/tests/test_dashboard.py</files>
  <action>
    Replace func.case with sqlalchemy.case in risk and KRI breach trend queries. Ensure the query continues to group by period and returns numeric counts.
    Add tests for /dashboard/risk-trends and /dashboard/kri-breach-trends that verify 200 responses and non-error aggregation for a small fixture dataset.
  </action>
  <verify>cd backend && pytest tests/test_dashboard.py -v</verify>
  <done>Trend endpoints work without SQL errors and have test coverage.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Alembic migration(s) apply cleanly
- [ ] Dashboard trend tests pass
- [ ] Notifications use lookup table + FK and insert without enum errors
</verification>

<success_criteria>

- Missing schema changes are captured in migrations
- Notification types evolve without enum DDL
- Dashboard trend endpoints run without SQL errors
</success_criteria>

<output>
After completion, create `.planning/phases/151-audit-resolution/151-13-SUMMARY.md`:

# Phase 151 Plan 13: Migrations + Dashboard Trends Summary

**[One-line summary of migration fixes and dashboard query hardening]**

## Accomplishments

- [Notification types normalized via lookup table + FK]
- [Missing columns migrated with defaults]
- [Dashboard trend queries fixed and tested]

## Files Created/Modified

- `backend/alembic/versions/*.py` - Schema migrations
- `backend/app/models/notification_type.py` - Notification type catalog model
- `backend/app/models/notification.py` - Column swap to lookup key
- `backend/app/models/__init__.py` - Model export updates
- `backend/app/schemas/notification.py` - Enum values updated
- `backend/app/api/v1/endpoints/dashboard.py` - Query fixes
- `backend/tests/test_dashboard.py` - Trend endpoint tests

## Decisions Made

- Option B: Notification type normalization via lookup table + FK

## Issues Encountered

[None]

## Next Step

Ready for 151-14-PLAN.md
</output>
