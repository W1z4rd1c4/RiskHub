---
phase: 151-audit-resolution
type: execute
---

<objective>
Align department and KRI endpoints with archived filtering and consistent control summary fields.

Purpose: Ensure list and summary endpoints match the default "exclude archived" behavior and return complete ControlSummary data.
Output: Updated department and KRI endpoints with consistent filtering and summaries; tests adjusted as needed.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/150-audit/150-02-FINDINGS.md
@backend/app/api/v1/endpoints/departments.py
@backend/app/api/v1/endpoints/kris.py
@backend/app/api/v1/endpoints/controls.py
@backend/app/schemas/control.py
@backend/tests/test_data_consistency.py
@backend/tests/test_kris_rbac.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix department control counts and list filtering</name>
  <files>backend/app/api/v1/endpoints/departments.py, backend/tests/test_data_consistency.py</files>
  <action>Exclude archived controls by default in department summaries and /departments/{id}/controls. When status is not provided, apply Control.status != archived. When status is provided, honor it. Populate ControlSummary fields (department_name, control_owner_name) by eager-loading department/control_owner and mapping like list_controls. Keep response model as list[ControlSummary] and avoid breaking the API shape.</action>
  <verify>pytest backend/tests/test_data_consistency.py -k control</verify>
  <done>Department control counts match list endpoints, archived controls are excluded by default, and ControlSummary fields are populated.</done>
</task>

<task type="auto">
  <name>Task 2: Exclude archived risks from KRI breaches</name>
  <files>backend/app/api/v1/endpoints/kris.py, backend/tests/test_kris_rbac.py</files>
  <action>Update /kris/breaches to filter out archived risks by default (join Risk and apply Risk.status != archived). Preserve department scoping and explicit department_id filter behavior. If you add an include_archived flag, default it to false and document it in the endpoint signature.</action>
  <verify>pytest backend/tests/test_kris_rbac.py</verify>
  <done>Breach list never includes KRIs linked to archived risks unless explicitly requested.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Department controls list excludes archived by default and includes summary fields
- [ ] KRI breaches list excludes archived risks by default
- [ ] Targeted backend tests pass
</verification>

<success_criteria>

- All tasks completed
- Department summaries and KRI breaches align with archived filtering
- No API response shape regressions
  </success_criteria>

<output>
After completion, create `.planning/phases/151-audit-resolution/151-01-SUMMARY.md`:

# Phase 151 Plan 01: Department and KRI List Alignment Summary

**[One-line summary of the filtering and summary fixes]**

## Accomplishments

- [Department controls list and counts aligned with archived filtering]
- [KRI breach list excludes archived risks by default]

## Files Created/Modified

- `backend/app/api/v1/endpoints/departments.py` - Department control filtering and summary mapping
- `backend/app/api/v1/endpoints/kris.py` - Breach list filtering
- `backend/tests/test_data_consistency.py` - Adjusted control count assertions if needed
- `backend/tests/test_kris_rbac.py` - Breach list coverage if needed

## Decisions Made

[None]

## Issues Encountered

[None]

## Next Step

Ready for 151-02-PLAN.md
</output>
