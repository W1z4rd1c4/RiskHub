---
phase: 151-audit-resolution
type: execute
---

<objective>
Align frontend access management and user pickers with access_scope and scoped visibility.

Purpose: Ensure admin/CRO can manage access, non-admins see relevant users, and dropdowns no longer 403 on user lists.
Output: Updated auth typing, permission gating, UsersPage behavior, and scoped user lookup usage across forms.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/151-audit-resolution/151-CONTEXT.md
@frontend/src/services/authApi.ts
@frontend/src/contexts/AuthContext.tsx
@frontend/src/hooks/usePermissions.ts
@frontend/src/components/layout/Sidebar.tsx
@frontend/src/services/userApi.ts
@frontend/src/pages/UsersPage.tsx
@frontend/src/components/access/AccessEditModal.tsx
@frontend/src/components/KRIForm.tsx
@frontend/src/components/kri/KRIModal.tsx
@frontend/src/pages/DepartmentDetailPage.tsx
@frontend/src/components/governance/ResolveOrphanModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update auth typing and permission gating for access_scope</name>
  <files>frontend/src/services/authApi.ts, frontend/src/contexts/AuthContext.tsx, frontend/src/hooks/usePermissions.ts, frontend/src/components/layout/Sidebar.tsx</files>
  <action>
    Extend authApi TokenResponse and AuthContext user shape to include access_scope, scope_label, and effective_permissions from the backend. Use effective_permissions for hasPermission checks.
    Update usePermissions so canManageAccess derives from user.access_scope === 'global' and canManagePrivileged remains admin/cro only. Keep existing permission helpers intact.
    Update Sidebar to show Access Management navigation based on canManageAccess (not users:write) and keep governance badge logic consistent.
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <done>Permission gating uses access_scope and auth typing matches backend responses.</done>
</task>

<task type="auto">
  <name>Task 2: Replace admin-only user lists with scoped lookup</name>
  <files>frontend/src/services/userApi.ts, frontend/src/pages/UsersPage.tsx, frontend/src/components/access/AccessEditModal.tsx, frontend/src/components/KRIForm.tsx, frontend/src/components/kri/KRIModal.tsx, frontend/src/pages/DepartmentDetailPage.tsx, frontend/src/components/governance/ResolveOrphanModal.tsx</files>
  <action>
    Add userApi.listVisibleUsers (hits /users/lookup) and update all dropdowns/lists that currently call listUsers to use the scoped lookup instead.
    UsersPage: if canManageAccess, use accessApi list; otherwise render a read-only scoped list from listVisibleUsers and hide edit/deactivate actions.
    AccessEditModal: use listVisibleUsers for manager options; disable role selection unless canManagePrivileged and show a clear hint when role changes are restricted.
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <done>User pickers work for non-admins without 403s, and access management UI is correctly gated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Access management gating + scoped user pickers</what-built>
  <how-to-verify>
    1. Run: `cd backend && uvicorn app.main:app --reload`
    2. Run: `cd frontend && npm run dev`
    3. As admin/CRO: visit http://localhost:5173/users and confirm access management UI loads with edit controls
    4. As non-admin: visit /users and confirm read-only scoped list is shown
    5. Open KRI form and confirm reporting owner dropdown loads without 403
    6. Open AccessEditModal and confirm role dropdown is disabled for non-admins
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] TypeScript check passes
- [ ] Access management nav + gating uses access_scope
- [ ] Scoped user pickers load for non-admins
</verification>

<success_criteria>

- Admin/CRO can manage access; non-admins see relevant users only
- User pickers no longer 403 for non-admin users
- Sidebar shows Access Management based on access_scope
  </success_criteria>

<output>
After completion, create `.planning/phases/151-audit-resolution/151-11-SUMMARY.md`:

# Phase 151 Plan 11: Frontend Access + Scoped Users Summary

**[One-line summary of access gating + scoped user pickers]**

## Accomplishments

- [Auth context + permission gating updated for access_scope]
- [Scoped user lookup wired across forms and lists]
- [UsersPage shows access management for admin/CRO and read-only scoped list for others]

## Files Created/Modified

- `frontend/src/services/authApi.ts` - Access scope fields
- `frontend/src/contexts/AuthContext.tsx` - User shape updated
- `frontend/src/hooks/usePermissions.ts` - Access-scope gating
- `frontend/src/components/layout/Sidebar.tsx` - Access Management nav gating
- `frontend/src/services/userApi.ts` - Scoped user lookup
- `frontend/src/pages/UsersPage.tsx` - Read-only scoped list for non-admins
- `frontend/src/components/access/AccessEditModal.tsx` - Scoped manager list + role gating
- `frontend/src/components/KRIForm.tsx` - Scoped reporting owner list
- `frontend/src/components/kri/KRIModal.tsx` - Scoped reporting owner list
- `frontend/src/pages/DepartmentDetailPage.tsx` - Scoped department users
- `frontend/src/components/governance/ResolveOrphanModal.tsx` - Scoped user list

## Decisions Made

None.

## Issues Encountered

[None]

## Next Step

Phase 151 plans ready for execution
</output>
