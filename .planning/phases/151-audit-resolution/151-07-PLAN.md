---
phase: 151-audit-resolution
type: execute
---

<objective>
Remove hard caps from grouped views, KRI lists, department details, and audit trail.

Purpose: Ensure UI lists and grouped summaries reflect the full dataset instead of the first page only.
Output: Paginated or fully fetched datasets for grouped views and list pages, with accurate totals.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/150-audit/150-03-FINDINGS.md
@frontend/src/pages/RisksPage.tsx
@frontend/src/pages/ControlsPage.tsx
@frontend/src/pages/KRIsPage.tsx
@frontend/src/pages/DepartmentDetailPage.tsx
@frontend/src/pages/AuditTrailPage.tsx
@frontend/src/services/kriApi.ts
@frontend/src/services/departmentApi.ts
@frontend/src/services/userApi.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch full datasets for grouped views and KRI list</name>
  <files>frontend/src/pages/RisksPage.tsx, frontend/src/pages/ControlsPage.tsx, frontend/src/pages/KRIsPage.tsx, frontend/src/services/kriApi.ts</files>
  <action>When viewMode is not "all", fetch all pages for risks and controls using response.total to build grouped views from complete data. For KRIs, use server pagination for viewMode "all" (page/size + total) and fetch all pages when grouped views are selected so group counts are accurate.</action>
  <verify>Manual: grouped view counts match totals when dataset exceeds 100 items.</verify>
  <done>Grouped views and KRI list are no longer capped at 100 items.</done>
</task>

<task type="auto">
  <name>Task 2: Add pagination for department detail tabs and audit trail</name>
  <files>frontend/src/pages/DepartmentDetailPage.tsx, frontend/src/services/departmentApi.ts, frontend/src/services/userApi.ts, frontend/src/pages/AuditTrailPage.tsx</files>
  <action>Implement pagination or "load more" for department risks/controls/KRIs/users using skip/limit and reset pagination when filters or tabs change. Replace the fixed audit trail limit with paginated fetching (skip/limit) and a UI control to navigate pages or load more records.</action>
  <verify>Manual: department tabs and audit trail can navigate beyond the initial page without missing records.</verify>
  <done>Department detail lists and audit trail are paginated and no longer capped at 50/100 items.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Grouped views use full datasets for accurate counts
- [ ] KRI page shows totals beyond the first 100 records
- [ ] Department detail tabs and audit trail paginate beyond initial limits
</verification>

<success_criteria>

- All tasks completed
- No list view is capped by a hardcoded limit
- Grouped views and totals reflect the full dataset
  </success_criteria>

<output>
After completion, create `.planning/phases/151-audit-resolution/151-07-SUMMARY.md`:

# Phase 151 Plan 07: Pagination and Grouped View Completeness Summary

**[One-line summary of pagination and grouping fixes]**

## Accomplishments

- [Grouped views fetch full datasets]
- [KRI list uses server pagination]
- [Department tabs and audit trail support pagination]

## Files Created/Modified

- `frontend/src/pages/RisksPage.tsx` - Grouped view fetching
- `frontend/src/pages/ControlsPage.tsx` - Grouped view fetching
- `frontend/src/pages/KRIsPage.tsx` - Pagination adjustments
- `frontend/src/pages/DepartmentDetailPage.tsx` - Tab pagination
- `frontend/src/pages/AuditTrailPage.tsx` - Pagination controls
- `frontend/src/services/kriApi.ts` - Pagination params
- `frontend/src/services/departmentApi.ts` - Paging usage
- `frontend/src/services/userApi.ts` - Paging usage

## Decisions Made

[None]

## Issues Encountered

[None]

## Next Step

Phase 151 plans ready for execution
</output>
