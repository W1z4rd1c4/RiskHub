---
phase: 151-audit-resolution
type: execute
---

<objective>
Tighten frontend permission gating for user management and approvals, and use safe role defaults.

Purpose: Prevent non-privileged users from seeing admin UI actions and avoid privileged default role selection.
Output: Permission-guarded user management routes/actions and safe default role selection in user creation.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/150-audit/150-03-FINDINGS.md
@frontend/src/App.tsx
@frontend/src/hooks/usePermissions.ts
@frontend/src/components/PermissionGate.tsx
@frontend/src/pages/UsersPage.tsx
@frontend/src/pages/UserNewPage.tsx
@frontend/src/pages/UserDetailPage.tsx
@frontend/src/pages/ApprovalsPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate user management routes and actions by permission</name>
  <files>frontend/src/App.tsx, frontend/src/pages/UsersPage.tsx, frontend/src/pages/UserNewPage.tsx, frontend/src/pages/UserDetailPage.tsx</files>
  <action>Add a permission-based route guard for user management. Require users:read for /users and /users/:id, and users:write for /users/new and edit/deactivate actions. Hide or disable edit/deactivate controls for users without users:write and provide a read-only empty state instead of exposing admin affordances.</action>
  <verify>Manual: log in as a non-privileged user and confirm user management routes/actions are hidden or redirected.</verify>
  <done>User management UI only exposes routes and actions allowed by permissions.</done>
</task>

<task type="auto">
  <name>Task 2: Use safe default role selection and align approvals gating</name>
  <files>frontend/src/pages/UserNewPage.tsx, frontend/src/pages/UsersPage.tsx, frontend/src/hooks/usePermissions.ts</files>
  <action>Update UserNewPage to default to a non-privileged role (control_owner or viewer) and require explicit selection if the safe role is missing; remove fallback to the first role. Align UsersPage role filters with seeded roles (e.g., control_owner instead of employee). Ensure approvals UI uses canResolveApprovals and relies on approvals:write being seeded in the backend.</action>
  <verify>Manual: user creation defaults to a safe role and never auto-selects admin/cro.</verify>
  <done>User creation defaults are safe and approvals UI aligns with permissions.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] User management routes are permission-gated
- [ ] User creation defaults to a safe role
- [ ] Approvals actions appear only when approvals:write is granted
</verification>

<success_criteria>

- All tasks completed
- No admin-only UI is exposed to unauthorized users
- Default role selection cannot elevate privileges
  </success_criteria>

<output>
After completion, create `.planning/phases/151-audit-resolution/151-05-SUMMARY.md`:

# Phase 151 Plan 05: Frontend Permission Gating Summary

**[One-line summary of user management and approvals gating fixes]**

## Accomplishments

- [User management routes/actions gated by permissions]
- [Safe default role selection enforced]
- [Approvals UI aligns with permissions]

## Files Created/Modified

- `frontend/src/App.tsx` - Route gating
- `frontend/src/pages/UsersPage.tsx` - Action gating and filters
- `frontend/src/pages/UserNewPage.tsx` - Safe defaults
- `frontend/src/pages/UserDetailPage.tsx` - Permission guard
- `frontend/src/hooks/usePermissions.ts` - Permission helpers if needed

## Decisions Made

[None]

## Issues Encountered

[None]

## Next Step

Ready for 151-06-PLAN.md
</output>
