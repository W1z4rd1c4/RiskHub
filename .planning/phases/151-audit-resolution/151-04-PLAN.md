---
phase: 151-audit-resolution
type: execute
---

<objective>
Normalize orphaned timestamps and harden audit scripts.

Purpose: Avoid timezone inconsistencies and prevent destructive or misleading script behavior.
Output: Consistent UTC timestamps and safer migration/verification scripts.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/150-audit/150-02-FINDINGS.md
@backend/app/services/orphaned_item_service.py
@backend/app/models/orphaned_item.py
@backend/scripts/seed_kris.py
@backend/scripts/verify_data_consistency.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize orphaned timestamps to UTC consistently</name>
  <files>backend/app/services/orphaned_item_service.py, backend/app/models/orphaned_item.py</files>
  <action>Replace mixed datetime.now(UTC).replace(tzinfo=None) and datetime.utcnow() calls with a single UTC strategy. If moving to aware timestamps, update OrphanedItem DateTime columns to timezone=True and use datetime.now(UTC) everywhere. If staying naive, use datetime.utcnow() consistently and remove replace(tzinfo=None) usage.</action>
  <verify>Manual: new orphaned items have consistent timestamp tz semantics (all UTC-aware or all naive UTC).</verify>
  <done>Orphaned timestamps are consistent and no longer mix naive and UTC-aware values.</done>
</task>

<task type="auto">
  <name>Task 2: Make seed_kris safe and avoid mismatched fallbacks</name>
  <files>backend/scripts/seed_kris.py</files>
  <action>Add argparse flags so destructive deletes require --force. Validate risks exist before deleting KRIs. Remove the global round-robin fallback; instead log unmatched KRIs (metric name + sheet + row) and skip or write a report for manual mapping. Keep the existing category/process matching logic intact.</action>
  <verify>python backend/scripts/seed_kris.py --help shows flags; running without --force preserves data when risks are missing.</verify>
  <done>seed_kris no longer deletes data prematurely and does not auto-link KRIs to unrelated risks.</done>
</task>

<task type="auto">
  <name>Task 3: Fix verify_data_consistency pagination and auth</name>
  <files>backend/scripts/verify_data_consistency.py</files>
  <action>Use skip/limit (or page/size) with total fields to compute counts instead of len(items). Add optional auth header support (token env var) and handle list responses vs paginated responses consistently. Iterate pages when totals exceed page size so results are not truncated.</action>
  <verify>python backend/scripts/verify_data_consistency.py (with backend running) returns consistent totals without size caps.</verify>
  <done>Consistency script reports accurate totals and works with authenticated APIs.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Orphaned timestamps use a single UTC strategy
- [ ] seed_kris requires explicit destructive intent and avoids global fallback
- [ ] verify_data_consistency uses pagination and total counts
</verification>

<success_criteria>

- All tasks completed
- Timestamp handling is consistent and UTC-based
- Audit scripts are safe and accurate
  </success_criteria>

<output>
After completion, create `.planning/phases/151-audit-resolution/151-04-SUMMARY.md`:

# Phase 151 Plan 04: Script and Timestamp Hygiene Summary

**[One-line summary of timestamp and script fixes]**

## Accomplishments

- [Orphaned timestamps standardized]
- [seed_kris destructive behavior guarded]
- [verify_data_consistency pagination fixed]

## Files Created/Modified

- `backend/app/services/orphaned_item_service.py` - Timestamp normalization
- `backend/app/models/orphaned_item.py` - Timezone configuration if needed
- `backend/scripts/seed_kris.py` - Safe delete and matching behavior
- `backend/scripts/verify_data_consistency.py` - Pagination and auth handling

## Decisions Made

[None]

## Issues Encountered

[None]

## Next Step

Ready for 151-05-PLAN.md
</output>
