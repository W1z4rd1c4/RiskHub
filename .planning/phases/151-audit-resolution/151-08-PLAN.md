---
phase: 151-audit-resolution
type: execute
---

<objective>
Fix KRI historization correctness with calendar-aligned periods and approval-applied corrections.

Purpose: Align KRI reporting windows to calendar periods, prevent backdating from corrupting current state, and ensure approvals apply history corrections.
Output: Updated historization/notification logic plus refreshed backend tests.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/151-audit-resolution/151-CONTEXT.md
@backend/app/services/kri_history_service.py
@backend/app/services/kri_deadline_service.py
@backend/app/api/v1/endpoints/kris.py
@backend/app/api/v1/endpoints/approvals.py
@backend/app/models/notification.py
@backend/app/schemas/kri.py
@backend/tests/test_kri_history.py
@backend/tests/test_kri_deadline_service.py
@backend/tests/test_kris_history_api.py
@backend/tests/test_approvals.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement calendar-aligned period helpers and history integrity</name>
  <files>backend/app/services/kri_history_service.py, backend/app/services/kri_deadline_service.py, backend/tests/test_kri_history.py, backend/tests/test_kri_deadline_service.py</files>
  <action>
    Add shared helpers to compute calendar-aligned periods from a date and frequency (daily = same day; weekly = ISO Mon–Sun; monthly = 1st–last day; quarterly = calendar quarters; annually = Jan–Dec). Use these helpers in KRIHistoryService and KRIDeadlineService so period boundaries are consistent.
    Update record_value to target the most recent closed period by default (period_end <= today); validate period_end matches a real calendar boundary; enforce grace window (15 days) and disallow non-privileged backdating to older periods. Only update KRI current_value/last_period_end when the recorded period_end is >= current last_period_end (never regress current state).
    Update apply_history_correction to treat “latest” by max(period_end) (tie-breaker recorded_at) before updating current_value; do not change current_value for older periods.
    Load recorded_by via selectinload in get_history to avoid async lazy loads.
    Update get_overdue_kris to compute overdue against the latest closed period end and due_date; return due_date/period_end derived from calendar periods.
    Update tests to cover calendar boundary math, non-priv window enforcement, backdated entries not overriding current, and overdue detection with aligned periods.
  </action>
  <verify>cd backend && pytest tests/test_kri_history.py tests/test_kri_deadline_service.py -v</verify>
  <done>Period logic is calendar-aligned, backdating cannot corrupt current state, and tests reflect the new rules.</done>
</task>

<task type="auto">
  <name>Task 2: Apply approval corrections and fix KRI deadline notification types</name>
  <files>backend/app/api/v1/endpoints/kris.py, backend/app/api/v1/endpoints/approvals.py, backend/app/services/kri_deadline_service.py, backend/app/models/notification.py, backend/app/schemas/kri.py, backend/tests/test_kris_history_api.py, backend/tests/test_approvals.py</files>
  <action>
    Route KRI current_value updates through KRIHistoryService.record_value in PUT /kris/{id} so value changes always create history entries and respect reporting windows.
    In approvals resolution, detect KRI history correction payloads (pending_changes.history_entry_id) and call apply_history_correction; only fall back to field-level edits for non-history updates.
    Fix deadline reminders to use correct NotificationType values (KRI_DUE_SOON for advance, KRI_DUE_TOMORROW or a new KRI_DEADLINE for due date, KRI_OVERDUE for overdue, KRI_NEAR_BREACH for near-breach). Add KRI_BREACHED if needed and update enum usage consistently.
    Extend KRI response metadata if needed (e.g., due_date) to support UI overdue display.
    Update API tests to verify approval-applied corrections and notification type routing.
  </action>
  <verify>cd backend && pytest tests/test_kris_history_api.py tests/test_approvals.py -v</verify>
  <done>Approvals apply history corrections, value updates create history entries, and deadline notifications use correct types.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] KRI history tests pass with calendar-aligned periods
- [ ] Approval resolution applies KRI history corrections
- [ ] Deadline reminders use correct NotificationType values
</verification>

<success_criteria>

- Calendar-aligned periods enforced in historization and reminders
- Backdated entries do not overwrite current KRI state
- Approval-based history corrections apply correctly
- Notification types align with reporting and breach semantics
  </success_criteria>

<output>
After completion, create `.planning/phases/151-audit-resolution/151-08-SUMMARY.md`:

# Phase 151 Plan 08: KRI Historization Corrections Summary

**[One-line summary of period alignment + approval/notification fixes]**

## Accomplishments

- [Calendar-aligned period logic + overdue computation]
- [Approval resolution applies history corrections]
- [Notification types corrected for KRI reporting]

## Files Created/Modified

- `backend/app/services/kri_history_service.py` - Period alignment + integrity safeguards
- `backend/app/services/kri_deadline_service.py` - Correct reminders
- `backend/app/api/v1/endpoints/kris.py` - Record-value routing
- `backend/app/api/v1/endpoints/approvals.py` - History correction application
- `backend/app/models/notification.py` - Notification types
- `backend/tests/test_kri_history.py` - Updated period tests
- `backend/tests/test_kri_deadline_service.py` - Updated reminder tests
- `backend/tests/test_kris_history_api.py` - API verification
- `backend/tests/test_approvals.py` - Correction approval coverage

## Decisions Made

- Calendar-aligned reporting periods (daily/weekly/monthly/quarterly/annual)

## Issues Encountered

[None]

## Next Step

Ready for Phase 151 Plan 09 (frontend KRI historization UI + overdue visibility)
</output>
