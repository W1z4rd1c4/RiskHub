---
phase: 151-audit-resolution
type: execute
---

<objective>
Harden access management guardrails and add scoped user lookup for non-admin visibility.

Purpose: Prevent role-change escalation and allow non-admin users to see relevant users within their scope.
Output: Restricted role edits, new scoped user lookup endpoint, and test coverage.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/151-audit-resolution/151-CONTEXT.md
@backend/app/api/v1/endpoints/access.py
@backend/app/api/v1/endpoints/users.py
@backend/app/schemas/user.py
@backend/app/core/permissions.py
@backend/tests/test_access_management.py
@backend/tests/test_users.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restrict role changes to admin/CRO with guardrails</name>
  <files>backend/app/api/v1/endpoints/access.py, backend/tests/test_access_management.py</files>
  <action>
    In update_access_user, block role_id changes unless current_user is admin or CRO. Keep department/manager updates available to global-scope users if desired.
    Add guardrails to prevent changing your own role to a non-admin/cro role and to prevent demoting the last admin/cro (role-based guard, parallel to the access_scope guard).
    Update tests to cover: risk_manager cannot change role_id; admin/cro can; last admin/cro cannot be demoted via role change.
  </action>
  <verify>cd backend && pytest tests/test_access_management.py -v</verify>
  <done>Only admin/CRO can change roles, and guardrails prevent last-admin lockout.</done>
</task>

<task type="auto">
  <name>Task 2: Add scoped user lookup endpoint for non-admins</name>
  <files>backend/app/api/v1/endpoints/users.py, backend/app/schemas/user.py, backend/tests/test_users.py</files>
  <action>
    Add a lightweight GET endpoint (e.g., /users/lookup) that returns visible users for the current user’s scope. For global users, return all active users; for department scope, return same-department users; for manager scope, return self + direct reports (and optionally same-department if manager_id missing).
    Include optional query params for text search (q) and include_inactive=false by default. Return minimal fields needed for pickers (id, name, email, role_name, department_id, manager_id).
    Add tests to verify scope filtering and auth-only access.
  </action>
  <verify>cd backend && pytest tests/test_users.py -v</verify>
  <done>Scoped user lookup works for non-admins without exposing global user lists.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Role-change guardrails enforced in access management API
- [ ] Scoped user lookup endpoint returns only visible users
- [ ] Backend tests pass for access management and user lookup
</verification>

<success_criteria>

- Admin/CRO can change any user’s role; non-admins cannot
- Scoped user lookup supports non-admin visibility without global exposure
- Guardrails prevent last admin/CRO demotion
  </success_criteria>

<output>
After completion, create `.planning/phases/151-audit-resolution/151-10-SUMMARY.md`:

# Phase 151 Plan 10: Access Guardrails + Scoped User Lookup Summary

**[One-line summary of access guardrails and scoped user listing]**

## Accomplishments

- [Role changes restricted to admin/CRO with guardrails]
- [Scoped user lookup endpoint added]

## Files Created/Modified

- `backend/app/api/v1/endpoints/access.py` - Role-change gating and guardrails
- `backend/app/api/v1/endpoints/users.py` - Scoped user lookup endpoint
- `backend/app/schemas/user.py` - User lookup schema
- `backend/tests/test_access_management.py` - Guardrail tests
- `backend/tests/test_users.py` - Lookup tests

## Decisions Made

None.

## Issues Encountered

[None]

## Next Step

Ready for Phase 151 Plan 11 (frontend access management + scoped user pickers)
</output>
