---
phase: 151-audit-resolution
type: execute
---

<objective>
Harden lookup access and default role safety, and ensure approvals permissions are seeded.

Purpose: Prevent unscoped metadata leakage and eliminate privileged role fallback during directory sync.
Output: Secured lookup endpoint, safe default role resolution, and approvals:write permission seeded.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/150-audit/150-02-FINDINGS.md
@backend/app/api/v1/endpoints/lookups.py
@backend/app/services/directory_sync_service.py
@backend/app/db/seed.py
@backend/app/core/permissions.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Secure lookup filters with auth and department scoping</name>
  <files>backend/app/api/v1/endpoints/lookups.py</files>
  <action>Require current_user via deps.get_current_user and scope Risk.process/category queries by the user's allowed department ids (get_user_department_ids). For privileged users, allow global values. Keep output sorted and exclude nulls.</action>
  <verify>Manual: confirm a department-scoped user only sees processes/categories from their departments.</verify>
  <done>Lookups endpoint is authenticated and returns scoped values for non-privileged users.</done>
</task>

<task type="auto">
  <name>Task 2: Remove privileged default role fallback and seed approvals permission</name>
  <files>backend/app/services/directory_sync_service.py, backend/app/db/seed.py</files>
  <action>Update _resolve_default_role to return the first non-privileged candidate (employee/control_owner/viewer) and raise if none exist; remove fallback to the first role by id. Add approvals:write to PERMISSIONS in seed.py so RolePermission mappings referencing approvals:write can be created.</action>
  <verify>pytest backend/tests/test_directory_sync.py</verify>
  <done>Directory sync fails fast without a safe default role and approvals permission is seeded.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Lookups endpoint requires auth and is department-scoped
- [ ] Default role resolution never falls back to privileged roles
- [ ] Approvals permission is present in seed data
</verification>

<success_criteria>

- All tasks completed
- Lookup metadata is no longer unscoped or unauthenticated
- Safe default role is enforced for directory sync
  </success_criteria>

<output>
After completion, create `.planning/phases/151-audit-resolution/151-03-SUMMARY.md`:

# Phase 151 Plan 03: Lookup and Default Role Safety Summary

**[One-line summary of lookup security and role safety changes]**

## Accomplishments

- [Lookups endpoint is authenticated and scoped]
- [Directory sync default role fallback removed]
- [Approvals permission seeded]

## Files Created/Modified

- `backend/app/api/v1/endpoints/lookups.py` - Auth and scoping
- `backend/app/services/directory_sync_service.py` - Default role resolution
- `backend/app/db/seed.py` - Approvals permission seeding

## Decisions Made

[None]

## Issues Encountered

[None]

## Next Step

Ready for 151-04-PLAN.md
</output>
