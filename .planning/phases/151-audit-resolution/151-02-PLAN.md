---
phase: 151-audit-resolution
type: execute
---

<objective>
Align dashboard metrics with archived filtering and make control trend errors observable.

Purpose: Ensure executive metrics match list views and avoid silent failures in trend data.
Output: Dashboard endpoints exclude archived items by default and surface control trend errors in logs or response signals.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/150-audit/150-02-FINDINGS.md
@backend/app/api/v1/endpoints/dashboard.py
@backend/tests/test_dashboard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exclude archived items from dashboard metrics</name>
  <files>backend/app/api/v1/endpoints/dashboard.py, backend/tests/test_dashboard.py</files>
  <action>Add include_archived (default false) to /dashboard/summary, /dashboard/departments, /dashboard/risk-distribution, and /dashboard/risks-by-cell. When include_archived is false, add Risk.status != archived and Control.status != archived to every count/aggregation (including by-status breakdowns). Keep department scoping intact and ensure queries use the same filter set across endpoints.</action>
  <verify>pytest backend/tests/test_dashboard.py</verify>
  <done>Dashboard metrics exclude archived risks/controls by default and remain consistent across endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Log and signal control trend errors</name>
  <files>backend/app/api/v1/endpoints/dashboard.py</files>
  <action>Add structured logging to get_control_trends so exceptions are not silent. Keep the zero-data path returning [] without logging, but on exceptions log with logger.exception and set a response header (e.g., X-Control-Trends-Error: 1) or raise HTTPException with a clear detail. Do not change the success response shape.</action>
  <verify>Manual: trigger a known bad filter and confirm the error is logged and signaled without returning a misleading success.</verify>
  <done>Control trend failures are detectable via logs or response signal, not silently swallowed.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Dashboard metrics exclude archived items by default
- [ ] Control trend errors are logged and signaled
</verification>

<success_criteria>

- All tasks completed
- Dashboard metrics align with list endpoint filters
- Control trend failures are observable
  </success_criteria>

<output>
After completion, create `.planning/phases/151-audit-resolution/151-02-SUMMARY.md`:

# Phase 151 Plan 02: Dashboard Metrics Alignment Summary

**[One-line summary of dashboard filtering and error handling changes]**

## Accomplishments

- [Dashboard metrics exclude archived items by default]
- [Control trend errors are logged and signaled]

## Files Created/Modified

- `backend/app/api/v1/endpoints/dashboard.py` - Archived filtering and error handling
- `backend/tests/test_dashboard.py` - Coverage adjustments if needed

## Decisions Made

[None]

## Issues Encountered

[None]

## Next Step

Ready for 151-03-PLAN.md
</output>
