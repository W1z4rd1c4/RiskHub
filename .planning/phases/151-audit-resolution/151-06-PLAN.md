---
phase: 151-audit-resolution
type: execute
---

<objective>
Align execution result enums and risk list data with backend behavior.

Purpose: Fix enum mismatches and remove incorrect KRI counting from paged data.
Output: Frontend execution result values match backend enums, risk list uses server-provided KRI data, and critical filtering is server-side.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/150-audit/150-03-FINDINGS.md
@frontend/src/services/executionApi.ts
@frontend/src/pages/AuditTrailPage.tsx
@frontend/src/components/executions/ExecutionHistory.tsx
@frontend/src/components/executions/ExecutionLogModal.tsx
@frontend/src/pages/DepartmentDetailPage.tsx
@frontend/src/pages/RisksPage.tsx
@frontend/src/services/riskApi.ts
@frontend/src/types/risk.ts
@backend/app/api/v1/endpoints/risks.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align execution result enums across frontend</name>
  <files>frontend/src/services/executionApi.ts, frontend/src/pages/AuditTrailPage.tsx, frontend/src/components/executions/ExecutionHistory.tsx, frontend/src/components/executions/ExecutionLogModal.tsx, frontend/src/pages/DepartmentDetailPage.tsx</files>
  <action>Update ExecutionResult to match backend enums (passed/failed/warning/not_applicable). Adjust filters, labels, and icon mappings in AuditTrailPage, ExecutionHistory, ExecutionLogModal, and DepartmentDetailPage. Ensure new executions are submitted with backend values.</action>
  <verify>npm run test:run</verify>
  <done>Execution status filters and badges match backend enum values.</done>
</task>

<task type="auto">
  <name>Task 2: Use backend KRI summary fields in risk list</name>
  <files>frontend/src/pages/RisksPage.tsx, frontend/src/services/riskApi.ts, frontend/src/types/risk.ts</files>
  <action>Remove per-risk KRI fetches and use risk list fields (kri_count, has_breach) returned by the backend. Preserve control_count logic and keep pagination totals accurate.</action>
  <verify>Manual: risk list KRI counts match backend list response without additional KRI calls.</verify>
  <done>Risk list KRI counts and breach flags are accurate regardless of KRI volume.</done>
</task>

<task type="auto">
  <name>Task 3: Add server-side critical filter for risks</name>
  <files>backend/app/api/v1/endpoints/risks.py, frontend/src/services/riskApi.ts, frontend/src/pages/RisksPage.tsx</files>
  <action>Add a critical boolean (or min_net_score) filter to list_risks that applies Risk.net_score >= 15. Wire the frontend critical filter to the new server-side parameter so totals reflect the full dataset.</action>
  <verify>pytest backend/tests/test_risks.py -k critical</verify>
  <done>Critical filter applies across all pages with accurate totals.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Execution result enums match backend values
- [ ] Risk list KRI counts are based on backend summary fields
- [ ] Critical filter uses server-side totals
</verification>

<success_criteria>

- All tasks completed
- Execution status UI matches backend enum values
- Risk list data is accurate and performant
  </success_criteria>

<output>
After completion, create `.planning/phases/151-audit-resolution/151-06-SUMMARY.md`:

# Phase 151 Plan 06: Enum Alignment and Risk List Fixes Summary

**[One-line summary of enum alignment and risk list fixes]**

## Accomplishments

- [Execution result enums aligned with backend]
- [Risk list uses backend KRI summaries]
- [Critical filter moved server-side]

## Files Created/Modified

- `frontend/src/services/executionApi.ts` - Enum alignment
- `frontend/src/pages/AuditTrailPage.tsx` - Result filters and labels
- `frontend/src/components/executions/ExecutionHistory.tsx` - Status mapping
- `frontend/src/components/executions/ExecutionLogModal.tsx` - Result options
- `frontend/src/pages/DepartmentDetailPage.tsx` - Result badges
- `frontend/src/pages/RisksPage.tsx` - KRI summary usage
- `frontend/src/services/riskApi.ts` - Critical filter param
- `backend/app/api/v1/endpoints/risks.py` - Critical filter implementation

## Decisions Made

[None]

## Issues Encountered

[None]

## Next Step

Ready for 151-07-PLAN.md
</output>
