---
phase: 151-audit-resolution
type: execute
---

<objective>
Expose KRI historization corrections and overdue visibility in the UI.

Purpose: Make overdue KRIs visible on KRI/risk views and the dashboard, and add a correction request flow that respects permissions.
Output: Overdue widget + badges, correction modal, and updated KRI history interactions.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/151-audit-resolution/151-CONTEXT.md
@frontend/src/types/kri.ts
@frontend/src/services/kriApi.ts
@frontend/src/components/kri/KRIValueModal.tsx
@frontend/src/pages/KRIDetailPage.tsx
@frontend/src/components/kri/KRIGaugeCard.tsx
@frontend/src/pages/RiskDetailPage.tsx
@frontend/src/pages/DashboardPage.tsx
@frontend/src/components/dashboard/KRIBreachWidget.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add overdue visibility on KRI, Risk, and Dashboard views</name>
  <files>frontend/src/types/kri.ts, frontend/src/services/kriApi.ts, frontend/src/components/kri/KRIGaugeCard.tsx, frontend/src/pages/RiskDetailPage.tsx, frontend/src/pages/KRIDetailPage.tsx, frontend/src/components/dashboard/KRIOverdueWidget.tsx, frontend/src/pages/DashboardPage.tsx</files>
  <action>
    Extend KRI types to include due_date/overdue metadata returned by the backend (or map from /kris/overdue). Update kriApi.getOverdue to accept department filter if supported.
    Add a new KRIOverdueWidget that fetches /kris/overdue and lists the most overdue items with days_overdue and links to the KRI detail page; wire it into DashboardPage alongside the breach widget and respect dashboard filters.
    Add overdue badges to KRI detail header and KRIGaugeCard (e.g., amber “OVERDUE”), and surface overdue status in RiskDetailPage’s KRI section by mapping overdue KRIs by kri_id.
    Keep empty/loading states consistent with existing dashboard widgets and avoid extra fetches by caching overdue results per filter change.
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <done>Overdue KRIs are visible on KRI detail, risk KRI cards, and the dashboard.</done>
</task>

<task type="auto">
  <name>Task 2: Add KRI history correction UI and privilege-aware backdating</name>
  <files>frontend/src/components/kri/KRIHistoryEditModal.tsx, frontend/src/pages/KRIDetailPage.tsx, frontend/src/components/kri/KRIValueModal.tsx</files>
  <action>
    Create KRIHistoryEditModal to collect new value + reason and call kriApi.requestHistoryEdit; handle 200 vs 202 responses (immediate update vs approval request) and refresh history on success.
    Add “Request Correction” actions in the KRI history view (timeline or a compact table/list) and pass the selected history entry into the modal.
    Update KRIValueModal to show backdating controls only for users who can resolve approvals (use permission helper rather than role-name checks).
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <done>Users can request corrections, and backdating is shown only to privileged accounts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Overdue KRI visibility + history correction workflow</what-built>
  <how-to-verify>
    1. Run: `cd backend && uvicorn app.main:app --reload`
    2. Run: `cd frontend && npm run dev`
    3. Visit: http://localhost:5173/dashboard and confirm the Overdue KRIs widget renders
    4. Visit: http://localhost:5173/kris/{id} and confirm overdue badge + correction action
    5. Submit a correction request and verify a success/approval message appears
    6. Visit: http://localhost:5173/risks/{id} and confirm overdue badge appears on KRI cards
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] TypeScript check passes
- [ ] Overdue widget renders with correct counts
- [ ] History correction flow triggers approval or immediate update
</verification>

<success_criteria>

- Overdue KRIs are visible on KRI detail, risk cards, and dashboard
- History correction requests are available and wired to the API
- Backdating is visible only to privileged users
  </success_criteria>

<output>
After completion, create `.planning/phases/151-audit-resolution/151-09-SUMMARY.md`:

# Phase 151 Plan 09: KRI Overdue + Correction UI Summary

**[One-line summary of overdue visibility + correction workflow]**

## Accomplishments

- [Overdue widget + badges wired to /kris/overdue]
- [History correction modal integrated into KRI detail]
- [Privilege-aware backdating UI]

## Files Created/Modified

- `frontend/src/components/dashboard/KRIOverdueWidget.tsx` - Overdue dashboard widget
- `frontend/src/pages/DashboardPage.tsx` - Widget integration
- `frontend/src/pages/KRIDetailPage.tsx` - Overdue badge + correction actions
- `frontend/src/components/kri/KRIHistoryEditModal.tsx` - Correction modal
- `frontend/src/components/kri/KRIValueModal.tsx` - Privileged backdating gating
- `frontend/src/components/kri/KRIGaugeCard.tsx` - Overdue badge
- `frontend/src/pages/RiskDetailPage.tsx` - Overdue badge mapping
- `frontend/src/services/kriApi.ts` - Overdue fetch updates
- `frontend/src/types/kri.ts` - Overdue/due_date typings

## Decisions Made

None.

## Issues Encountered

[None]

## Next Step

Ready for Phase 151 Plan 10 (access management guardrails + scoped user lookup)
</output>
