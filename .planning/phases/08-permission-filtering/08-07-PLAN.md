<objective>
Fix issues identified during Phase 8 review by 5 subagents.

Purpose: Address high-priority bugs and gaps in approval workflow logic, frontend robustness, and test coverage.
Output: Consistent endpoint behavior, resilient UI, and comprehensive tests.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-plan.md
@.planning/phases/08-permission-filtering/08-07-SUMMARY.md
</execution_context>

<context>
@.planning/phases/08-permission-filtering/08-06-SUMMARY.md
@backend/app/api/v1/endpoints/approvals.py
@frontend/src/pages/ApprovalsPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harmonize Control Deletion Status</name>
  <files>backend/app/api/v1/endpoints/approvals.py</files>
  <action>
When approving a CONTROL deletion, set status to `archived` (not `inactive`) to match direct delete behavior.
Currently line ~229-241 sets `inactive`, while direct delete in controls.py sets `archived`.
  </action>
  <verify>`pytest backend/tests/test_approval_workflow.py` passes</verify>
  <done>Approved control deletions now set `archived` status</done>
</task>

<task type="auto">
  <name>Task 2: Block Updates During Pending Delete</name>
  <files>
    backend/app/api/v1/endpoints/risks.py
    backend/app/api/v1/endpoints/controls.py
    backend/app/api/v1/endpoints/kris.py
  </files>
  <action>
In update (PATCH) endpoints, check for ANY pending approval (including DELETE), not just EDIT.
Return 409 Conflict if a delete approval is already pending.
  </action>
  <verify>Manual test: create delete request, then try to PATCH → expect 409</verify>
  <done>Update endpoints reject changes when delete is pending</done>
</task>

<task type="auto">
  <name>Task 3: Fix Frontend Double-Submit</name>
  <files>frontend/src/pages/ApprovalsPage.tsx</files>
  <action>
- Add early guard in `handleResolve` to return if `isSubmitting` is already true.
- Disable approve/reject buttons using `disabled={isSubmitting}`.
  </action>
  <verify>Manual: rapidly double-click Approve button → only one request sent</verify>
  <done>Double-submit prevented in approval resolution</done>
</task>

<task type="auto">
  <name>Task 4: Add Frontend Error State</name>
  <files>frontend/src/pages/ApprovalsPage.tsx</files>
  <action>
- Add `error` state variable.
- On `fetchApprovals` failure, set error state and display a banner.
- Clear error on successful fetch.
  </action>
  <verify>Manual: simulate API failure → error banner appears</verify>
  <done>Error state displayed when list fails to load</done>
</task>

<task type="auto">
  <name>Task 5: Add Permission Edge Case Tests</name>
  <files>backend/tests/test_approvals.py</files>
  <action>
Add tests for:
- Non-approver cannot approve/reject others' requests (403).
- Cannot cancel already-resolved request (400).
- Invalid status transitions return errors.
  </action>
  <verify>`pytest backend/tests/test_approvals.py` passes with new tests</verify>
  <done>Permission and transition edge cases covered</done>
</task>

</tasks>

<verification>
- [ ] All backend tests pass: `pytest backend/tests/`
- [ ] Manual E2E: Double-click approve → no duplicate
- [ ] Manual E2E: Error banner on API failure
- [ ] Manual E2E: PATCH during pending delete → 409
</verification>

<success_criteria>
- Control deletion via approval sets `archived` status.
- Updates blocked when delete is pending.
- No duplicate submissions from UI.
- Clear error feedback on failure.
- Expanded test coverage for edge cases.
</success_criteria>

<output>
Create `.planning/phases/08-permission-filtering/08-07-SUMMARY.md`
</output>
