<objective>
Refinement and Optimization of the backend and frontend components.
Purpose: Address technical debt, modernize patterns, and improve performance.
Output: Pydantic V2 compliant codebase, timezone-aware timestamps, and optimized frontend polling.
</objective>

<execution_context>
@.planning/phases/08-permission-filtering/08-06-SUMMARY.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Pydantic V2 Migration</name>
  <files>
    backend/app/core/config.py
    backend/app/schemas/user.py
    backend/app/schemas/approval_request.py
    backend/app/schemas/execution.py
  </files>
  <action>
    - Migrate `class Config` to `model_config` Dict pattern.
    - Update `orm_mode = True` to `from_attributes = True`.
  </action>
</task>

<task type="auto">
  <name>Task 2: UTC Deprecation Fixes</name>
  <files>
    backend/app/api/v1/endpoints/approvals.py
    backend/app/api/v1/endpoints/controls.py
    backend/app/core/security.py
    backend/app/db/seed.py
    backend/app/models/approval_request.py
  </files>
  <action>
    - Replace `datetime.utcnow()` with `datetime.now(UTC)`.
    - Ensure all timestamp defaults use timezone-aware objects.
  </action>
</task>

<task type="auto">
  <name>Task 3: Sidebar Badge Optimization</name>
  <files>frontend/src/components/layout/Sidebar.tsx</files>
  <action>
    - Implement a 60-second polling interval for `pendingCount` instead of fetching on every navigation.
  </action>
</task>

<task type="auto">
  <name>Task 4: Standardize JSON Field Handling</name>
  <files>
    backend/app/models/approval_request.py
    backend/app/api/v1/endpoints/risks.py
    backend/app/api/v1/endpoints/approvals.py
  </files>
  <action>
    - Migrate `ApprovalRequest.pending_changes` from `Text` to native `JSON` type.
    - Remove manual `json.dumps` and `json.loads` calls in the API layer.
  </action>
</task>

</tasks>

<verification>
- [ ] All backend tests pass: `pytest backend/tests/`
- [ ] No more Pydantic V2 deprecation warnings.
- [ ] Sidebar badge updates periodically via polling.
</verification>
