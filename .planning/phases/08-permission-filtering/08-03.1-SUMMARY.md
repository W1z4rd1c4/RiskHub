# Plan 08-03.1 Summary: Edit Approval Backend

**Extended approval workflow to support edit operations on critical items and sensitive fields.**

## Changes Made

### Model & Schema Extensions
- Added `ApprovalActionType` enum (DELETE, EDIT)
- Added `action_type` and `pending_changes` fields to `ApprovalRequest`
- Migration `49d28c3c644e` applied

### Permission Helpers
- `is_critical_risk(risk)` - Returns true if `is_priority=True` OR `net_score >= 15`
- `has_sensitive_field_changes()` - Detects changes to owner, department, category, is_priority

### Approval Endpoints
- Updated `approve_request` to auto-apply pending changes for EDIT approvals
- Response now includes `action_type` and `pending_changes`

### PATCH Endpoint Changes

| Endpoint | Approval Triggers |
|----------|-------------------|
| `PATCH /risks/{id}` | Sensitive field changes (owner, dept, category, is_priority→false) |
| `PATCH /controls/{id}` | Linked to critical risk OR sensitive field changes (owner, dept) |
| `PUT /kris/{id}` | Linked to critical risk |

## Behavior

- **Privileged users** (Risk Manager, CRO, Admin): Edit immediately
- **Non-privileged users**: 
  - Trigger approval → 202 with `approval_id`
  - Item stays visible, changes stored as pending
  - On approval → changes auto-applied

## Files Modified

- `backend/app/models/approval_request.py`
- `backend/app/schemas/approval_request.py`
- `backend/app/core/permissions.py`
- `backend/app/api/v1/endpoints/approvals.py`
- `backend/app/api/v1/endpoints/risks.py`
- `backend/app/api/v1/endpoints/controls.py`
- `backend/app/api/v1/endpoints/kris.py`
- `backend/alembic/versions/49d28c3c644e_add_approval_edit_support.py`

---
*Completed: 2025-12-27*
