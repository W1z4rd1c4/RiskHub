---
phase: 09-notification-system
plan: 04
type: execute
---

<objective>
Build frontend notification UI with bell icon, dropdown panel, and notification center page.

Purpose: Enable users to view and manage their in-app notifications.
Output: Bell icon in header with badge, dropdown panel, and full `/notifications` page.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-plan.md
@.planning/phases/09-notification-system/09-CONTEXT.md
@.agent/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@frontend/src/services/approvalsApi.ts (reference for API service patterns)
@frontend/src/components/layout/Sidebar.tsx (badge count pattern from approvals)
@frontend/src/types/approval.ts (reference for type patterns)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create notification types and API service</name>
  <files>frontend/src/types/notification.ts, frontend/src/services/notificationsApi.ts</files>
  <action>
    Create TypeScript types:
    ```typescript
    // types/notification.ts
    export type NotificationType = 
      | 'approval_pending' 
      | 'approval_resolved' 
      | 'kri_due_soon' 
      | 'kri_due_tomorrow' 
      | 'kri_overdue' 
      | 'kri_near_breach';
    
    export interface Notification {
      id: number;
      type: NotificationType;
      title: string;
      message: string;
      resource_type?: string;
      resource_id?: number;
      is_read: boolean;
      created_at: string;
    }
    
    export interface NotificationListResponse {
      items: Notification[];
      total: number;
      skip: number;
      limit: number;
      unread_count: number;
    }
    ```
    
    Create API service with functions:
    - `getNotifications(params)` - List with pagination and unread_only filter
    - `getUnreadCount()` - For badge
    - `markAsRead(id)` - Single notification
    - `markAllAsRead()` - Bulk mark
    
    Use existing apiClient with authentication.
  </action>
  <verify>`npx tsc --noEmit` passes</verify>
  <done>Types and API service created with 4 functions</done>
</task>

<task type="auto">
  <name>Task 2: Create NotificationBell component with dropdown</name>
  <files>frontend/src/components/notifications/NotificationBell.tsx</files>
  <action>
    Create component with:
    - Bell icon (use lucide-react BellIcon)
    - Red badge showing unread count (hide if 0)
    - Dropdown panel on click showing recent unread notifications (max 5)
    - "Mark all as read" button in dropdown footer
    - "View all" link to /notifications page
    - Auto-refresh unread count every 60 seconds (same pattern as Sidebar approval badge)
    
    Style with glassmorphism matching existing UI:
    - Dropdown: backdrop-blur, semi-transparent background
    - Notification items: hover state, truncated message preview
    
    Each notification item shows:
    - Icon based on type (AlertCircle for KRI, CheckCircle for approved, etc.)
    - Title (bold)
    - Message preview (truncated)
    - Time ago (e.g., "5 minutes ago")
    
    Click on notification navigates to relevant resource and marks as read.
  </action>
  <verify>Component renders: Check `/notifications` route in browser</verify>
  <done>NotificationBell with dropdown, badge, and navigation</done>
</task>

<task type="auto">
  <name>Task 3: Add NotificationBell to header/sidebar</name>
  <files>frontend/src/components/layout/Sidebar.tsx</files>
  <action>
    Import and add NotificationBell component:
    - Place in header area next to user profile/logout
    - Should be visible on all authenticated pages
    - Position: right side of header, before user menu
    
    Ensure it doesn't conflict with existing "Workflow" badge logic.
    May need to adjust layout/spacing.
  </action>
  <verify>Bell icon visible in header after login</verify>
  <done>NotificationBell appears in header on all pages</done>
</task>

<task type="auto">
  <name>Task 4: Create NotificationsPage component</name>
  <files>frontend/src/pages/NotificationsPage.tsx, frontend/src/App.tsx</files>
  <action>
    Create full notifications page:
    - Title: "Notifications"
    - Tabs: "All", "Unread"
    - List of notifications with full message (not truncated)
    - Each notification shows: type icon, title, full message, timestamp
    - Mark as read button (or auto-mark when viewing)
    - Pagination (load more or page numbers)
    - Empty state: "No notifications yet"
    - Click navigates to resource (risk/control/kri/approval)
    
    Add route in App.tsx:
    `<Route path="/notifications" element={<NotificationsPage />} />`
    
    Match existing page styling (glassmorphism cards, dark theme).
  </action>
  <verify>Navigate to /notifications, see list of notifications</verify>
  <done>Full notifications page with tabs, list, and navigation</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Notification bell icon with dropdown and full notifications page</what-built>
  <how-to-verify>
    1. Run: `cd frontend && npm run dev`
    2. Login as any user
    3. Verify: Bell icon visible in header with badge count
    4. Click bell: Dropdown shows recent notifications (may be empty initially)
    5. Click "View all": Navigates to /notifications page
    6. Verify: Page shows tabs (All/Unread) and notification list
    7. Create an approval request to generate a notification (delete a risk as Employee)
    8. Verify: Badge count increments, notification appears in dropdown
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npx tsc --noEmit` passes
- [ ] NotificationBell renders in header with correct styling
- [ ] Dropdown shows notifications with proper formatting
- [ ] /notifications page loads with tabs and list
- [ ] Mark as read updates unread count
</verification>

<success_criteria>
- NotificationBell component with badge and dropdown
- Bell integrated into header/sidebar
- Full /notifications page with tabs
- Navigation from notification to resource works
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-notification-system/09-04-SUMMARY.md`
</output>
