---
phase: 09-notification-system
plan: 07
type: execute
---

<objective>
Add Notifications tab to user Settings page for managing notification preferences.

Purpose: Allow users to toggle notifications per type through a polished UI.
Output: NotificationSettings component, Settings tab integration, API wiring, i18n translations.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-plan.md
@.planning/phases/09-notification-system/09-CONTEXT.md
@.planning/phases/09-notification-system/09-06-PLAN.md
</execution_context>

<context>
@.planning/PROJECT.md
@frontend/src/pages/SettingsPage.tsx (existing tabs structure)
@frontend/src/components/settings/index.ts
@frontend/src/services/notificationsApi.ts
@frontend/src/types/notification.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TypeScript types for notification preferences</name>
  <files>frontend/src/types/notification.ts</files>
  <action>
    Add to existing notification.ts:
    
    ```typescript
    export interface NotificationPreferences {
      approval_pending: boolean;
      approval_resolved: boolean;
      kri_due_soon: boolean;
      kri_due_tomorrow: boolean;
      kri_overdue: boolean;
      kri_near_breach: boolean;
      kri_breach_detected: boolean;
    }
    
    export type NotificationPreferencesUpdate = Partial<NotificationPreferences>;
    ```
  </action>
  <verify>`cd frontend && npx tsc --noEmit`</verify>
  <done>TypeScript types for preferences defined</done>
</task>

<task type="auto">
  <name>Task 2: Add API methods for notification preferences</name>
  <files>frontend/src/services/notificationsApi.ts</files>
  <action>
    Add to existing notificationsApi.ts:
    
    ```typescript
    async getPreferences(): Promise<NotificationPreferences> {
      const response = await apiClient.get<NotificationPreferences>('/notifications/preferences');
      return response.data;
    },
    
    async updatePreferences(updates: NotificationPreferencesUpdate): Promise<NotificationPreferences> {
      const response = await apiClient.put<NotificationPreferences>('/notifications/preferences', updates);
      return response.data;
    },
    ```
    
    Import NotificationPreferences, NotificationPreferencesUpdate types.
  </action>
  <verify>`cd frontend && npx tsc --noEmit`</verify>
  <done>API methods for get/update preferences added</done>
</task>

<task type="auto">
  <name>Task 3: Create NotificationSettings component</name>
  <files>frontend/src/components/settings/NotificationSettings.tsx</files>
  <action>
    Create component with:
    - Fetch preferences on mount using `notificationsApi.getPreferences()`
    - Display toggles for each notification type in two groups:
      1. **Approval Notifications**: approval_pending, approval_resolved
      2. **KRI Notifications**: kri_due_soon, kri_due_tomorrow, kri_overdue, kri_near_breach, kri_breach_detected
    - Use Switch/Toggle component (or styled checkbox) for each setting
    - Optimistic updates: Toggle immediately, call API, rollback on error
    - Show loading skeleton while fetching
    - Use `useTranslation('settings')` for labels
    
    Style to match existing settings components (glass-card sections, similar spacing).
    Include icons: Bell for approvals, AlertTriangle for KRI warnings.
  </action>
  <verify>Component renders without errors in browser</verify>
  <done>NotificationSettings component with grouped toggles and API integration</done>
</task>

<task type="auto">
  <name>Task 4: Export NotificationSettings from settings index</name>
  <files>frontend/src/components/settings/index.ts</files>
  <action>
    Add export:
    ```typescript
    export { NotificationSettings } from './NotificationSettings';
    ```
  </action>
  <verify>`cd frontend && npx tsc --noEmit`</verify>
  <done>NotificationSettings exported from barrel file</done>
</task>

<task type="auto">
  <name>Task 5: Add Notifications tab to SettingsPage</name>
  <files>frontend/src/pages/SettingsPage.tsx</files>
  <action>
    1. Import NotificationSettings and Bell icon:
       `import { Bell } from 'lucide-react';`
    
    2. Update TabId type:
       `type TabId = 'profile' | 'appearance' | 'localization' | 'notifications' | 'documentation';`
    
    3. Add tab to tabs array (after localization, before documentation):
       `{ id: 'notifications' as TabId, label: t('tabs.notifications', 'Notifications'), icon: Bell },`
    
    4. Add tab content rendering:
       `{activeTab === 'notifications' && <NotificationSettings />}`
  </action>
  <verify>Tab visible in Settings page, content loads</verify>
  <done>Notifications tab added to Settings page</done>
</task>

<task type="auto">
  <name>Task 6: Add i18n translations</name>
  <files>frontend/public/locales/en/settings.json, frontend/public/locales/cs/settings.json</files>
  <action>
    Add translation keys to both locale files:
    
    **English (en/settings.json):**
    ```json
    "tabs": {
      "notifications": "Notifications"
    },
    "notifications": {
      "title": "Notification Preferences",
      "subtitle": "Choose which notifications you want to receive",
      "section_approval": "Approval Notifications",
      "section_kri": "KRI Notifications",
      "approval_pending": "New approval requests",
      "approval_pending_desc": "When a new request needs your approval",
      "approval_resolved": "Request resolutions",
      "approval_resolved_desc": "When your requests are approved or rejected",
      "kri_due_soon": "KRI due reminders",
      "kri_due_soon_desc": "Reminder before KRI reporting deadline",
      "kri_due_tomorrow": "KRI deadline alerts",
      "kri_due_tomorrow_desc": "When KRI reporting is due tomorrow",
      "kri_overdue": "KRI overdue alerts",
      "kri_overdue_desc": "When KRI reporting is overdue",
      "kri_near_breach": "KRI approaching limits",
      "kri_near_breach_desc": "When KRI values approach threshold limits",
      "kri_breach_detected": "KRI breach alerts",
      "kri_breach_detected_desc": "When KRI values exceed limits"
    }
    ```
    
    **Czech (cs/settings.json):**
    ```json
    "tabs": {
      "notifications": "Notifikace"
    },
    "notifications": {
      "title": "Nastavení notifikací",
      "subtitle": "Vyberte, které notifikace chcete dostávat",
      "section_approval": "Schvalovací notifikace",
      "section_kri": "KRI notifikace",
      "approval_pending": "Nové žádosti o schválení",
      "approval_pending_desc": "Když nová žádost vyžaduje vaše schválení",
      "approval_resolved": "Výsledky žádostí",
      "approval_resolved_desc": "Když jsou vaše žádosti schváleny nebo zamítnuty",
      "kri_due_soon": "Připomínky KRI",
      "kri_due_soon_desc": "Připomínka před termínem vykazování KRI",
      "kri_due_tomorrow": "Upozornění na termín KRI",
      "kri_due_tomorrow_desc": "Když je vykazování KRI zítra",
      "kri_overdue": "Upozornění na opožděné KRI",
      "kri_overdue_desc": "Když je vykazování KRI po termínu",
      "kri_near_breach": "KRI blížící se limitům",
      "kri_near_breach_desc": "Když hodnoty KRI dosahují prahových limitů",
      "kri_breach_detected": "Upozornění na překročení KRI",
      "kri_breach_detected_desc": "Když hodnoty KRI překračují limity"
    }
    ```
  </action>
  <verify>No missing translation warnings in console</verify>
  <done>English and Czech translations added for all notification preference labels</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Notification Settings tab in user Settings page with toggles for all 7 notification types</what-built>
  <how-to-verify>
    1. Run: `cd frontend && npm run dev`
    2. Login as any user
    3. Navigate to Settings page (gear icon or /settings)
    4. Click "Notifications" tab
    5. Verify: Two sections displayed (Approval Notifications, KRI Notifications)
    6. Verify: 7 toggles with labels and descriptions
    7. Toggle one setting off
    8. Refresh page - verify setting persisted
    9. Switch language to Czech - verify all labels translated
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run build` succeeds
- [ ] `cd frontend && npx tsc --noEmit` passes
- [ ] Notifications tab visible in Settings
- [ ] Toggles persist across page refresh
- [ ] Both English and Czech translations work
</verification>

<success_criteria>
- NotificationSettings component created with all 7 toggles
- Tab integrated into SettingsPage
- API calls to get/update preferences working
- UI matches existing settings design aesthetic
- i18n complete for English and Czech
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-notification-system/09-07-SUMMARY.md`
</output>
