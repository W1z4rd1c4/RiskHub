---
phase: 09-notification-system
plan: 01
type: execute
---

<objective>
Create Notification model, schemas, and database migration for the in-app notification system.

Purpose: Establish the data layer for storing and retrieving user notifications.
Output: Notification SQLAlchemy model, Pydantic schemas, and applied Alembic migration.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-plan.md
@.planning/phases/09-notification-system/09-CONTEXT.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/app/models/approval_request.py (reference for enum patterns)
@backend/app/schemas/approval_request.py (reference for Pydantic V2 patterns)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Notification model with NotificationType enum</name>
  <files>backend/app/models/notification.py</files>
  <action>
    Create SQLAlchemy model with:
    - id: Integer primary key
    - user_id: Integer FK to users.id (recipient), index it
    - type: Enum (NotificationType with values: APPROVAL_PENDING, APPROVAL_RESOLVED, KRI_DUE_SOON, KRI_DUE_TOMORROW, KRI_OVERDUE, KRI_NEAR_BREACH)
    - title: String(255)
    - message: Text
    - resource_type: String(50), nullable (e.g., 'risk', 'control', 'kri', 'approval')
    - resource_id: Integer, nullable
    - is_read: Boolean, default False, index it
    - created_at: DateTime with timezone, default now(UTC)
    - expires_at: DateTime with timezone, nullable
    
    Add relationship to User model.
    Use Pydantic V2 compatible patterns (from app.models.approval_request reference).
    Add composite index on (user_id, is_read) for efficient unread count queries.
  </action>
  <verify>Python import succeeds: `python -c "from app.models.notification import Notification, NotificationType"`</verify>
  <done>Model class defined with all fields, enum, and indexes</done>
</task>

<task type="auto">
  <name>Task 2: Create Pydantic schemas for Notification</name>
  <files>backend/app/schemas/notification.py</files>
  <action>
    Create Pydantic V2 schemas using model_config and from_attributes:
    - NotificationBase: Shared fields (title, message, resource_type, resource_id)
    - NotificationCreate: Internal use for creating notifications (adds user_id, type)
    - NotificationRead: Full response schema with id, is_read, created_at, type as string
    - NotificationListResponse: Paginated response {items, total, skip, limit, unread_count}
    - NotificationTypeEnum: StrEnum for API serialization
    
    Follow patterns from approval_request.py schemas.
    Include type validation for NotificationType enum.
  </action>
  <verify>Python import succeeds: `python -c "from app.schemas.notification import NotificationRead, NotificationCreate"`</verify>
  <done>All schemas defined with Pydantic V2 patterns</done>
</task>

<task type="auto">
  <name>Task 3: Export model and schemas in __init__.py files</name>
  <files>backend/app/models/__init__.py, backend/app/schemas/__init__.py</files>
  <action>
    Add imports and exports for:
    - models/__init__.py: Notification, NotificationType
    - schemas/__init__.py: NotificationCreate, NotificationRead, NotificationListResponse, NotificationTypeEnum
    
    Follow existing export patterns in these files.
  </action>
  <verify>`python -c "from app.models import Notification; from app.schemas import NotificationRead"`</verify>
  <done>Notification accessible from top-level imports</done>
</task>

<task type="auto">
  <name>Task 4: Create and apply Alembic migration</name>
  <files>backend/alembic/versions/[auto]-add_notifications.py</files>
  <action>
    Generate migration with:
    `alembic revision --autogenerate -m "add_notifications"`
    
    Verify migration includes:
    - notifications table with all columns
    - Foreign key to users table
    - Indexes: ix_notifications_user_id, ix_notifications_is_read, composite (user_id, is_read)
    - Enum type for notification_type
    
    Apply migration:
    `alembic upgrade head`
    
    Handle PostgreSQL enum creation properly (CREATE TYPE for notification_type).
  </action>
  <verify>`alembic current` shows head revision, `alembic check` has no pending changes</verify>
  <done>Migration created and applied, notifications table exists</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && python -c "from app.models import Notification"` succeeds
- [ ] `cd backend && python -c "from app.schemas import NotificationRead"` succeeds
- [ ] `alembic current` shows latest migration
- [ ] Database has notifications table with correct schema
</verification>

<success_criteria>
- Notification model with 6 notification types created
- Pydantic V2 schemas following existing patterns
- Migration applied successfully
- No TypeScript/Python type errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-notification-system/09-01-SUMMARY.md`
</output>
