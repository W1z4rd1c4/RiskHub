# Plan 253-06: Frontend Route Export Unification

## Objective

Make route-entry page modules default-export consistently and remove top-level wrapper pages by promoting real implementations into the route entry files.

## Steps

1. Promote Dashboard, Admin Console, and Issue Detail implementations into top-level page files.
2. Split combined form-route modules into explicit route files.
3. Update router lazy imports to plain default imports.

## Verification

- `cd frontend && npm run test:run -- src/pages`
- `cd frontend && npm run lint`
- `cd frontend && npx tsc --noEmit`
