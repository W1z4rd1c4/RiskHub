# Plan 253-05: Approval and Runtime Boundary Cleanup

## Objective

Replace the approval helper bag with a single public orchestration entrypoint, stop DB-driven startup log reconfiguration, and enumerate CORS to actual frontend needs.

## Steps

1. Add a single public approval resolution function in `backend/app/services/approval_execution_service.py`.
2. Update `approvals/resolve.py` to call that entrypoint only.
3. Apply log rotation explicitly in the admin endpoint after persistence.
4. Enumerate CORS methods and headers in `backend/app/main.py`.

## Verification

- `cd backend && venv/bin/pytest -q ../tests/backend/pytest/test_admin_logs.py ../tests/backend/pytest/test_approval_field_whitelist.py ../tests/backend/pytest/test_approval_edit_apply.py ../tests/backend/pytest/test_approvals.py ../tests/backend/pytest/test_approval_workflow.py`
