# Plan 253-08: Benchmark Isolation and Testing Docs

## Objective

Make the default backend regression benchmark-free, add explicit fast-vs-DB-truth targets, and align `docs/TESTING.md` with the simplified CI surface.

## Steps

1. Mark `tests/backend/pytest/test_api_benchmarks.py` with `benchmark`.
2. Exclude `benchmark` from default backend regression commands and the SQLite PR lane.
3. Add `test-fast`, `test-db-contracts`, and `test-benchmarks` targets.
4. Update testing docs to reflect the new public path and the `frontend/src/services/session/` package.

## Verification

- `make -f scripts/Makefile test`
- `make -f scripts/Makefile test-fast`
- `TEST_DATABASE_URL=postgresql+asyncpg://riskhub:riskhub_dev@localhost:5432/riskhub_test make -f scripts/Makefile test-postgres-ci`
