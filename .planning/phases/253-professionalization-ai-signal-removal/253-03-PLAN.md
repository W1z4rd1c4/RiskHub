# Plan 253-03: Repo Surface Cleanup

## Objective

Delete boilerplate code-folder READMEs, remove the empty `backend/app/tests` tree, and split CODEOWNERS by subsystem without inventing fake owners.

## Steps

1. Remove the generic READMEs under `frontend/src/services`, `frontend/src/components`, and `backend/app/tests`.
2. Update README coverage rules to exempt the deleted boilerplate surfaces.
3. Split `.github/CODEOWNERS` by subsystem.

## Verification

- `make -f scripts/Makefile quality-repo-contracts`
- `python3 scripts/security/validate_public_repo_hygiene.py`
