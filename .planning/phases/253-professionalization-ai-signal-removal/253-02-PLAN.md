# Plan 253-02: Governance Demotion

## Objective

Move docs topology, debt budgets, cleanup audits, suppression budgets, and other maintainer-only checks into a separate non-protected workflow and remove non-core PR workflow triggers.

## Steps

1. Add `.github/workflows/maintenance-governance.yml`.
2. Move `startup-smoke.yml` off `pull_request`.
3. Move `release-parity-pr.yml` to manual-only execution.

## Verification

- `cd backend && venv/bin/pytest -q ../tests/backend/pytest/test_workflow_pin_validator.py`
- `python3 scripts/check_docs_contract.py`
