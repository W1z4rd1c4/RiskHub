# Plan 253-04: Backend Bootstrap Collapse

## Objective

Collapse the backend bootstrap barrel and helper split into `backend/app/main.py` while preserving startup, health, security-header, and production-setting behavior.

## Steps

1. Move runtime/bootstrap helper logic into `backend/app/main.py`.
2. Delete `bootstrap.py`, `bootstrap_app.py`, `bootstrap_runtime.py`, and `bootstrap_validation.py`.
3. Replace ownership-preservation bootstrap tests with behavior-level app-factory tests.

## Verification

- `cd backend && venv/bin/pytest -q ../tests/backend/pytest/test_app_factory_contracts.py ../tests/backend/pytest/test_production_hardening.py ../tests/backend/pytest/test_security_headers.py ../tests/backend/pytest/test_health.py`
