# Plan 253-01: Protected CI Simplification

## Objective

Remove docs/process maintenance gates from the PR-required lint workflow and keep only frontend correctness plus repo/security contract checks in the public path.

## Steps

1. Simplify `.github/workflows/lint.yml`.
2. Remove docs-topology, debt-budget, cleanup-audit, suppression-budget, and ratchet checks from the protected path.
3. Update workflow contract tests.

## Verification

- `cd backend && venv/bin/pytest -q ../tests/backend/pytest/test_workflow_pin_validator.py`
- `python3 scripts/security/validate_workflow_pins.py`
- `python3 scripts/security/validate_repo_hardening.py`
