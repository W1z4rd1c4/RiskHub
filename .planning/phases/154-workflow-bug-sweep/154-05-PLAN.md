---
phase: 154-workflow-bug-sweep
type: execute
domain: testing
---

<objective>
Add/extend automated tests (Playwright + pytest as needed) to cover the fixed workflows end-to-end: cross-department Control Detail, link/unlink, KRI history visibility, and approval-queued UX.

Purpose: Prevent regressions in the most business-critical flows: creating and managing risks/controls/KRIs and their relationships.
Output: Updated test suites that fail before fixes and pass after, plus a documented regression run.
</objective>

<context>
@.planning/phases/154-workflow-bug-sweep/DISCOVERY.md
@frontend/e2e/cross-department/control-owner-access.spec.ts
@frontend/e2e/cross-department/link-access.spec.ts
@frontend/e2e/approval-workflows/status-flow.spec.ts
@backend/tests/test_cross_department_access.py
@backend/tests/test_kris_rbac.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend E2E coverage for control-side linking + resilient detail page</name>
  <files>frontend/e2e/cross-department/control-owner-access.spec.ts, frontend/e2e/cross-department/link-access.spec.ts</files>
  <action>
Add/adjust Playwright assertions to explicitly validate the repaired workflows:
- Control owner can open a cross-department Control Detail page and see the “linked risks” section (or a controlled empty state), without a generic “Failed to load control details.”
- From Control Detail, user can open the “Manage Risk Linkage” dialog, search/select a risk they are allowed to access, and create/unlink a link successfully.

Avoid:
- Overly brittle selectors; prefer stable `data-testid` if present, otherwise robust text/role selectors.
  </action>
  <verify>
`cd frontend && npx playwright test e2e/cross-department/control-owner-access.spec.ts --project=chromium`
  </verify>
  <done>
E2E tests cover control-side link/unlink and fail if RBAC regresses.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add E2E checks for approval-queued UX and KRI history access</name>
  <files>frontend/e2e/approval-workflows/status-flow.spec.ts, frontend/e2e/permissions/kris-crud.spec.ts</files>
  <action>
Add E2E assertions that:
- When an action returns 202 (approval created), the UI shows “Submitted for approval” (or equivalent) and does not claim the edit/delete applied immediately.
- A KRI reporting owner (or risk owner if policy) can view KRI history entries in the UI (Risk Detail history tab or KRI detail view) for cross-department cases.

If the existing fixtures/data do not include the needed scenario, extend seed data in the smallest possible way (prefer using existing Phase 179 data patterns).
  </action>
  <verify>
`cd frontend && npx playwright test e2e/approval-workflows/status-flow.spec.ts --project=chromium`
  </verify>
  <done>
E2E tests verify approval-queued messaging and KRI history visibility per policy.
  </done>
</task>

</tasks>

<verification>
- [ ] Targeted Playwright specs pass
- [ ] Backend pytest suites added/updated in 154-02 remain green
</verification>

<success_criteria>
- Automated tests cover the repaired workflows and prevent silent regressions.
- The test run is documented in the summary so a developer can rerun it quickly.
</success_criteria>

<output>
After completion, create `.planning/phases/154-workflow-bug-sweep/154-05-SUMMARY.md` with the exact commands run and a short list of new/updated test cases.
</output>

