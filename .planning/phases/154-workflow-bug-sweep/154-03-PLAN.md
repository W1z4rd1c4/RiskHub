---
phase: 154-workflow-bug-sweep
type: execute
domain: backend
---

<objective>
Fix backend API contract gaps that make linking/search workflows behave incorrectly (risk search filters missing; link schema mismatch for risk description) and update tests/contracts accordingly.

Purpose: Ensure frontend filters actually filter, and linked-risk UI has the fields it expects.
Output: Expanded `GET /risks` filtering + consistent Control↔Risk link response schema.
</objective>

<context>
@.planning/phases/154-workflow-bug-sweep/DISCOVERY.md
@backend/app/api/v1/endpoints/risks.py
@backend/app/schemas/risk.py
@frontend/src/components/LinkManagementDialog.tsx
@frontend/src/types/control.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing process/category filters to `GET /risks`</name>
  <files>backend/app/api/v1/endpoints/risks.py</files>
  <action>
Extend `GET /risks` to support the filters already sent by the linking UI:
- Add optional query params: `process` and `category` (and optionally `subprocess` if it exists in UI filters).
- Apply them as `where` clauses on the `Risk` query (respecting existing department scoping and archived filtering).

Avoid:
- Breaking existing sort/count logic (keep pagination, count_query, and eager-load behavior intact).
  </action>
  <verify>
`cd backend && pytest -q` for any risk-related tests that cover list filtering, plus a quick manual `curl` showing process/category are honored.
  </verify>
  <done>
`GET /risks` returns filtered results when `process` and/or `category` are provided, matching `DISCOVERY.md` acceptance criteria.
  </done>
</task>

<task type="auto">
  <name>Task 2: Align Control↔Risk link response schema with frontend expectations</name>
  <files>backend/app/schemas/risk.py, backend/app/api/v1/endpoints/controls.py, backend/app/api/v1/endpoints/risks.py</files>
  <action>
Resolve the schema mismatch where the frontend expects `risk.description` on linked risks returned from control endpoints:
- Update `RiskBriefForLink` (or the link response model used by control-risk links) to include `description` (and any other fields that are already used in UI cards).
- Ensure both `GET /controls/{id}/risks` and `GET /risks/{id}/controls` return consistent link shapes (risk/control brief objects) without null surprises.

Avoid:
- Making the brief models so “fat” that they turn into full entity payloads; include only fields used by UI.
  </action>
  <verify>
Backend endpoints return the new field(s) and existing frontend types can be updated without `any` or unsafe casts.
  </verify>
  <done>
The link responses include the fields the UI renders (including risk description) and the contract is consistent across both sides.
  </done>
</task>

</tasks>

<verification>
- [ ] `GET /risks` honors `process`/`category`
- [ ] Link responses contain the fields the UI renders (no missing `risk.description`)
</verification>

<success_criteria>
- Linking search filters behave as users expect.
- Linked risk/control cards render consistently because the backend contract matches the frontend types.
</success_criteria>

<output>
After completion, create `.planning/phases/154-workflow-bug-sweep/154-03-SUMMARY.md` documenting new query params and the link schema contract.
</output>

