---
phase: 154-workflow-bug-sweep
type: execute
domain: fullstack
---

<objective>
Confirm and document the end-to-end workflow bugs found in the app scan (create risk/control/KRI, link/unlink, cross-department ownership, approvals) and turn them into an executable fix backlog with clear acceptance criteria.

Purpose: Align backend RBAC, frontend gating/UX, and API contracts so core workflows behave consistently and tests cover them.
Output: `DISCOVERY.md` with confirmed evidence + a prioritized fix checklist that the remaining 154-xx plans implement.
</objective>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md

Core flows (frontend):
@frontend/src/pages/RiskDetailPage.tsx
@frontend/src/pages/ControlDetailPage.tsx
@frontend/src/components/RiskForm.tsx
@frontend/src/components/ControlForm.tsx
@frontend/src/components/LinkManagementDialog.tsx
@frontend/src/components/linking/LinkSearchPanel.tsx
@frontend/src/components/linking/ExistingLinksPanel.tsx
@frontend/src/components/kri/KRIModal.tsx
@frontend/src/types/approval.ts

Core flows (backend):
@backend/app/api/v1/endpoints/risks.py
@backend/app/api/v1/endpoints/controls.py
@backend/app/api/v1/endpoints/kris.py
@backend/app/core/permissions.py

Existing tests that already touch these areas:
@backend/tests/test_cross_department_access.py
@backend/tests/test_kris_rbac.py
@frontend/e2e/cross-department/control-owner-access.spec.ts
@frontend/e2e/cross-department/link-access.spec.ts
@frontend/e2e/approval-workflows/status-flow.spec.ts
@frontend/e2e/approval-workflows/tiered-approval.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a confirmed findings matrix (evidence + expected behavior)</name>
  <files>.planning/phases/154-workflow-bug-sweep/DISCOVERY.md</files>
  <action>
Create `.planning/phases/154-workflow-bug-sweep/DISCOVERY.md` with:
- A short “What this phase fixes” section focused on workflows (create risk/control/KRI, link/unlink, KRI history, approvals).
- A table with columns: Issue, Severity, Evidence (file:line), Repro Steps (role + UI path + API call), Expected Behavior, Actual Behavior, Fix Plan ID (154-02..154-05), Acceptance Criteria.

At minimum, confirm and document these issues with file+line evidence:
1) Control owner cannot reliably load Control Detail cross-department because `/controls/{id}/risks` is department-only and `Promise.all` fails the page.
2) Control owner cannot link/unlink risks from Control Detail cross-department for the same reason (control-side linking endpoints are department-only).
3) KRI reporting owner (and likely risk owner) cannot view `/kris/{id}/history` cross-department due to unconditional `check_department_access`.
4) HTTP 202 “approval created” responses are not surfaced in the UI (forms and deletes navigate away / claim success).
5) Link dialog “process/category” filters are sent for risk search but backend `GET /risks` does not support these filters.
6) Control↔Risk link contract mismatch: frontend expects risk.description in control-linked risks, backend schema omits it.

Do NOT propose fixes here—only confirmed evidence and crisp acceptance criteria.
  </action>
  <verify>
Open `DISCOVERY.md` and ensure every row has a concrete evidence pointer (file:line) and a falsifiable acceptance criterion.
  </verify>
  <done>
`DISCOVERY.md` exists, is actionable, and covers all known issues + any additional ones discovered while validating.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run targeted test suites to confirm current behavior and record failures</name>
  <files>.planning/phases/154-workflow-bug-sweep/DISCOVERY.md</files>
  <action>
Run (or instruct running) the narrowest possible suites and record results (pass/fail + key error messages) under a “Confirmation Runs” section in `DISCOVERY.md`:

Backend (fast, targeted):
- `cd backend && pytest -q tests/test_cross_department_access.py`
- `cd backend && pytest -q tests/test_kris_rbac.py`

Frontend (targeted e2e):
- `cd frontend && npx playwright test e2e/cross-department/control-owner-access.spec.ts --project=chromium`
- `cd frontend && npx playwright test e2e/cross-department/link-access.spec.ts --project=chromium`
- `cd frontend && npx playwright test e2e/approval-workflows/status-flow.spec.ts --project=chromium`

If any of these suites are too slow in the current environment, capture that fact and the next-best local confirmation step (e.g., single-spec runs).
  </action>
  <verify>
`DISCOVERY.md` includes a “Confirmation Runs” section with command lines and captured outcomes.
  </verify>
  <done>
We have a baseline of what currently breaks and which tests already cover (or miss) the failing workflows.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Lock the intended access rules for cross-department viewing/linking and KRI history</decision>
  <context>
Several issues depend on the “correct” business rule, especially for cross-department ownership (control owner, risk owner, KRI reporting owner). We need explicit choices so fixes and tests reflect the intended policy.
  </context>
  <options>
    <option id="mirror-risk-side">
      <name>Mirror risk-side access checks everywhere</name>
      <pros>Most consistent; reuse the same helper checks on both risk-side and control-side endpoints; fewer surprises in UI</pros>
      <cons>Requires carefully defining “access to other side” when linking (avoid allowing a control owner to link to arbitrary risks they cannot view)</cons>
    </option>
    <option id="department-only-on-control-side">
      <name>Keep control-side endpoints department-only</name>
      <pros>Strict and simple policy</pros>
      <cons>Breaks Control Detail workflows for legitimate control owners; conflicts with existing cross-dept ownership patterns and E2E intent</cons>
    </option>
  </options>
  <resume-signal>Select: mirror-risk-side or department-only-on-control-side</resume-signal>
</task>

</tasks>

<verification>
- [ ] `DISCOVERY.md` has evidence + acceptance criteria for every issue
- [ ] Targeted pytest and playwright runs recorded
- [ ] Decision recorded and referenced by 154-02..154-05 plans
</verification>

<success_criteria>
- The developer can reproduce every bug from `DISCOVERY.md` without “guessing where” or “guessing expected behavior”.
- Each issue is assigned to a specific 154-xx plan with clear acceptance criteria.
</success_criteria>

<output>
After completion, create `.planning/phases/154-workflow-bug-sweep/154-01-SUMMARY.md` summarizing the confirmed issue list and the chosen access-rule decision.
</output>

