# Plan 250-06: Simplify `RiskDetailPage.tsx` by extracting tab contents

## Objective
Refactor `frontend/src/pages/RiskDetailPage.tsx` (~718 lines) by extracting the **tab bodies** into focused components, while keeping data fetching, modals, and handlers behavior identical.

## Non-goals / Invariants (do not change)
- Do not change routes, navigation targets, or API calls.
- Do not change the tab model: this page has **two** tabs today:
  - `overview`
  - `history` (aggregated KRI history)
- Do not change when data is fetched (initial fetch on mount; history fetch when history tab active).
- Do not change modal/dialog behavior (LinkManagementDialog, ControlCreateDialog, KRIModal, ConfirmDialog).

## Current State (verified)
- **File**: `frontend/src/pages/RiskDetailPage.tsx`
- Tab switch is controlled by `activeTab: 'overview' | 'history'`.
- Overview tab contains multiple large sections inline (risk matrices, classification/ownership cards, KRIs grid, linked controls + dialogs, timestamps).
- History tab aggregates KRI history by fetching `kriApi.getHistory(kri.id, { size: 50 })` for each KRI when `activeTab === 'history'`.

## Implementation Steps

### 1) Extract overview tab JSX into a component
- [CREATE] `frontend/src/components/risks/RiskDetailOverviewTab.tsx`
  - Keep it as a pure presentational component: no fetching.
  - Props should be explicit and reflect current usage:
    - `risk: Risk`
    - `linkedControls: RiskControlLink[]`
    - `overdueKRIs: OverdueKRI[]`
    - callbacks and UI state setters already owned by `RiskDetailPage`:
      - `onOpenAddKri`, `onLinkControl`, `onUnlinkControl`, `onOpenCreateControl`, `onOpenLinkDialog`, etc.
    - `getColor/getDisplayName` from `useRiskTypes()` (pass them in rather than re-hooking).

### 2) Extract history tab JSX into a component
- [CREATE] `frontend/src/components/risks/RiskDetailKriHistoryTab.tsx`
  - Props:
    - `items: HistoryTimelineItem[]`
    - `loading: boolean`
    - `hasKRIs: boolean`
  - Render the same `<HistoryTimeline ... />` with the same empty message logic.

### 3) Refactor `RiskDetailPage.tsx` to orchestrate only
- [MODIFY] `frontend/src/pages/RiskDetailPage.tsx`
  - Keep:
    - state, handlers, and effects as-is
    - tab buttons as-is (or optionally extract a tiny `RiskDetailTabs` component if it clearly reduces noise)
  - Replace large JSX blocks with:
    - `<RiskDetailOverviewTab ... />`
    - `<RiskDetailKriHistoryTab ... />`

### 4) Optional cleanup: fix local helpers only if reused
- `hexToRgba()` is currently local and only used here; keep it local unless another file needs it.

## Verification
Automated:
- `cd frontend && npm run build`

Manual smoke:
- Open a risk detail page:
  - overview renders identically
  - link/unlink control works and dialogs open/close correctly
  - add/edit/delete KRI still works via KRIModal
  - switching to history triggers aggregated history load and renders timeline
  - archive risk confirm dialog still works

## Rollback Strategy
- Commit before extraction.
- If props become unwieldy, revert and extract only one component first (overview tab), then proceed.

## Success Criteria
- `RiskDetailPage.tsx` is substantially shorter and easier to scan.
- No UI/behavior changes; build passes.

## Estimated Effort
- **Scope**: Medium
- **Risk**: Low (UI restructuring)
