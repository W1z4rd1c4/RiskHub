# Plan 250-05: Extract shared step indicator + minimal wizard state (ControlForm + RiskForm)

## Objective
Reduce duplication between:
- `frontend/src/components/ControlForm.tsx` (~794 lines)
- `frontend/src/components/RiskForm.tsx` (~765 lines)

…by extracting the **shared step indicator UI** and (optionally) a **minimal current-step state helper**, without changing validation behavior or navigation rules.

## Non-goals / Invariants (do not change)
- Do not change form fields, API payloads, or submit behavior.
- Do not change validation rules or when errors appear.
- Preserve step click semantics:
  - Control form: can click back and the immediate next step (via its current logic).
  - Risk form: can click only previously completed steps.
- Do not introduce a “one hook to rule them all” that forces a single validation/error model (these two forms differ).

## Current State (verified)
Both forms share:
- `steps` array of `{ id, title, icon }`
- `currentStep` state and `nextStep/prevStep` boundary logic
- nearly identical step indicator markup (icon bubble + label + completed checkmark)

Key differences to preserve:
- ControlForm uses a single `error: string | null` and step-specific `validateStep0/1/2`.
- RiskForm uses `fieldErrors` plus an `error: string | null`, and only allows clicking completed steps.

## Implementation Steps

### 1) Extract the step indicator UI into a reusable component
- [CREATE] `frontend/src/components/ui/StepIndicator.tsx`
  - Props should be flexible enough to preserve both click policies:
    - `steps: Array<{ id: string; title: string; icon: React.ComponentType<{ className?: string }> }>`
    - `currentStep: number`
    - `isStepClickable: (index: number) => boolean`
    - `onStepClick: (index: number) => void`
  - Keep styling consistent with the existing indicator (same classes and checkmark behavior).

### 2) Refactor `ControlForm.tsx` to use `StepIndicator`
- [MODIFY] `frontend/src/components/ControlForm.tsx`
  - Replace the inline “Multi-step indicator” block with `<StepIndicator ... />`
  - Keep existing navigation logic:
    - `isStepClickable`: `idx <= currentStep + 1`
    - `onStepClick`: existing `handleStepClick`
  - Do not change validation functions or error handling.

### 3) Refactor `RiskForm.tsx` to use `StepIndicator`
- [MODIFY] `frontend/src/components/RiskForm.tsx`
  - Replace the inline “Multi-step indicator” block with `<StepIndicator ... />`
  - Preserve click policy:
    - `isStepClickable`: `idx < currentStep`
    - `onStepClick`: `setCurrentStep(idx)` (only for clickable steps)

### 4) Optional: extract minimal step-state helper (only if it reduces noise)
If you still see duplicated boundary logic, add a tiny hook:
- [CREATE] `frontend/src/hooks/useStepWizard.ts`
  - Responsibility: clamp next/prev/goTo within `[0, steps.length - 1]`
  - Do **not** handle validation or error state (leave that in each form).
  - RiskForm’s `nextStep(e)` currently prevents default/propagation; keep that behavior in RiskForm (don’t bake event handling into a shared hook unless both use it).

## Verification
Automated:
- `cd frontend && npm run build`
- `cd frontend && npm run lint` (if already part of normal workflow)

Manual smoke:
- Risk create flow:
  - step navigation (back-only clicks), validation errors still show on the same steps, submit works
- Control create flow:
  - step navigation (back + immediate next click), validation errors still block progression, submit + optional link risk still works
- Edit flows for both forms still load initial data and submit successfully

## Rollback Strategy
- Commit before extracting the component.
- If anything regresses visually, revert and re-introduce the StepIndicator with 1:1 copied markup before further cleanup.

## Success Criteria
- Step indicator markup exists in one place (`StepIndicator.tsx`).
- Both forms keep identical navigation/validation behavior.
- No UI regressions; build passes.

## Estimated Effort
- **Scope**: Medium
- **Risk**: Low (UI refactor; easy to verify manually)
