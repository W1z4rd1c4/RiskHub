---
phase: 251-spaghetti-simplification-2
type: execute
domain: frontend
---

<objective>
Simplify `frontend/src/components/LinkManagementDialog.tsx` (~517 lines) by extracting search and existing-links sections into focused subcomponents and tightening types, without changing any linking/unlinking behavior.

Purpose: This dialog is shared across pages; simplifying it reduces ongoing UX and bug risk.
Output: Smaller LinkManagementDialog, less `any`, same UX, build passes.
</objective>

<execution_context>
@.agent/workflows/simplify.md
@.agent/references/plan-format.md
</execution_context>

<context>
@frontend/src/components/LinkManagementDialog.tsx
@frontend/src/services/controlApi.ts
@frontend/src/services/riskApi.ts
@frontend/src/components/ui/ThemedSelect.tsx
@frontend/src/lib/utils.ts
@frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split dialog into SearchPanel + ExistingLinksPanel</name>
  <files>frontend/src/components/linking/LinkSearchPanel.tsx, frontend/src/components/linking/ExistingLinksPanel.tsx, frontend/src/components/LinkManagementDialog.tsx</files>
  <action>
Create two presentational subcomponents:
- `LinkSearchPanel`:
  - owns rendering of search input, filters, result list, and “link” actions
- `ExistingLinksPanel`:
  - renders existing links list + unlink actions

Keep `LinkManagementDialog` as the orchestrator for:
- shared state (open/close, mode, selected effectiveness/notes)
- calling `onLink/onUnlink`

Preserve:
- props contract of `LinkManagementDialog` (do not change callers)
- `showSearch` / `showLinks` semantics
  </action>
  <verify>cd frontend && npm run build</verify>
  <done>Dialog internals are modular; external API unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Tighten types and remove `Record<string, unknown>` where feasible</name>
  <files>frontend/src/components/LinkManagementDialog.tsx, frontend/src/components/linking/LinkSearchPanel.tsx, frontend/src/components/linking/ExistingLinksPanel.tsx</files>
  <action>
Replace the ad-hoc `LinkItem`/`SearchResult` shapes with small, explicit types that reflect actual use in the UI:
- For “existing link”, use the subset of fields required for rendering (id, risk/control identifiers, effectiveness, notes, display fields).
- For “search result”, model the display fields used in the component.

Avoid:
- Importing full backend types if they cause coupling; keep types local but explicit.
  </action>
  <verify>cd frontend && npm run build</verify>
  <done>Fewer `any`-like shapes; TypeScript remains strict; build passes</done>
</task>

</tasks>

<verification>
- [ ] `cd frontend && npm run build`
- [ ] Manual smoke: open dialog in both modes (risk-to-control and control-to-risk), search, link, unlink, and verify no UI regressions
</verification>

<success_criteria>
- LinkManagementDialog is significantly smaller and easier to reason about.
- No behavior changes to linking/unlinking flows.
</success_criteria>

<output>
After completion, create `.planning/phases/251-spaghetti-simplification-2/251-10-SUMMARY.md`.
</output>

