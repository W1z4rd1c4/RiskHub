---
phase: 251-spaghetti-simplification-2
type: execute
domain: frontend
---

<objective>
Simplify `frontend/src/pages/RisksPage.tsx` (~646 lines) by extracting shared list-page utilities (debounced search, pending approvals fetch, grouped-view full-fetch) into small hooks and components, without changing UI behavior.

Purpose: Make the risk register page easier to modify and reduce duplication with `ControlsPage.tsx`.
Output: Smaller RisksPage, shared hooks in `frontend/src/hooks/`, build/tests pass.
</objective>

<execution_context>
@.agent/workflows/simplify.md
@.agent/references/plan-format.md
</execution_context>

<context>
@frontend/src/pages/RisksPage.tsx
@frontend/src/pages/ControlsPage.tsx
@frontend/src/services/approvalsApi.ts
@frontend/src/services/riskApi.ts
@frontend/src/components/tables
@frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create small shared hooks (debounce + pending approvals)</name>
  <files>frontend/src/hooks/useDebouncedValue.ts, frontend/src/hooks/usePendingApprovalIds.ts</files>
  <action>
Create two small, reusable hooks:

1) `useDebouncedValue<T>(value: T, delayMs: number): T`
- Replace the ad-hoc debounce effect in RisksPage (and later ControlsPage).

2) `usePendingApprovalIds(resourceType: 'risk' | 'control')`
- Encapsulate the “paginate approvalsApi.list({ status: 'pending', ... }) and build a Set of resource_ids” logic.
- Preserve semantics: only include approvals where `resource_type` matches.

Avoid:
- One giant “useListPage” abstraction; keep hooks single-purpose.
  </action>
  <verify>cd frontend && npm run build</verify>
  <done>Hooks exist and RisksPage can use them without behavior changes</done>
</task>

<task type="auto">
  <name>Task 2: Refactor RisksPage to use the hooks and shrink effects</name>
  <files>frontend/src/pages/RisksPage.tsx</files>
  <action>
Refactor `RisksPage.tsx`:
- Replace local debounce state/effect with `useDebouncedValue(search, 300)`.
- Replace the “fetchPending approvals” effect with `usePendingApprovalIds('risk')`.

Keep invariants:
- URL param handling for `breached=true` and `critical=true` stays identical.
- Existing fetch behavior and pagination stays identical.
- No UI/label changes.
  </action>
  <verify>cd frontend && npm run build</verify>
  <done>RisksPage has fewer effects and less state; build passes</done>
</task>

</tasks>

<verification>
- [ ] `cd frontend && npm run build`
- [ ] Manual smoke: risk register filters/search/sort still work; pending approval lock indicators still show
</verification>

<success_criteria>
- RisksPage is materially smaller and more readable.
- Shared hooks are reused by ControlsPage in the next plan.
</success_criteria>

<output>
After completion, create `.planning/phases/251-spaghetti-simplification-2/251-06-SUMMARY.md`.
</output>

