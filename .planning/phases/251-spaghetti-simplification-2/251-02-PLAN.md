---
phase: 251-spaghetti-simplification-2
type: execute
domain: backend
status: already-complete
---

<objective>
~~Simplify `backend/app/api/v1/endpoints/departments.py` by extracting repeated stats/query patterns into focused helpers and making pagination/scoping rules explicit.~~

**STATUS: ALREADY COMPLETE (as of Phase 250 refactoring)**

This plan's intended simplifications were completed during Phase 250. The current `departments.py` already includes:
- Scoping helpers: `_get_scoped_department_ids`, `_assert_department_in_scope`, `_clamp_pagination`
- Stats builder functions: `_count_active_users_by_dept`, `_count_risks_by_dept`, `_count_high_risks_by_dept`, `_count_controls_by_dept`, `_count_kris_by_dept`, `_count_breaching_kris_by_dept`, `_sum_net_scores_by_dept`
- Comprehensive docstrings on all endpoints
</objective>

<pre_flight>
## Pre-flight Checklist (Before Execution)

If you're considering executing this plan, verify first:

- [x] **Check scoping helpers**: `_get_scoped_department_ids`, `_assert_department_in_scope`, `_clamp_pagination` exist (lines 45-76)
- [x] **Check stats builders**: All `_count_*_by_dept` helpers exist (lines 84-161)
- [x] **Check `list_departments()`**: Uses count map helpers, reads as orchestration-only (lines 164-217)
- [x] **Check endpoint docstrings**: All sub-endpoints have comprehensive docstrings with invariants

**If all items above are checked, this plan is already complete. Do not re-execute.**
</pre_flight>

<execution_context>
@.agent/workflows/simplify.md
@.agent/references/plan-format.md
</execution_context>

<context>
@backend/app/api/v1/endpoints/departments.py
@backend/app/core/permissions.py
@backend/app/core/pagination.py
@backend/tests/test_departments.py
</context>

<tasks>

<task type="auto" status="complete">
  <name>Task 1: Extract scoping + pagination helpers (local or small service)</name>
  <files>backend/app/api/v1/endpoints/departments.py</files>
  <action>
**ALREADY DONE**: Private helpers extracted:
- `_get_scoped_department_ids(current_user)` (lines 45-51)
- `_assert_department_in_scope(department_id, db, current_user)` (lines 54-67)
- `_clamp_pagination(skip, limit)` (lines 70-76)

Note: `_assert_department_in_scope` enforces 404/403 semantics; sub-endpoints use it consistently.
  </action>
  <verify>cd backend && python3 -m pytest -q tests/test_departments.py</verify>
  <done>Department scoping/pagination logic reads clearly and tests pass</done>
</task>

<task type="auto" status="complete">
  <name>Task 2: Extract stats builders used by list/detail (keep metric semantics unchanged)</name>
  <files>backend/app/api/v1/endpoints/departments.py</files>
  <action>
**ALREADY DONE**: Stats builder functions extracted (lines 84-161):
- `_count_active_users_by_dept(db)`
- `_count_risks_by_dept(db)` — excludes archived (Risk.status != archived)
- `_count_high_risks_by_dept(db)` — net_score >= 16
- `_count_controls_by_dept(db)` — excludes archived (Control.status != archived)
- `_count_kris_by_dept(db)` — via Risk join, excludes archived risks
- `_count_breaching_kris_by_dept(db)` — outside limits, excludes archived risks
- `_sum_net_scores_by_dept(db)`

`list_departments()` (lines 164-217) now reads as:
1. Load visible departments
2. Compute count maps (one helper call each)
3. Build response objects
  </action>
  <verify>cd backend && python3 -m pytest -q tests/test_departments.py</verify>
  <done>`list_departments()` is orchestration-only; helpers encapsulate query details; tests pass</done>
</task>

<task type="auto" status="complete">
  <name>Task 3: Make per-endpoint invariants explicit (no behavioral changes)</name>
  <files>backend/app/api/v1/endpoints/departments.py</files>
  <action>
**ALREADY DONE**: Docstrings added to all endpoints:
- `list_departments()` (lines 169-174): Scoping, excludes inactive/archived
- `get_department()` (lines 227-232): Access rules, exclusions, metric sources
- `list_department_risks()` (lines 417-423): Access, default archived exclusion, pagination
- `list_department_controls()` (lines 483-488): Access, default archived exclusion, pagination
- `list_department_kris()` (lines 533-539): Access, archived risk exclusion, pagination
  </action>
  <verify>cd backend && python3 -m pytest -q tests/test_departments.py</verify>
  <done>Endpoints remain unchanged but easier to audit for RBAC/metrics semantics</done>
</task>

</tasks>

<verification>
- [x] `cd backend && python3 -m pytest -q tests/test_departments.py` (passed during Phase 250)
- [x] Manual smoke: department head sees only their department(s); privileged sees all
</verification>

<success_criteria>
- ✅ Departments endpoints are easier to reason about without changing scoping or metrics.
- ✅ No query semantics drift (archived filtering and threshold ranges unchanged).
</success_criteria>

<output>
Plan marked as already-complete. No SUMMARY.md required for this plan since work was done in Phase 250.
</output>
