---
phase: 251-spaghetti-simplification-2
type: execute
domain: backend
---

<objective>
Simplify `backend/app/api/v1/endpoints/admin.py` (~934 lines) by extracting schemas and repeated “admin-only” gating into clear dependencies, while preserving every route and permission rule.

Purpose: Reduce risk in security/ops endpoints by making the code auditable and less error-prone.
Output: `admin.py` becomes a thin orchestrator; schemas live under `backend/app/schemas/admin.py`; tests pass.
</objective>

<execution_context>
@.agent/workflows/simplify.md
@.agent/references/plan-format.md
</execution_context>

<context>
@backend/app/api/v1/endpoints/admin.py
@backend/app/core/security.py
@backend/app/core/permissions.py
@backend/app/core/snapshot_service.py
@backend/tests/test_admin_logs.py
@backend/tests/test_siem_logging.py
@backend/tests/test_log_rotation_config.py
@backend/tests/test_directory_sync.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract admin schemas to `app/schemas/admin.py` (Pydantic v2)</name>
  <files>backend/app/api/v1/endpoints/admin.py, backend/app/schemas/admin.py</files>
  <action>
Create `backend/app/schemas/admin.py` and move endpoint-local Pydantic models there (e.g., orphan stats/fix, system stats/health, logs responses, snapshot responses).

Rules:
- Use Pydantic v2 style (`BaseModel` + `ConfigDict` if needed).
- Keep field names and response structures identical (this is schema relocation only).
- Update `admin.py` imports to use `app.schemas.admin`.
  </action>
  <verify>cd backend && python3 -m pytest -q tests/test_admin_logs.py tests/test_log_rotation_config.py</verify>
  <done>`admin.py` has no inline schema classes; tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Replace repeated admin checks with a dependency (no RBAC change)</name>
  <files>backend/app/api/v1/endpoints/admin.py</files>
  <action>
`admin.py` currently repeats “Admin only check” by inspecting `current_user.role.name.lower()`.
Extract a single dependency:
- `def require_platform_admin(current_user: User = Depends(get_current_user)) -> User`
  - preserves existing semantics exactly (only platform admin role, not “privileged scope”)

Then update endpoints to depend on `admin_user: User = Depends(require_platform_admin)` and remove duplicated checks.

Avoid:
- Broadening access (do not switch to `is_privileged_user` unless the file already intends that).
  </action>
  <verify>cd backend && python3 -m pytest -q tests/test_admin_logs.py</verify>
  <done>Admin-only gating is centralized and consistent; tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Extract internal helpers for the three main domains (logs, sessions, snapshots)</name>
  <files>backend/app/api/v1/endpoints/admin.py</files>
  <action>
Refactor inside `admin.py` to reduce long functions by extracting private helpers:
- Logs domain: file reading + filtering + response assembly
- Sessions domain: list active sessions + revoke logic
- Snapshots domain: capture/list/get snapshot orchestration

Keep invariants:
- No route/path changes.
- Same response payloads (keys, shapes, ordering).
- Same error handling status codes.

Avoid splitting into multiple router modules unless absolutely necessary (keep lean).
  </action>
  <verify>cd backend && python3 -m pytest -q tests/test_admin_logs.py tests/test_siem_logging.py tests/test_log_rotation_config.py</verify>
  <done>`admin.py` is shorter and grouped by domain; tests pass</done>
</task>

</tasks>

<verification>
- [ ] `cd backend && python3 -m pytest -q tests/test_admin_logs.py`
- [ ] `cd backend && python3 -m pytest -q tests/test_siem_logging.py`
- [ ] `cd backend && python3 -m pytest -q tests/test_log_rotation_config.py`
- [ ] Manual smoke: admin console loads logs/snapshots endpoints; non-admin gets 403
</verification>

<success_criteria>
- Centralized admin-only gating via a dependency.
- No schema classes embedded in `admin.py`.
- Same operational behaviors, validated by existing tests.
</success_criteria>

<output>
After completion, create `.planning/phases/251-spaghetti-simplification-2/251-03-SUMMARY.md`.
</output>

