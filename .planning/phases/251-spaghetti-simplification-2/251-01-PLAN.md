---
phase: 251-spaghetti-simplification-2
type: execute
domain: backend
status: already-complete
---

<objective>
~~Simplify `backend/app/api/v1/endpoints/controls.py` by extracting repeated approval/notification boilerplate and clarifying the control update/delete approval rules.~~

**STATUS: ALREADY COMPLETE (as of Phase 250 refactoring)**

This plan's intended simplifications were completed during Phase 250. The current `controls.py` already includes:
- `create_approval_request_with_audit` usage in both `update_control()` and `delete_control()`
- Extracted private helpers: `_build_pending_changes`, `_first_high_risk_linked_risk`
- Query scoping helpers: `_apply_department_scoping`, `_apply_process_category_filters`
</objective>

<pre_flight>
## Pre-flight Checklist (Before Execution)

If you're considering executing this plan, verify first:

- [x] **Check `delete_control()`**: Already uses `create_approval_request_with_audit` (line ~593)
- [x] **Check `update_control()`**: Already uses `create_approval_request_with_audit` (line ~430)
- [x] **Check helper extraction**: `_build_pending_changes` and `_first_high_risk_linked_risk` exist (lines 28-58)
- [x] **Check query helpers**: `_apply_department_scoping` and `_apply_process_category_filters` exist (lines 61-106)

**If all items above are checked, this plan is already complete. Do not re-execute.**
</pre_flight>

<execution_context>
@.agent/workflows/simplify.md
@.agent/references/plan-format.md
@.agent/references/checkpoints.md
</execution_context>

<context>
@backend/app/api/v1/endpoints/controls.py
@backend/app/core/approval_helpers.py
@backend/app/core/permissions.py
@backend/app/services/notification_service.py
@backend/app/models/approval_request.py
@backend/tests/test_controls.py
@backend/tests/test_approval_workflow.py
</context>

<tasks>

<task type="auto" status="complete">
  <name>Task 1: Consolidate approval creation boilerplate using existing helper</name>
  <files>backend/app/api/v1/endpoints/controls.py, backend/app/core/approval_helpers.py</files>
  <action>
**ALREADY DONE**: Both `update_control()` and `delete_control()` now use `create_approval_request_with_audit(...)`.

Transaction semantics preserved:
- Approval persisted/committed first via the helper
- Notifications attempted afterward with their own commit/rollback (lines 437-461, 604-627)
  </action>
  <verify>cd backend && python3 -m pytest -q tests/test_controls.py tests/test_approval_workflow.py</verify>
  <done>`controls.py` no longer duplicates the approval-create boilerplate; tests pass</done>
</task>

<task type="auto" status="complete">
  <name>Task 2: Extract small local helpers for readability (pending_changes + linked-risk checks)</name>
  <files>backend/app/api/v1/endpoints/controls.py</files>
  <action>
**ALREADY DONE**: Private helpers extracted:
- `_build_pending_changes(control, update_data)` (lines 28-40)
- `_first_high_risk_linked_risk(db, control_id)` (lines 43-58)

Approval trigger semantics unchanged (linked high-risk, sensitive fields, control-owner edits).
  </action>
  <verify>cd backend && python3 -m pytest -q tests/test_controls.py</verify>
  <done>`update_control()` and `delete_control()` read as high-level orchestration with small helpers; no behavior changes</done>
</task>

<task type="auto" status="complete">
  <name>Task 3: Keep list-controls query readable without changing query semantics</name>
  <files>backend/app/api/v1/endpoints/controls.py</files>
  <action>
**ALREADY DONE**: Query helpers extracted:
- `_apply_department_scoping(db, query, current_user, department_id_filter)` (lines 61-90)
- `_apply_process_category_filters(query, process, category)` (lines 93-106)

Join/distinct behavior preserved for search across linked risks.
  </action>
  <verify>cd backend && python3 -m pytest -q tests/test_controls.py</verify>
  <done>`list_controls()` remains behavior-identical but easier to follow</done>
</task>

</tasks>

<verification>
- [x] `cd backend && python3 -m pytest -q tests/test_controls.py` (passed during Phase 250)
- [x] `cd backend && python3 -m pytest -q tests/test_approval_workflow.py` (passed during Phase 250)
- [x] Manual smoke: verified as non-privileged control owner, edits create approval requests
</verification>

<success_criteria>
- ✅ No route/signature/response changes.
- ✅ Approval creation uses `create_approval_request_with_audit` (no duplicated IntegrityError boilerplate).
- ✅ Control approval rules remain identical and are easier to audit.
</success_criteria>

<output>
Plan marked as already-complete. No SUMMARY.md required for this plan since work was done in Phase 250.
</output>
