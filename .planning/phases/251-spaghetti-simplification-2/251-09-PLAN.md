---
phase: 251-spaghetti-simplification-2
type: execute
domain: frontend
---

<objective>
Simplify `frontend/src/pages/KRIDetailPage.tsx` (~563 lines) by extracting tab content and converting history transformation logic into pure helpers, without changing KRI CRUD/value/correction behavior.

Purpose: Make KRI historization UX easier to extend and less fragile.
Output: Smaller KRIDetailPage + extracted components/helpers; build passes.
</objective>

<execution_context>
@.agent/workflows/simplify.md
@.agent/references/plan-format.md
</execution_context>

<context>
@frontend/src/pages/KRIDetailPage.tsx
@frontend/src/services/kriApi.ts
@frontend/src/services/riskApi.ts
@frontend/src/components/history
@frontend/src/components/kri/KRIModal.tsx
@frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract overview tab rendering into a component</name>
  <files>frontend/src/components/kris/KRIDetailOverviewTab.tsx, frontend/src/pages/KRIDetailPage.tsx</files>
  <action>
Create `KRIDetailOverviewTab` as a presentational component that renders the current overview UI, driven by props:
- `kri`, `linkedRisk`, permissions booleans, and handlers to open modals

Keep all UI identical (icons, layout, labels).
  </action>
  <verify>cd frontend && npm run build</verify>
  <done>KRIDetailPage overview JSX moved out; build passes</done>
</task>

<task type="auto">
  <name>Task 2: Extract history tab rendering + transformations</name>
  <files>frontend/src/components/kris/KRIDetailHistoryTab.tsx, frontend/src/pages/KRIDetailPage.tsx</files>
  <action>
Create `KRIDetailHistoryTab` that receives:
- `history` entries, `total`, loading state, and handlers for edit/correct actions

If KRIDetailPage contains mapping logic to `HistoryTimelineItem` / `HistoryTrendPoint`, move that into pure helper functions in the same component module (or a small `frontend/src/components/kris/historyTransform.ts` if reused).

Preserve:
- the `kriApi.getHistory(kriId, { size: 50 })` behavior
- tab switching semantics
  </action>
  <verify>cd frontend && npm run build</verify>
  <done>History tab is encapsulated; KRIDetailPage becomes orchestration-only</done>
</task>

</tasks>

<verification>
- [ ] `cd frontend && npm run build`
- [ ] Manual smoke: view history tab, open value modal, open history correction modal, delete KRI still behaves the same
</verification>

<success_criteria>
- KRIDetailPage is materially smaller and easier to modify.
- No changes to KRI workflows or history semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/251-spaghetti-simplification-2/251-09-SUMMARY.md`.
</output>

