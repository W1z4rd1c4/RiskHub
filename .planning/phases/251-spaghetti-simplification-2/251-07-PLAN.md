---
phase: 251-spaghetti-simplification-2
type: execute
domain: frontend
---

<objective>
Refactor `frontend/src/pages/ControlsPage.tsx` (~466 lines) to reuse the shared hooks from 251-06 and reduce duplicated “fetch all pages for grouped view” logic, without changing UI behavior.

Purpose: Keep list pages consistent and easier to maintain.
Output: Smaller ControlsPage; build passes.
</objective>

<execution_context>
@.agent/workflows/simplify.md
@.agent/references/plan-format.md
</execution_context>

<context>
@frontend/src/pages/ControlsPage.tsx
@frontend/src/hooks/useDebouncedValue.ts
@frontend/src/hooks/usePendingApprovalIds.ts
@frontend/src/services/controlApi.ts
@frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adopt shared hooks for debounce + pending approvals</name>
  <files>frontend/src/pages/ControlsPage.tsx</files>
  <action>
Update ControlsPage to use:
- `useDebouncedValue(search, 300)` instead of local debouncing effect
- `usePendingApprovalIds('control')` instead of local “fetchPending approvals” logic (if present/needed)

Preserve invariants:
- No changes to filters, view modes, pagination, export behavior.
  </action>
  <verify>cd frontend && npm run build</verify>
  <done>ControlsPage uses shared hooks; build passes</done>
</task>

<task type="auto">
  <name>Task 2: Extract grouped-view full-fetch into a small local helper</name>
  <files>frontend/src/pages/ControlsPage.tsx</files>
  <action>
ControlsPage currently fetches all pages for grouped views using a loop.
Extract a local helper like `fetchAllControlsForGroupedViews({ search, status })` that:
- keeps the pageSize=100 logic
- preserves ordering of concatenation
- preserves error handling and loading state semantics

Avoid:
- Creating a cross-entity generic fetcher unless it is clearly reused in RisksPage already.
  </action>
  <verify>cd frontend && npm run build</verify>
  <done>ControlsPage fetch flow is easier to read and maintain</done>
</task>

</tasks>

<verification>
- [ ] `cd frontend && npm run build`
- [ ] Manual smoke: Controls page “all” view and grouped views behave identically
</verification>

<success_criteria>
- ControlsPage contains less duplicated infra logic and reads as orchestration-only.
- Shared hooks are used consistently across list pages.
</success_criteria>

<output>
After completion, create `.planning/phases/251-spaghetti-simplification-2/251-07-SUMMARY.md`.
</output>

