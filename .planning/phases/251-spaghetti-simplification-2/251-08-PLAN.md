---
phase: 251-spaghetti-simplification-2
type: execute
domain: frontend
---

<objective>
Simplify `frontend/src/pages/ActivityLogPage.tsx` (~559 lines) by extracting lookups + filtering + fetching into a hook and extracting the filter bar UI, while preserving RBAC gating and view-mode behavior.

Purpose: Make activity log filtering safer to evolve (audit/compliance critical UI).
Output: Smaller ActivityLogPage; same behavior; build/test pass.
</objective>

<execution_context>
@.agent/workflows/simplify.md
@.agent/references/plan-format.md
</execution_context>

<context>
@frontend/src/pages/ActivityLogPage.tsx
@frontend/src/services/activityLogApi.ts
@frontend/src/services/lookupApi.ts
@frontend/src/services/riskApi.ts
@frontend/src/hooks/usePermissions.ts
@frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract data fetching + filters into `useActivityLogPageState`</name>
  <files>frontend/src/hooks/useActivityLogPageState.ts, frontend/src/pages/ActivityLogPage.tsx</files>
  <action>
Create `useActivityLogPageState()` that owns:
- view mode state + selectors
- search debounce (can reuse `useDebouncedValue` if available)
- building `ActivityLogFilters` payloads
- fetching entries + total with correct pagination
- loading lookup data (users/departments/risks) used by selectors

Preserve invariants:
- Early return gating when `!canViewActivityLog` must remain before any API calls.
- Existing tabs/view modes and their semantics must not change.
  </action>
  <verify>cd frontend && npm run build</verify>
  <done>ActivityLogPage shrinks to orchestration + render; build passes</done>
</task>

<task type="auto">
  <name>Task 2: Extract filter UI into a component</name>
  <files>frontend/src/components/activity-log/ActivityLogFilterBar.tsx, frontend/src/pages/ActivityLogPage.tsx</files>
  <action>
Extract the filter UI (search, date, action, selectors) into `ActivityLogFilterBar`.
Keep:
- all labels, styling, and control behavior unchanged
- props explicit (avoid passing a giant object if possible)
  </action>
  <verify>cd frontend && npm run build</verify>
  <done>Filter bar is reusable and ActivityLogPage is less nested</done>
</task>

</tasks>

<verification>
- [ ] `cd frontend && npm run build`
- [ ] Manual smoke: activity log loads, filters work, and RBAC gating blocks unauthorized users without making API calls
</verification>

<success_criteria>
- ActivityLogPage readability improved without changing compliance-sensitive behaviors.
</success_criteria>

<output>
After completion, create `.planning/phases/251-spaghetti-simplification-2/251-08-SUMMARY.md`.
</output>

