---
phase: 251-spaghetti-simplification-2
type: execute
domain: backend
---

<objective>
Simplify report export endpoints (`backend/app/api/v1/endpoints/reports.py`) by extracting repeated scoping/query/response patterns and making RBAC invariants explicit, without changing generated bytes or filters.

Purpose: Keep reporting RBAC safe while reducing duplication across pdf/excel endpoints.
Output: Cleaner `reports.py`, unchanged exports, tests passing.
</objective>

<execution_context>
@.agent/workflows/simplify.md
@.agent/references/plan-format.md
</execution_context>

<context>
@backend/app/api/v1/endpoints/reports.py
@backend/app/services/report_service.py
@backend/app/core/permissions.py
@backend/tests/test_reports_rbac.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared “empty scoped response” helpers</name>
  <files>backend/app/api/v1/endpoints/reports.py</files>
  <action>
In `reports.py`, the endpoints repeat patterns for:
- “user has no departments” → return an empty PDF/Excel file
- StreamingResponse setup (content-disposition, media type)

Extract small helpers:
- `_stream_pdf(filename, bytes)`
- `_stream_excel(filename, bytes)`
- `_empty_controls_pdf_response()` / `_empty_risks_excel_response()` etc (or parameterize by generator function)

Preserve invariants:
- Same filenames (`get_filename(...)`)
- Same media types and headers
- Same “empty report” behavior for `dept_ids == []`
  </action>
  <verify>cd backend && python3 -m pytest -q tests/test_reports_rbac.py</verify>
  <done>Endpoint bodies shrink; responses remain identical; RBAC tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Extract query builders per report type (no generic archived helpers)</name>
  <files>backend/app/api/v1/endpoints/reports.py</files>
  <action>
Create per-entity query builders that keep semantics explicit:
- `_controls_report_query(dept_ids, department_id, status_filter)`
- `_risks_report_query(dept_ids, department_id, status_filter)`
- `_audit_trail_query(dept_ids, department_id, from_date, to_date, result_filter, ...)` (matching existing params)

Avoid:
- A generic helper that assumes common archive fields; keep each query builder explicit.
  </action>
  <verify>cd backend && python3 -m pytest -q tests/test_reports_rbac.py</verify>
  <done>Queries are readable and auditable; tests pass</done>
</task>

</tasks>

<verification>
- [ ] `cd backend && python3 -m pytest -q tests/test_reports_rbac.py`
- [ ] Manual smoke: download reports as dept head and confirm only scoped data appears
</verification>

<success_criteria>
- `reports.py` contains minimal duplication, with explicit, non-generic query builders.
- No behavior changes in bytes produced by `report_service.py`.
</success_criteria>

<output>
After completion, create `.planning/phases/251-spaghetti-simplification-2/251-05-SUMMARY.md`.
</output>

