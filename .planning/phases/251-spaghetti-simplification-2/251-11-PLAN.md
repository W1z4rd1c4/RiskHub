---
phase: 251-spaghetti-simplification-2
type: execute
domain: planning
---

<objective>
Align the Phase 251 plan set with the *current* codebase reality (post-Phase 250), by correcting the two plans that were identified as slightly stale (`251-01` and `251-02`) and adding pre-flight invariants/checks so execution won’t accidentally re-do work or introduce semantic drift.

Purpose: Ensure developers can execute 251 plans safely and mechanically with minimal back-and-forth.
Output: Updated `251-01-PLAN.md` and `251-02-PLAN.md` with corrected scope + guardrails; no code changes.
</objective>

<execution_context>
@.agent/workflows/simplify.md
@.agent/references/plan-format.md
</execution_context>

<context>
@.planning/phases/251-spaghetti-simplification-2/251-01-PLAN.md
@.planning/phases/251-spaghetti-simplification-2/251-02-PLAN.md
@backend/app/api/v1/endpoints/controls.py
@backend/app/api/v1/endpoints/departments.py
@backend/app/core/approval_helpers.py
@backend/app/core/permissions.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct `251-01` scope based on current `controls.py`</name>
  <files>.planning/phases/251-spaghetti-simplification-2/251-01-PLAN.md</files>
  <action>
Update `251-01-PLAN.md` to reflect the current state of `backend/app/api/v1/endpoints/controls.py`:

- Remove/adjust any instruction that implies `delete_control()` still uses duplicated approval-create boilerplate. It already uses `create_approval_request_with_audit` today.
- Narrow “approval-create boilerplate extraction” to `update_control()` (EDIT approvals) and any remaining duplicated `IntegrityError/log/commit` blocks still present there.
- Add an explicit invariant about **notification transaction semantics**:
  - preserve the pattern “approval persisted/committed first, then notifications attempted with their own commit/rollback”
  - do not accidentally fold notification side effects into the approval-create helper transaction
- Add a pre-flight checklist section that instructs the executor to first locate:
  - all `ApprovalRequest(` creation sites in `controls.py`
  - which ones already call `create_approval_request_with_audit`
  - which ones still do try/except boilerplate

Do not introduce new refactor scope; just correct the plan to avoid redundant work.
  </action>
  <verify>Plan text references match current `controls.py` behavior and avoids re-doing already-done work</verify>
  <done>`251-01-PLAN.md` no longer contains stale assumptions about `delete_control()` and includes explicit transaction invariants</done>
</task>

<task type="auto">
  <name>Task 2: Correct `251-02` scope based on current `departments.py`</name>
  <files>.planning/phases/251-spaghetti-simplification-2/251-02-PLAN.md</files>
  <action>
Update `251-02-PLAN.md` to reflect the current state of `backend/app/api/v1/endpoints/departments.py`:

- Avoid proposing new scoping/pagination helpers that duplicate existing ones:
  - `departments.py` already enforces per-department access using `_assert_department_in_scope(...)`.
  - sub-endpoints already use `skip/limit` with `DEFAULT_PAGE_SIZE`/`MAX_PAGE_SIZE`.
- Narrow the plan to the highest-value simplifications that still preserve behavior:
  - extract the repeated “count maps” inside `list_departments()`
  - extract the large “detail stats” builders inside `get_department()` (risk distribution, risk_by_status, control_stats, recent_executions)
  - keep archived filtering rules explicit per entity (Risk status vs Control status vs KRI via Risk join)
- Keep (or lightly tighten) docstrings only if they clarify invariants not already documented—avoid churn.

Do not change plan intent; just make it accurate and less likely to cause executor churn.
  </action>
  <verify>Plan text references match current `departments.py` helpers and endpoint signatures</verify>
  <done>`251-02-PLAN.md` focuses on real duplication (stats/query blocks) and avoids duplicating existing access/pagination helpers</done>
</task>

<task type="auto">
  <name>Task 3: Add a shared “Plan Pre-flight” section to both plans</name>
  <files>.planning/phases/251-spaghetti-simplification-2/251-01-PLAN.md, .planning/phases/251-spaghetti-simplification-2/251-02-PLAN.md</files>
  <action>
Add a small, consistent “Pre-flight” section to both plans that enforces the Phase 250/251 simplify principles:
- Confirm target duplication still exists before extracting (avoid rework).
- Confirm invariants (RBAC, archived semantics, status codes) are written down before touching code.
- Require a narrow test subset to run after the change (as already specified in each plan).

Keep it short and actionable; don’t add generic boilerplate.
  </action>
  <verify>Both plans have a clear “Pre-flight” section and remain concise</verify>
  <done>Plans are self-correcting and reduce risk of semantic drift during execution</done>
</task>

</tasks>

<verification>
- [ ] Only plan files changed (no code changes) when executing this plan
- [ ] `251-01-PLAN.md` and `251-02-PLAN.md` accurately reflect current code state and avoid redundant work
</verification>

<success_criteria>
- `251-01-PLAN.md` no longer asks to refactor already-refactored sections and explicitly preserves notification transaction semantics.
- `251-02-PLAN.md` no longer proposes duplicating existing scoping/pagination helpers; it targets real duplication in stats/query assembly.
- Both plans contain a short “Pre-flight” checklist that forces executor validation before refactoring.
</success_criteria>

<output>
No SUMMARY required for plan hygiene, but if you want recordkeeping, create:
` .planning/phases/251-spaghetti-simplification-2/251-11-SUMMARY.md`
with a bullet list of the corrections made to 251-01 and 251-02.
</output>

