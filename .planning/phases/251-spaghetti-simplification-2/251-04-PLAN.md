---
phase: 251-spaghetti-simplification-2
type: execute
domain: backend
---

<objective>
Simplify user management endpoints (`backend/app/api/v1/endpoints/users.py` and `backend/app/api/v1/endpoints/access.py`) by extracting shared query options and response builders, without changing RBAC or response shapes.

Purpose: Reduce repeated `selectinload(...)` patterns and make privileged gating auditable.
Output: Leaner endpoint modules + unchanged behavior + tests passing.
</objective>

<execution_context>
@.agent/workflows/simplify.md
@.agent/references/plan-format.md
</execution_context>

<context>
@backend/app/api/v1/endpoints/users.py
@backend/app/api/v1/endpoints/access.py
@backend/app/core/permissions.py
@backend/app/models/user.py
@backend/tests/test_users.py
@backend/tests/test_access_management.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce a shared “user query options” helper (small, explicit)</name>
  <files>backend/app/api/v1/endpoints/users.py, backend/app/api/v1/endpoints/access.py, backend/app/core/user_query_options.py</files>
  <action>
Create `backend/app/core/user_query_options.py` with a small function like:
- `def user_selectinload_options(*, include_permissions: bool) -> list`

Use it in:
- `users.py` for list/get/create/update reload queries (role, department, manager)
- `access.py` for list/update queries (role permissions selectinloads are heavier and should be toggled by `include_permissions=True`)

Preserve invariants:
- No changes to which relationships are loaded for each endpoint (only centralize the option lists).
- No changes to filtering or access checks.
  </action>
  <verify>cd backend && python3 -m pytest -q tests/test_users.py tests/test_access_management.py</verify>
  <done>Relationship-loading duplication is reduced; tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Keep access management response building consistent and typed</name>
  <files>backend/app/api/v1/endpoints/access.py</files>
  <action>
`access.py` already uses `_build_access_user_read(...)` and `_build_role_with_permissions(...)`.
Make sure all endpoints reuse these builders consistently and remove any ad-hoc response assembly.

Avoid:
- Sharing builders between `users.py` and `access.py` (schemas differ; keep lean).
  </action>
  <verify>cd backend && python3 -m pytest -q tests/test_access_management.py</verify>
  <done>`access.py` builds responses in one way only; tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Centralize privileged gating helpers (readability-only)</name>
  <files>backend/app/api/v1/endpoints/access.py</files>
  <action>
Ensure privileged gating remains explicit and unchanged:
- `_require_privileged` stays strict on `is_privileged_user(user)` (global scope).
- `_can_manage_privileged_status` stays limited to `ADMIN_PRIVILEGED_ROLES = {'admin','cro'}`.

Refactor only to reduce repetition and clarify intent (no semantic changes).
  </action>
  <verify>cd backend && python3 -m pytest -q tests/test_access_management.py</verify>
  <done>Access management RBAC is clearer and unchanged</done>
</task>

</tasks>

<verification>
- [ ] `cd backend && python3 -m pytest -q tests/test_users.py`
- [ ] `cd backend && python3 -m pytest -q tests/test_access_management.py`
</verification>

<success_criteria>
- Endpoint files are smaller; selectinload option duplication reduced.
- No RBAC or schema changes; tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/251-spaghetti-simplification-2/251-04-SUMMARY.md`.
</output>

