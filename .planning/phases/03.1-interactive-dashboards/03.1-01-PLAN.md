---
phase: 03.1-interactive-dashboards
type: execute
---

<objective>
Build fully interactive dashboards with global filters, category selections, and department-specific views for deep insight exploration.

Purpose: Transform the static dashboard into an engaging, exploratory data visualization experience.
Output: Production-ready interactive dashboard with filters, drill-downs, and multi-view capabilities.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-plan.md
@.agent/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dashboards/03-02-PLAN.md (prerequisite: current dashboard)
@frontend/src/pages/DashboardPage.tsx
@frontend/src/components/dashboard/*
@backend/app/api/v1/endpoints/dashboard.py
@backend/app/schemas/dashboard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Global Filter Context and FilterBar Component</name>
  <files>frontend/src/contexts/DashboardFilterContext.tsx, frontend/src/components/dashboard/FilterBar.tsx, frontend/src/types/dashboard.ts</files>
  <action>
Create a React Context for dashboard-wide filter state and a premium FilterBar UI component:

1. **DashboardFilterContext.tsx** - Global filter state management:
   - `selectedDepartmentId: number | null` (null = all departments)
   - `selectedRiskLevel: 'all' | 'critical' | 'high' | 'medium' | 'low'`
   - `selectedControlStatus: string | null` (active, inactive, pending, etc.)
   - `selectedControlForm: string | null` (preventive, detective, corrective)
   - `dateRange: { from: Date | null, to: Date | null }` (optional time filter)
   - Provide setters and a `resetFilters()` function

2. **FilterBar.tsx** - Premium interactive filter UI:
   - Department dropdown (from departments API)
   - Risk Level toggle buttons (All, Critical, High, Medium, Low)
   - Control Status dropdown
   - Control Form/Type dropdown
   - Active filter chips with remove buttons
   - "Clear All" button
   - Glassmorphism styling consistent with existing design

3. **Update dashboard.ts types**:
   - Add `DashboardFilters` interface for type safety

Wrap DashboardPage with the filter context provider.
  </action>
  <verify>npm run build succeeds, FilterBar renders on dashboard, filters update context state</verify>
  <done>FilterContext created, FilterBar displays with dropdowns, state updates correctly</done>
</task>

<task type="auto">
  <name>Task 2: Backend Filter Parameters for Dashboard Endpoints</name>
  <files>backend/app/api/v1/endpoints/dashboard.py, backend/app/schemas/dashboard.py, frontend/src/services/dashboardApi.ts</files>
  <action>
Add optional query parameters to all dashboard endpoints for filtered data:

1. **Update dashboard.py endpoints** - Add query parameters:
   - `department_id: Optional[int] = None` - Filter by department
   - `risk_level: Optional[str] = None` - Filter by risk score range (critical, high, medium, low)
   - `control_status: Optional[str] = None` - Filter by control status
   - `control_form: Optional[str] = None` - Filter by control form

2. **Modify SQL queries** to apply filters:
   - `/summary` - Filter controls and risks by department_id
   - `/departments` - If department_id provided, return only that department
   - `/risk-distribution` - Filter by department_id and risk_level
   - `/control-trends` - Filter by department_id and control_status

3. **Update dashboardApi.ts** - Pass filters as query params:
   - Update all fetch functions to accept optional filters object
   - Build query string from active filters
   - Types for filter params

4. **Update DashboardPage.tsx** to pass filters from context to API calls
  </action>
  <verify>
Start backend: `cd backend && source venv/bin/activate && uvicorn app.main:app --reload`
Test filtered endpoints:
- `curl http://localhost:8000/api/v1/dashboard/summary?department_id=1`
- `curl http://localhost:8000/api/v1/dashboard/risk-distribution?risk_level=critical`
  </verify>
  <done>All dashboard endpoints accept filter params, frontend sends filters, data updates based on selection</done>
</task>

<task type="auto">
  <name>Task 3: Interactive RiskDistributionMatrix with Click Navigation</name>
  <files>frontend/src/components/dashboard/RiskDistributionMatrix.tsx, frontend/src/components/dashboard/RiskDrilldownModal.tsx</files>
  <action>
Make risk matrix cells clickable to explore individual risks:

1. **Update RiskDistributionMatrix.tsx**:
   - Add `onClick` prop: `onCellClick?: (probability: number, impact: number) => void`
   - Make cells with count > 0 visually clickable (cursor-pointer, hover effect)
   - Add keyboard accessibility (Enter/Space to activate)
   - Animate cell on click with micro-interaction

2. **Create RiskDrilldownModal.tsx** - Modal showing risks in selected cell:
   - Header showing "Risks at P{x} × I{y}"
   - List of risk names with severity badges
   - Click risk name to navigate to /risks/{id}
   - Premium modal styling with backdrop blur
   - Close on escape/backdrop click

3. **Fetch risks for cell** - Add new API endpoint or use existing:
   - Option A: Filter /risks endpoint with probability & impact params
   - Option B: New `/dashboard/risks-by-cell?probability=X&impact=Y`
   - Return: list of { id, name, net_score, department_name }

4. **Wire up in DashboardPage.tsx**:
   - Add state for selected cell
   - Render RiskDrilldownModal when cell selected
  </action>
  <verify>npm run build, click matrix cell, modal shows correct risks, click risk navigates to detail</verify>
  <done>Matrix cells clickable, modal displays risks, navigation to risk detail works</done>
</task>

<task type="auto">
  <name>Task 4: Department View Switcher with Deep-Dive Dashboard</name>
  <files>frontend/src/components/dashboard/DepartmentViewSelector.tsx, frontend/src/pages/DashboardPage.tsx</files>
  <action>
Add department-focused view mode for deep exploration:

1. **DepartmentViewSelector.tsx** - View mode toggle:
   - "Executive View" (all departments, high-level metrics)
   - "Department View" (single department focus, detailed breakdown)
   - Premium tab/toggle UI with animation
   - When department selected, enter Department View automatically

2. **Update DashboardPage.tsx** for Department View mode:
   - Show department-specific header with name and key stats
   - Replace summary cards with department-specific metrics:
     - Total Controls in dept
     - Active vs Inactive breakdown
     - High/Critical risks count
     - Compliance rate gauge
   - Show department's risk matrix distribution only
   - Show control trends for department only
   - Add "Controls List" and "Risks List" quick-access sections

3. **Department comparison mode** (stretch goal):
   - Side-by-side metrics for 2 departments
   - Sparkline comparisons
  </action>
  <verify>Switch between Executive/Department views, department filter updates all charts, deep-dive shows detailed metrics</verify>
  <done>View switcher works, Department View shows focused data, navigation intuitive</done>
</task>

<task type="auto">
  <name>Task 5: Category Breakdown Charts (Status, Form, Frequency)</name>
  <files>frontend/src/components/dashboard/CategoryBreakdownCharts.tsx</files>
  <action>
Visualize the controls_by_status, controls_by_form, controls_by_frequency data:

1. **CategoryBreakdownCharts.tsx** - Interactive pie/donut charts:
   - Use Recharts PieChart with custom colors
   - Three mini charts: By Status, By Form, By Frequency
   - Hover shows percentage and count
   - Click segment filters dashboard to that category
   - Responsive layout (stack on mobile)

2. **Color scheme**:
   - Status: Active (green), Inactive (gray), Pending (amber)
   - Form: Preventive (blue), Detective (purple), Corrective (orange)
   - Frequency: Use gradient from daily (dark) to yearly (light)

3. **Integration**:
   - Add below summary stats or in new "Control Analytics" section
   - Connect click handlers to filter context
  </action>
  <verify>npm run build, charts render with correct data, clicking segment updates filters</verify>
  <done>Category charts interactive, filter integration works, design matches premium aesthetic</done>
</task>

<task type="auto">
  <name>Task 6: Enhanced DepartmentTable with Sorting and Quick Actions</name>
  <files>frontend/src/components/dashboard/DepartmentTable.tsx</files>
  <action>
Upgrade DepartmentTable to be fully interactive:

1. **Sortable columns**:
   - Click header to sort by column (toggle asc/desc)
   - Sort icon indicator
   - Sort by: name, control_count, risk_count, high_risk_count, compliance_rate

2. **Quick action buttons**:
   - "View Controls" → /controls?department={id}
   - "View Risks" → /risks?department={id}
   - "Set as Focus" → Enter Department View mode

3. **Visual enhancements**:
   - Row highlighting based on compliance (red border for <50%, yellow <80%)
   - Expandable row showing top 3 risks/controls preview
   - Hover card showing quick stats

4. **Filter integration**:
   - Highlight selected department row
   - Click row enters Department View
  </action>
  <verify>Click column headers to sort, quick actions navigate correctly, row click triggers department filter</verify>
  <done>Table fully sortable, actions work, visual feedback on selection</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Fully interactive dashboard with global filters, clickable risk matrix, department views, category charts, and enhanced table</what-built>
  <how-to-verify>
    1. Start backend: `cd backend && source venv/bin/activate && uvicorn app.main:app --reload`
    2. Start frontend: `cd frontend && npm run dev`
    3. Visit: http://localhost:5173 → Login → Dashboard
    
    **Filter Bar Tests:**
    4. Select a department from dropdown → All charts/tables update
    5. Click risk level toggle → Data filters accordingly
    6. Click filter chips to remove → Data resets
    7. Click Clear All → All filters reset
    
    **Risk Matrix Tests:**
    8. Click a cell with count > 0 → Modal opens showing risks
    9. Click risk name in modal → Navigates to risk detail page
    10. Press Escape → Modal closes
    
    **Department View Tests:**
    11. Click "Department View" tab → UI changes to focused mode
    12. Click department row in table → Enters Department View for that dept
    13. Verify all charts show department-specific data
    
    **Category Charts Tests:**
    14. Hover pie chart segments → Shows tooltip with count
    15. Click segment → Filters dashboard to that category
    
    **Table Tests:**
    16. Click column headers → Sorts table
    17. Click quick action buttons → Navigates correctly
    
    **Responsive Tests:**
    18. Resize browser to mobile → Layout adapts gracefully
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors in frontend
- [ ] `python -m pytest` passes in backend (if tests exist)
- [ ] All filter interactions work end-to-end
- [ ] Risk matrix drill-down navigates correctly
- [ ] Department view shows filtered data
- [ ] Category charts interactive
- [ ] Table sorting works
- [ ] Responsive on mobile

Manual Testing:
- Start both servers as described above
- Navigate through all interactive features
- Verify API calls in Network tab show correct filter params
</verification>

<success_criteria>

- Global FilterBar with department, risk level, status, form filters
- All dashboard endpoints accept and apply filter parameters
- RiskDistributionMatrix cells clickable with drill-down modal
- Department View mode with focused metrics
- Category breakdown charts (pie/donut) interactive
- DepartmentTable sortable with quick actions
- Premium styling maintained throughout
- Human verification passed

</success_criteria>

<output>
After completion, create `.planning/phases/03.1-interactive-dashboards/03.1-01-SUMMARY.md`
</output>
