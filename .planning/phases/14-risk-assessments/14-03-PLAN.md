---
phase: 14-risk-assessments
plan: 14-03
title: Notifications + reminders + activity logging
type: execute
wave: 1
depends_on:
  - 14-02
autonomous: true
files_modified:
  - backend/app/models/notification.py
  - backend/app/services/notification_service.py
  - backend/app/services/questionnaire_deadline_service.py
  - backend/app/models/activity_log.py
  - backend/app/i18n/en.py
  - backend/app/i18n/cs.py
  - backend/app/main.py
  - frontend/src/components/settings/NotificationSettings.tsx
  - frontend/src/components/notifications/NotificationBell.tsx
---

<objective>
Add localized in-app notifications, scheduled reminders, and activity log entries for questionnaire send/submission.

Purpose: Provide workflow visibility (“pending questionnaire”), nudges (due soon/overdue), and auditability.
Output: New NotificationTypes + scheduled checks + localized strings + ActivityLog events.
</objective>

<context>
@backend/app/models/notification.py
@backend/app/models/user.py
@backend/app/services/kri_deadline_service.py
@backend/app/services/notification_service.py
@backend/app/models/activity_log.py
@backend/app/core/activity_logger.py
@backend/app/i18n/__init__.py
@backend/app/main.py
@frontend/src/components/notifications/NotificationBell.tsx
@frontend/src/components/settings/NotificationSettings.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add questionnaire notification types (+ enum constraint migration if needed)</name>
  <files>backend/app/models/notification.py</files>
  <action>
Extend `NotificationType` with:
- `QUESTIONNAIRE_SENT = "questionnaire_sent"`
- `QUESTIONNAIRE_DUE_SOON = "questionnaire_due_soon"`
- `QUESTIONNAIRE_OVERDUE = "questionnaire_overdue"`
- `QUESTIONNAIRE_SUBMITTED = "questionnaire_submitted"`

Important: `Notification.type` uses an enum + DB constraint. Add/adjust an Alembic migration if required to update the constraint cleanly (match previous enum/constraint migrations in the repo).
  </action>
  <verify>cd backend && alembic upgrade head</verify>
  <done>DB accepts new notification types and existing notifications still load.</done>
</task>

<task type="auto">
  <name>Task 2: Localize questionnaire notification content using user.preferred_language</name>
  <files>backend/app/services/notification_service.py, backend/app/i18n/en.py, backend/app/i18n/cs.py</files>
  <action>
Add message keys for EN/CS in backend i18n:
- `notifications.questionnaire_sent_title`
- `notifications.questionnaire_sent_message`
- `notifications.questionnaire_due_soon_title`
- `notifications.questionnaire_due_soon_message`
- `notifications.questionnaire_overdue_title`
- `notifications.questionnaire_overdue_message`
- `notifications.questionnaire_submitted_title`
- `notifications.questionnaire_submitted_message`

Use placeholders like `{risk_name}`, `{due_date}`, `{actor_name}`.

When creating questionnaire notifications, translate per recipient:
- Determine locale as `user.preferred_language` (fallback `en`).
- Build `title` and `message` via `app.i18n.t(key, locale=..., **kwargs)`.

Do not change existing Approval/KRI notification behavior; apply localization only to new questionnaire notifications in this phase.
  </action>
  <verify>cd backend && python -m compileall app</verify>
  <done>Questionnaire notifications are created with localized titles/messages.</done>
</task>

<task type="auto">
  <name>Task 3: Add scheduled due-soon/overdue reminder service</name>
  <files>backend/app/services/questionnaire_deadline_service.py, backend/app/main.py</files>
  <action>
Create `QuestionnaireDeadlineService` modeled after `KRIDeadlineService`:
- Query open questionnaires (`sent`/`in_progress`).
- “Due soon”: due date is exactly 7 days from today (or within a 24h window).
- “Overdue”: due date < now; send weekly at most (use duplicate lookback).
- Use duplicate prevention similar to `_check_duplicate_notification` in `KRIDeadlineService`.
- Create notifications to the assignee (`assigned_to_user_id`) for due soon/overdue.

Wire into the existing APScheduler job setup in `backend/app/main.py` (where KRI checks are scheduled) so it runs daily.
  </action>
  <verify>cd backend && python -m compileall app</verify>
  <done>Deadline service runs via scheduler and generates notifications without spamming duplicates.</done>
</task>

<task type="auto">
  <name>Task 4: Log send + submit events to Activity Log</name>
  <files>backend/app/models/activity_log.py, backend/app/services/risk_questionnaire_service.py, backend/app/api/v1/endpoints/risk_questionnaires.py</files>
  <action>
Extend `ActivityEntityType` with `RISK_QUESTIONNAIRE` and log:
- On send: `ActivityAction.CREATE` for the questionnaire instance
- On submit: `ActivityAction.STATUS_CHANGE` (or `UPDATE`) with a clear description

Store `department_id` from the risk for correct activity scoping.

Minimum required by product: log “sent” and “submitted”.
  </action>
  <verify>cd backend && pytest -q</verify>
  <done>Activity Log shows questionnaire send/submission events.</done>
</task>

<task type="auto">
  <name>Task 5: Expose questionnaire notification preference toggles in UI</name>
  <files>frontend/src/components/settings/NotificationSettings.tsx, frontend/src/components/notifications/NotificationBell.tsx</files>
  <action>
Add new notification preference keys (matching `NotificationType.value`):
- `questionnaire_sent`
- `questionnaire_due_soon`
- `questionnaire_overdue`
- `questionnaire_submitted`

Update:
- Settings notification toggles list (add translation keys + short descriptions).
- Notification bell icon mapping to show reasonable icons/colors for these types.

Keep existing behavior unchanged for other notification types.
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <done>Users can enable/disable questionnaire notifications, and bell shows correct icons.</done>
</task>

</tasks>

<verification>
Before declaring complete:
- [ ] `cd backend && alembic upgrade head`
- [ ] `cd backend && pytest -q`
- [ ] `cd frontend && npx tsc --noEmit`
</verification>

<must_haves>
- Assignee receives sent/due-soon/overdue notifications.
- RM/CRO receives submission notification (wired in Plan 14-02 submit path).
- Activity Log includes send + submit.
- All new notification content is localized EN/CS via user preferences.
</must_haves>

<output>
After completion, create `.planning/phases/14-risk-assessments/14-03-SUMMARY.md` with:
- Notification types added
- Scheduler wiring notes
- Activity log event examples
</output>

