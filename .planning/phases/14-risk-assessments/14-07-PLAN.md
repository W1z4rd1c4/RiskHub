---
phase: 14-risk-assessments
plan: 14-07
title: End-to-end verification (tests + Playwright)
type: execute
wave: 3
depends_on:
  - 14-03
  - 14-05
  - 14-06
autonomous: true
files_modified:
  - backend/tests/api/v1/test_risk_questionnaires_notifications.py
  - frontend/e2e/questionnaires.spec.ts
---

<objective>
Add end-to-end coverage for the questionnaire workflow and lock in business logic via targeted backend tests + Playwright.

Purpose: Prevent regressions and validate the full UX: CRO/RM sends → owner fills → RM/CRO notified → history visible.
Output: Focused tests that cover lifecycle, scoping, and notifications.
</objective>

<context>
@backend/app/services/questionnaire_deadline_service.py
@frontend/src/pages/RiskDetailPage.tsx
@frontend/src/pages/RiskHubPage.tsx
@frontend/e2e
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend tests for reminder + notification creation</name>
  <files>backend/tests/api/v1/test_risk_questionnaires_notifications.py</files>
  <action>
Add tests that verify:
- On send: `questionnaire_sent` notification is created for the assignee.
- On submit: `questionnaire_submitted` notification is created for RM/CRO recipients.
- Deadline service:
  - due soon generates `questionnaire_due_soon` with duplicate prevention
  - overdue generates `questionnaire_overdue` with duplicate prevention

Use deterministic dates (freeze time or set due_at explicitly).
  </action>
  <verify>cd backend && pytest -q tests/api/v1/test_risk_questionnaires_notifications.py</verify>
  <done>Notification behavior is locked with tests and is locale-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright flow test</name>
  <files>frontend/e2e/questionnaires.spec.ts</files>
  <action>
Add a Playwright test covering:
1) Log in as CRO or Risk Manager → open a known risk → send questionnaire from the Risk Assessment tab.
2) Log in as Risk Owner → open same risk → fill questionnaire and submit.
3) Verify:
   - Risk Assessment tab shows “Submitted” badge and history entry.
   - RM/CRO sees a new in-app notification (or it appears on /notifications).

Use existing login helpers and (if available) Phase 179 deterministic seed data; otherwise extend seed data minimally to ensure:
- a risk with owner exists
- CRO/RM and owner users exist
  </action>
  <verify>cd frontend && npx playwright test -g \"questionnaire\"</verify>
  <done>E2E test passes and demonstrates the full workflow.</done>
</task>

</tasks>

<verification>
Before declaring complete:
- [ ] `cd backend && pytest -q`
- [ ] `cd frontend && npx playwright test -g "questionnaire"`
</verification>

<must_haves>
- Full workflow is validated end-to-end.
- Notifications and history are observable in UI.
</must_haves>

<output>
After completion, create `.planning/phases/14-risk-assessments/14-07-SUMMARY.md`.
</output>

