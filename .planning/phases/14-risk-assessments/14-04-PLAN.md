---
phase: 14-risk-assessments
plan: 14-04
title: Risk detail tab + questionnaire history grid
type: execute
wave: 2
depends_on:
  - 14-02
autonomous: true
files_modified:
  - frontend/src/pages/RiskDetailPage.tsx
  - frontend/src/components/risks/RiskDetailQuestionnairesTab.tsx
  - frontend/src/services/riskQuestionnairesApi.ts
  - frontend/src/types/riskQuestionnaire.ts
  - frontend/src/i18n/locales/en/risks.json
  - frontend/src/i18n/locales/cs/risks.json
---

<objective>
Add a 3rd “Risk Assessment” tab on the Risk detail page showing questionnaire status + full history (most recent first).

Purpose: Give risk viewers a clear audit trail and give RM/CRO the “send” entry point on a single risk.
Output: New tab UI + API wiring + localized labels.
</objective>

<context>
@frontend/src/pages/RiskDetailPage.tsx
@frontend/src/components/risks/RiskDetailKriHistoryTab.tsx
@frontend/src/services/riskApi.ts
@frontend/src/i18n/locales/en/risks.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add API client + types for questionnaires</name>
  <files>frontend/src/services/riskQuestionnairesApi.ts, frontend/src/types/riskQuestionnaire.ts</files>
  <action>
Create a small client wrapper consistent with existing `*Api.ts` modules:
- `listForRisk(riskId)`
- `sendForRisk(riskId)`
- `get(questionnaireId)`
- `saveDraft(questionnaireId, answers)`
- `submit(questionnaireId, answers)`

Define types that mirror backend schemas for list/detail payloads.
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <done>Frontend compiles with the new API client + types.</done>
</task>

<task type="auto">
  <name>Task 2: Add RiskDetailQuestionnairesTab and wire into RiskDetailPage</name>
  <files>frontend/src/components/risks/RiskDetailQuestionnairesTab.tsx, frontend/src/pages/RiskDetailPage.tsx</files>
  <action>
Implement `RiskDetailQuestionnairesTab` matching existing styling:
- Show current “open” questionnaire (if any) with badge:
  - Pending (`sent`), In progress, Overdue
- Show a grid/table of historical questionnaires (including the current one), newest first.
  - Columns: Status, Sent at, Due date, Submitted at, Sent by, Submitted by
  - Row click opens detail view (Plan 14-05) or expands inline.

Update `RiskDetailPage.tsx`:
- Add a new tab button next to Overview and KRI History.
- Localize tab label using `risks.*` keys.
- Ensure tab switching preserves existing behavior.
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <done>Risk page has a 3rd tab and renders questionnaire history without TS errors.</done>
</task>

<task type="auto">
  <name>Task 3: Add role-gated “Send questionnaire” CTA (single risk)</name>
  <files>frontend/src/components/risks/RiskDetailQuestionnairesTab.tsx</files>
  <action>
Add a primary CTA in the tab header:
- Visible only to RM/CRO
- Disabled with tooltip if risk has no owner
- On click:
  - call `sendForRisk(riskId)`
  - refresh list
  - show toast/banner success/failure

If backend returns “open already exists” (409), show a friendly message and link/open the existing open questionnaire.
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <done>RM/CRO can send a questionnaire from a risk; non-privileged users cannot.</done>
</task>

<task type="auto">
  <name>Task 4: Localize labels and badges (EN/CS)</name>
  <files>frontend/src/i18n/locales/en/risks.json, frontend/src/i18n/locales/cs/risks.json</files>
  <action>
Add translation keys for:
- Tab label: “Risk Assessment”
- Status badges: Pending / In progress / Submitted / Overdue
- Empty state text and CTA labels
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <done>Risk assessment tab UI is localized in EN/CS.</done>
</task>

</tasks>

<verification>
Before declaring complete:
- [ ] `cd frontend && npx tsc --noEmit`
- [ ] Manual: open a risk and confirm the new tab renders and lists instances.
</verification>

<must_haves>
- New Risk Detail tab exists next to Overview and KRI History.
- History list is newest-first and shows required badges.
- RM/CRO can send from this tab.
</must_haves>

<output>
After completion, create `.planning/phases/14-risk-assessments/14-04-SUMMARY.md` noting:
- New components/routes
- How badge logic is computed
</output>

