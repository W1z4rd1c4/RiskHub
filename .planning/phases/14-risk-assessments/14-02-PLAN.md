---
phase: 14-risk-assessments
plan: 14-02
title: Questionnaire API + RBAC
type: execute
wave: 1
depends_on:
  - 14-01
autonomous: true
files_modified:
  - backend/app/api/v1/router.py
  - backend/app/api/v1/endpoints/risk_questionnaires.py
  - backend/app/services/risk_questionnaire_service.py
  - backend/tests/api/v1/test_risk_questionnaires.py
---

<objective>
Ship the backend API for sending, listing, viewing, drafting, and submitting per-risk questionnaires with correct RBAC and department scoping.

Purpose: Enable the Risk Detail tab + “my pending questionnaires” workflow view + batch send integration later.
Output: New router + endpoints + service helpers + regression tests.
</objective>

<context>
@.planning/phases/14-risk-assessments/14-CONTEXT.md
@backend/app/api/v1/router.py
@backend/app/api/v1/endpoints/risks.py
@backend/app/core/permissions.py
@backend/app/models/risk.py
@backend/app/models/role.py
@backend/app/models/risk_questionnaire.py
@backend/app/schemas/risk_questionnaire.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add questionnaire endpoints</name>
  <files>backend/app/api/v1/router.py, backend/app/api/v1/endpoints/risk_questionnaires.py</files>
  <action>
Create a new endpoint module `backend/app/api/v1/endpoints/risk_questionnaires.py` and include it in `backend/app/api/v1/router.py` with a stable prefix:
- Suggested prefix: `/questionnaires`

Endpoints (suggested shapes):
1) `GET /risks/{risk_id}/questionnaires`
   - Anyone who can access the Risk can list.
   - Return newest-first (submitted_at desc, then sent_at desc).

2) `POST /risks/{risk_id}/questionnaires/send`
   - Only RM or CRO (role name: `risk_manager` or `cro`).
   - Requires `risk.owner_id` set; otherwise 400.
   - Enforce “at most one open questionnaire per risk” in code:
     - If an existing instance with status in (`sent`, `in_progress`) exists → 409.
   - Creates `sent_at=now`, `due_at=now+15d`, status=`sent`, template_key/version.

3) `GET /questionnaires/{questionnaire_id}`
   - Anyone who can access the underlying risk can view.
   - If current user is allowed to submit and status is `sent`, optionally mark it `in_progress` (UX: “pending → in progress”).

4) `PATCH /questionnaires/{questionnaire_id}/draft`
   - Allowed for Risk Owner or Department Head (same dept as risk).
   - Only when status in (`sent`, `in_progress`).
   - Updates `answers` JSON and sets status=`in_progress`.

5) `POST /questionnaires/{questionnaire_id}/submit`
   - Allowed for Risk Owner or Department Head (same dept as risk).
   - Only when status in (`sent`, `in_progress`).
   - Validates required keys for “v1” questionnaire (implemented as a constant list in service layer).
   - Sets status=`submitted`, `submitted_at=now`, `submitted_by_user_id=current_user.id`.

6) `GET /questionnaires/inbox`
   - Returns open questionnaires the current user can act on:
     - If user is Risk Owner: items where `assigned_to_user_id == me` and open status.
     - If user is Department Head: items for their department’s risks that are open.
   - Used by the frontend “Workflow” integration (and optional sidebar badge later).

All endpoints must enforce department scoping via existing patterns:
- Load Risk and call `check_department_access(risk.department_id, current_user)` (or apply `dept_ids` filtering).
- Avoid leaking existence: use consistent 404 for inaccessible risks where appropriate (match existing `risks.py` patterns).
  </action>
  <verify>cd backend && python -m compileall app</verify>
  <done>API routes exist and basic behavior is implemented with scoping and role gates.</done>
</task>

<task type="auto">
  <name>Task 2: Add service helpers for shared business rules</name>
  <files>backend/app/services/risk_questionnaire_service.py</files>
  <action>
Add a small service module encapsulating:
- `QUESTIONNAIRE_TEMPLATE_KEY = "risk_owner_reassessment"`
- `QUESTIONNAIRE_TEMPLATE_VERSION = "v1"`
- `QUESTION_KEYS_V1: list[str]` and `REQUIRED_KEYS_V1: set[str]` (for submit validation)
- helper: `can_submit_questionnaire(current_user, risk) -> bool` implementing:
  - risk owner OR (role == department_head AND user.department_id == risk.department_id)
- helper: `can_send_questionnaire(current_user) -> bool` implementing:
  - role in {risk_manager, cro}

Keep endpoint modules thin and re-usable for batch send (Plan 14-06).
  </action>
  <verify>cd backend && python -m compileall app</verify>
  <done>Business rules are centralized and reused across endpoints.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for RBAC + lifecycle</name>
  <files>backend/tests/api/v1/test_risk_questionnaires.py</files>
  <action>
Add pytest coverage for:
- Scoping: user cannot list/view questionnaires for out-of-scope risk.
- Send permissions: only RM/CRO can send; requires risk.owner_id.
- Single-open enforcement: second send returns 409 while an open questionnaire exists.
- Submit permissions: owner and dept head can submit; others cannot.
- Post-submit immutability: draft/submit after submitted returns 409/400 (consistent error).
- Inbox endpoint returns only actionable items for the caller.

Use existing auth fixtures/patterns (e.g., `auth_client`, `X-Mock-User-Id`, role fixtures) consistent with the current suite.
  </action>
  <verify>cd backend && pytest -q tests/api/v1/test_risk_questionnaires.py</verify>
  <done>Questionnaire API is protected by tests and behaves deterministically.</done>
</task>

</tasks>

<verification>
Before declaring complete:
- [ ] `cd backend && pytest -q tests/api/v1/test_risk_questionnaires.py`
</verification>

<must_haves>
- Correct RBAC for send vs submit vs view.
- Department scoping matches Risk visibility rules.
- Lifecycle and history are stable and test-covered.
</must_haves>

<output>
After completion, create `.planning/phases/14-risk-assessments/14-02-SUMMARY.md` with:
- Route list
- RBAC rules implemented
- Tests added
</output>

