---
phase: 14-risk-assessments
plan: 14-05
title: Questionnaire detail + submission form (v1 questions)
type: execute
wave: 2
depends_on:
  - 14-04
autonomous: true
files_modified:
  - frontend/src/components/risks/RiskQuestionnaireDetail.tsx
  - frontend/src/components/risks/riskQuestionnaireQuestions.ts
  - frontend/src/i18n/locales/en/risks.json
  - frontend/src/i18n/locales/cs/risks.json
---

<objective>
Implement the questionnaire detail view with a v1 question set and a submit flow for Risk Owner / Department Head, plus read-only mode after submission.

Purpose: Complete the “toggle questionnaire to risk owner” workflow end-to-end on the Risk detail page.
Output: A polished questionnaire form experience with draft + submit + immutable post-submit view.
</objective>

<context>
@.planning/phases/14-risk-assessments/14-RESEARCH.md
@frontend/src/components/risks/RiskDetailQuestionnairesTab.tsx
@frontend/src/i18n/locales/en/risks.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define v1 question set as stable keys</name>
  <files>frontend/src/components/risks/riskQuestionnaireQuestions.ts, frontend/src/i18n/locales/en/risks.json, frontend/src/i18n/locales/cs/risks.json</files>
  <action>
Create a `riskQuestionnaireQuestions.ts` module exporting an ordered list of questions for template `risk_owner_reassessment@v1`, with:
- `key` (e.g., `risk_assessment.q1_description_changed`)
- `type` (`boolean`, `single_select`, `multi_select`, `text`, `textarea`, `number`)
- `required` boolean
- optional `options` (for selects; options themselves should also be translation keys)

Add EN/CS i18n strings for:
- Section titles (Risk changes / Controls / KRIs / Outlook)
- Question labels
- Option labels

Keep the question set to ~10–15 questions to start, matching the Phase 14 research list.
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <done>Question set is defined and fully localizable.</done>
</task>

<task type="auto">
  <name>Task 2: Build questionnaire detail component (read-only + editable)</name>
  <files>frontend/src/components/risks/RiskQuestionnaireDetail.tsx, frontend/src/components/risks/RiskDetailQuestionnairesTab.tsx</files>
  <action>
Implement a detail view that opens from the history grid (modal, drawer, or inline panel):
- Shows metadata (sent_at, due_at, status, assignee, sender).
- If status is `submitted`: render answers read-only.
- If status is open and user can submit:
  - render inputs for the v1 question set
  - provide “Save progress” (draft) and “Submit” buttons
  - lock submission after success and refresh the list

UX requirements:
- Show “Overdue” badge if due date passed and not submitted.
- After submit, the form becomes read-only and “submitted” badge is shown.
- Validation: required questions block submit (client-side).
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <done>Users can view and (if allowed) complete and submit the questionnaire from the Risk page.</done>
</task>

</tasks>

<verification>
Before declaring complete:
- [ ] `cd frontend && npx tsc --noEmit`
- [ ] Manual: as Risk Owner, fill questionnaire and submit; then confirm it becomes read-only.
</verification>

<must_haves>
- Risk Owner / Department Head can submit.
- No edits after submission.
- Questions + UI are localized EN/CS.
</must_haves>

<output>
After completion, create `.planning/phases/14-risk-assessments/14-05-SUMMARY.md` with:
- v1 question list summary
- Form UX decisions (modal vs inline, draft handling)
</output>

