---
phase: 156.1-admin-role-rbac-hardening
plan: 156.1-05
type: execute
wave: 3
depends_on: [156.1-01, 156.1-02, 156.1-03, 156.1-04]
files_modified:
  - backend/tests/test_access_management.py
  - backend/tests/test_admin_logs.py
  - frontend/src/pages/__tests__/rbac_gating.test.tsx
  - frontend/e2e/admin.spec.ts
  - docs/BUSINESS_LOGIC.md
  - docs/admin/user-management.md
  - docs/admin/riskhub-config.md
autonomous: true
---

<objective>
Run final regression and documentation reconciliation so admin-role hardening ships with clear business rules and enforceable tests.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Build a focused authorization regression matrix</name>
  <files>backend/tests/test_access_management.py, frontend/src/pages/__tests__/rbac_gating.test.tsx, frontend/e2e/admin.spec.ts</files>
  <action>
Ensure matrix covers:
- Admin/CRO can mutate access-user fields.
- Global non-admin users cannot mutate access-user fields.
- Read/list access behavior remains correct for global privileged users and department heads.
- Admin log config save works with canonical payload.
- Legacy log-config payload compatibility is validated at API level only (not frontend default path).
  </action>
  <verify>All targeted backend/frontend/E2E tests pass locally.</verify>
  <done>Critical regressions are covered across API + UI layers.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile business and admin documentation</name>
  <files>docs/BUSINESS_LOGIC.md, docs/admin/user-management.md, docs/admin/riskhub-config.md</files>
  <action>
Update role and permission documentation to explicitly state:
- Admin is platform/admin-console focused and not implied wildcard business superuser.
- Access-user mutations are admin/CRO-only.
- Admin log config uses split app/audit fields; legacy payload support is transitional.

Ensure wording is consistent across business logic and admin guides.
  </action>
  <verify>Docs do not conflict with implemented backend/frontend behavior.</verify>
  <done>Operational and product documentation is trustworthy for support and audit use.</done>
</task>

<task type="auto">
  <name>Task 3: Closeout gate and rollback criteria</name>
  <files>.planning/phases/156.1-admin-role-rbac-hardening/*</files>
  <action>
Record completion criteria in summary:
- Zero unauthorized access-user mutation path.
- No frontend/backend log-config schema mismatch.
- No seed/fixture wildcard-admin drift.

Define rollback trigger:
- Any newly blocked access-management flow for admin/CRO.
- Any 422/500 regression from canonical admin log-config save path.
  </action>
  <verify>Gate checklist is captured in summary and executable by release reviewers.</verify>
  <done>Phase has explicit release/rollback criteria.</done>
</task>

</tasks>

<must_haves>
- Cross-layer behavior parity (backend auth, frontend gating, docs).
- Regression tests cover both allow and deny cases.
- Rollback triggers are concrete and testable.
</must_haves>

<verification>
- [ ] `cd backend && venv/bin/pytest tests/test_access_management.py tests/test_admin_logs.py -q`
- [ ] `cd frontend && npx tsc --noEmit`
- [ ] `cd frontend && npm run test:run -- src/pages/__tests__/rbac_gating.test.tsx`
- [ ] `cd frontend && npx playwright test e2e/admin.spec.ts --project=chromium`
</verification>

