---
phase: 156.1-admin-role-rbac-hardening
plan: 156.1-04
type: execute
wave: 2
depends_on: [156.1-01, 156.1-03]
files_modified:
  - /Users/stefanlesnak/Antigravity/Risk App 2/backend/scripts/seed_roles_permissions.py
  - /Users/stefanlesnak/Antigravity/Risk App 2/backend/tests/conftest.py
  - /Users/stefanlesnak/Antigravity/Risk App 2/frontend/src/test/mocks/handlers.ts
  - /Users/stefanlesnak/Antigravity/Risk App 2/docs/TESTING.md
  - /Users/stefanlesnak/Antigravity/Risk App 2/backend/app/db/rbac_seed_contract.py
autonomous: true
---

<objective>
Converge seed/test/mock contracts to the canonical admin model so implementation, tests, and docs stop disagreeing about admin permissions.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Align or deprecate legacy role seed script with explicit warning path</name>
  <files>backend/scripts/seed_roles_permissions.py</files>
  <action>
Bring legacy script behavior in line with canonical RBAC:
- Remove wildcard full-access grant assumptions for admin unless explicitly intended by canonical contract.
- Prefer delegating to canonical seed contract path, or add a clear runtime warning that script is legacy and non-authoritative.
- Ensure script does not reintroduce `admin = *:*` drift in environments where it is still used.
  </action>
  <verify>Running legacy script no longer creates admin wildcard divergence from canonical seed contract.</verify>
  <done>Seed entrypoints produce consistent admin permission model.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize backend fixtures and frontend mock users</name>
  <files>backend/tests/conftest.py, frontend/src/test/mocks/handlers.ts</files>
  <action>
Update fixtures/mocks so admin permissions reflect platform/admin-console behavior, not wildcard superuser assumptions.

Introduce explicit fixture naming for special-case wildcard users if still needed in isolated tests (for example `superuser_wildcard`), instead of overloading default `admin` fixture semantics.
  </action>
  <verify>Default admin fixtures/mocks align with canonical RBAC contract.</verify>
  <done>Tests no longer pass due to outdated wildcard assumptions.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile testing documentation with current admin contract</name>
  <files>docs/TESTING.md</files>
  <action>
Update testing docs to reflect:
- canonical admin permissions,
- when wildcard fixtures are allowed (if any),
- expected role setup for access-management and admin-console tests.
  </action>
  <verify>Docs match fixture behavior and seeded role semantics.</verify>
  <done>No misleading wildcard-admin documentation remains.</done>
</task>

</tasks>

<must_haves>
- Canonical RBAC contract remains single source of truth.
- Legacy script cannot silently diverge admin semantics.
- Fixture naming makes wildcard usage explicit and opt-in.
</must_haves>

<verification>
- [ ] `cd backend && venv/bin/pytest tests/test_access_management.py -q`
- [ ] `cd frontend && npm run test:run -- src/pages/__tests__/rbac_gating.test.tsx`
- [ ] Manual check: `docs/TESTING.md` examples match current fixtures
</verification>
