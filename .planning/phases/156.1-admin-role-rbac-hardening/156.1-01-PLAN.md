---
phase: 156.1-admin-role-rbac-hardening
plan: 156.1-01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/v1/endpoints/access.py
  - backend/app/schemas/access.py
  - backend/tests/test_access_management.py
autonomous: true
---

<objective>
Eliminate unauthorized access-user mutations by requiring admin/CRO authority for all writable access-management fields.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit admin/CRO write guard for access-user mutations</name>
  <files>backend/app/api/v1/endpoints/access.py</files>
  <action>
Implement a dedicated mutation guard (separate from read/list guard):
- Keep existing privileged-scope gate for list/read endpoints.
- For `PATCH /access/users/{id}`, require:
  1) global scope, and
  2) role in `{admin, cro}`.

Apply this guard before processing any update payload field (`role_id`, `department_id`, `manager_id`, `access_scope`).

Preserve existing protections:
- prevent self-demotion from privileged access,
- prevent removing last admin/CRO from privileged pool.
  </action>
  <verify>Non-admin global users receive 403 for any PATCH payload on `/access/users/{id}`.</verify>
  <done>Access mutations are blocked unless caller is admin/CRO.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten schema comments and endpoint contract text</name>
  <files>backend/app/schemas/access.py, backend/app/api/v1/endpoints/access.py</files>
  <action>
Update docstrings/comments to explicitly state:
- list/read access data may be visible to privileged/global users,
- mutations are admin/CRO-only.

Keep wire schema unchanged unless strictly required for validation correctness.
  </action>
  <verify>Endpoint docs and comments reflect the enforced contract.</verify>
  <done>No ambiguous "privileged can edit" messaging remains in backend code.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for mutation authorization</name>
  <files>backend/tests/test_access_management.py</files>
  <action>
Add focused tests:
- `risk_manager` (global scope) cannot patch `department_id`.
- `risk_manager` (global scope) cannot patch `manager_id`.
- `risk_manager` cannot patch mixed payload with only non-role fields.
- `admin` (and/or CRO fixture, if available) can patch allowed fields successfully.
- existing self-demotion/last-admin checks remain green.
  </action>
  <verify>`cd backend && venv/bin/pytest tests/test_access_management.py -q` passes.</verify>
  <done>Mutation authorization behavior is locked by tests.</done>
</task>

</tasks>

<must_haves>
- Backend remains the source of truth for authorization.
- No writable access-user field can be changed by non-admin/CRO callers.
- Error status is consistent (`403`) for denied mutation attempts.
</must_haves>

<verification>
- [ ] `cd backend && venv/bin/pytest tests/test_access_management.py -q`
- [ ] `cd backend && venv/bin/pytest tests/test_access_management.py -k "update or demotion" -q`
</verification>

