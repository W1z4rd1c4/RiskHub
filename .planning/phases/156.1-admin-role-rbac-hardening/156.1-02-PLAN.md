---
phase: 156.1-admin-role-rbac-hardening
plan: 156.1-02
type: execute
wave: 2
depends_on: [156.1-01]
files_modified:
  - frontend/src/authz/policy.ts
  - frontend/src/hooks/usePermissions.ts
  - frontend/src/pages/UsersPage.tsx
  - frontend/src/components/access/UsersTable.tsx
  - frontend/src/components/access/AccessEditModal.tsx
  - frontend/src/pages/__tests__/rbac_gating.test.tsx
autonomous: true
---

<objective>
Align frontend access-management editing controls with backend authorization so UI never advertises edits that will 403.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Separate read-access vs edit-access capabilities in frontend authz model</name>
  <files>frontend/src/authz/policy.ts, frontend/src/hooks/usePermissions.ts</files>
  <action>
Introduce explicit capability for access-user edits (for example `canEditAccessUsers`), derived as:
- `isPlatformAdmin || isCRO`.

Keep read/list access capability (`canManageAccess` or equivalent) available for global privileged visibility use-cases.
  </action>
  <verify>Authz layer clearly distinguishes read/list capability from edit capability.</verify>
  <done>Frontend can gate edit actions without removing read visibility.</done>
</task>

<task type="auto">
  <name>Task 2: Gate edit entry points and modal submit path</name>
  <files>frontend/src/pages/UsersPage.tsx, frontend/src/components/access/UsersTable.tsx, frontend/src/components/access/AccessEditModal.tsx</files>
  <action>
Update UI wiring:
- Show edit button only when `canEditAccessUsers` is true.
- Ensure modal submit is blocked when caller lacks edit capability (defensive check).
- Keep table visibility and permission chips available for users that can read but not edit.

If unauthorized edit interaction is somehow triggered, show a clear local error instead of firing mutation.
  </action>
  <verify>Risk manager/global users can view access data but cannot trigger mutation requests from UI.</verify>
  <done>Frontend behavior mirrors backend mutation policy exactly.</done>
</task>

<task type="auto">
  <name>Task 3: Add frontend regression tests for new gating split</name>
  <files>frontend/src/pages/__tests__/rbac_gating.test.tsx</files>
  <action>
Add/adjust tests for access page behavior:
- admin/CRO can see edit controls.
- global non-admin role (for example risk_manager) can view but cannot edit.
- department-head access mode behavior remains unchanged for department-scoped listing.

Prefer component-level assertion that edit controls are absent, not only route-level checks.
  </action>
  <verify>`cd frontend && npm run test:run -- src/pages/__tests__/rbac_gating.test.tsx` passes.</verify>
  <done>Edit gating regressions are caught in automated tests.</done>
</task>

</tasks>

<must_haves>
- No UI path allows unauthorized access-user patch attempts.
- Existing list/read access behavior for privileged non-admin users remains intact.
- Permission model names are unambiguous (`view` vs `edit`).
</must_haves>

<verification>
- [ ] `cd frontend && npm run test:run -- src/pages/__tests__/rbac_gating.test.tsx`
- [ ] `cd frontend && npx tsc --noEmit`
</verification>

