---
phase: 156.1-admin-role-rbac-hardening
plan: 156.1-01
status: complete
completed: 2026-02-11
---

# Summary 156.1-01: Backend Access-User Mutation Hardening

## Outcome
- Enforced admin/CRO-only authorization for all access-user mutations in backend.
- Preserved existing self-demotion and last-admin/CRO guardrails.

## Changes
- Updated `backend/app/api/v1/endpoints/access.py`:
  - Added `_require_access_user_write()` and applied it to `PATCH /api/v1/access/users/{id}`.
  - Kept read/list gate (`_require_privileged`) separate from write gate.
  - Removed field-specific inline checks and centralized mutation authorization.
- Updated `backend/app/schemas/access.py` docstring to state admin/CRO-only mutation policy.
- Added regression tests in `backend/tests/test_access_management.py`:
  - Non-admin global (`risk_manager`) denied for `department_id` mutation.
  - Non-admin global denied for `manager_id` mutation.
  - Non-admin global denied for mixed non-role mutation payload.
  - Admin mutation allow-path verified.

## Verification
- `cd backend && venv/bin/pytest tests/test_access_management.py -q` → `9 passed`
- `cd backend && venv/bin/pytest tests/test_access_management.py tests/test_admin_logs.py -q` → `19 passed` (includes this surface)
