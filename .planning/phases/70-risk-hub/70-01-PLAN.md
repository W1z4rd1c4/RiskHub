---
phase: 70-risk-hub
type: execute
---

<objective>
Enhance Admin Role with comprehensive Platform Administration features.

Purpose: Expand Admin role from just "User Management" to a full "Platform Admin" (IT) role. They manage users, monitor system health, view technical logs, and manage sessions.
Output: Enhanced Admin Console with User Management, System Health, and Audit Logs tabs.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@backend/app/api/v1/endpoints/users.py
@backend/app/api/v1/endpoints/admin.py
@frontend/src/pages/AdminConsolePage.tsx (existing shell from 70-05 needs splitting)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Admin capabilities in role.py (Revised)</name>
  <files>backend/app/models/role.py</files>
  <action>
    1) Remove `RoleType.ADMIN` from `privileged_roles()` (business auth).
    2) Create `system_admin_roles()` returning `{RoleType.ADMIN}`.
    3) Docstrings: Admin = Platform Admin (Users, Logs, Health). CRO = Business Admin (Risk Hub).
  </action>
  <verify>`grep -n "ADMIN" backend/app/models/role.py`</verify>
  <done>Admin defined as Platform Admin.</done>
</task>

<task type="auto">
  <name>Task 2: Implement System Health API</name>
  <files>backend/app/api/v1/endpoints/admin.py</files>
  <action>
    Create Admin-only endpoints:
    1) `GET /admin/health`: Database connection status, Redis status (if any), Disk usage, Uptime.
    2) `GET /admin/stats`: Total users, active sessions (approx), total requests (counters).
  </action>
  <verify>`curl /api/v1/admin/health` as admin</verify>
  <done>System health endpoints created.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Technical Audit Log API</name>
  <files>backend/app/api/v1/endpoints/admin.py</files>
  <action>
    Create Admin-only endpoints:
    1) `GET /admin/logs`: Fetch recent system events (Login, Failed Login, User Suspended, Config Change).
    2) Reuse existing `ActivityLog` but expose system-level events here that might be hidden from business users.
  </action>
  <verify>View logs as admin</verify>
  <done>Technical log API created.</done>
</task>

<task type="auto">
  <name>Task 4: Update Admin Console UI</name>
  <files>frontend/src/pages/AdminConsolePage.tsx, frontend/src/components/admin/*</files>
  <action>
    Update Admin Console (visible only to Admin):
    - **Tab 1: User Management** (Existing UsersPage content embedded or linked).
    - **Tab 2: System Health** (New):
      - Status cards (DB: Connected, Uptime: 24h).
      - Charts (error rates, if metrics available).
    - **Tab 3: Application Logs** (New):
      - Table of recent system events/errors.
    - **Tab 4: Sessions** (New):
      - List active users, "Force Logout" button (revokes token/user active).
  </action>
  <verify>Admin Console shows full IT monitoring suite</verify>
  <done>Admin Console upgraded to Platform Admin dashboard.</done>
</task>

<task type="auto">
  <name>Task 5: Enforce Risk Hub Blindness</name>
  <files>backend/app/api/v1/endpoints/riskhub.py</files>
  <action>
    Ensure all `/riskhub` endpoints return 403 Forbidden for Admin role. Admin should technically manage the platform without seeing the business rules inside.
  </action>
  <verify>Admin gets 403 on Risk Hub keys</verify>
  <done>Admin restricted from Business Config.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Admin has full User/System/Log management
- [ ] Admin cannot access Risk Hub (403 Forbidden)
- [ ] CRO cannot access Admin Console (403 Forbidden)
- [ ] CRO cannot manage users (except via Admin Console if dual-role, but by default CRO role has no user mgmt)
</verification>

<success_criteria>

- Admin role = Platform Admin (IT)
- CRO role = Business Admin (Risk Hub)
- Distinct consoles for distinct responsibilities
</success_criteria>

<output>
After completion, create `.planning/phases/70-risk-hub/70-01-SUMMARY.md`
</output>
