---
phase: 70-risk-hub
type: execute
---

<objective>
Build the Risk Hub UI - a CRO dashboard for managing system configuration.

Purpose: Provide CRO (Chief Risk Officer) with a centralized interface to manage risk types, global settings, and approval rules. This is the business authority console, NOT for Admin.
Output: New Risk Hub page accessible only to CRO role.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@frontend/src/components/layout/Sidebar.tsx
@frontend/src/hooks/usePermissions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Risk Hub page shell</name>
  <files>frontend/src/pages/RiskHubPage.tsx (NEW), frontend/src/pages/index.ts</files>
  <action>
    1) Create `RiskHubPage` with premium glassmorphism design:
       - Header: "Risk Hub" with CRO branding (e.g., command center icon)
       - Tabbed layout:
         - Tab 1: Risk Types (manage risk categories)
         - Tab 2: System Settings (thresholds, feature toggles)
         - Tab 3: Approval Rules (workflow configuration)
    
    2) Add route `/risk-hub` in router.
    
    3) Export from pages index.
    
    4) Use consistent design language with other pages.
  </action>
  <verify>Page loads at /risk-hub</verify>
  <done>Risk Hub page shell created.</done>
</task>

<task type="auto">
  <name>Task 2: Add Risk Hub to Sidebar (CRO-only)</name>
  <files>frontend/src/components/layout/Sidebar.tsx, frontend/src/hooks/usePermissions.ts</files>
  <action>
    1) Add "Risk Hub" item to Sidebar with CommandCenter/Settings icon.
    2) Gate visibility: only show for CRO role (NOT Admin).
    3) Add permission check: `canAccessRiskHub` in usePermissions.
       - Returns true only if user.role.name === "cro"
    4) Position prominently in sidebar navigation.
  </action>
  <verify>Login as CRO → see Risk Hub in Sidebar. Login as Admin → Risk Hub NOT in Sidebar.</verify>
  <done>Risk Hub accessible from Sidebar for CRO only.</done>
</task>

<task type="auto">
  <name>Task 3: Risk Types management panel</name>
  <files>frontend/src/components/riskhub/RiskTypesPanel.tsx (NEW)</files>
  <action>
    Create panel with:
    1) Table listing all risk types (code, display_name, is_active, is_system, risk_count).
    2) "Add Risk Type" button → modal form (code, display_name, description).
    3) Edit button per row → modal edit (cannot change code of system types).
    4) Delete button (soft-delete) with confirmation:
       - Show warning: "X risks will be marked as uncategorized."
       - Prevent deletion of system types (strategic, operational).
    5) Restore button for inactive types.
  </action>
  <verify>Full CRUD flow works for non-system types</verify>
  <done>Risk Types management UI complete.</done>
</task>

<task type="auto">
  <name>Task 4: System Settings panel</name>
  <files>frontend/src/components/riskhub/SystemSettingsPanel.tsx (NEW)</files>
  <action>
    Create panel with:
    1) Grouped sections by category:
       - **Risk Thresholds**: High/Critical/Medium min scores
       - **Approvals**: Toggle switches for approval requirements
       - **Notifications**: Reminder days, escalation settings
    2) Editable fields with appropriate inputs (number, toggle, text).
    3) Save button per section with confirmation.
    4) Show "Last updated by [CRO name] on [date]".
    5) Validation for numeric ranges.
  </action>
  <verify>Change a threshold, verify persistence</verify>
  <done>System Settings UI complete.</done>
</task>

<task type="auto">
  <name>Task 5: Approval Scenarios panel</name>
  <files>frontend/src/components/riskhub/ApprovalScenariosPanel.tsx (NEW)</files>
  <action>
    Create panel with:
    1) Table of approval scenarios:
       - Columns: Scenario Name, Description, Requires Approval? (Toggle), Approvers
    2) No "Add Rule" button (fixed list).
    3) Edit button per row:
       - Modal to toggle "Requires Approval"
       - Multi-select for "Approver Roles"
    4) Visual status indicators (Green key for enabled, Grey for disabled).
  </action>
  <verify>Toggle a scenario, verify it persists</verify>
  <done>Approval Scenarios UI complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Risk Hub with Risk Types, System Settings, and Approval Rules tabs</what-built>
  <how-to-verify>
    1. Login as CRO user
    2. Navigate to Risk Hub from sidebar
    3. Verify all three tabs load correctly
    4. Test adding a new risk type
    5. Test modifying a system setting
    6. Test viewing approval rules
    7. Verify Admin user cannot see Risk Hub in sidebar
  </how-to-verify>
  <resume-signal>Type "approved" to continue or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Risk Hub page accessible only to CRO
- [ ] Risk Types tab functional (CRUD)
- [ ] System Settings tab functional (view/edit)
- [ ] Approval Rules tab functional (view/CRUD)
- [ ] Admin cannot access Risk Hub
- [ ] `npm run lint` passes
</verification>

<success_criteria>

- CRO has centralized Risk Hub for configuration
- Admin excluded from Risk Hub access
- Full CRUD for risk types, settings, and rules
- Premium, consistent UI design
</success_criteria>

<output>
After completion, create `.planning/phases/70-risk-hub/70-05-SUMMARY.md`
</output>
