# Plan: Session Management & Token Refresh

## Objective
Implement enterprise-grade session management with refresh tokens, sliding sessions, and force logout capabilities.

## Execution Context
- **Phase**: 19 Production Deployment & Enterprise Integration
- **Plan**: 19-11
- **Depends on**: 19-08 (Azure AD SSO)

## Tasks

### 1. Refresh Token Implementation
- [ ] Create `backend/app/core/tokens.py`:
  - Access token (short-lived, 15 min)
  - Refresh token (long-lived, 7 days)
  - Token rotation on refresh
- [ ] Add `refresh_tokens` table for tracking active sessions
- [ ] Create `/auth/refresh` endpoint

### 2. Sliding Session Support
- [ ] Implement session extension on activity
- [ ] Configure sliding window duration (default: 30 min)
- [ ] Add session timeout warning to frontend

### 3. Force Logout / Session Revocation
- [ ] Add `/auth/logout` endpoint (invalidate tokens)
- [ ] Add `/admin/users/{id}/revoke-sessions` endpoint
- [ ] Implement "Logout all devices" functionality
- [ ] Add session list to user profile

### 4. Frontend Session Management
- [ ] Update `AuthContext.tsx` to handle token refresh
- [ ] Add automatic token refresh before expiry
- [ ] Handle session expiry gracefully (redirect to login)
- [ ] Show session timeout warning

### 5. Session Tracking
- [ ] Store session metadata (device, IP, last active)
- [ ] Show active sessions in Admin Console
- [ ] Log session events to audit log

## Verification
- [ ] Access token expires after 15 min
- [ ] Refresh token successfully extends session
- [ ] Force logout invalidates all sessions
- [ ] Frontend handles expiry without data loss

## Output
- `backend/app/core/tokens.py`
- Updated auth endpoints
- `refresh_tokens` table migration
- Updated frontend session handling
