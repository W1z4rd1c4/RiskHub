---
phase: 10-historization
plan: 01
type: execute
---

<objective>
Add KRI historization data model, reporting ownership, and reporting-period fields in the backend.

Purpose: Persist KRI value history, reporting cadence, and responsibility so APIs and reminders can enforce the 15-day window and approvals.
Output: Updated KRI model + schemas, new KRI value history model (with period boundaries), and Alembic migration.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-historization/10-CONTEXT.md
@backend/app/models/key_risk_indicator.py
@backend/app/models/control.py
@backend/app/schemas/kri.py
@backend/app/schemas/control.py
@backend/app/models/__init__.py
@backend/alembic
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend KRI model and add KRI value history model with reporting ownership</name>
  <files>backend/app/models/key_risk_indicator.py, backend/app/models/kri_history.py, backend/app/models/__init__.py</files>
  <action>
    Add a KRIFrequency enum (daily, weekly, monthly, quarterly, annually) modeled after ControlFrequency.
    In KeyRiskIndicator:
    - Add frequency column (String(20)) defaulting to KRIFrequency.quarterly.value.
    - Add reporting_owner_id FK to users (nullable), relationship reporting_owner.
      If null, reporting responsibility falls back to the linked Risk owner (handled in service layer).
    - Add last_period_end Date (period end the latest reported value belongs to).
    - Add last_reported_at DateTime (submission time, server_default func.now()).
    - Add relationship history_entries to KRIValueHistory with cascade delete.
    
    Create new KRIValueHistory model in backend/app/models/kri_history.py:
    - Table name: kri_value_history
    - Fields: id, kri_id FK (indexed), period_start (Date), period_end (Date),
      recorded_at (DateTime, server_default func.now), recorded_by_id FK to users (nullable),
      value (Float), lower_limit (Float), upper_limit (Float), unit (String(50)),
      breach_status (String(10)).
    - Relationship to KeyRiskIndicator and User.
    - Index on (kri_id, period_end) and (kri_id, recorded_at) for time-series queries.
    
    Export KRIFrequency and KRIValueHistory in models/__init__.py.
  </action>
  <verify>cd backend && python -c "from app.models.key_risk_indicator import KeyRiskIndicator, KRIFrequency; from app.models.kri_history import KRIValueHistory"</verify>
  <done>KeyRiskIndicator has frequency/reporting_owner/last_period_end/last_reported_at and KRIValueHistory model exists with period boundaries</done>
</task>

<task type="auto">
  <name>Task 2: Update KRI schemas for ownership, frequency, and history</name>
  <files>backend/app/schemas/kri.py</files>
  <action>
    Add KRIFrequencyEnum mirroring the model enum.
    Extend KRIBase/KRICreate/KRIUpdate with frequency (default quarterly) and reporting_owner_id.
    Add last_period_end + last_reported_at to KRIResponse for UI display, and optional reporting_owner_name.
    Add KRIHistoryEntry (id, kri_id, period_start, period_end, recorded_at, value, lower_limit, upper_limit, unit, breach_status, recorded_by_id)
    and KRIHistoryListResponse (items, total, page, size) schemas.
    Add KRIRecordValue schema for value recording payload (value, recorded_at optional, period_end optional for privileged backdating).
    Add KRIHistoryEdit schema for correction requests (value, reason) to feed approval workflow.
    Keep computed breach_status in KRIResponse unchanged.
  </action>
  <verify>cd backend && python -c "from app.schemas.kri import KRIResponse, KRIHistoryEntry, KRIRecordValue"</verify>
  <done>KRI schemas include reporting_owner, frequency, period metadata, and history payloads</done>
</task>

<task type="auto">
  <name>Task 3: Add Alembic migration for historization fields</name>
  <files>backend/alembic/versions/*-add_kri_history.py</files>
  <action>
    Create migration:
    - Add key_risk_indicators.frequency (String(20), server_default 'quarterly')
    - Add key_risk_indicators.reporting_owner_id (FK to users, nullable)
    - Add key_risk_indicators.last_period_end (Date, nullable)
    - Add key_risk_indicators.last_reported_at (DateTime, server_default now)
    - Create kri_value_history table with FK to key_risk_indicators and users
    - Add indexes on kri_value_history.kri_id, (kri_id, period_end), (kri_id, recorded_at)
    
    Backfill existing KRIs:
    - Set frequency to 'quarterly' where null.
    - Set last_reported_at to last_updated (fallback to created_at) so existing values are treated as reported.
    - Set last_period_end to date(last_updated) (fallback to created_at).
    - Optionally backfill reporting_owner_id from linked risk.owner_id when available.
  </action>
  <verify>cd backend && alembic current</verify>
  <done>Migration created with new columns and history table, backfill included</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Models import cleanly with new KRIFrequency and KRIValueHistory
- [ ] KRI schemas expose reporting_owner, frequency, period metadata, and history payloads
- [ ] Alembic migration adds columns, history table, and indexes
</verification>

<success_criteria>
- KRI frequency, reporting owner, last_period_end, and last_reported_at are stored in the database
- KRI value history model/table exists with period boundaries and value snapshots
- Schemas expose history, ownership, and frequency for API and UI use
</success_criteria>

<output>
After completion, create `.planning/phases/10-historization/10-01-SUMMARY.md`
</output>
