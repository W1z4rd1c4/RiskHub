---
phase: 10-historization
plan: 03
type: execute
---

<objective>
Expose KRI reporting ownership, frequency inputs, value recording, and history views in the frontend (no charts yet).

Purpose: Show current KRI values on Risk detail, full history on KRI detail, and enable reporting owners to submit values within the window.
Output: Updated KRI types/API client, ownership + frequency fields in forms, record-value and correction UI, and overdue KRI dashboard widget.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-plan.md
@.agent/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-historization/10-CONTEXT.md
@frontend/src/types/kri.ts
@frontend/src/services/kriApi.ts
@frontend/src/components/KRIForm.tsx
@frontend/src/components/kri/KRIModal.tsx
@frontend/src/pages/KRIDetailPage.tsx
@frontend/src/pages/KRIsPage.tsx
@frontend/src/pages/RiskDetailPage.tsx
@frontend/src/pages/DashboardPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update KRI types and API client for ownership, history, and overdue data</name>
  <files>frontend/src/types/kri.ts, frontend/src/services/kriApi.ts</files>
  <action>
    Add KRIFrequency type/const (daily, weekly, monthly, quarterly, annually).
    Extend KeyRiskIndicator with frequency, reporting_owner_id, reporting_owner_name, last_period_end, last_reported_at.
    Extend KRICreate/KRIUpdate to include frequency and reporting_owner_id.
    Add KRIHistoryEntry (period_start/period_end) and KRIHistoryListResponse types plus KRIRecordValue payload.
    Add KRIHistoryEdit payload for corrections (value + reason).
    Add OverdueKRI response type (due_date, days_overdue) for dashboard.
    Add API methods:
    - recordValue(kriId, payload) -> POST /kris/{id}/values
    - getHistory(kriId, params) -> GET /kris/{id}/history
    - requestHistoryEdit(kriId, entryId, payload) -> PATCH /kris/{id}/history/{entryId}
    - getOverdue(params) -> GET /kris/overdue
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <done>Types and API client compile with ownership/history/overdue fields</done>
</task>

<task type="auto">
  <name>Task 2: Add reporting owner + frequency fields to KRI create/edit forms</name>
  <files>frontend/src/components/KRIForm.tsx, frontend/src/components/kri/KRIModal.tsx, frontend/src/services/userApi.ts</files>
  <action>
    Add reporting owner selector (user dropdown) to KRIForm step 2 and KRIModal.
    Fetch user list via existing user API patterns; allow empty selection to fall back to Risk owner.
    Add frequency selector (dropdown) alongside owner selection.
    Default to quarterly for new KRIs and prefill for edits.
    Ensure reporting_owner_id + frequency persist in submit payloads.
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <done>Reporting owner and frequency are editable and saved through both KRI form entry points</done>
</task>

<task type="auto">
  <name>Task 3: Add record-value + history UI, overdue dashboard widget, and risk detail badges</name>
  <files>frontend/src/components/kri/KRIValueModal.tsx, frontend/src/components/kri/KRIHistoryEditModal.tsx, frontend/src/pages/KRIDetailPage.tsx, frontend/src/pages/RiskDetailPage.tsx, frontend/src/pages/DashboardPage.tsx</files>
  <action>
    Create KRIValueModal with:
    - input for new value
    - optional recorded_at (date/time) for privileged backdated entries
    - submit handler calling kriApi.recordValue and refreshing KRI details
    
    In KRIDetailPage:
    - Add "Record Value" button for users with risks:write permission
    - Show reporting owner (or Risk owner fallback), frequency, last_period_end, last_reported_at, and due_date
    - Render full history list (period_end, value, recorded_at, recorded_by)
    - Add "Request Correction" action per history entry:
      * Non-privileged users submit correction request (value + reason)
      * Privileged users can edit directly
    - Keep visuals limited to tables/labels (no charts; visualization is Phase 11)
    
    In RiskDetailPage:
    - Ensure KRI cards show current value plus overdue badge (if due_date passed)
    
    In DashboardPage:
    - Add an "Overdue KRIs" widget listing KRIs past due_date (from /kris/overdue)
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <done>Users can record values, view full history, and see overdue KRIs on dashboard/risk detail</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>KRI frequency fields and record-value workflow</what-built>
  <how-to-verify>
    1. Run: `cd backend && uvicorn app.main:app --reload`
    2. Run: `cd frontend && npm run dev`
    3. Visit: http://localhost:5173/kris/new and create a KRI with frequency = quarterly
    4. Assign a reporting owner (or leave blank to test fallback)
    5. Open the new KRI detail page and click "Record Value"
    6. Confirm last reported/period end updates and history list renders
    7. Visit Dashboard and confirm the Overdue KRIs widget loads (empty or populated)
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] TypeScript typecheck passes
- [ ] Frequency is visible and editable in KRI forms
- [ ] Record value modal updates KRI detail data
- [ ] Human verification checkpoint approved
</verification>

<success_criteria>
- KRI frequency and reporting owner can be set/updated in UI
- Users can record new KRI values and view full history on KRI detail
- Overdue KRIs are visible on the dashboard tab
- No historical charts added (reserved for Phase 11)
</success_criteria>

<output>
After completion, create `.planning/phases/10-historization/10-03-SUMMARY.md`
</output>
