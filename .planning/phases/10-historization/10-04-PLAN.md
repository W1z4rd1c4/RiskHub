---
phase: 10-historization
plan: 04
type: execute
---

<objective>
Validate KRI historization, reporting windows, reminders, and correction workflow.

Purpose: Ensure backend logic, API endpoints, approvals, and UI workflow behave as expected.
Output: Automated tests + manual verification checklist for reporting cadence and overdue visibility.
</objective>

<execution_context>
@.agent/workflows/gsd/execute-plan.md
@.agent/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-historization/10-CONTEXT.md
@backend/tests
@backend/app/services/kri_deadline_service.py
@backend/app/api/v1/endpoints/kris.py
@backend/app/schemas/kri.py
@frontend/src/pages/KRIDetailPage.tsx
@frontend/src/components/kri/KRIValueModal.tsx
@frontend/src/pages/DashboardPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backend tests for reporting windows, reminders, and history</name>
  <files>backend/tests/test_kri_history.py, backend/tests/test_kri_deadline_service.py</files>
  <action>
    Create test_kri_history.py:
    - record_value creates KRIValueHistory with period_start/end and updates current_value/last_period_end
    - non-privileged users cannot record values for past periods once new period starts
    - privileged users can backdate via period_end override
    - history query returns items in period_end desc order
    
    Update test_kri_deadline_service.py to include frequency-based reminders:
    - advance reminder fires before period_end (default 7 days)
    - deadline notification fires on due_date (period_end + 15 days)
    - overdue reminder repeats with 49-day lookback
    - Existing breach/near-breach tests still pass
  </action>
  <verify>cd backend && pytest tests/test_kri_history.py tests/test_kri_deadline_service.py -v</verify>
  <done>Backend tests cover history recording, reporting windows, and reminder cadence</done>
</task>

<task type="auto">
  <name>Task 2: Add API/RBAC tests for KRI history + correction endpoints</name>
  <files>backend/tests/api/v1/test_kris_history.py, backend/tests/test_kris_rbac.py, backend/tests/api/v1/test_approvals.py</files>
  <action>
    Add API tests to verify:
    - POST /api/v1/kris/{id}/values returns 200 and updates current_value
    - GET /api/v1/kris/{id}/history returns paginated history entries
    - GET /api/v1/kris/overdue returns overdue KRIs for dashboard
    - Read-only users cannot record values (403)
    - Non-privileged correction request creates ApprovalRequest
    - Approving correction updates history entry and current_value when latest period
    
    Reuse existing auth_client fixtures and KRI creation helpers.
  </action>
  <verify>cd backend && pytest tests/api/v1/test_kris_history.py -v</verify>
  <done>API tests cover history/overdue endpoints, approvals, and permission checks</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>KRI historization + frequency reminders end-to-end</what-built>
  <how-to-verify>
    1. Start backend: `cd backend && uvicorn app.main:app --reload`
    2. Start frontend: `cd frontend && npm run dev`
    3. Create a KRI with frequency = quarterly
    4. Record a new value and confirm last period end updates
    5. Try to backdate as non-privileged user (should be blocked or require approval)
    6. Submit a correction request and approve it as Risk Manager; confirm history entry updates
    7. POST to `/api/v1/notifications/trigger-kri-check` as Admin/CRO and verify advance/deadline/overdue notifications
    8. Check Dashboard for Overdue KRIs widget
  </how-to-verify>
  <resume-signal>Type "approved" if validation passes, or list issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Backend tests pass for history + reminders
- [ ] API tests pass for history/overdue endpoints, approvals, and RBAC
- [ ] Manual verification checklist completed
</verification>

<success_criteria>
- History recording and retrieval work end-to-end
- Reporting windows and reminders are generated without breaking breach notifications
- UI flow for recording values and requesting corrections works as expected
- Overdue KRIs appear on the dashboard tab
</success_criteria>

<output>
After completion, create `.planning/phases/10-historization/10-04-SUMMARY.md`
</output>
