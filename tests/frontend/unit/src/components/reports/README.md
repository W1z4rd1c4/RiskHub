# Report component tests

Vitest coverage for shared report and export UI components.

Keep interaction, accessibility, failure, and retry behavior for `ExportDialog`
here. Entity-specific request filters and export schemas remain covered by the
owning register and service tests.
