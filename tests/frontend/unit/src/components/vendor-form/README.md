# tests/frontend/unit/src/components/vendor-form

Unit-test suite for the vendor form components. Test files live in the
`__tests__/` subdirectory (currently the vendor register section accessibility tests).
