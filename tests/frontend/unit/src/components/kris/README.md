# KRIs Component Tests

Vitest coverage for KRIs components that use the pluralized domain namespace.

Prefer adding new tests beside the component namespace already used by the implementation. If a component moves between `kri` and `kris`, update this README or consolidate the test directory as part of that change.
