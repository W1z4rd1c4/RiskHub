# tests/frontend/unit/src/pages/dashboard

## Purpose

Focused unit coverage for the internal modules behind the public dashboard route facade.

## Notes

- Keep route-level behavior tests under `tests/frontend/unit/src/pages/__tests__/`.
- Add helper/module tests here when dashboard internals gain reusable logic.
