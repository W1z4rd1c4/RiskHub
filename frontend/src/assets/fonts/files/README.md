# Font binaries

This directory contains the repository-vendored WOFF2 files used by `../fonts.css`.

The authoritative paths and SHA-256 checksums are recorded in `../manifest.json`. Do not add remote font URLs or unverified binaries.
