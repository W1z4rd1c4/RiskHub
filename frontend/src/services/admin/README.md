# frontend/src/services/admin

## Purpose

Internal modules behind the public `@/services/adminApi` facade.

## Notes

- Keep `frontend/src/services/adminApi.ts` as the stable public import.
- Preserve the current admin endpoint surface and response shapes.
