# frontend/src/components/controls

## Purpose

UI components for `controls` area.

## Contents

- `ControlGaugeCard.tsx`

## Notes

`ControlGaugeCard.tsx` renders the canonical backend-derived control
`monitoring_status` (`new`, `needs_review`, `failed`, `passed`) and should not
recompute execution-health logic locally.

Keep this README updated when responsibilities or structure in this folder change.
