# frontend/src/components/tables/tableError

Shared table error-state UI. `TableErrorState.tsx` renders a consistent error/empty
state for data tables, `tableErrorContract.ts` and `types.ts` define the typed
contract callers pass in, and `index.ts` re-exports the public surface.
