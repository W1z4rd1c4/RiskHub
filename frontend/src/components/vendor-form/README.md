# frontend/src/components/vendor-form

## Purpose

Internal modules for the public `@/components/VendorForm` facade.

## Notes

- Keep `frontend/src/components/VendorForm.tsx` as the stable public entrypoint.
- Prefer hook/module extraction here over growing the facade again.
