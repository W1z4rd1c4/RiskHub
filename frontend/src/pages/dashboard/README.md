# frontend/src/pages/dashboard

## Purpose

Internal modules behind the public `@/pages/DashboardPage` route facade.

## Notes

- Keep the route entrypoint stable through `frontend/src/pages/DashboardPage.tsx`.
- Extract presentation/state helpers here instead of regrowing the route component.
