# frontend/src/pages/assets

Route-level Assets page. Contains the asset form (`AssetForm.tsx`), asset-to-vendor
link sections (`AssetLinkSections.tsx`), table column definitions (`assetColumns.tsx`),
and the page/detail state hooks (`useAssetsPageState.ts`, `useAssetDetailState.ts`)
with their pure presentation helpers.
