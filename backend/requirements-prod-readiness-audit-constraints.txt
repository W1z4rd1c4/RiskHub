# Production-readiness audit constraints.
# The audit composes the canonical development lock and adds the audit tool as
# an explicit requirement. The canonical lock already contains the full
# resolver output for this combined environment.
-c requirements-dev-constraints.txt
pip-audit==2.10.0
