# Legacy Docker Compose Production Path

Docker Compose with dockerized PostgreSQL is no longer a first-class production deployment path.

Primary production targets are:

- `docker` with external PostgreSQL
- `linux` without Docker and with external PostgreSQL

Use [production.md](./production.md) for the supported operator flow.
